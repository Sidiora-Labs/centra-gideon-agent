[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gideon-client"
version = "0.1.1"
description = "Gideon Client — async Python client for the Gideon Gateway"
readme = "README.md"
license = { text = "MIT" }
# Deliberately broader than core (>=3.12): the client is a thin, dependency-light
# library meant to be embeddable in tools on older interpreters.
requires-python = ">=3.9"

authors = [
    { name = "Gideon contributors" },
]

keywords = ["gideon", "client", "ai", "agent", "gateway"]

classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries",
    "Framework :: AsyncIO",
]

dependencies = ["aiohttp>=3.9"]

# NOTE: [project.urls] MUST come after the bare `dependencies` key — a table
# header placed earlier would absorb `dependencies` into the urls table (a TOML
# table runs until the next header), zeroing the runtime deps. (Same footgun
# fixed in the core pyproject, plan 34 T1.1.)
[project.urls]
Homepage      = "https://gideon.dev"
Documentation = "https://github.com/Gideon/Gideon/tree/main/docs"
Source        = "https://github.com/Gideon/Gideon"
Issues        = "https://github.com/Gideon/Gideon/issues"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "hypothesis>=6.0", "aiohttp"]

[tool.setuptools.packages.find]
include = ["gideon_client*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
