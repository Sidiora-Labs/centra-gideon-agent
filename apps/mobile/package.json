{
  "name": "gideon-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capacitor shell that wraps the gateway's served companion route. Contains no copy of the companion UI.",
  "scripts": {
    "test": "mkdir -p ../../reports && node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=../../reports/ci-web-mobile.junit.xml test/*.test.mjs",
    "cap": "cap",
    "add:ios": "cap add ios",
    "add:android": "cap add android",
    "assets:ios": "npx -y @capacitor/assets generate --ios",
    "assets:android": "npx -y @capacitor/assets generate --android",
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "build:ios": "cap sync ios && cap build ios",
    "build:android": "cap sync android && cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/push-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
