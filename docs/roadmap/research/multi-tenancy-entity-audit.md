# Multi-Tenancy Entity Audit — Provider Seams, Single-Tenant Assumptions, and Harness-Side Readiness

**Status:** RESEARCH (code-evidence audit, 2026-07-14; §6-7 rescoped 2026-07-14 to harness-side readiness)
**Scope:** Audit Gideon's readiness to be a good citizen of multi-tenant providers — a task provider whose tasks aren't all assigned to this harness's owner, a trigger store containing other users' triggers, a memory provider returning other contributors' memories. Gideon itself stays personal; the harness handles foreign-attributed records sensibly, and sharing semantics live at the shared-store / application level.
**Method:** direct code reads of every provider seam and entity store in `src/gideon/`, plus overlap mapping against the 27 roadmap plans. Every claim carries a file anchor. Paths relative to `Gideon/src/gideon/` unless noted.

---

## 0. Executive Summary

Gideon's provider architecture is genuinely strong on the *capability* axis — models, channels, agents, search, actions, prompts, skills, tools all plug in via apps with zero core edits (`docs/architecture/provider-boundary.md`). But the *entity-storage* axis is almost uniformly local-single-file: schedules, projects, inbox items, knowledge items, memory records, chat sessions, and notifications all persist as JSON/JSONL/sqlite under one un-keyed `~/.gideon/` home (`config/loader.py:161 config_dir()`). Exactly **one** entity today has a true storage-provider seam a remote backend could implement: **tasks** (`tasks/provider.py TaskProvider` ABC + `tasks/registry.py` aggregation with per-provider routing and `readonly` write-guarding). Memory has a *contract* (`memory_providers/base.py MemoryProvider`) shaped right but a registry that hard-defaults to the native store and a resolution path that bypasses the registry entirely.

Identity is the deeper gap: there is **one owner** everywhere. `GatewayServices.owner_id` (gateway_services.py:58) is "the primary owner's channel-user id"; auth modes (`auth/modes.py`) authenticate *the machine's user*, not *a* user; the SEL logs `caller_identity` as a session key (sel.py:68), not a principal; approvals route to "the" dashboard/DM. No entity record carries an author/actor field. Two usernames' records flowing through one harness today would corrupt nothing (append-mostly stores) but would be *indistinguishable* — no attribution anywhere.

The good news: the harness-side fix is small. What the harness needs is a username identity, contributor attribution on entity records, an owner-filter at trigger arm time, and provenance-weighting on recall (§6) — sharing semantics, permissions, and coordination belong to the shared store / application design, not the harness.

---

## 1. Provider-Pluggable Entities Today (the seam inventory)

The extension registry is `providers/registry.py`: apps declare `provider: {type, implementation}` in their manifest (`apps/manifest.py:453 PROVIDER_TYPES` = {model, agent, task, channel, inbox, skills, knowledge, memory, notification, tool, workflow, search, action, prompt} — guarded by `test_manifest_types_match_handlers` to equal the runtime `_TypeHandler` set). `providers/loader.py` loads each enabled app and registers contributions through typed handlers. Apps are separate OS processes with their own network stacks (`apps/permissions.py`: `network` is declaration-only, unenforced by design) — **an app can already host or call a remote backend today**; what varies is whether the *entity contract* lets that backend own the data.

Seam-by-seam (contract → storage → remote-capability verdict):

| Seam | Contract | Where entity data lives | Remote-capable today? |
|---|---|---|---|
| **Tasks** | `tasks/provider.py TaskProvider` — async `list_tasks/get_task/create_task/update_task/delete_task`, `get_comments/add_comment`, `readonly` property | Native: one-JSON-per-file `~/.gideon/tasks/t-<hex8>.json` (`tasks/native.py`); but data lives **inside the provider** — registry only routes | **YES.** The only full storage seam. `tasks/registry.py` aggregates across providers (`list_all_tasks` fans out, `create_task(provider_name=...)` routes, read-only providers rejected on write at registry.py:90). The `provider` field on a task is a real routing key, not a tag — `provider='project'` is a registered read-only projection provider. An app registering an Asana-class `TaskProvider` (type `task` → `TaskTypeHandler`, providers/registry.py:196) works with zero core edits. Missing: identity/attribution fields, sync/offline semantics, conflict handling. |
| **Memory** | `memory_providers/base.py MemoryProvider` — `put/get/delete/query/vector_query/embed/append_event/read_events` + lifecycle hooks (`on_turn_start`, `on_pre_compress`, `on_memory_write`, `on_delegation`) | Native `VectorMemoryStore`: `memory.db` + `memory_index.db` + FAISS under config dir; markdown mirror in `workspace/memory` (cwd-partitioned via `memory_dir_for_cwd()`) | **CONTRACT YES, WIRING NO.** The ABC is record-shaped and backend-agnostic — a remote "team learnings" store could implement it. But `memory_providers/registry.py:get_default_provider()` hard-instantiates `VectorMemoryStore`, and the note at registry.py:31-34 says real resolution goes through `ContextBuilder.get_memory_for` (cwd-partitioned) wrapped by `MemoryService` — the registry's named-provider lookup is vestigial. Methods are **sync** (called from the turn hot path) — a remote impl must cache/spool. A *secondary* provider (recall-federation: query team memory alongside native) is nearer than a replacement. |
| **Knowledge** | `knowledge_providers/base.py KnowledgeProvider` — async `list_sources/search/get_item` (+ optional `ingest/delete_item`) | Items live in core-owned `workspace/knowledge/knowledge.db` (`knowledge/__init__.py:18 knowledge_db_path`) + `files/`; providers are **federated search sources**, not the store | **PARTIALLY — for search federation only.** The seam is read-shaped (a Google-Drive-style source). Note: `knowledge_providers/registry.py:45 search_all` is verified dead (no core caller — EXTERNAL-ACCESS plan §Disposition confirms). A team knowledge *store* would either be a search-federation provider (works today, second-class: no ingestion pipeline, no embedding index) or needs the store itself seamed (it isn't). |
| **Inbox (sources)** | `inbox_providers/base.py MessageSourceProvider` — async `poll/send_reply/add_reaction/get_channel_history/resolve_user_name` | Items persist in **core-owned** `~/.gideon/inbox.json` (`inbox.py:35 _ITEMS_FILE`, `InboxStore` at inbox.py:196) | **INGESTION ONLY.** The seam is where items come *from* (poll-shaped, so a remote source is natural); the item store, read/triage state, alerts, retention are core-local with no seam. |
| **Channels** | `channel_transports/base.py ChannelTransportProvider` — `connect/disconnect/send(OutboundMessage)`, optional `receive() → ChannelMessage` iterator, `capabilities` + `channel_delivery.py ChannelDelivery` protocol (deliver_text/deliver_rich/streaming/open_dm/resolve_user_name/build_thread_link) | No entity store — a transport | **YES by nature** (Slack is remote). Notably the *only* seam whose messages carry a sender id — the closest thing to a second-identity surface in the system (see §3). |
| **Models** | `llm/registry.py register_type/build` + `ModelCatalog`/`ModelManager`; bindings in `active_models.json` | Provider instances in `config.json providers[]`; no entity data | **YES** — remote APIs are the normal case. The pattern to copy: use-case bindings + per-instance settings + `test_connection`. |
| **Search** | `search_providers/base.py SearchProvider` (+ `SearchCapabilities`, keyless-floor selection in registry) | No entity store | **YES** — already mostly remote APIs. |
| **Actions** | `action_providers/base.py ActionProvider` — async `execute(context) → ActionResult`, `supports_dry_run/supports_blocking` | No store; invoked by triggers/hooks/workflows. New providers must join `ALLOWED_HOOK_PROVIDERS` (validation.py:555) for hook use | **YES** — an action can call anything (webhook-action precedent). Relevant as the *effector* half of team integration (post to team tracker), not the shared-store half. |
| **Prompts** | `prompt_providers/` (PromptTypeHandler, providers/registry.py:321) — catalog/engine/runtime; native provider owns `~/.gideon/prompts/*.yaml` | Per-provider | **YES in shape** — a read-only remote prompt library provider is straightforward (the TASKS-SOPS plan already specifies a git-synced read-only workflow-def provider on the same pattern). |
| **Workflows** | `workflows/registry.py` + `WorkflowProvider` ABC (WorkflowTypeHandler, providers/registry.py:213), `readonly=True` supported | Native: `~/.gideon/workflows/<name>/` | **YES (read-only today)** — TASKS-SOPS explicitly plans a git-synced def-library provider app. A team SOP library is *this seam, already planned*. |
| **Tools** | `tool_providers/` registry → tool modules in `mcp_core._TOOL_MODULES`; plus MCP servers via `~/.gideon/mcp.json` (`providers/mcp_instances.py`) | n/a | **YES** — MCP is the standard remote-tool path; a team can already expose shared capabilities as a remote MCP server. |
| **Skills** | `skills/marketplace.py SkillsMarketplace` implementations on `get_default_skills_registry()`; installs via `install_guarded` (quarantine→scan→TOCTOU-closed commit→`.gideon-lock.json`) | Installed copies local `~/.gideon/skills/` | **YES for distribution** — a team skill hub is a marketplace instance (AGENT-PACKS §6 CatalogMarketplace makes it one class). Pull-only: no push/publish seam. |
| **Agents** | `EntitySeamHandler` (providers/registry.py:364) — **not a provider**; source_of_truth is `config.json agents{}` | config | **NO seam** — agents/knowledge/inbox/notification/skills types registered as EntitySeamHandlers are lifecycle-acknowledged but entity data lives elsewhere (registry.py:531-563). A shared team agent roster would arrive via AGENT-PACKS distribution, not a live provider. |
| **Notifications** | EntitySeamHandler; delivery prefs in `entity_settings/`; log at `notifications.jsonl` (dashboard/state.py:141) via `DashboardState.notify` (state.py:1027) → `notification_allowed()` gate | core-local JSONL | **NO** — pluggable notification *delivery* is an explicitly absent "future design" seam (WORK-CONTAINERS §runs-inbox notes it deliberately). |
| **Embeddings** | `embedding_providers/` (base + registry) | n/a | **YES** — resolution via use-case bindings like models. |

**Verdict pattern:** capability seams (model/search/action/channel/tool/skill) are remote-ready; entity-storage seams exist only for tasks (full), memory (contract only), knowledge+inbox (ingestion/search half only), workflows+prompts (read-only half). Everything else is core-owned local files.

---

## 2. Entities WITHOUT Provider Seams (the gap list)

For each: storage, mediation, and what a seam would take. "Mediating class" matters because a single mediator = EASY seam (swap its backend), scattered file I/O = HARD.

| Entity | Storage (verified) | Mediation | Seam difficulty |
|---|---|---|---|
| **Schedules/triggers** | `~/.gideon/crons.json` (`schedule.py:60 _CRONS_FILE`), fcntl `.crons.lock`, mtime `_sync()` for cross-process pickup; plus siblings `hooks.json`, `event_triggers.json`, `autonudge.json` | **Centralized**: `ScheduleService` inline I/O (schedule.py:483 — note: there is no `CronStore` class; persistence is inline in the service via `_load`/:1343, `_save`/:1405, `_sync` mtime reload/:1331); all surfaces (API facade `/api/triggers`, MCP `mcp_schedule.py`, CLI) go through it — but MCP/CLI create fresh `ScheduleService` instances over the SAME file. Ticking: single re-armed asyncio task, ≤30s poll, croniter minute-match | **MEDIUM.** The service's contract *is* a local file (mtime-sync, fcntl, same-file multi-process convention baked into MCP tools writing the store directly from another process). The clean seam appears **after** AUTOMATION-SUBSTRATE unifies 4 stores into `triggers.json` behind one `TriggerService` — seam then = a TriggerStore interface + provider impls. Harness-side rule for a store containing other users' rows: the local service *arms and fires only the owner's triggers* (author == owner filter at arm time; execution stays local — scheduler-dispatches-never-executes doctrine); foreign rows render visible-but-inert. Any cross-user coordination is the store's design space, not the harness's. |
| **Tasks** | `~/.gideon/tasks/t-*.json`, `tasks/task_lists/`, comments sidecars (tasks/native.py) | `tasks/registry.py` façade — every surface (chat tool, UI handlers, workflows) routes through it | **DONE (the seam exists).** `TaskProvider` ABC (tasks/provider.py) is a full async CRUD contract; apps CAN register via `TaskTypeHandler` (providers/registry.py:196). Note: `provider='project'` appears in docstrings but NO such implementation is registered in-tree — "project" is a planned read-only projection. Remaining gap is semantic, not structural: no `author/assignee` identity fields on `Task` (tasks/models.py), no updated_at-based conflict semantics, no offline cache for a flaky remote, and TASKS-SOPS is about to add write-guards (`workflow_binding.managed` rejection at the registry chokepoint) that a remote provider must honor. |
| **Projects** | `~/.gideon/projects/p-<hex8>/project.json` + `context/` + worktrees (tasks/hierarchy.py; top-level, NOT under tasks/) | `tasks/hierarchy.py` + hierarchy_handlers | **MEDIUM-HARD.** JSON metadata could seam like tasks, but a project *owns local dirs* (context_dir, worktrees) that loops/workflows/memory-partitioning bind to as filesystem paths (WORK-CONTAINERS §R14: project sessions run with cwd = context_dir). A "shared project" is really shared *metadata + tasks + knowledge tags* over per-member local working dirs — the WORK-R15 export/import contract is the nearest primitive. |
| **Inbox items** | `~/.gideon/inbox.json` (single JSON, InboxStore inbox.py:196) + state file 0600 | `inbox.py` + `inbox_service.py` (retention loop, alerts-at-ingestion) | **MEDIUM.** One store class, but item ids/read-state/alert evaluation assume one reader. Alerts are evaluated at ingestion (documented: keyword edits don't re-evaluate) — a shared inbox would re-open that. Lower value for teams (channels already do shared conversation). |
| **Knowledge items** | `workspace/knowledge/knowledge.db` (WAL sqlite) + `files/` originals; FTS + embedding indexes derived | `KnowledgeStore` singleton (`knowledge/__init__.py:45`) | **MEDIUM.** Single store class behind one accessor = swappable in principle; but the ingestion pipeline (doc_parser, embeddings, FTS) writes locally-derived indexes, and WORK-CONTAINERS adds `project_id`/`sharing_policy: private|shared` metadata (§R14) — a *pre-built tenancy vocabulary*. Near-term team story: a `KnowledgeProvider` search-federation source over a team store (seam exists), with `sharing_policy: shared` items eligible for *push* to it (push seam missing). |
| **Memory store** | `memory.db`/`memory_index.db`/FAISS + markdown workspace; cwd-partitioned (`memory_dir_for_cwd()`, loader.py:219) | `MemoryService` wrapping the provider; recall via ContextBuilder | **MEDIUM (as secondary), HARD (as replacement).** The `MemoryProvider` ABC fits, but: sync methods on the turn hot path; FAISS/embedding locality; the learning lifecycle (LEARNING-FLYWHEEL: extract→stage→propose→accept) assumes one human approver. Harness-side rule for a shared provider: recall hits carry **contributor provenance** (explicitly labeled + fenced — the exact mechanism WORK-CONTAINERS §R14 already specifies for cross-project recall: "surfaced explicitly labeled with source project name... labels are metadata, not instructions"), ranking weights the owner's own memories higher, and writes attribute the owner's username. How the store itself governs writes is the store's design, not the harness's. |
| **Chat sessions** | `sessions/*.jsonl` (year-sharded), `session_map.json`; `ConversationLog` (history.py) + `SessionManager` | Centralized log classes | **HARD and mostly wrong-target.** Sessions are per-harness by nature; the sharing surface is *channel-shared threads* (see §4 sync_bridge) not a shared session store. |
| **Notifications log** | `notifications.jsonl`, trim at 2× cap (dashboard/state.py:141) | `DashboardState.notify` → `notification_allowed()` | **EASY mechanically, deliberately deferred** — pluggable delivery is a named future seam. Team-shared notification routing rides whatever addressing concept identity work introduces. |
| **Workflow runs / Run Ledger** | today `workflows/` + loop dirs; WORKFLOWS-V2 brings `templates/<slug>/vN.json` + run journals + effect ledger | v2 engine | **DEFER to the v2 engine** — the engine's Run Ledger + `project_id` + journal-event-sourcing is being designed now; tenancy fields (`actor`, `origin_harness`) are cheap to add at design time and expensive later. |

---

## 3. Single-Tenant Assumptions (what breaks first)

1. **One owner identity, stringly and channel-scoped.** `GatewayServices.owner_id` — "the primary owner's channel-user id, or `""` if unset" (gateway_services.py:58); `DashboardState(owner_id="")` (state.py:663). The Slack app *auto-claims* the first DM sender as owner (trust-on-first-use: `apps/slack-channel/slack_runtime/events.py:1325-1338 claim_owner`; handler.py:759), persisted via the credential store ("runtime-discovered credentials such as a channel app's auto-claimed owner id", loader.py:240). events.py:604 says it outright: "Multi-user is still [unsupported]". There is no `Principal`/`Actor` type anywhere in core.
2. **Auth authenticates access, not identity.** `auth/modes.py`: none / local_token / api_key / oauth2. Tokens gate *the whole gateway*; `generate_token(user_id, app=...)` (dashboard/token_auth.py) carries an app claim for the permission sandbox but the `user_id` is the single dev/owner. EXTERNAL-ACCESS deliberately scopes itself to "one user letting their own tools talk to their own assistant — not a multi-tenant API product" (its §Overview soul guardrail), and that scope stands.
3. **One un-keyed home.** Everything under `config_dir()` (`~/.gideon` or `GIDEON_HOME`, loader.py:161) with no tenant/user key in any path. Two humans on one backend is fine (homes are per-human); the assumption that bites is **entity ids are locally minted with no origin** — `t-<hex8>`, `p-<hex8>`, trigger ids: 8-hex collision space with no `machine_id`/`origin` field, so merging two harnesses' stores can collide and can't attribute. (DURABILITY-AND-SYNC already introduces a stable per-machine `machine_id` in its shard manifest — the natural origin key.)
4. **No author on anything.** `Task` has no creator/assignee; `ScheduleJob` has `created_by: agent|user|system` (a *kind*, not a *who*); memory records have no author; `TaskComment.add_comment(author="")` accepts a free string nobody sets. First thing a shared backend needs and the cheapest to add.
5. **Approvals route to "the" user.** Tool approval prompts, trust/YOLO state (`trust_mode.py` — ONE process-global auto-approve state), needs-input inbox, `DashboardState.notify` — all assume the one dashboard/DM. Correct for a single-owner harness; WORKFLOWS-V2's owner binding ("only the run's requester can approve from a shared channel; non-owner replies ignored") already covers the shared-channel case.
6. **SEL identity is a session key.** `sel.py:68 caller_identity: str  # session key or user identifier` — fine for a single-user harness; noted as an observation only.
7. **sync_bridge.py is a proto-multi-tenant surface, narrowly.** It hands a dashboard session to a channel thread (`handoff_to_channel` → `open_dm(owner_id)` → `set_channel_link`) and back (`!resume`). The session becomes visible in a *shared* medium (a Slack channel), and the owner-binding rule exists precisely because non-owners can now see and reply. But linkage is owner-centric (DM to owner_id); nothing models a second participant. It's evidence the channel layer is where second identities first appear — sender ids already flow through `ChannelMessage`, and EXTERNAL-ACCESS §6's `sender_trust.json` (per-transport `{sender_id: {state, display, paired_at}}` + pairing codes) is the first core store keyed by *someone other than the owner*.
8. **App platform is tenant-ready-ish where it matters.** Per-app DATA_DIR (`storage` permission), app-scoped 1-hour tokens with cookie/Authorization stripping at the proxy (apps.py::api_app_proxy), permission middleware in all auth modes. An app CAN host a team-backend client today (own process, own network). What's missing is not app capability — it's the entity contracts (§1/§2) and identity fields the app would need to speak.

**What breaks first if two humans share an entity backend:** nothing crashes — attribution silently collapses. Both harnesses write as "the user"; approval prompts land on whichever dashboard is attached; id collisions are possible; deletes resurrect on the other side (no tombstones — DURABILITY-AND-SYNC adds them); and any shared *automation* would fire N times (once per harness) because nothing filters arming down to the owner's own triggers.

---

## 4. Prior Art In-Repo — Overlap Map (the new plan must slot IN)

| Existing plan | What it builds that multi-tenancy needs | Overlap verdict |
|---|---|---|
| **DURABILITY-AND-SYNC** (Wave 0+3) | The whole entity-replication substrate: per-entity JSONL shards + SHA manifest; `machine_id`; merge strategies (`union_by_id`, `lww_by_updated_at`, `append_dedup`, `sqlite_attach_ignore`); **tombstones**; sha-divergence conflict records with propose-only LLM merge; a NEW `sync` provider type (`SyncTransportProvider`: push/list_remote/pull/cas_registry/test) + `sdk/sync.py`; secrets-never-sync | **UNCHANGED.** Single-user cross-machine machinery, scoped as designed ("single user... no server component of ours"). Multi-user stores are provider apps' territory; the harness's only touch is reserving an optional author string in still-forming schemas. |
| **EXTERNAL-ACCESS** (Wave 3) | Inbound identity + trust: fail-closed bearer surfaces, per-client records/bindings/scopes/budgets (`inbound_clients.json`), sender-trust substrate with DM pairing codes (`sender_trust.json`), inbound audit, A2A gateway (workflows callable by external agents), headless profile + owner binding | **UNCHANGED.** Its soul guardrail ("not a multi-tenant API product") stands. Outbound — the harness connecting to a shared backend — is just a provider app (no core change), which is the only direction the harness-readiness work needs. |
| **AGENT-PACKS** (Wave 4) | Cross-instance entity portability: `.gideon` packs, dependency closure, redaction, provenance recomputed-from-contents, fresh-id rewriting + `imported-N` collisions, `{source, computedHash}` locks/drift, skill-catalog marketplaces, `pack_owned` update semantics | **UNCHANGED.** Packs stay the async share-by-copy distribution lane; nothing in harness-side multi-user tolerance touches them. |
| **WORKFLOWS-V2 family** | Run Ledger + journals; **owner binding on remote-channel gates** (requester-only approval); WORK-CONTAINERS: knowledge `project_id` + `sharing_policy: private|shared`, cross-partition recall with labeled provenance + fence framing (the provenance-label mechanism memory tolerance reuses); TASKS-SOPS: registry write-chokepoint + git-synced read-only def-library provider; AUTOMATION-SUBSTRATE: `triggers.json` unification behind one TriggerService, `created_by` tags | **TWO TOUCHES.** (1) Where schemas are still forming (v2 ledger/journal rows, `triggers.json` rows), reserve an optional author/contributor string at design time — cheap now, breaking later. (2) The trigger-store unification is the hard prerequisite for the trigger provider seam (build one seam, not four). Nothing else changes. |
| **sync_bridge.py** (shipped) | Dashboard↔channel session handoff; sessions visible in shared channels | Proto-surface only (§3.7). Confirms channels are where second identities already exist; the owner-binding rule is its guard. |
| **App platform** (shipped) | Remote-capable app processes, per-app storage/token scoping, provider registration | The delivery vehicle: every team backend should arrive as an app implementing an entity-provider contract. Missing piece is contracts, not platform. |

**Non-overlap (genuinely new ground, all harness-side):** (a) a username identity + optional author/assignee/contributor fields on entity records; (b) the owner-filter at trigger arm time (only the owner's rows arm; foreign rows visible-but-inert); (c) the trigger-store provider seam (post-substrate); (d) contributor-labeled, owner-weighted memory recall.

---

## 5. Tenancy-Readiness Matrix

Legend: ✅ yes · 🟡 partial · ❌ no. "Identity-aware" = records carry a who. "Conflict/merge" = today's code (DURABILITY-AND-SYNC will add the middle column for everything it covers, single-user).

| Entity | Provider seam? | Remote-capable? | Identity-aware? | Conflict/merge semantics? | Missing for team-shared |
|---|---|---|---|---|---|
| Tasks | ✅ `TaskProvider` + routing registry | ✅ contract is async request-shaped | ❌ no author/assignee | ❌ (LWW-by-overwrite; no tombstones) | author/assignee fields; assignee-aware display + mine-vs-everyone filter; MINE-only work counters |
| Task lists / comments | ✅ (rides TaskProvider) | ✅ | ❌ (`author=""` free string) | ❌ | set author from owner username |
| Projects | ❌ (hierarchy.py local) | ❌ (owns local dirs) | ❌ | ❌ | share metadata layer only; WORK-R15 export as primitive; per-member local dirs |
| Schedules/triggers | ❌ (ScheduleService inline I/O = the file) | ❌ (fcntl+mtime multi-process protocol) | 🟡 `created_by` kind, not who | 🟡 `_merge_crons` by name (snapshot-restore only) | wait for `triggers.json`/TriggerService; then TriggerStore seam + owner-only arming (foreign rows visible-but-inert) |
| Inbox items | 🟡 sources only (`MessageSourceProvider`) | 🟡 sources yes, store no | 🟡 sender ids on messages | ❌ | store seam; per-reader read-state; low priority |
| Knowledge items | 🟡 search federation (`KnowledgeProvider`) | 🟡 read path yes | 🟡 `project_id`+`sharing_policy` incoming (WORK-R14) | ❌ | contributor label passthrough on federated hits (rides the existing source label — near-free) |
| Memory | 🟡 ABC right, registry vestigial | 🟡 (sync hot-path methods) | ❌ | 🟡 `_merge_memory` ATTACH+INSERT-OR-IGNORE | contributor-labeled + fenced recall; owner-weighted ranking; attributed writes |
| Chat sessions | ❌ | ❌ | 🟡 origin labels (dashboard/channel/cron); channel senders | ❌ | wrong target — share via channels + owner binding |
| Notifications | ❌ (deliberate) | ❌ | ❌ | n/a | addressing concept; deferred seam |
| Workflow defs | ✅ `WorkflowProvider` (readonly) | ✅ (git-sync provider planned) | ❌ | 🟡 lock/drift convention (flywheel) | write-back/publish half; else packs |
| Workflow runs | ❌ (v2 engine forming) | ❌ | 🟡 owner binding on gates | ✅ journal event-sourced (v2) | add `actor`/`origin` to ledger schema NOW |
| Prompts | ✅ provider seam | ✅ | ❌ | ❌ | read-only team library provider is cheap |
| Skills | ✅ marketplace seam (pull) | ✅ | 🟡 lock provenance | ✅ lock/drift | team hub = marketplace instance; publish flow missing |
| Agents | ❌ EntitySeamHandler (config-owned) | ❌ | ❌ | ❌ | distribute via packs; live sharing not warranted |
| Models/search/actions/channels/tools/embeddings | ✅ | ✅ | n/a (capabilities) | n/a | nothing — already the pattern to copy |

---

## 6. Harness-Side Readiness Gaps

What the harness itself is missing to behave correctly as one client of a multi-tenant store (everything else — sharing semantics, permissions, coordination — is the shared store's / application's design space, not the harness's):

1. **No username identity.** One `owner_id` string (gateway_services.py:58), TOFU-claimed by a channel app; onboarding collects only a display name (`dashboard.user_name`). The harness has no stable identity string to attribute its own writes with.
2. **No contributor attribution on entity records.** `Task` has no author/assignee; `TaskComment.author` is an unset free string; memory records carry no contributor; `ScheduleJob.created_by` is a *kind* (agent|user|system), not a *who*. A provider returning records attributed to other people has nowhere to put the "who," and the harness would treat everything as the owner's.
3. **No owner-filter on triggers.** Nothing distinguishes "created by this harness's owner" from "created by someone else" — a trigger source containing other users' rows would have them all armed and fired locally. The filter belongs at arm time in the (post-substrate) `TriggerService`.
4. **No provenance-weighting on recall.** Memory recall has no contributor labels and no owner-preference in ranking; foreign-contributed memories would rank and render indistinguishably from the owner's own. (The labeled-provenance rendering for cross-project recall — WORK-R14 — is the existing mechanism to reuse.)

---

## 7. Recommended Slot-In

These gaps are closed by the rescoped plan `docs/roadmap/plans/TEAM-SHARED-ENTITIES.md` ("Multi-Tenant Entity Readiness — The Harness as a Good Citizen of Shared Stores"): a first-boot username + optional attribution fields defaulting to the owner, per-entity tolerance semantics (assignee-aware tasks with MINE-only work counters; owner-only trigger arming with foreign rows visible-but-inert; contributor-labeled, owner-weighted memory recall), the trigger-store provider seam (gated on AUTOMATION-SUBSTRATE's `triggers.json` unification — one seam, not four), and one proof-of-concept trigger-provider app validating the integration path end-to-end. Where schemas are still forming (triggers.json, v2 ledger rows), optional author/contributor string fields are reserved at design time.

---

*Related reading: `docs/architecture/provider-boundary.md` (the capability-seam tenet), `docs/architecture/app-platform.md` (delivery vehicle), `docs/roadmap/plans/DURABILITY-AND-SYNC.md`, `EXTERNAL-ACCESS.md`, `AGENT-PACKS.md`, `WORKFLOWS-V2*.md`, and `docs/research/learnings/ecosystem-and-interop.md` + `automation-and-triggers.md`.*
