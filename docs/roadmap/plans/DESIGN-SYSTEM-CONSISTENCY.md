# DESIGN-SYSTEM-CONSISTENCY

**Status:** DECOMPOSED — the executable work now lives in [`../atomic/DSC.md`](../atomic/DSC.md) as 12 atomic plan(s).

This plan was split because parts of it blocked on other plans, which forced it to sit half-done while other work ran. Each atom below its own file executes start-to-finish in one go; the dependency graph lives in [`../atomic/dag.json`](../atomic/dag.json).

The original design record is kept below — execution logs, measured findings and owner rulings are the reason this document still matters.

---
# Plan: Design-System Consistency — One Coherent Surface, Everywhere

**Status:** DONE — closed by owner 2026-07-24 (created 2026-07-18, roadmap rev 10; owner ask: UI/UX consistencies). S1 audit + S2 token/primitive hardening + S3 interaction/a11y/responsive parity all landed across cy1–cy18 (see `## Execution log`); WCAG AA reached on all 11 schemes in both modes with a permanent per-scheme contrast guard; every byte-identical convergence cluster consolidated onto shared primitives; the primitive-adoption + inline-font-weight ratchets and the strict token-lint are live and block regression. **Owner-closed with a known, honestly-recorded tail** (not silently dropped): the authenticated axe-per-route CI gate (T3.2/T3.4) and the V3 full-app keyboard/reduced-motion/phone walkthrough need a seeded, authenticated, per-route CI harness that was not stood up here; the `ErrorState` primitive (single faithful adopter) and the harness-gated `CodeCockpitPage`-class raw-button *redesign* are deferred as owner/design decisions, not consistency work. Ratchets keep the surface from drifting back; any of the deferred items can be picked up as follow-on plans.
🟢 **The harness that tail was waiting for now has an owner (2026-08-04):**
[PLATFORM-HARDENING-FLOORS](PLATFORM-HARDENING-FLOORS.md) §4 builds the offline **fake-model E2E
harness** — a real gateway booted against a packaged fake model provider with a seeded
authenticated session, then Playwright shelled at it (no model, no credentials, no network, no
cost) — and `SH4.3` explicitly mounts this plan's `T3.2`/`T3.4` axe-per-route a11y rail on it. So
T3.2/T3.4/V3 are **DEFERRED with a named owner**, not open-ended.
**Created:** 2026-07-18
**Wave:** 2 — a consistency-hardening pass that every other product-surface plan then inherits; run it early so new work lands on a clean baseline.
**Depends on:** nothing hard (audits + hardens the shipped design system). Feeds FLUID-MOTION (52 — consistent components are the substrate motion animates), APP-PLATFORM-EVOLUTION (48 — apps consume the exported primitives), and every page-touching plan (43, 49, 50).
**Scope:** the design system is mature (tokens, `tokenRegistry`, a token-lint test, 41 shell primitives, `web/PRODUCT.md` + `web/DESIGN.md`) — but 20+ pages built over time drift from it. This plan is a **consistency audit + hardening**: verify every surface uses tokens (not hardcoded values), the shell primitives (not bespoke chrome), consistent interaction patterns, WCAG AA parity, and dark/light + responsive parity — then ratchet the lint so drift can't return. **Soul guardrail:** this is *consistency*, not redesign — no new visual language, no page rewrites; bring outliers to the existing system (`DESIGN.md`/`PRODUCT.md` are authority). Where a page genuinely needs a pattern the system lacks, add it to the system *once* (a new shared primitive), never a one-off.

---

## Context (code recon, 2026-07-18)

Strong foundation: `web/src/design/` — `tokens.css` (brand/surface/content/semantic/glow/typography/layout/shape/elevation/3D/motion token groups), `tokenRegistry.ts` (typed `Token` union, 11 `GROUPS`), `tokenLint.test.ts` + `tokenLint.allowlist.json` (**a linter already exists** — the ratchet target), `schemes.ts` (light/dark), `runtime.ts` (canvas glow), `motion.ts`, `gradients.ts`. Shell primitives (41 in `web/src/ui/`): TopBar, ListScaffold, SidePanel, HeaderActions, NavRail, Modal, Surface, Button, IconButton, Toggle, Segmented, Combobox, Popover, Toaster, WorkbenchLayout, etc. `PRODUCT.md` sets the doctrine ("companion not console," earned familiarity, everything-is-a-token, WCAG AA). `web/src/ui/cx.ts` (class util).

**Where drift lives (the audit will quantify):** the two big pages (`ChatPage.tsx` 3384 LOC, `CodeCockpitPage.tsx` 3386 LOC, loop cockpits ~1000+ LOC each) predate later primitives and likely carry bespoke chrome + hardcoded values the allowlist tolerates; empty states vary (plan 43/49 add `EmptyState`); the two first-party UI apps (Minutes/Growth) don't use the system at all (plan 48 migrates them); dark/light parity and focus-visible/reduced-motion coverage are per-component, not audited system-wide.

## Design

- **S1 — The audit (measure before touching):** an automated inventory — extend the token-lint to *report* (not just fail on allowlist) every hardcoded color/spacing/radius/shadow/duration in `web/src`, grouped by file; a primitive-adoption scan (pages using bespoke chrome vs shell primitives); a WCAG scan (axe over each route) and a reduced-motion/focus-visible coverage check. Output: `docs/design/consistency-audit.md` — a ranked drift list. **No fixes in S1** — this is the map.
- **S2 — Token + primitive hardening:** fix the ranked drift — replace hardcoded values with tokens (shrinking the allowlist as you go), migrate bespoke chrome to shell primitives, consolidate near-duplicate components (e.g. multiple ad-hoc button/menu variants → the canonical primitive). Each fix shrinks the lint allowlist; the end state = allowlist near-empty, lint strict.
- **S3 — Interaction + a11y + responsive parity:** standardize interaction patterns (list selection, empty states via the shared `EmptyState`, loading skeletons, error states, confirm dialogs — one pattern each, documented in a `docs/design/patterns.md` gallery); WCAG AA parity pass (every route: contrast, focus-visible ring, keyboard nav, reduced-motion alternative); dark/light parity pass (every token used in both, no theme-only hardcodes); responsive pass (no horizontal body scroll; the phone-viewport paths for plan 44's companion). Ratchet: the lint + an a11y CI check now *block* regressions (feeds plan 33 rails).

## Contracts & Interfaces (mostly frontend hardening; the enforceable rails pinned — conventions per [AGENTS.md](../../../AGENTS.md))

### C1 — The consistency rails (CI-enforced, mount in plan 33 `ci.yml` web/rails)
- **Token lint (strict):** `web/src/design/tokenLint.test.ts` extended — every hardcoded design value in `web/src` must be a token or an explicit, justified allowlist entry (`tokenLint.allowlist.json` with a `reason` field per entry). Ratchets down each session; end state near-empty.
- **A11y check:** axe-core over each route (headless) in web CI — no serious/critical violations; reduced-motion + focus-visible asserted.
- **Primitive-adoption check:** a lint that flags bespoke chrome elements (raw `<button>`, ad-hoc modals) outside `web/src/ui/` primitives, allowlisted with reasons.

### C2 — Pattern gallery (`docs/design/patterns.md` + a Storybook-lite route, optional)
Canonical usage of each shared primitive + each interaction pattern (selection, empty, loading, error, confirm). This is the "how to stay consistent" reference every page-touching plan cites.

### Integration points
- **Touches:** every `web/src/pages/*` surface (consistency fixes) + `web/src/ui/*` (consolidation) + `web/src/design/*` (token/lint hardening).
- **Consumed by:** 52 (motion animates consistent components), 48 (apps import the hardened primitives via UI SDK), 43/49/50 (new surfaces inherit the patterns + `EmptyState`).
- **Rails into:** plan 33 CI (`web`/`rails` jobs gain the token-lint-strict + axe checks).
- **Authority:** `web/DESIGN.md` + `web/PRODUCT.md` (unchanged — this plan conforms TO them).

## Task breakdown (executor-ready — run under the roadmap session discipline in [AGENTS.md](../../../AGENTS.md))

### Session 1 — The audit (map only)

| ID | Task | Files | Done when |
|---|---|---|---|
| T1.1 | Extend token-lint into a reporter: emit every hardcoded design value in `web/src` grouped by file + type, without failing the build | `web/src/design/tokenLint.test.ts` (+ a report script) | report lists all drift with file:line; counts by category |
| T1.2 | Primitive-adoption + a11y + parity scan → the ranked audit doc | `docs/design/consistency-audit.md`, scan scripts | audit ranks the worst offenders (likely ChatPage/CodeCockpit/loop cockpits); each entry actionable |
| V1 | Validation: the audit is complete + accurate (spot-check 5 findings against the code) | — | holds; NO fixes made this session |

### Session 2 — Token + primitive hardening

| ID | Task | Files | Done when |
|---|---|---|---|
| T2.1 | Fix the top-ranked drift: hardcoded values → tokens; shrink the allowlist (each removed entry = a real fix) | the ranked pages, `tokenLint.allowlist.json` | allowlist materially smaller; those pages token-clean; visual diff shows no regression (screenshot compare) |
| T2.2 | Migrate bespoke chrome → shell primitives; consolidate duplicate components into the canonical primitive | outlier pages, `web/src/ui/` | duplicates removed; pages use shared primitives; suite + build green |
| T2.3 | Add any genuinely-missing primitive to the system ONCE (if the audit found a real gap) with a pattern-gallery entry | `web/src/ui/`, `docs/design/patterns.md` | new primitive documented + adopted where needed; not a one-off |
| V2 | Validation: the hardened pages look identical-or-better; token-lint allowlist near-empty for them | — | holds |

### Session 3 — Interaction + a11y + responsive parity

| ID | Task | Files | Done when |
|---|---|---|---|
| T3.1 | Standardize interaction patterns (selection/empty/loading/error/confirm) across pages via shared primitives; document each in `patterns.md` | pages, `docs/design/patterns.md` | each pattern has one implementation + one doc entry; pages conform |
| T3.2 | WCAG AA parity pass: axe over every route → fix serious/criticals; focus-visible + keyboard nav + reduced-motion alternatives everywhere | pages, `ui/` | axe clean on all routes; keyboard-only walkthrough of the app works |
| T3.3 | Dark/light + responsive parity: every token used in both themes (no theme-only hardcodes); no horizontal body scroll at phone widths | pages, tokens | theme toggle on every page shows no broken surface; phone-width pass clean |
| T3.4 | Ratchet: mount token-lint-strict + axe into plan 33 web/rails CI (block regressions) | `.github/workflows/ci.yml` (coordinate with plan 33) | a new hardcoded value or a11y regression turns CI red |
| V3 | Validation: full app walkthrough in both themes, keyboard-only, reduced-motion, and a phone viewport — consistent throughout | — | holds |

## Owner tasks (real world)
1. **Review the audit** (S1 output) and confirm the priority order — you may weight the surfaces you use most.
2. **Taste sign-off on any consolidation** where two patterns merge into one (e.g. if two menu styles become one) — that's a visual-identity call.
3. Confirm the WCAG target stays **AA** (PRODUCT.md says AA) — AAA is not proposed.

## Risks & open questions
- **"Consistency" creeping into "redesign"** — the soul guardrail forbids it; every S2/S3 change is verified by screenshot-diff to show no unintended visual change (only conformance). If a fix *requires* a visual change, that's an owner taste call (task 2), not silent scope.
- **The two mega-pages** (ChatPage/CodeCockpit) are the highest-effort, highest-risk fixes — do them in small, screenshot-verified increments; a full rewrite is explicitly out of scope (would be its own plan).
- **Open:** Storybook-lite route vs a static pattern doc — default: static `patterns.md` (zero new dep); add a live gallery route only if the team grows (ratchet).

---

## Execution log

### 2026-07-22 — S1 cycle 1 (audit reporter + drift map)
- **DONE (T1.1):** Built the drift reporter — `web/src/design/consistencyAudit.report.ts` (`scanDrift()` inventories color/spacing/radius/shadow/duration + a primitive-adoption scan, grouped by file:line; `rankFiles()` weighted worst-first) and `web/src/design/consistencyAudit.test.ts` (runs the scan, emits `docs/design/consistency-audit.json`; measure-only, never fails on drift). Zero new deps — mirrors the existing `tokenLint.test.ts` pattern, runs under `npm test`.
- **DONE (T1.2):** Authored `docs/design/consistency-audit.md` — the ranked drift map from measured data. V1 spot-check: 5 findings verified against code (all hold).
- **DISCOVERY:** Token layer is already near-clean (**7 raw-value hits total**, most legitimate: SVG drag durations, generated HSL graph palette, CodeMirror `0.9em`, one `calc(-36px)` widget offset). The DOMINANT drift is **primitive adoption**: **420 raw `<button>` + 206 raw form elements across 112 files**, topped by the two mega-pages (CodeCockpit 51, ChatPage 34). `Modal` is already the sole canonical dialog (0 bespoke). → S2's payload is primitive consolidation, not token cleanup.
- **DISCOVERY:** There is **no shared form-field primitive** — the one genuine system gap (T2.3): a `TextField`/`Field` in `web/src/ui/` + `patterns.md` entry, to land before migrating the 206 raw inputs.
- **BLOCKED (S2 prerequisite):** the Playwright visual-regression harness is not yet built (`web/package.json` has only `vitest`; no `@playwright/test`/`axe-core`). Per the brief this harness must exist and capture baselines BEFORE any S2 fix. Next cycle: stand it up.
- **Verification:** `cd web && npx tsc --noEmit` clean; `npx vitest run` 72/72 green (+2 from the reporter). **No fixes made this session** (S1 is map-only).

### 2026-07-22 — S1 cycle 2 (a11y static coverage scan)
- **DONE (T1.2, a11y portion):** Extended the reporter with `scanA11y()` (reduced-motion + focus-visible coverage) and wired it into the JSON + audit doc. Replaced the "pending" a11y placeholder with measured coverage.
- **DISCOVERY:** a11y coverage is **global-by-default, not per-component** — `tokens.css` has a `*` reduced-motion rule AND a global `:focus-visible` ring, and `App.tsx` wraps everything in Framer `<MotionConfig reducedMotion="user">`. So the 144 animated files + 171 `outline-none` occurrences (70 files) are all covered by the global nets — NOT violations to fix individually (fixing them = churn). The S3 ratchet must PROTECT the two global rules (assertion now in consistencyAudit.test.ts) and add dynamic axe-per-route (needs harness).
- **Verification:** `npx tsc --noEmit` clean; `npx vitest run` 72/72 green. Static a11y scan complete; dynamic axe-per-route + keyboard walkthrough remain BLOCKED on the Playwright harness (next cycle). No fixes made (S1 map-only).

### 2026-07-22 — S1→S2 cycle 3 (Playwright visual + a11y harness — the safety rail)
- **DONE (S2 hard prerequisite — infrastructure):** Built the visual-regression + a11y harness in `web/` mirroring the gideon.dev pattern — `playwright.config.ts` (platform-qualified `toHaveScreenshot` baselines, `maxDiffPixelRatio` tolerance, animations disabled, vite-preview webServer), `e2e/routes.ts` (route SoT), `e2e/helpers.ts` (theme seed + settle), `e2e/visual.spec.ts` (16 routes × 2 themes), `e2e/a11y.spec.ts` (axe wcag2aa, fail on serious/critical), `e2e/README.md`, npm scripts (`e2e`, `e2e:visual`, `e2e:a11y`, `e2e:update`, `e2e:report`). Added `@playwright/test` + `@axe-core/playwright` devDeps; `npx playwright install chromium` succeeds; gitignored run artifacts.
- **BLOCKED (baseline capture):** the built SPA gates first render on an authenticated identity/config fetch (`identity.tsx` `loaded` gate; gateway needs the owner `pc_token` cookie). A fresh unauthenticated Playwright context (both vs `vite preview` and vs the live gateway :10000) renders a **blank body** → a naive capture produced 32 *identical blank* PNGs. **Those were discarded, NOT committed** (a false safety rail is worse than none). Recorded the auth-seed default (owner/CI runs `e2e:update` against an authenticated session; CI mints a test token via a global-setup stub).
- **HONEST STATUS:** harness is **wired but dormant** — infra proven (config parses, 64 tests listed, chromium installed, preview serves real HTML/JS), but valid baselines + a real axe reading await an authenticated session I can't create autonomously. My earlier same-cycle "32/32 clean / zero-diff" reading was against the blank app and is INVALID — corrected here.
- **Evidence:** `npm run build` green; `playwright test --list` = 64 tests; `npx playwright install chromium` ok; probe showed `#root` empty (0 chars) unauthenticated → root cause identified. `npx tsc --noEmit` clean; `npx vitest run` 72/72 green (unit suite untouched). No baselines committed.
- **NEXT:** land the auth-seed (`e2e/auth.setup.ts` + storageState) so S2 visual verification is trustworthy — this is now the true S2 gate, ahead of the first primitive-consolidation fix.

### 2026-07-22 — S2 cycle 4 (first consolidation: notification unread-rail + tone-chip helpers)
- **DONE (T2.2, pure-conformance consolidation — no visual change):** the verbatim-duplicated unread accent rail (`boxShadow: inset 2px 0 0 0 ${tone}`) and tinted icon-chip background (`color-mix(in srgb, ${tone} 16%, transparent)`) lived in both `NotificationsPage.tsx` (Row, 3 sites) and `NotificationBell.tsx` (ShadeRow, 2 sites). Extracted to `unreadRail(tone, acked)` + `toneChipBg(tone)` in `notificationMeta.ts` (the module both already import). Output is byte-identical → zero visual change; safe to make without the (auth-blocked) visual harness.
- **Guard:** `notificationMeta.test.ts` pins the exact helper output (inset rail string, 16% tint string, token-routed tones) so the consolidation can't silently drift.
- **Measured effect:** the audit reporter's shadow-drift count dropped 2 → 1 (the single canonical helper is the only remaining `inset …px` source). Duplication eliminated.
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **75/75** green (72 + 3 new guard tests). Chose this over the `WidgetFrame calc(-36px)` fix (DEFERRED — that 36px is a sibling-toolbar height, a layout coupling, not a scale value; changing it needs visual verification which is auth-blocked).
- Because the visual harness is dormant (auth blocker), I am prioritizing consolidations that are byte-identical by construction (provably zero visual change) — the brief's "proceed on all pure-conformance fixes" — and deferring anything that could shift pixels until baselines can be captured.

### 2026-07-22 — S2 cycle 5 (T2.3: the missing form-field primitive + pattern gallery)
- **DONE (T2.3 — the one genuine system gap):** added `web/src/ui/TextField.tsx` — `TextField` (single-line) + `TextArea` (multi-line) with `size` (sm/md/lg → h-8/9/10), `surface` (container/high/base), `mono`, and `leadingIcon` variants. It is the CANONICAL EXTRACTION of the exact input shape ~200 raw `<input>`s already hand-roll (`rounded-md bg-surface-* h-* px-3 … outline-none focus:ring-2 focus:ring-inset focus:ring-primary/50`) — no new visual language. Additive only: touches ZERO existing pixels until pages migrate, so safe to land while the visual harness is auth-blocked.
- **DONE (C2 — pattern gallery seeded):** created `docs/design/patterns.md` with the `TextField`/`TextArea` entry (canonical shape, props, usage, migrate-when), plus target entries for Button/IconButton (420 raw buttons to migrate) and Modal (already canonical). S3 interaction patterns stubbed.
- **Guard:** exported `textFieldClass()`/`textAreaClass()` and pinned them in `TextField.test.tsx` (logic-level, matching the repo's test convention — no component renderer is wired for `.tsx` `render()` here; I discovered `@testing-library/react`'s `render` errors under this vitest config, so I test the class contract directly).
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **82/82** green (+7 primitive tests); `npm run build` green.
- **NEXT:** the 206 raw-input migration + the 420 raw-button migration are now unblocked design-wise but PIXEL-AFFECTING → gated on the visual-baseline auth blocker (cycle 3). Continue byte-identical consolidations meanwhile.

### 2026-07-22 — S2→S3 cycle 6 (T3.4 ratchet: primitive-adoption CI guard)
- **DONE (C1 third rail / T3.4):** added `web/src/design/primitiveAdoption.test.ts` + `primitiveAdoption.baseline.json` — a bespoke-chrome ratchet asserting raw `<button>` (baseline 420), raw `<input>/<textarea>/<select>` (206), and ad-hoc dialogs (0) counts may only DECREASE. A NEW raw element turns the CI `web` job red. It runs in the EXISTING vitest step (no browser needed), so it's live in CI now — proven by a synthetic +1 that fails and reverts clean.
- **DONE (CI wiring):** documented the mounted rails in `.github/workflows/ci.yml` (the `web` job's vitest step already carries `tokenLint.test.ts` token-lint-strict + the new primitive-adoption ratchet). Two of C1's three rails are CI-enforced now; the axe-per-route rail (web/e2e/) mounts here once the harness auth-seed lands.
- **Anti-churn:** the ratchet is count-based and can only tighten — as S2 migrations replace raw chrome with Button/IconButton/TextField, lower the baseline in the same commit to lock the gain (a soft warn fires if the live count drifts >20 below without a ratchet).
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **86/86** green (+4 ratchet tests); YAML well-formed (no tabs, structure intact); no Python touched → `make lint` N/A. Committed on main.

### 2026-07-22 — S3 cycle 7 (T3.1: empty-state pattern clarity — rename + document)
- **DISCOVERY:** the name `EmptyState` was used for TWO genuinely different components — the full-height centered page-empty (`ui/ListScaffold`) and a deliberately compact dashboard-slot strip (`pages/dashboard/widgets/kit.tsx`, whose own comment justifies why it's NOT centered). Not an accidental duplicate to merge — two intentional patterns whose shared name made them LOOK like a bug.
- **DONE (T3.1, zero-pixel refactor):** renamed the slot variant `EmptyState` → `SlotEmptyState` in `kit.tsx` and its 5 importers (Suggestions, TasksWidget, ScheduleWidget, ActiveWork, ActionCenter). Same component, same JSX output → zero visual change; resolves the name collision. Documented BOTH as distinct canonical patterns in `docs/design/patterns.md` (page-empty vs slot-empty, with a pick-by-context table + usage) so future pages choose correctly instead of re-rolling a third ad-hoc empty.
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **86/86** green; `npm run build` green; primitive-adoption ratchet green (rename didn't change counts). Ad-hoc empties (`ChatActivityPanel.Empty`, `NotificationsPage.EmptyFeed`) flagged in patterns.md to adopt one of the two as touched (pixel-affecting → harness-gated).

### 2026-07-22 — S2 cycle 8 (fvs() weight helper + .fw-* utilities — canonicalize inline font weights)
- **DISCOVERY (audit deepening):** `font-variation-settings: "wght" <n>` is set INLINE ~180 times across pages (75× 500, 48× 600, 42× 550, 11× 470, 6× 400, 1× 650) — the single largest repeated inline-style pattern, with no shared home.
- **DONE (additive, zero-pixel):** added `web/src/design/fontWeight.ts` (`fvs(n)` → the identical `{ fontVariationSettings: '"wght" <n>' }`, plus `withWeight()`) and matching `.fw-400/470/500/550/600/650` utility classes in `tokens.css`. Both emit the SAME output the call sites hand-write — byte-identical drop-ins. Documented in `patterns.md` (prefer a type-role when the element maps to one; use fvs()/.fw-* for weight-only nudges).
- **DONE (proof migration):** converted the 2 sites in `NotificationsPage.tsx` to `fvs(500)` (byte-identical). ~180 remain — incremental, each a provable zero-pixel swap; safe to grind down without the visual harness.
- **Guard:** `fontWeight.test.ts` pins `fvs()`/`withWeight()` output.
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **88/88** green (+2); `npm run build` green (CSS utilities compile).

### 2026-07-22 — S2 cycle 9 (font-weight migration batch: NotificationBell + HeaderActions)
- **DONE (T2.1-style conformance, byte-identical):** fully migrated `NotificationBell.tsx` (4 inline `wght` → `fvs()`/`withWeight()`) and `HeaderActions.tsx` (3) to the fvs() helper. Combined-style sites use `withWeight(existing, n)` (keeps the other props); pure sites use `fvs(n)`. Both files now carry ZERO inline `font-variation-settings`.
- **Progress:** inline `wght` count ~180 → **167** (13 migrated cumulatively across NotificationsPage/NotificationBell/HeaderActions). Each swap is a provable zero-pixel drop-in; grinding down incrementally, self-contained `ui/` files first, mega-pages (LoopCockpit 11, CodeCockpit 10) last per the brief's risk order.
- **Evidence:** `grep -c` confirms 0 inline wght in both files; `npx tsc --noEmit` clean; `npx vitest run` **88/88** green; `npm run build` green.

### 2026-07-22 — S2 cycle 10 (font-weight migration batch: 6 more ui/ primitives)
- **DONE (byte-identical conformance):** migrated all inline `wght` in `Button` (1), `Segmented` (1), `NavRail` (1), `FilterMenu` (1), `BoardCollapse` (1), `SystemWidget` (4) → `fvs()` (combined styles via `{ ...other, ...fvs(n) }`). Each touched ui/ primitive now carries ZERO inline `font-variation-settings`.
- **Progress:** inline `wght` count 167 → **158** (9 this batch; 22 cumulative). Self-contained `ui/` primitives are now largely done; remaining are page-level + the mega-pages (last).
- **Evidence:** all 6 files grep-clean (0 inline wght); `npx tsc --noEmit` clean; `npx vitest run` **88/88** green; `npm run build` green.

### 2026-07-22 — S3 cycle 11 (extend the ratchet: inline font-weight cap)
- **DONE (C1/T3.4 — 4th rail):** added `countInlineFontWeights()` to the reporter and an `inlineFontWeight` cap (baseline 168) to `primitiveAdoption.test.ts` + baseline.json. The inline `font-variation-settings "wght"` count may only shrink — a new inline weight turns the CI web job red, locking in the ~22-site fvs() migration and enforcing fvs()/.fw-* going forward.
- **CORRECTION:** the true inline-weight total is **168** (scanner, across all `.ts`/`.tsx`), not the ~158 my earlier `.tsx`-only grep reported — it missed `.ts` files (e.g. `liveMarkdown.ts` ×7, a CodeMirror theme object that is editor-internals, not a JSX inline style, so it legitimately stays). Baseline set to the authoritative scanner count.
- **Proof:** lowering the baseline to 167 fails the new rail; restored to 168 passes.
- **Evidence:** `npx tsc --noEmit` clean; `npx vitest run` **89/89** green (+1 rail); ratchet regression-tested. Committed on main.

### 2026-07-22 — S3 cycle 12 (T3.1/C2: document the remaining interaction patterns)
- **DONE (C2 pattern gallery — interaction patterns):** documented the app's EXISTING canonical implementations in `docs/design/patterns.md` so pages stop re-rolling them:
  - **Confirm/prompt/alert** → `ui/dialog/` (`confirm`, `confirmDelete`, `promptInput`, `promptForm`, `alertDialog` — the window.confirm/prompt replacement; `alertDialog({tone:'danger'})` is the canonical error state).
  - **Loading/skeleton** → `ui/ListScaffold` (`Skeleton`, `ListSkeleton`, `FormSkeleton`, `CardGridSkeleton`, `Loading` — all `aria-busy`).
  - **Selection/list rows** → `ListRow`.
- Docs-only cycle (no code touched). Every documented export was spot-checked to exist (`ui/dialog/index.ts`, `ui/ListScaffold.tsx`, `.skeleton` in tokens.css). This completes the C2 gallery's primitive+pattern coverage except the optional shared inline-error-banner (a follow-up only if the audit finds enough ad-hoc ones).
- **Evidence:** all documented symbols verified present; `npx vitest run` still 89/89 (unchanged — no code touched). Committed on main.

### 2026-07-22 — cycle 13 (CI-lockfile fix + owner/CI handoff)
- **FIX (CI-critical):** committed `package-lock.json` for the harness deps (`@playwright/test`, `@axe-core/playwright`, `axe-core`) — they were installed in the harness cycle but never locked, so CI's `npm ci` would have failed on the missing entries. Now locked (verified all 3 present).
- **DONE (handoff):** wrote `docs/design/consistency-handoff.md` — the minutes-long unblock for the ONE gating item (seed the harness auth session) + the exact worst-first backlog the unblock enables (CodeCockpit/ChatPage button migration, 206-input → TextField, finish font-weight, ad-hoc empties, dark/light + responsive parity), each ratchet-tracked.
- **State at handoff:** full DoD gate GREEN on main — `npx tsc --noEmit` clean, `npx vitest run` 89/89, `npm run build` green. 13 commits, all safe (additive infra, provably-zero-pixel conformance, or docs). Everything pixel-affecting is correctly gated on the owner/CI auth-seed.

### 2026-07-23 — cycle 14 (rebase + baseline capture: the gating item is CLOSED)
- **DISCOVERY (root cause of the auth-seed blocker):** the "blank shell without a session" was never an auth gap — it was the v0.1.0 dual-React mount crash (fixed on main by the React-18 pin, PR #24). Against a tokenless dev gateway (`GIDEON_AUTH_MODE=none`, seeded empty home, `user_name` set via `PUT /api/dashboard/config` so the app leaves Onboarding) the SPA mounts fully with NO storage state. Recipe recorded in `playwright.config.ts`; `e2e/auth.setup.ts` remains for token-auth gateways only.
- **DONE:** branch rebased onto main (React-18 toolchain; lockfile conflict resolved by regenerating from main's lockfile + the harness deps). All 32 Darwin visual baselines captured and re-verified deterministic (second run 32/32, zero diff). Harness fix: bounded `networkidle` wait to 5s — polling routes (Agents) never idle and the unbounded wait equaled the test timeout.
- **Evidence:** `npm ci` clean; typecheck clean; vitest 89/89; build green; render smoke 5/5; visual 32/32 twice.

### 2026-07-23 — S2 cycle 15 (dynamic axe scan: ALL serious violations cleared)
- **DONE (a11y, the S1 audit's deferred dynamic scan):** full axe suite now 32/32 green (16 routes × 2 themes). Three recurring serious violations fixed:
  1. `HeaderActions` measurement probes → `inert` (aria-hidden-focus, 26 route-failures);
  2. composer accessible name moved onto CM's contenteditable via `EditorView.contentAttributes` (aria-prohibited-attr + aria-input-field-name);
  3. color tokens re-solved numerically for AA on every surface each hue renders on (white/canvas + own 14–16% color-mix tint): light primary→`#c8452e` (old emphasis; brand stays coral), emphasis→`#a33922`, ok→`#076e37`, warn→`#954c19`, danger→`#af2f29`, info→`#145cbf`; dark danger→`#f66c66`, info→`#5e99f9`; NavRail section-label opacity 0.55→0.65. tokens.css + tokenRegistry + coral scheme updated in lockstep.
- **DEVIATION (scoped follow-up, not silently widened):** non-default schemes (honey/jade/…) still carry pre-AA light values — queued as its own cycle; the axe rail only guards the default scheme today.
- **Visual baselines regenerated** for the intentional color shift (all 32) and re-verified deterministic.
- **Evidence:** axe 32/32; visual 32/32 (×2); typecheck clean; vitest 89/89; render smoke 5/5.

### 2026-07-23 — S2 cycle 16 (mega-page button migration wave 1 + empty-state consolidation)
- **DONE (T2.2 — CodeCockpit button wave 1):** migrated the highest-risk mega-page's raw chrome to `Button`/`IconButton`, **51→23 raw** `<button>`. Closed real gaps the migration surfaced in the primitives themselves (`Button.tsx`, `IconButton.tsx`) so the page's variants map onto the canonical API without new one-offs.
- **DONE (T2.2 — ChatPage button wave 1):** `ChatPage.tsx` **34→19 raw** `<button>`.
- **DONE (T3.1 — empty-state consolidation):** folded the two remaining ad-hoc empty states (`ChatActivityPanel`, `NotificationsPage`) into the canonical `EmptyState` pattern.
- **Ratchet:** `primitiveAdoption.baseline.json` `rawButton` lowered **420→377** in lockstep, locking every migrated site (a regression re-adds red).
- **Evidence:** typecheck clean; vitest **89/89**; build green; ratchet re-verified (lowering below live count fails, current passes). Visual verification of these pixel-affecting changes pending against the tokenless dev gateway (cycle 14 recipe) before the branch's final baseline refresh.

### 2026-07-23 — S2 cycle 17 (fix a density-doctrine bug in the TextField primitive + correct the audit premise)
- **VISUAL VERIFICATION (the cycle-16 waves, now confirmed):** brought up the tokenless dev gateway (`AUTH_MODE=none`, seeded empty, `user_name` set — cycle 14 recipe) and ran the full harness against the built SPA on the branch tip. **visual 32/32 + axe 32/32 green** → the 3 post-baseline commits (CodeCockpit/ChatPage button waves, empty-state consolidation) produced ZERO unintended pixel diff and no a11y regression. The safety rail confirms "consistency not redesign" held.
- **DISCOVERY (latent defect in the cycle-5 primitive):** `ui/TextField`/`TextArea` hardcoded a fixed `px-3`/`py-2`. But density is a LIVE user setting — `appearance.tsx` sets `data-ui="dense"|"cli"` on `<html>`, re-scaling `--space-scale` (1 → 0.8 → 0.68), and the system's explicit doctrine is *"no per-component density px — density falls out app-wide for free"* (tokens.css P19). A fixed `px-3` would make the field STOP tightening at dense/cli, and since `px-m` is the majority padding in the raw inputs it was built to replace (232 `px-m` vs 148 `px-3` app-wide), every future migration off it would silently regress density. Caught before adoption (the primitive is imported nowhere yet).
- **DONE (fix):** `px-3`→`px-m`, `py-2`→`py-s` in `textFieldClass`/`AREA_BASE` (leading-icon `pr-3`→`pr-m`; `pl-9` stays — it clears the fixed-position icon). `px-m == 12px == px-3` at the default *comfortable* density, so this is **byte-identical there** (proven: visual 32/32 unchanged) while correctly tightening at dense/cli. Updated `TextField.test.tsx` contract (px-m) + the primitive's doc comment.
- **CORRECTION (audit premise):** the S1 audit's "**no** shared form-field primitive" was wrong — `pages/tasks/formControls.tsx` already exports an adopted, density-correct, a11y-wired `Field`/`TextInput`/`TextArea`/`Select` family (tasks/schedule/inbox import it). Recorded in the `ui/TextField` header. Two form families is itself a consistency defect, BUT collapsing them is a "two patterns → one" merge (different radius/size) = an **OWNER TASTE call** (plan Owner task 2) — flagged, not silently forced.
- **Evidence:** typecheck clean; vitest **89/89**; build green; visual **32/32**; axe **32/32** (all against the live gateway).
- **OPEN (owner taste, queued):** (1) merge `ui/TextField` ↔ `formControls` form family into one home (a visual-identity call — pixels shift on one call-site set); (2) the 206 raw-input → shared-primitive migration is design-unblocked now the primitive is density-correct, but each swap is pixel-verified. Continuing byte-identical/zero-diff work meanwhile.

### 2026-07-23 — S2 cycle 18 (font-weight migration batch: Markdown + PlanningWalkthrough)
- **DONE (T2.1-style, byte-identical):** migrated all inline `font-variation-settings "wght"` in the two shared `ui/` primitives — `Markdown.tsx` (5: table-header/h2/h3/h4 → `fvs(500)`, strong → `fvs(600)`) and `PlanningWalkthrough.tsx` (4: three pure → `fvs(550/600)`, one combined color+weight → `withWeight({color}, 550)`). Both files now carry ZERO inline weight strings. Continues the cycle-9/10 "self-contained ui/ primitives first" order.
- **Ratchet:** `inlineFontWeight` baseline lowered **168→159** (authoritative scanner count) in lockstep — locks the 9-site gain; a new inline weight turns CI red.
- **Note (unrelated false positives):** the impeccable hook flagged two `<img>` in `Markdown.tsx` (`InlineArtifactImage`) as "broken/placeholder image" — they are the real markdown-image renderer with dynamic `src` + a 404-degradation path (pre-existing, not touched by this change). Classified false-positive; left unchanged.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 159); build green; visual **32/32** (byte-identical swap, no baseline moved).

### 2026-07-23 — S2 cycle 19 (font-weight migration batch: 7 page-level files)
- **DONE (T2.1-style, byte-identical):** migrated all inline `font-variation-settings "wght"` across 7 self-contained page files — KnowledgeListPage (5), WorkflowDetail (4, incl. a conditional `nested ? undefined : fvs(550)` + a color-combined `withWeight`), TasksListPage (3), DiagnosticsPanel (3, incl. a dynamic-color `withWeight`), DesignPanel (3), ProjectsSection (3), CommentLayer (3). Pure sites → `fvs(n)`; color/conditional-combined → `withWeight()`/inline-`fvs()`. All 7 now carry ZERO inline weight strings.
- **Ratchet:** `inlineFontWeight` baseline **159→135** (24 sites, authoritative scanner count) in lockstep.
- **Progress:** cumulative inline-weight migration 168→135 (33 sites across cycles 18–19). Remaining are the mega-pages (CodeCockpit/ChatPage/LoopCockpit — last per risk order) + a few multi-site pages, plus legitimate `.ts` editor-internals (liveMarkdown CodeMirror theme) that correctly stay.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 135); build green; visual **32/32** (byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 20 (font-weight migration batch: the settings cluster, 16 files)
- **DONE (T2.1-style, byte-identical):** migrated every inline `font-variation-settings "wght"` across the entire `pages/settings/*` cluster — ProvidersPanel (2), SettingsPage (2), ModelBackends (2), settingsWidgets (2), PortabilityPanel (2), MemoryPanel (2), bento (2), plus 9 single-site panels (UpdatesPanel, settingsUI, MultiInstanceCard, VoicePanel, AppsPanel, SecurityPanel, SearchPanel, ProviderCard, ModelsPanel). 22 pure → `fvs(n)`; bento's stat value (weight + `color: tone`) → `withWeight({ color: tone }, 600)`. Settings subtree now carries ZERO inline weight strings.
- **Method:** scripted uniform substitution (byte-identical text swap) with per-file import insertion; caught + hand-fixed one bad insertion where the "last import" heuristic split a multi-line `import { … } from './bento'` block (settingsWidgets) — moved the `fvs` import below the block.
- **Ratchet:** `inlineFontWeight` baseline **135→112** (23 sites) in lockstep.
- **Progress:** cumulative inline-weight migration 168→112 (56 sites across cycles 18–20). Remaining: mega-pages (LoopCockpit 11, CodeCockpit 10, LoopPlanReview 8, ChatPage 6, DesignCockpit 5 — last per risk order), a scatter of chat/loops/tools files, and the legitimate `.ts` editor internals (`ui/composer/liveMarkdown.ts` CodeMirror theme, `design/consistencyAudit.report.ts` self-referential regex) that correctly stay.
- **Pre-existing note:** the impeccable hook flagged 13 `design-system-font-size` literals in settingsWidgets — pre-existing size-ramp drift (T2.1 scope), untouched by this weight-only cycle; left visible, not silenced.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 112); build green; visual **64/64** (Settings light+dark byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 21 (font-weight migration batch: loops/code/agents cluster, non-mega)
- **DONE (T2.1-style, byte-identical):** migrated the inline `font-variation-settings "wght"` across the medium/small loops·code·agents files — LoopPlanReview (8), DesignCockpitPage (5: 4 pure + 1 combined `withWeight({ background, color }, 600)`), CodePlanReview (4), AgentsListPage (3), LoopsListPage (2), DesignStepPreview (1), AgentDetail (1), AgentForm (1 combined). 23 pure → `fvs(n)`; 2 multi-prop → `withWeight()`.
- **Held per risk order:** the two true mega-pages **LoopCockpitPage (11)** and **CodeCockpitPage (10)** deliberately NOT touched this cycle — they carry conditional/many-site weights and are the last-per-risk surfaces; they get their own dedicated cycle.
- **Method:** scripted pure substitution with a hardened import-insertion (after the first `from '…'` line, robust vs multi-line first imports — the fix for cycle 20's settingsWidgets snag); 2 combined sites hand-edited.
- **Ratchet:** `inlineFontWeight` baseline **112→87** (25 sites) in lockstep.
- **Progress:** cumulative inline-weight migration 168→87 (81 sites across cycles 18–21). Remaining: the 2 mega-pages (21 sites), a scatter across chat/tools/tasks/ui, plus the legitimate `.ts` editor internals that stay.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 87); build green; visual **64/64** (Agents/Loops/Store routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 22 (font-weight migration batch: chat cluster)
- **DONE (T2.1-style, byte-identical):** migrated the inline `font-variation-settings "wght"` across `pages/chat/*` + `ui/chat/*` — ChatActivityPanel (2), ApprovalCard (2: 1 pure + 1 combined), SessionSkillsReview (2), SdlcProgressCard (2: 1 pure + 1 combined multi-line), MessageUser (2: 1 multi-prop `withWeight` + 1 pure), ChatFilePanel (1), ToolCard (1), toolRenderers/native (1), MessageAssistant (1). 10 pure → `fvs(n)`; 4 multi-prop/combined → `withWeight()`.
- **Method:** hand-edited (this cluster had several multi-line + conditional-color + multi-prop style objects that don't fit the uniform script). `native.tsx` import at `../../../` depth; ui/chat + pages/chat at `../../`.
- **Ratchet:** `inlineFontWeight` baseline **87→73** (14 sites) in lockstep.
- **Progress:** cumulative inline-weight migration 168→73 (95 sites across cycles 18–22). Remaining: the 2 mega-pages (LoopCockpit 11 + CodeCockpit 10), a scatter across tools/tasks/knowledge/settings-leaf/ui-primitives, plus the legitimate `.ts` editor internals that stay.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 73); build green; visual **64/64** (all routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 23 (font-weight migration batch: shared ui/ primitives)
- **DONE (T2.1-style, byte-identical):** migrated inline `font-variation-settings "wght"` across the shared `ui/` primitives (highest consistency value — these render on every route) — Segmented, Popover, NavRail, FilterMenu, WidgetFrame, ReactWidgetFrame, composer/controls, composer/SlashMenu, composer/MentionMenu. 9 sites across 9 files: 5 pure `fvs(n)` + 4 conditional/combined `withWeight()`/`fvs(cond ? a : b)` (Segmented preserved its `undefined`-else via `on ? withWeight(...) : { color: fg }`).
- **Ratchet nuance (important):** the ratchet regex `/fontVariationSettings:\s*["'\`]\s*"wght"/` only counts **literal-form** inline weights (a quote must immediately follow the colon). 4 of these 9 sites were **ternary-form** (a variable follows the colon: `… : selected ? '"wght" 550' : …`) and were NEVER in the scanner's count — migrating them is a real consistency win but does not move the ratchet. So the baseline dropped by the 5 literal sites only: **73→68** (NOT 62). This is the first cycle where migrated-sites ≠ ratchet-delta; recorded so the arithmetic reconciles in the audit trail.
- **Progress:** cumulative inline-weight migration now covers all shared ui/ primitives + settings + loops/code/agents(non-mega) + chat. Literal-form ratchet 168→68. Remaining literal sites: the 2 mega-pages (LoopCockpit 11 + CodeCockpit 10), ChatPage (6), and a ~20-file single-site scatter (tasks/tools/knowledge/inbox/skills/triggers/workflows/files/terminal/app-onboarding). Legit stays: `liveMarkdown.ts` (CodeMirror theme, off-ramp weights 620–700) + `consistencyAudit.report.ts` (the scanner's own regex).
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 68); build green; visual **64/64** (all routes incl. NavRail/Segmented byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 24 (font-weight migration batch: the single/double-site scatter, 28 files)
- **DONE (T2.1-style, byte-identical):** drained the entire remaining single/double-site scatter across `pages/*` + `app/*` — 34 literal-form sites over 28 files. Areas: app (ErrorBoundary, Onboarding, onboarding/StepStack), tasks (TaskDetail, TaskForm, TaskBoard, TaskGraph), tools (ToolInspector, ToolOutput, ToolsPage), inbox (InboxPage, InboxDetail), triggers (TriggerCreatePage, TriggersListPage), workflows (WorkflowsListPage, WorkflowForm), terminal (TerminalView, TerminalDrawer), knowledge (KnowledgeDetail, KnowledgeCreatePage, AudioRecorder), prompts (PromptsListPage), files (artifacts/ArtifactList, artifacts/ArtifactViewer, browse/FileViewer), skills (SkillsPage, SkillProposals, MarketplaceDetail), apps (AppsSection). 29 pure → `fvs(n)`; 5 multi-prop/combined → `withWeight()` (Onboarding: bg+color; StepStack: +fontSize; TaskGraph: +color; ToolsPage: bg+color; MarketplaceDetail: dynamic-color). All 34 sites were literal-form, so the ratchet moved by the full count.
- **Method:** scripted pure substitution with per-path depth-computed import insertion (after the first `from '…'` line — the cycle-21 hardened heuristic; verified no multi-line-import snags this time); 5 combined sites hand-edited.
- **Ratchet:** `inlineFontWeight` baseline **68→34** (34 sites) in lockstep. Ground-truth reconciled against the scanner directly (excludes `design/` + `*.test.*`): remaining 34 = LoopCockpit 11 + CodeCockpit 10 + ChatPage 6 + `liveMarkdown.ts` 7.
- **Progress:** cumulative inline-weight migration **168→34** (129 sites across cycles 18–24). The scatter is now fully drained — the ONLY remaining literal sites are the 3 deliberately risk-ordered mega-surfaces (**LoopCockpitPage 11, CodeCockpitPage 10, ChatPage 6** — conditional/many-site, last-per-risk, each gets its own dedicated cycle) plus the one legitimate `.ts` stay (`ui/composer/liveMarkdown.ts` — CodeMirror editor theme, off-ramp weights 620–700 not in the `FontWeight` type). Clean atomic stopping point for the font-weight sub-thread.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 34); build green; visual **64/64** (all routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 25 (font-weight migration: mega-page 1/3 — LoopCockpitPage)
- **DONE (T2.1-style, byte-identical):** migrated all 11 inline `font-variation-settings "wght"` in `pages/loops/LoopCockpitPage.tsx` (the first of the three deliberately risk-ordered mega-surfaces — each gets its own dedicated, separately-verified cycle). 10 pure → `fvs(n)`; 1 conditional `on ? { … } : undefined` → `on ? fvs(600) : undefined` (preserves the `undefined`-else, same pattern as cycle 23's Segmented). All literal-form, so the full 11 moved the ratchet.
- **Ratchet:** `inlineFontWeight` baseline **34→23** in lockstep.
- **Progress:** cumulative **168→23** (140 sites). Remaining literal sites: **CodeCockpitPage (10)** + **ChatPage (6)** — the last two mega-surfaces, each its own cycle — plus the legit `liveMarkdown.ts` (7) stay. (11+10+6+7 = 34 → now 23 after this page.)
- **Evidence:** typecheck clean; vitest ratchet **5/5** green at 23; build green; visual **32/32** (all routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 26 (font-weight migration: mega-page 2/3 — CodeCockpitPage)
- **DONE (T2.1-style, byte-identical):** migrated all 10 inline `font-variation-settings "wght"` in `pages/code/CodeCockpitPage.tsx` (second risk-ordered mega-surface). 3 pure → `fvs(n)`; 7 color-combined `{ color: <expr>, fontVariationSettings: '"wght" 550' }` → `withWeight({ color: <expr> }, 550)` — each preserving its exact color expression (`var(--color-info)` ×3, `var(--color-warn)` ×3, `meta.tone` ×1). All literal-form, full 10 moved the ratchet.
- **Ratchet:** `inlineFontWeight` baseline **23→13** in lockstep.
- **Progress:** cumulative **168→13** (150 sites). Remaining literal sites: **ChatPage (6)** — the final mega-surface, its own cycle — plus the legit `liveMarkdown.ts` (7) stay. (6+7 = 13.)
- **Evidence:** typecheck clean; vitest ratchet **5/5** green at 13; build green; visual **32/32** (all routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 27 (font-weight migration: mega-page 3/3 — ChatPage) ⇒ SUB-THREAD COMPLETE
- **DONE (T2.1-style, byte-identical):** migrated all 6 inline `font-variation-settings "wght"` in `pages/ChatPage.tsx` (final risk-ordered mega-surface, depth-1 import). 5 pure → `fvs(n)`; 1 combined `{ fontVariationSettings: '"wght" 550', color: c.color || … }` → `withWeight({ color: … }, 550)` (weight-then-color order; `withWeight` spreads `...style` then sets the weight, so semantically identical — style-object key order does not affect rendering). All literal-form, full 6 moved the ratchet.
- **Ratchet:** `inlineFontWeight` baseline **13→7** in lockstep.
- **✅ INLINE FONT-WEIGHT SUB-THREAD COMPLETE.** Cumulative **168→7** (156 sites migrated across cycles 18–27). Ground-truth scanner reconciled: the **only** remaining literal site is `ui/composer/liveMarkdown.ts` (7) — the legitimate CodeMirror editor-theme stay (`.cm-rmd-h1..h6`/`.cm-rmd-strong` off-ramp weights 620/640/660/680/700, outside the `FontWeight` type, inside multi-prop CSS objects). It correctly stays; the baseline (7) now equals it exactly, so any *new* inline weight anywhere still turns CI red. Every migratable inline `fontVariationSettings "wght"` in the app now routes through `fvs()`/`withWeight()`. Clean atomic completion of the sub-thread.
- **Evidence:** typecheck clean; vitest **89/89** (ratchet green at 7); build green; visual **64/64** (all routes byte-identical, no baseline moved).

### 2026-07-23 — S2 cycle 28 (form-family convergence, part 1/2: delete the dead primitive) — Owner task 2
- **Context (the taste call, now resolved by judgement per owner delegation "go with your judgement"):** the S1 audit and cycle-16 correction left TWO form-field families coexisting: `ui/TextField.tsx` (the low-level primitive — `TextField`/`TextArea` + `textFieldClass`/`textAreaClass`) and `pages/tasks/formControls.tsx` (`Field`/`TextInput`/`TextArea`/`DateInput`/`Select`/`ChipInput` + task editors). The cycle-16 header comment explicitly flagged collapsing them as an Owner task-2 "two patterns → one" taste call. **Ground truth settled it:** `ui/TextField` has **ZERO adopters** (grep across `web/src`: only its own test imports it) — it is dead code. `formControls`' generic family is the de-facto standard (**23 importers**) and carries better a11y (the `Field`→`aria-labelledby` label-context wiring). So the "risky two-patterns merge" is actually a **clean-break dead-code removal**: keep the adopted family, delete the unused primitive. No new visual language; consistency-not-redesign preserved — in fact the competing shape is *removed*.
- **DONE (clean break):** `git rm web/src/ui/TextField.tsx web/src/ui/TextField.test.tsx` (primitive + its class-contract test — the test only asserted the deleted helpers' class strings, so it dies with the file). Updated the 3 now-stale `TextField/TextArea`-primitive references in the ratchet's guidance strings/comments (`primitiveAdoption.test.ts` ×2 + `primitiveAdoption.baseline.json` `_comment`) to name the real shared form family (`Field/TextInput/TextArea/Select`) instead of the removed primitive — so the CI failure hint points contributors at a family that still exists.
- **Ratchet:** unchanged. `ui/TextField.tsx` lived in the `ui/`-exempt tree (`scanPrimitives` skips `rel.startsWith('ui/')`), so its raw `<input>`/`<textarea>` were never counted; deleting it moves no primitive count. Baseline stays `{ rawButton: 377, rawInput: 206, rawDialog: 0, inlineFontWeight: 7 }`.
- **Zero pixels:** dead code (0 render sites) — nothing on any surface can change. Verified anyway.
- **Evidence:** typecheck clean; vitest **82/82** (was 84 — the 2 deleted primitive class-contract tests are gone; ratchet still green at the unchanged baseline); build green; visual **64/64** (all routes byte-identical, no baseline moved).
- **NEXT (cycle 29, part 2/2):** relocate the `formControls` *generics* (`Field`/`useFieldLabelId`+`FieldLabelCtx`/`TextInput`/`TextArea`/`DateInput`/`Select`/`ChipInput` + the `Segmented` re-export) into a new `web/src/ui/forms.tsx` so the canonical form family lives under `ui/` with the other primitives; the task-specific editors (`DependencyEditor`/`ChecklistEditor`/`NotesEditor`, which import `./dag`/`./taskMeta`/`TaskNote`) stay in `pages/tasks/formControls.tsx` (a `ui/ → pages/` dep would be backwards). Split, not rename: 23 importers repoint generics to `ui/forms`; `TaskForm.tsx` gets a split import. Moving 5 generic raw-input-bearing components into the `ui/`-exempt tree drops them from `rawInput` — ratchet DOWN by the live-measured count (do not guess). Byte-identical; own gated commit.

### 2026-07-23 — S2 cycle 29 (form-family convergence, part 2/2: relocate generics → ui/forms) — Owner task 2
- **DONE (byte-identical relocation):** created `web/src/ui/forms.tsx` holding the canonical generic form family — `Field` + `useFieldLabelId`/`FieldLabelCtx` (the `aria-labelledby` label-context a11y wiring), `TextInput`, `TextArea`, `DateInput`, `Select`, `ChipInput`, and the `Segmented`/`SegOption` re-export. Every export is a **byte-identical copy** of its `formControls` original; the *only* substantive change is the `Segmented` re-export path (`'../../ui/Segmented'` → `'./Segmented'` now that it lives in `ui/`). `pages/tasks/formControls.tsx` is trimmed to just the three task-specific editors (`DependencyEditor`/`ChecklistEditor`/`NotesEditor`) + their real deps (`framer-motion`, `IconButton`, `Bud`/`Reorderable`, `spring`, `./dag`, `./taskMeta`, `TaskItem`/`TaskNote`) — they stay in `pages/` because a `ui/ → pages/` domain dependency would be backwards.
- **Importers repointed (ground-truthed, not guessed):** grep found **24** importer files. **23** pull only generics → path rewritten `'../tasks/formControls'` → `'../../ui/forms'` (all at `pages/X/` depth, uniform). **`TaskForm.tsx`** is the sole file pulling both → split into two imports (generics from `'../../ui/forms'`, the 3 editors from `'./formControls'`). Verified no external consumer imports `useFieldLabelId`/`FieldLabelCtx` (only the module itself), and every `Segmented`/`SegOption` direct import elsewhere already points at `ui/Segmented` (untouched). `AgentDetail.tsx`'s `RoutingNotesEditor` was a substring false-positive, not an importer.
- **Ratchet DOWN (live-measured via `scanDrift()`):** `rawButton` **377→376** (−1) and `rawInput` **206→201** (−5), in lockstep in the same commit. The −5 inputs are TextInput/TextArea/DateInput/Select + ChipInput's field; the −1 button is ChipInput's chip-remove `<button>` — all moved into the `ui/`-exempt tree (`scanPrimitives` skips `rel.startsWith('ui/')`), so they leave the count legitimately. `inlineFontWeight` (7) / `rawDialog` (0) unchanged. Baseline now `{ rawButton: 376, rawInput: 201, rawDialog: 0, inlineFontWeight: 7 }`.
- **Design-hook (impeccable) font-size findings:** the relocated/trimmed files re-flagged pre-existing `text-[0.7rem]`/`text-[0.75rem]`/`text-[0.8125rem]`/`text-[0.875rem]` literals (off the DESIGN.md ramp). Classified **intentional-existing, left unchanged**: they are byte-identical copies of shipped code, this cycle is a pure relocation (normalizing them would move Playwright baselines — forbidden here — and is a redesign that must apply uniformly across all form surfaces, not smuggled into a file-move). No inline-disable added; suppression waits on explicit owner sign-off.
- **Zero pixels:** byte-identical component bodies; only import paths + file locations changed. Confirmed.
- **Evidence:** typecheck clean (validates all 25 rewired imports; strict `noUnusedLocals` = no dangling import from the split); vitest **82/82** (ratchet green at the tightened baseline); build green; visual **64/64** (all routes byte-identical, no baseline moved). Regenerated `docs/design/consistency-audit.json` reflects only the expected count deltas (formControls 7btn/8in → 6btn/3in; `filesScanned` +1 for the new file; a11y occurrences conserved).
- **✅ FORM-FAMILY CONVERGENCE COMPLETE (Owner task 2).** One canonical form family, under `ui/` with the other primitives; the dead competing primitive removed (cycle 28); task editors correctly isolated in `pages/tasks/`. Every form call-site in the app now imports from a single `ui/forms` home.

### 2026-07-23 — S2 cycle 30 (T2.3: codify the standard-field size/surface scale)
- **Context (owner directive):** owner asked for "the right thing by the system — a correct combination of option 2 (standardize input sizes: canonical field-height scale, family variants, accepting deliberate pixel changes for off-scale outliers) and option 3 (grow the family API: type/onBlur/disabled/leadingIcon)". Soul guardrail held: **consistency, not redesign.**
- **DONE (T2.3 — the scale, byte-identical):** `TextInput` now carries a principled **size** (sm/md/lg → h-8/h-9/h-10) × **surface** (container/high/base) scale via `FIELD_SIZE`/`FIELD_SURFACE` tables + a shared `INPUT_BASE` invariant-chrome const. Defaults (lg/container/0.9375rem) reproduce the prior fixed chrome **token-for-token** — verified by `forms.test.tsx` render assertions (7 tests: byte-identical defaults, on-ramp size steps with a negative guard that no size emits the off-ramp `text-[0.875rem]`, surface-swaps-only-fill, TextArea/Select default chrome). Every size uses a DESIGN.md-blessed type size (0.8125rem/0.9375rem); 0.875rem/14px stays off-ramp drift, never a tier.
- **DONE (adopter):** the one byte-identical raw input — `AgentForm` CheckList search — migrated to `TextInput size="sm" surface="base"`. Ratchet `rawInput` **201→200** in lockstep.
- **DELIBERATE SCOPE CORRECTION (clean-break tenet):** an initial pass of this cycle also grew the *behavioral/structural* API (`type/onBlur/disabled/inputMode/min/max/step/maxLength/spellCheck/required/ref/leadingIcon`) ahead of adopters. A full audit of **all 139 raw inputs** falsified that: (a) the numeric-stepper props (`min/max/step/inputMode`) serve only `type=number` fields, and **every** stepper (ScheduleForm/ChatPanel/MemoryPanel/InboxSettingsPanel ×2) diverges on **width** (`w-24`/`w-28`/`w-20` vs the primitive's `w-full`) + `text-right`/`tabular-nums` — a distinct compact role a full-width field cannot render, so those props are **permanently dead here**; (b) the owner-requested props (`type`/`onBlur`/`disabled`/`leadingIcon`) have **no same-commit adopter** — their honest adopters (ToolsPage `type=password`+mono fields; the compact-search cluster `leadingIcon` via a reviewed 1px geometry snap) land in the follow-on cluster migrations. Per **clean break** (no API a caller doesn't yet use), the props were trimmed; `TextArea`/`DateInput`/`Select` reverted to their exact pre-scale bodies. They + each behavioral prop **grow in lockstep with the first real adopter, never ahead of one** — so option 3 lands *correctly* (adopter-backed) rather than speculatively. End state the owner asked for; only commit sequencing changed so no commit ships dead API.
- **Design-hook (impeccable) findings:** `forms.tsx` L172 (`text-[0.875rem]`, ChipInput draft field) is **pre-existing** drift (byte-identical to commit `1c76149`, shifted line only because the trim shrank the file) — classified intentional-existing, unchanged (T2.1 normalization, separate + uniform). `forms.test.tsx` L53/L57 are **test-assertion string literals** (the on-ramp expectation + the negative off-ramp guard), not applied styles — the mechanism that *enforces* the ramp; intentional false positives, unchanged. No suppressions added (await explicit owner sign-off).
- **Zero pixels:** default `TextInput` render is token-for-token the pre-scale field (the trim even dropped Phase-A's inert `disabled:opacity-50`, restoring exact parity); `AgentForm` search migration byte-identical at rest. Confirmed.
- **Evidence:** typecheck clean; vitest **87/87** (ratchet green at 200; −2 vs prior 89 = the two dead-API tests removed with the trim); build green; visual **64/64** (all routes byte-identical, no baseline moved). `consistency-audit.json` timestamp-only churn reverted (no count delta — no raw-input site changed beyond the already-counted AgentForm migration).
- **Baseline now `{ rawButton: 376, rawInput: 200, rawDialog: 0, inlineFontWeight: 7 }`.**
- **NEXT (cluster migrations):** grow `leadingIcon` in lockstep with the compact-search cluster (ArchivePanel/AuditPanel/MemoryPanel — bare leading-icon filters), a **deliberate reviewed pixel snap** (`left-2.5→left-3`, `pl-8→pl-9`) — owner review required (pixels move). Byte-identical adopters that need no new prop (ToolsPage mcp fields → `size="md" surface="high"`, once a `mono` prop or equivalent lands with its first `font-mono` adopter) proceed autonomously. Compound searches (ModelsPanel/TasksListPage: trailing clear-button + `type=search`) and numeric steppers stay distinct roles for dedicated primitives, not forced onto the standard field.

### 2026-07-23 — S2 cycle 31 (cluster migration 1: ToolsPage add-server modal → the scale) — owner-authorized density fix
- **Context (owner directive, "Proceed — treat as intended fixes"):** the cycle-30 density audit established that the primitive's `INPUT_BASE` uses density-aware `px-m` (`= calc(12px * var(--space-scale))`) while the hand-rolled ToolsPage inputs used fixed `px-3` (`= 12px`). `px-m == px-3` **only at comfortable density** — at dense/cli the primitive tightens and the raw field does not. So no remaining raw-input cluster is a truly free byte-identical swap; every migration moves pixels at dense/cli. Owner reviewed this and authorized proceeding, treating the density-correction as a deliberate improvement (updating Playwright baselines where pixels legitimately move; growing behavioral props in lockstep with adopters).
- **DONE (T2.3 adopter — first cluster):** migrated the **7** raw `<input>` in `AddToolServerModal` (`pages/tools/ToolsPage.tsx`) — mcp branch (name/command/args) + openai branch (name/endpoint/apiKey/toolFilter) — to `<TextInput size="md" surface="high" [mono] [type="password"]>`. The `mcpInputCls` shape (`h-9` + `bg-surface-high` + `px-3` + `text-[0.8125rem]` + shared focus/placeholder chrome) maps token-for-token onto `size="md"` (`h-9 text-[0.8125rem]`) × `surface="high"` (`bg-surface-high`), with `px-3`→`px-m` the sole authorized density-correction. The env `<textarea>` (distinct `min-h-16 py-2` shape) stays raw; `mcpInputCls` remains for it.
- **DONE (option 3, adopter-backed prop growth — never ahead of an adopter):** grew `TextInput` with exactly the two props these 7 fields need — `type?: 'text' | 'password'` (the API-Key field masks its secret) and `mono?: boolean` (command/args/endpoint/apiKey/toolFilter are `font-mono` technical values, mirroring `TextArea`'s existing `mono`). Both are honest, same-commit-adopted; no speculative API. Default `type` stays absent (byte-identical to the pre-scale field, which set none) — locked by `forms.test.tsx`'s `getAttribute('type')).toBeNull()` assertion.
- **Ratchet DOWN (live-measured via `scanDrift()`, not guessed):** `rawInput` **200→193** (−7) in lockstep in the same commit; `rawButton` (376) / `rawDialog` (0) / `inlineFontWeight` (7) unchanged. `consistency-audit.json` regenerated: ToolsPage raw-input **8→1** (the 1 remaining is the deliberately-left env textarea), score 24→17; `generatedAt` timestamp rides along with the real count delta.
- **a11y note (unchanged):** ToolsPage has a LOCAL bespoke `Field` (its own duplicate, not the canonical `ui/forms` one) that publishes **no** `FieldLabelCtx`, so `TextInput` inside it emits no `aria-labelledby` — byte-identical accessible-name behavior to the raw inputs it replaced (each was label-associated only visually). The generated `id` is unreferenced/non-visual. Consolidating ToolsPage's local `Field` onto the canonical one is a separate future task, not smuggled in here.
- **Deliberate pixel change (owner-authorized, review-pending):** at **dense/cli** density the 7 fields now tighten with the rest of the app (correct density-doctrine behavior); at **comfortable** density they are byte-identical. The visual harness runs at comfortable, so it stays **64/64 green with no baseline change** — the authorized pixel move is only observable at dense/cli, which the harness does not yet snapshot. Flagged for owner review of dense/cli screenshots.
- **Design-hook (impeccable):** the shifting-line re-flags of pre-existing off-ramp literals (`forms.tsx` ChipInput `text-[0.875rem]`; `forms.test.tsx` L53/L57 test-assertion literals; ToolsPage's own pre-existing `text-[…]` findings) are all classified pre-existing/intentional as in cycle 30 — no suppressions added (await explicit owner sign-off).
- **Evidence:** typecheck clean; vitest **87/87** (ratchet green at 193); build green; visual **64/64** (comfortable-density byte-identical, no baseline moved). Gateway live on `127.0.0.1:10400` for review.
- **Baseline now `{ rawButton: 376, rawInput: 193, rawDialog: 0, inlineFontWeight: 7 }`.**
- **NEXT (cluster 2):** compact-search cluster (ArchivePanel/AuditPanel/MemoryPanel) — grow `leadingIcon` + the deliberate 1px geometry snap (`left-2.5→left-3`, `pl-8→pl-9`); surface before/after screenshots for owner review since that snap moves pixels at *all* densities.

### 2026-07-23 — S2 cycle 32 (cluster migration 2: the bare leading-icon search fields → the scale) — owner-authorized geometry decision
- **Context (owner decision via AskUserQuestion):** the audit premise from cycle 31 ("3 outliers snapping onto a single canonical geometry") was FALSIFIED on full inspection — the app shipped **two** distinct leading-icon search geometries, not one: `left-2.5`/`pl-8` (the plurality, ~8 fields, mostly in un-snapshotted Settings sub-panels) vs `left-3`/`pl-9` (~6 fields). Rather than proceed on the stale premise I surfaced the two-geometry reality to the owner. **Owner blessed `left-3`/`pl-9`** — the geometry the deleted `ui/TextField` primitive had *defined* but which never shipped (0 adopters) — as canonical. Consequence the owner explicitly accepted: the `left-2.5`/`pl-8` fields snap OUT by ~1px, and most live in Settings sub-panels the top-level route harness does not snapshot, so there are few reviewable snapshots for the movers.
- **DONE (option 3, adopter-backed prop growth):** grew `TextInput` with exactly one new prop — `leadingIcon?: ReactNode` — replicating the deleted primitive's exact geometry: a `pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-on-surface-low` icon span wrapping the input, with the input taking `pl-9 pr-m` (clears the fixed `left-3` glyph; keeps the canonical density-aware right pad). `INPUT_BASE` lost its baked-in `px-m` so padding applies conditionally (`leadingIcon ? 'pl-9 pr-m' : 'px-m'`) — the two never both emit, avoiding a `padding-inline`+`padding-left` cascade race. This is *consistency, not redesign*: the shape already existed in-tree.
- **DONE (T2.3 adopters — 6 bare leading-icon filters):** migrated to `<TextInput … leadingIcon={<Search size={14} />}>`:
  - `settings/ArchivePanel.tsx` (`size="md" surface="high"`), `settings/AuditPanel.tsx` (`md`/`high`), `settings/MemoryPanel.tsx` **only the MemoryAuditPanel filter** (`md`/`high`) — the :272 studio search (off-ramp `text-[0.8rem]`) is HELD for a separate T2.1 text-normalization task, not smuggled in.
  - `workflows/WorkflowForm.tsx` WorkflowRefPicker (`sm`/`base`, `autoFocus`), `files/artifacts/ArtifactList.tsx` (`sm`/`high`), `tasks/formControls.tsx` DependencyEditor picker (`sm`/`base`, `autoFocus`, load-bearing random `name`).
- **a11y fix (root-caused, not worked around):** DependencyEditor's raw input emitted BOTH a random `name` (anti-autofill) AND `aria-label`. `TextInput`'s accessible-name logic was `aria-label={!labelId && !name ? ariaLabel : undefined}` — a `name` suppressed an explicit `ariaLabel`, silently dropping the accessible name (a `name` is NOT an accessible name). Fixed the shared logic to `claimsFieldLabel = !!labelId && !name` → `aria-labelledby={claimsFieldLabel ? labelId : undefined}` / `aria-label={claimsFieldLabel ? undefined : ariaLabel}`, so a `name` never suppresses `ariaLabel`. Verified byte-identical for every other caller (the `AppsSection` `name=` callers pass no `ariaLabel`; the `ariaLabel`-passing callers are all bare filters outside any `Field`, so `labelId` is undefined). Locked by a new `forms.test.tsx` assertion (`ariaLabel survives a name`).
- **Ratchet DOWN (live-measured via `scanDrift()`, not guessed):** `rawInput` **193→187** (−6, one per migrated field) in the same commit; `rawButton` (376) / `rawDialog` (0) / `inlineFontWeight` (7) unchanged. `consistency-audit.json` regenerated: exactly six raw-input decrements (ArchivePanel/AuditPanel/ArtifactList 1→0, MemoryPanel 9→8, formControls/WorkflowForm 3→2) — no collateral.
- **Deliberate pixel change (owner-authorized, low snapshot coverage):** converging onto `left-3`/`pl-9 pr-m` moves the `left-2.5`/`pl-8` movers ~1px at *all* densities (plus the usual `px`→`px-m` density behavior at dense/cli). All 6 movers live in interaction-gated pickers (WorkflowForm/DependencyEditor) or un-snapshotted Settings sub-panels + the Files artifacts rail, so the top-level route harness captures none of them — visual stays **64/64 green with no baseline change**. The owner explicitly accepted this low coverage when blessing the `left-3`/`pl-9` geometry.
- **Design-hook (impeccable):** `forms.test.tsx` L53/L57 (on-ramp assertion + `text-[0.875rem]` off-ramp *negative guard*) re-flagged as before — test-assertion literals that MUST stay verbatim for the scale test to red on drift; classified intentional, no suppressions added.
- **Validation finding (driven from the UI, then fixed):** opening the WorkflowForm ref-picker in the browser revealed its search reported its accessible name as **"Steps"** (inherited from the enclosing `<Field label="Steps">` via `aria-labelledby`), making the `ariaLabel="Find a workflow to add"` I passed **dead** — and inconsistent with its structurally-identical DependencyEditor sibling (which reads "Find a prerequisite task"). Root cause: the picker had no `name`, so `claimsFieldLabel` was true and the Field label won. Fix (mirrors DependencyEditor): give it a random `name={`wf-ref-search-${useId()}`}` — defeats autofill AND keeps its own label, honoring the Field's documented "multi-control Fields keep per-input labels" contract. This picker was the ONLY one of the 6 adopters nested inside a `Field`; the other 4 are bare filters where `ariaLabel` already wins. Behavioral-only (adds a `name` attr) — no primitive-count / pixel change.
- **Evidence:** typecheck clean; vitest **89/89** (+2: leadingIcon geometry + ariaLabel-survives-name; ratchet green at 187); build green; visual **64/64** (no baseline moved); UI-validated the Audit filter + both pickers on the live gateway (accessible names correct). Gateway live on `127.0.0.1:10400` for review.
- **Baseline now `{ rawButton: 376, rawInput: 187, rawDialog: 0, inlineFontWeight: 7 }`.**
- **NEXT (cluster 3):** the compound-search cluster (LocalModelManager `pr-8`, FilesSection `pr-7`, ChatPage/ModelsPanel/TasksListPage) — `type="search"` + a clear button is a distinct role; likely its own dedicated primitive that inherits the `left-3`/`pl-9` geometry rather than overloading `TextInput`. Then numeric steppers (ScheduleForm/ChatPanel/MemoryPanel/InboxSettingsPanel×2 — distinct role) and the off-ramp text-normalization (MemoryPanel:272 etc., T2.1).

### 2026-07-23 — cycle 33 (validation finding: settings bento inline switches were inert — interaction correctness fix)
- **Owner-reported bug (validate-as-a-user):** on the Settings home the bento cards expose inline controls (switches/segmented/selects) for direct interaction, but "the toggles are not interactable." Reproduced + root-caused rather than worked around.
- **ROOT CAUSE:** `pages/settings/bento.tsx` `Switch` wrapped the canonical `Toggle` in `<span … onClickCapture={(e) => e.stopPropagation()}>`. `onClickCapture` fires in the **capture phase** (root→target); `stopPropagation()` there halted the click *before* it reached the inner `<button role="switch">`, so `Toggle`'s own `onClick` never fired — every inline switch on the settings cards was inert. The sibling controls `SegToggle` (L154) and `InlineSelect` (L171) already stop propagation in the **bubble phase** (plain `onClick` on the element), which lets the target's handler run first, *then* blocks the bubble to the full-card nav overlay. `Switch` was the lone outlier — the only `onClickCapture` in the entire `web/src` tree.
- **DONE (fix, one-token):** `onClickCapture` → `onClick` on the `Switch` wrapper span (bubble-phase stop, matching its siblings) + a comment explaining the phase choice. The click now reaches the switch button (fires the toggle) and is then prevented from bubbling to the nav overlay.
- **Guard (new `bento.test.tsx`):** two tests locking both halves of the card-control contract — (1) clicking the switch fires its `onToggle` AND does not reach an enclosing (nav-overlay stand-in) `onClick`; (2) a `disabled` switch is a no-op. Proven to FAIL against the pre-fix capture-phase code (`onToggle` called 0 times) and pass after — a true regression guard, not a tautology.
- **Zero visual / zero primitive-count change:** behavior-only (event phase); no raw element added/removed, no pixels moved. `bento.test.tsx` is a `.test.tsx` so the scanner excludes it — ratchet + audit JSON unchanged (`{ rawButton: 376, rawInput: 187, rawDialog: 0, inlineFontWeight: 7 }`).
- **Evidence:** typecheck clean; vitest **91/91** (+2 bento tests; ratchet green at 187); build green; visual **64/64** (no baseline moved); UI-validated on the live gateway — clicked "Send on Enter" on the Chat card: it toggled off→on and stayed on `#/settings` (no unwanted navigation), round-tripped back to its original state. Committed atomically (`fix(settings):`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, not pushed).

### 2026-07-23 — cycle 34 (cluster 3: the compound-search primitive — SearchField, 12 fields) — owner-authorized scope & style
- **Context (owner decisions via AskUserQuestion):** cycle 32's "NEXT" foreshadowed this — the compound-search cluster (`type="search"` + a clear affordance) is a distinct role from the bare leading-icon `TextInput`, so it gets its OWN `ui/` primitive rather than overloading `TextInput`. Two owner rulings framed the build: (1) **clear-(X) style = "spring-pop circle"** (soft `rounded-full size-6` circle, framer-motion `AnimatePresence` scale 0→1 `bounce.subtle`, `whileTap` 0.88, `X size 14`, idle `text-on-surface-low` / hover `bg-surface-highest`); (2) **scope = "Everything (A + B)"** — migrate ALL compound-search fields, both the absolute-overlay family (Family A, 8 fields) AND the flex-row chip family (Family B, 4 palette fields), despite the AskUserQuestion preview flagging Family B as bordering on redesign.
- **Soul-honoring mitigation for the A+B scope (consistency, not redesign):** ONE primitive, TWO structural variants, because the app genuinely ships two search shells and folding one onto the other WOULD be a redesign. `overlay` (default) codifies the dominant list/page search (relative box, magnifier pinned `left-3`, input `pl-9 pr-9`, clear pinned `right-2.5`, owns `type="search"` + Escape-to-clear + the size→height/radius scale + focus ring). `inline` preserves each palette's own transparent flex-chip identity (bg/radius/padding stay on the caller's row; the input is `bg-transparent`, the magnifier + trailing slot are flex children; no height, no focus ring). Every value is a shape already in-tree — the clear-X converges on the owner's spring-pop, the geometry inherits the cycle-32-blessed `left-3`/`pl-9` inset.
- **DONE (the primitive):** `web/src/ui/SearchField.tsx` — `SearchField` with `variant` (overlay|inline), `size` (sm/md/lg → overlay h-8/9/10 @ 0.8125/0.8125/0.9375rem, rounded-md/md/pill), `surface`, built-in spring-pop `ClearButton` (`dense` shrinks it for compact inline rows), `clearable` opt-out (the ⌘K/⌘P palettes clear by Esc/backspace convention — they decline the button), `clearOnEscape` (inline opt-in; overlay always clears), `trailingSlot` (a `<kbd>esc</kbd>`/spinner after the clear-X), `inlineIconSize`, and passthroughs (`inputRef`, `onFocus`, `spellCheck`/`autoCapitalize`/`autoCorrect`, `name`). Guarded by `web/src/ui/SearchField.test.tsx` (13 tests: named type=search, ariaLabel-over-placeholder, clear-X show/click/Escape per variant, caller-onKeyDown pre-empt, clearable opt-out, trailing slot, inputRef, **+ the two validate-as-a-user regression guards below**).
- **DONE (12 field migrations — Family A overlay, 8):** `app/CommandPalette.tsx`* , `pages/settings/SettingsHome.tsx` (hero), `pages/settings/DiagnosticsPanel.tsx`, `pages/settings/LocalModelManager.tsx`, `pages/settings/ModelsPanel.tsx`, `pages/files/FilesSection.tsx`, `pages/code/WorkspacePicker.tsx`, `ui/ListControls.tsx` (in-`ui/`, ratchet-exempt). **Family B inline, 4:** `app/CommandPalette.tsx` (⌘K), `pages/chat/PromptPalette.tsx`, `pages/code/CodeCockpitPage.tsx` (⌘P quick-open), `pages/tasks/TasksListPage.tsx`. Each migration pruned only truly-dead lucide symbols (`Search`/`X` removed where the file no longer hand-rolls them; kept where still used elsewhere) — verified by a clean typecheck.
- **DEVIATION (documented) — ListControls lost its focus-within magnifier micro-animation:** the shared primitive draws a static leading magnifier (the 7-of-8 overlay majority), so ListControls' bespoke focus-within icon nudge was dropped to match the majority rather than special-case the primitive. Consistency-over-flourish, flagged for owner review.
- **Deliberate pixel move (owner-authorized) — SettingsHome hero search normalized `h-11`/`0.95rem` → lg `h-10`/`0.9375rem`:** an off-ramp size snapping onto the blessed scale (~4px vertical reflow). This is the ONE mover the top-level route harness snapshots → **Settings light+dark baselines regenerated intentionally** (`e2e/__screenshots__/visual.spec.ts/settings-{light,dark}-darwin.png`), recorded here per the "regenerate touched surfaces intentionally, never silently keep/revert" rule. All other movers live in interaction-gated palettes/pickers the route harness doesn't capture.
- **Two validate-as-a-user regressions caught in the live UI (pure code review missed both) and root-caused, not worked around:**
  1. **Inline focus ring bleed** — the inline palette input inherited `focus:ring-2 focus:ring-inset focus:ring-primary/50` from the shared `INPUT_CHROME`, drawing a Coral inset RECTANGLE inside the rounded ⌘K row (new chrome = a redesign of a hero surface). Verified against git HEAD that all 4 inline originals shipped `outline-none` with NO ring → split the ring into overlay-only `OVERLAY_FOCUS`; inline keeps `outline-none`. Confirmed live at computed-style level (inline className `outline-none text-[0.9375rem]`, boxShadow none). **Locked by a new unit assertion** ("applies the inset focus ring only in the overlay variant").
  2. **Native `::-webkit-search-cancel-button` glyph** — `type="search"` makes Chromium auto-render a native `×` once there's a value; it double-rendered beside the spring-pop clear-X and gave the `clearable={false}` palettes a clear button they declined. The originals had no `type` so never showed it. Suppressed with `[&::-webkit-search-cancel-button]:hidden` on `INPUT_CHROME` (both variants). Confirmed live: the compiled rule is in the served bundle and the Settings hero shows exactly ONE clear affordance (the 24×24 spring-pop circle), no double glyph; the ⌘K palette shows none. **Locked by a new unit assertion** ("suppresses the native webkit search-cancel glyph in both variants").
- **FALSE POSITIVE (classified, unchanged, no suppression):** `CodeCockpitPage.tsx` L740/741 side-tab hook finding — a toast card `borderLeft: 3px solid ${tone}` where `tone` is a severity color. Domain-appropriate, pre-existing, out of this task's scope. Left as-is with no inline suppression (impeccable suppressions need explicit owner sign-off).
- **Ratchet DOWN (live-measured via `scanDrift()`, not guessed) in the same commit:** `rawButton` **376→369** (−7: migrated fields whose clear-X was a literal `<button>`), `rawInput` **187→176** (−11: the 11 non-`ui/` field migrations — ListControls is in `ui/`, exempt); `rawDialog` (0) / `inlineFontWeight` (7) unchanged. `consistency-audit.json` regenerated and KEPT (not reverted): the diff is substantive, not timestamp-only — `filesScanned` 299→300 (the new SearchField.tsx), CodeCockpitPage 5→4 raw-input, ChatPage 11→10, FilesSection/WorkspacePicker/DiagnosticsPanel/TasksListPage decrements, files re-ranked — a deliberate DEVIATION from the "revert timestamp churn" habit, justified because the payload carries real signal.
- **Evidence:** typecheck clean; vitest **104/104** (+13 SearchField, of which 2 are the regression guards; ratchet green at 369/176; consistencyAudit green); build green (SPA served live via the `static/dist` symlink, bundle `index-DPC_vnqT.js`); visual **64/64** (only the two authorized Settings baselines moved). UI-validated every migrated surface on the live gateway `127.0.0.1:10400` (empty-seed E2E instance): ⌘K palette (inline, clearable=false — no ring, no glyph, no clear button), Settings hero (overlay, clearable — single spring-pop circle, ring on focus, no native glyph). Committed atomically (`feat(design):`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **Baseline now `{ rawButton: 369, rawInput: 176, rawDialog: 0, inlineFontWeight: 7 }`.**
- **NEXT (cluster 4):** the remaining bare-filter/off-ramp text normalizations (MemoryPanel:272 studio search @ `text-[0.8rem]`, T2.1) and the numeric steppers (ScheduleForm/ChatPanel/MemoryPanel/InboxSettingsPanel×2 — a distinct role, its own primitive). The compound-search cluster is now fully consolidated onto SearchField.

  \* CommandPalette appears in both families — its ⌘K modal is the inline field; it also hosted a leading-`Search` import that the migration pruned.

### 2026-07-23 — cycle 35 (cluster 4a: the canonical numeric stepper — NumberField, 3 verbatim dups) — byte-identical, autonomous
- **Context:** cycle 34's "NEXT" foreshadowed the numeric steppers as "a distinct role, its own primitive." Scope this cycle = the THREE near-**verbatim-duplicated** clamp-on-blur steppers, which share BOTH the exact chrome AND the identical fiddly behavior (local string state so a half-typed value isn't clobbered mid-edit; on blur/Enter → parse → clamp to `[min,max]` → commit only if changed; revert an empty/NaN entry to the last good value): `ChatPanel.NumberInput` (+ its `NumberRow` wrapper's inline twin), `AgentDefaultsPanel.NumberRow`'s inline `<input type=number>`, and `InboxSettingsPanel.NumDays`. This is a true byte-identical consolidation — it dedups LOGIC, not just chrome — so it proceeded autonomously (no taste call to surface). The other numeric inputs (ScheduleForm cadence, MemoryPanel weights) are genuinely different roles and stay out of scope.
- **DONE (the primitive):** `web/src/ui/forms.tsx` `NumberField` — `value`/`onChange`/`min`/`max`/`step`/`ariaLabel` + a single visual axis `width` (`w-24` default; `w-20` opt for the narrow Inbox field — the ONE axis the three dups varied). Chrome is invariant and reproduces the prior hand-rolled field token-for-token (`h-8 rounded-md bg-surface-high px-2 text-right text-[0.8125rem] text-on-surface tabular-nums outline-none focus:ring-2 focus:ring-inset focus:ring-primary/50`). Deliberately NOT the standard `TextInput` size/surface scale: a stepper is a distinct role (fixed-width, right-aligned, tabular-nums) that must not be forced onto the full-width text field — same call cycle 30 made. Accessible-name resolution mirrors the family: an explicit `ariaLabel` wins; else it claims a wrapping `ui/forms` `Field` label via `aria-labelledby` (`useFieldLabelId()`). `settingsUI.Row` is a plain label div (NOT a `FieldLabelCtx` provider), so all three call sites pass `ariaLabel={label}` — a pure a11y GAIN, since the raw inputs they replace had **no accessible name at all**.
- **DONE (3 call-site migrations):** `pages/settings/ChatPanel.tsx` (deleted the local `NumberInput` component; both its call sites — the warm-pool-size `Row` and the `NumberRow` helper — now render `NumberField`), `pages/settings/AgentDefaultsPanel.tsx` (`NumberRow` renderer's inline input → `NumberField`; 5 live steppers: max-subagents/max-turns/timeout/min-memory/soft-stop), `pages/settings/InboxSettingsPanel.tsx` (`NumDays` helper inlined at the call site as `NumberField width="w-20"` + the kept "days" suffix span; dead helper deleted). Backend serialization invariant verified: `AppConfig.to_dict()` uses `asdict(self.agent)` so every `agent.*` field is always present — AgentDefaults' prior `?? ''` fallback normalizes safely to `Number(cfg[field] ?? min ?? 0)` with zero pixel/behavior change (the panel also gates on `FormSkeleton` until `cfg` loads).
- **Ratchet DOWN (live-measured via `scanDrift()`, not guessed) in the same commit:** `rawInput` **176→173** (−3: the three non-`ui/` field migrations; the primitive itself lives in `ui/`, exempt). `rawButton` (369) / `rawDialog` (0) / `inlineFontWeight` (7) unchanged.
- **Evidence:** typecheck clean; vitest **113/113** (+9 new `NumberField` tests in `forms.test.tsx`: default+width chrome byte-check, clamp-to-max on blur, no-commit-when-unchanged, empty/NaN revert, Enter-commits, external-value resync, ariaLabel name, Field `aria-labelledby` claim; ratchet green at 173); build green (SPA served live via the `static/dist` symlink, bundle `index-DkYJj9np.js`); visual **64/64 — NO baseline moved** (widths preserved exactly → byte-identical; the Settings route shell is pixel-unchanged). UI-validated all three panels live on `127.0.0.1:10400` (empty-seed E2E instance, verified served bundle hash): all 9 steppers render at the exact prior geometry (`w-24`=96px / `w-20`=80px, `h-8`=32px) with correct accessible names + suffixes; clamp-on-blur confirmed end-to-end through the real backend PATCH round-trip (Agent max-subagents 99→16, Inbox retention 0→1, Chat autocompact 5000→90); the conditional warm-pool-TTL row correctly appears when pool size >0; all dev state restored. Committed atomically (`refactor(design):`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **Baseline now `{ rawButton: 369, rawInput: 173, rawDialog: 0, inlineFontWeight: 7 }`.**
- **NEXT (cluster 4b):** the remaining bare-filter/off-ramp text normalizations (MemoryPanel:272 studio search @ `text-[0.8rem]`, a T2.1 text-scale normalization — don't smuggle it into a chrome migration). The verbatim-duplicated numeric-stepper role is now consolidated onto NumberField; any future stepper adopts it rather than hand-rolling.

### 2026-07-23 — cycle 36 (cluster 4b: T2.1 type-scale normalization — app-wide off-ramp `text-[…]` sweep + ramp reconciliation) — pixel-moving, owner-directed
- **Context:** cycle 35's "NEXT" flagged the off-ramp text literals (MemoryPanel:272 `text-[0.8rem]` and its kin) as a distinct **T2.1 text-scale normalization** — explicitly *not* to be smuggled into a chrome migration. Unlike every prior byte-identical chrome cycle, normalizing a font-size literal MOVES rendered pixels, so this is a taste/pixel call: surfaced to the owner before editing. **Four owner decisions** (3 AskUserQuestion rounds): (1) **scope = full uniform sweep** — every off-ramp `text-[Nrem]`/`text-[Npx]` literal app-wide, not just the one flagged site; (2) **sub-floor text = add a blessed `caption` tier** (0.75rem/12px) rather than round the ~sub-0.8125rem drift up; (3) **`0.875rem` (166×) snaps DOWN** to `small` 0.8125rem; (4) **`1rem`/16px snaps UP** to the existing `body-l` 1.0625rem rung (no new "subtitle" tier — a mid-round correction: I had proposed one on the false premise that 16–17px "wasn't on the machine ramp," then found `tokens.css` already defines `body-l`/`label-l` at 1.0625rem, surfaced the correction, and the owner chose the existing rung), **and reconcile the DESIGN.md frontmatter to mirror the true tokens.css ramp**.
- **DONE (ramp reconciliation — `web/DESIGN.md` + `web/src/design/tokens.css`):** the impeccable font-size detector parses DESIGN.md's YAML `typography` frontmatter as the blessed scale (±0.5px); that block was an incomplete *subset* of the richer `tokens.css` `data-type` role system and had been false-flagging legit shipped rungs (1.0625/1.75/2/2.25rem). Replaced the frontmatter `typography` block with a **strict superset** mirroring tokens.css exactly (display-l/m/s · headline-l/m · title-l · body-l · body-m/label-m · body-s · **caption** · mono) — verified a superset: it *adds* 0.75/1.0625/1.75/2/2.25rem and *removes nothing* the old block blessed, so it cannot introduce a new finding. Added the new `[data-type="caption"]` role to tokens.css (`font-size: 0.75rem; line-height: 1rem; font-variation-settings: "wght" 470` — mid-weight like label-s, since the sub-floor text is overwhelmingly chip/badge/timestamp meta). Updated the prose `### Hierarchy` with the discrete-rungs sentence (`0.75 · 0.8125 · 0.9375 · 1.0625 · 1.25 · 1.5 · 1.75 · 2 · 2.25 · 2.625rem`) and a Caption bullet. **Blessed px set after reconciliation: 12·13·15·17·20·24·28·32·36·42px.**
- **DONE (the sweep — 139 source files):** every off-ramp `text-[Nrem]`/`text-[Npx]` class literal snapped to the nearest blessed rung via a deterministic, idempotent throwaway transform (auditable, then deleted). **~934 literals snapped:** **664 → caption 0.75rem** (from 0.6/0.62/0.625/0.65/0.66/0.68/0.6875/0.7/0.72/0.74/0.76/0.78rem), **243 → small 0.8125rem** (from 0.8/0.82/0.84/0.85/0.875rem), **11 → body 0.9375rem** (0.9/0.95rem), **16 → body-l 1.0625rem** (1/1.05/1.15rem/16px). Two paired chat edits carry the 1rem→1.0625rem intent through inline styles too: `ChatPage.tsx:2441` composer `text-[16px]`→`text-[1.0625rem]` (still ≥16px, preserves mobile-no-zoom) and `MessageUser.tsx:33` user-bubble `fontSize: '16px'`→`'1.0625rem'`. The 0.75rem tier needed only *legalization* (no code change) at its ~295 pre-existing sites once the caption role was blessed. Diff is surgical: **0 non-`text-[…]` changed lines** across all 139 files besides those 2 intentional inline edits + the DESIGN.md/tokens.css comment additions (verified line-by-line).
- **Deliberately OUT of scope (documented, not swept):** the pre-existing off-ramp sizes in **rendered-document / non-chrome** contexts — `tokens.css` `.doc` scoped-prose block (h1 1.9rem, h2 1.45rem, h3 1.2rem, h4 1.02rem, base 1rem: an article/markdown typography scale distinct from app chrome), `content/exporters.ts` (1.2rem, export HTML), `ui/widget/widgetSrcdoc.ts` (14px, sandboxed widget iframe), `ui/composer/liveMarkdown.ts` (CodeMirror editor-theme mono), and `fonts.css` brand `@font-face` families. These pre-date this cycle (proven: the OLD frontmatter didn't bless them either), and folding a rendered-document type scale into a chrome-consistency sweep would be a *document-rendering redesign* — a separate taste call, not a normalization. Left for a future dedicated pass.
- **No ratchet move:** text-size has NO in-repo CI ratchet (`consistencyAudit.report.ts` tracks chrome/weights/color, not font-size); the real gate for this sweep = the impeccable font-size detector staying clean on changed files + the Playwright visual baselines. `docs/design/consistency-audit.json` diff was **timestamp-only** (`generatedAt`, zero real signal — a font-size sweep touches no primitive/a11y counts) → reverted, kept out of the commit. Baseline unchanged: `{ rawButton: 369, rawInput: 173, rawDialog: 0, inlineFontWeight: 7 }`.
- **Evidence:** impeccable font-size detector (`detect.mjs --scope type`) **clean (EXIT=0) across all 139 changed files** — control-verified live (an off-ramp `0.66rem` probe flags; a `0.75rem` probe passes, confirming the reconciled caption rung loads); typecheck clean; vitest **113/113** (incl. the `forms.test.tsx:57` negative guard `not.toContain('text-[0.875rem]')`, correctly the ONLY surviving `0.875rem` — a test-assertion string, excluded from the sweep); build green (`index-DFNQ27Vp.js` + `index-D29aUe84.js`); visual **64/64**. **2 baselines regenerated** — `tools-light-darwin.png` + `tools-dark-darwin.png` (~3% pixels; ToolsPage is the one route shell whose top-level text moved: 0.7→0.75, 0.875→0.8125, 1rem→1.0625 title) — diff confirmed purely intended text-size reflow before `--update-snapshots`. Most swept text lives in un-snapshotted settings sub-panels / chat / memory studio, so no other baseline moved. UI-validated live on `127.0.0.1:10400` (empty-seed E2E instance, verified served bundle hash): Tools / Settings·agent / Memory studio all render clean, and DOM font-size introspection confirmed every rendered leaf is on-ramp (192@12px, 79@13px, 16@15px; the lone raw 16px was the non-rendered `<title>` in `<head>`). Committed atomically (`refactor(design): T2.1 normalize off-ramp text sizes to the blessed ramp`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT:** the type ramp is now uniformly enforced across app chrome + the frontmatter mirrors tokens.css, so any NEW off-ramp `text-[…]` literal reddens the impeccable hook. Remaining T2 threads: the **rendered-document `.doc` type scale** (the deliberately-deferred taste call above — decide whether it earns its own blessed document-tier set or should converge onto the chrome ramp) and any non-`text-[…]` inline `fontSize` literals in chrome (chat pair handled this cycle; audit the rest). Chrome-migration clusters (raw-button consolidation, `rawButton: 369`) remain the larger S2 body of work.

### 2026-07-23 — cycle 37 (T2.3: extract the `SquareIconButton` dense-square icon primitive + migrate its five hand-rolled copies) — new shared primitive, owner-directed
- **Context:** cycle 36's "NEXT" flagged raw-button consolidation (`rawButton: 369`) as the larger S2 body. Before touching call sites I surfaced two taste calls (2 AskUserQuestion rounds): (1) **Raw-btn scope = "authorize as intended fixes"** — proceed cluster-by-cluster, update Playwright baselines where motion/pixels legitimately move, owner reviews snapshots after each cycle (enables the full 369 consolidation over time); (2) **square icon-btn on-state = "bg-fill hover + press motion"** — idle `text-on-surface-low` / hover `bg-surface-high + text-on-surface` / `on` primary tint / `disabled` opacity-40 / motion `whileTap` press-spring, reduced-motion safe ("matches ContentSurface, FileViewer; aligns with the animated IconButton doctrine"). The dense **square** icon-button (size-7, rounded-md, small glyph) is a **genuine missing primitive**, distinct from the round/large-glyph/pill `ui/IconButton` — it was hand-rolled as five separate `IconBtn` copies. This cycle extracts it (T2.3) and migrates the family. Soul guardrail honoured: consolidation onto the shape that already shipped five times, not a redesign.
- **DONE (the primitive — `web/src/ui/SquareIconButton.tsx` + `SquareIconButton.test.tsx`):** new shared primitive under `ui/` (exempt from the scanner). Renders `grid size-7 place-items-center rounded-md transition-colors` — idle `text-on-surface-low`, hover `bg-surface-high + text-on-surface`, `on` (selected) coral tint (`text-primary` + a token-routed `color-mix(in srgb, var(--color-primary) 14%, transparent)` bg chip), `disabled` `opacity-40 cursor-not-allowed` with onClick suppressed. Press springs via framer `whileTap` (expressiveness-scaled through `expr()`, `useReducedMotion()`-safe, `spring.spatialFast`). Accepts either an `icon` (LucideIcon, `iconSize` default 14) or `children` (for state-swapping glyphs — spinner⇄wifi, X⇄Pencil), a required `label` (accessible name **and** `title` tooltip). **Naming collision resolved:** the copies overloaded `active` to mean *busy→disabled* (settings: `disabled={active}`) in some, *selected→coral* (ContentSurface: `color: active ? primary`) in others — the primitive splits this into orthogonal **`on`** (selected) + **`disabled`** (busy). 6 tests pin the four visual states + the semantics (idle chrome / on-tint / disabled dim+inert+aria / disabled-never-lit / label-as-name-and-title + click-fires-once / children render).
- **DONE (the 5-copy consolidation):** two byte-identical settings copies — `pages/settings/MultiInstanceCard.tsx` (Test `active={testing}`→`disabled={testing}`, Edit `on={editing}`, Delete; local `IconBtn` def deleted → file's raw-button 1→0) and `pages/settings/ModelBackends.tsx` (Test `active`→`disabled`, View-models `on={showModels}`, Edit `on={editing}`, Delete; local `IconBtn` deleted → 2→1, the surviving count being the **password-toggle `<button>` deliberately left for a later slice** — a distinct role positioned *inside* the input via `inputCls`) — plus three `ui/` near-variants: `ui/content/ContentSurface.tsx` (word-wrap `active={wrap}`→`on={wrap} iconSize={13}`, Copy, Export; local `IconBtn` deleted, `motion`/`spring` retained by `ToggleBtn`'s `layoutId` pill), `ui/widget/WidgetFrame.tsx` (Save-as-artifact `disabled={savePending} on={saved}`, Download, Open-in-new-tab, Minimize/Expand; local `IconBtn` with color-only on-state deleted), `ui/widget/ReactWidgetFrame.tsx` (Open-in-new-tab, Minimize/Expand; simplest local `IconBtn` deleted). The three `ui/` copies' **color-only on-state** (`style={{ color: primary }}`) homogenized onto the primitive's bg-chip on-state — an authorized pixel move (owner decision 2 above), consistent with the settings/ContentSurface tint.
- **Ratchet: `rawButton` 369 → 367** (delta **−2**, in the same commit — `web/src/design/primitiveAdoption.baseline.json`). The delta is 2 not ~5 because the scanner (`consistencyAudit.report.ts`) counts `<button>` **text occurrences**, and everything under `web/src/ui/` is exempt: so the three `ui/` `IconBtn` defs never counted, and each settings copy's single `IconBtn` **function definition** counted exactly once → MultiInstanceCard(−1) + ModelBackends(−1). Verified live via a throwaway `scanDrift()` probe before/after. `docs/design/consistency-audit.json` regenerated by the vitest run carries **real signal** (ModelBackends raw-button 2→1, MultiInstanceCard 1→0, filesScanned +1 for the new primitive file, WidgetFrame drift snippet line +1 from the added import) — a legitimate ride-along, kept in the commit (not timestamp-only, unlike cycle 36's font sweep).
- **Doc:** `docs/design/patterns.md` gains a `### SquareIconButton` entry under Buttons — canonical shape, props table, the `on`-vs-`disabled` naming note, usage snippet, and "migrate to it when you see a hand-rolled `size-7 … rounded-md … place-items-center` icon `<button>`; the round pill (`IconButton`) and this square dense form are the two canonical icon-action shapes — pick by density, don't hand-roll a third."
- **Evidence:** typecheck clean (one self-corrected miss: migrated ContentSurface's call sites + deleted its local def but initially forgot the import → typecheck flagged it → added `import { SquareIconButton } from '../SquareIconButton'` in the same pass); vitest **119/119** (incl. `primitiveAdoption.test.ts` against 367 + the 6 new SquareIconButton tests); build green (chunk-size warnings pre-existing); visual **64/64 with ZERO baseline changes** — every migrated surface lives in un-snapshotted settings sub-panels or interaction-gated content/widget toolbars, so no route-shell snapshot legitimately moved (owner-authorized baseline updates were therefore unneeded this cycle). UI-validated live on `127.0.0.1:10400` (empty-seed E2E instance): created a scratch `.md` in the dev Files workspace, entered Edit mode to surface the ContentSurface toolbar, toggled word-wrap ON, and confirmed via `browser_evaluate` the on-state renders exactly as designed — `className` includes `text-primary`, computed `color: rgb(255,107,91)` (=#ff6b5b brand coral), `backgroundColor: color(srgb 1 0.419608 0.356863 / 0.14)`, inline `background: color-mix(in srgb, var(--color-primary) 14%, transparent)` (token-routed, no hardcoded hex), box 28×28, borderRadius 12px; scratch file then deleted via the confirmDelete dialog (workspace restored). Committed atomically (`refactor(design): T2.3 extract SquareIconButton dense icon primitive`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT:** the dense-square role now has a canonical home + ratchet floor at 367. Authorized follow-on raw-button clusters (owner decision 1, "intended fixes"): the retained **ModelBackends password-toggle** (input-embedded, its own slice), **FileViewer** icon buttons, **ToolsPage** (~8 rawButton incl. the danger-hover Remove), and the always-primary Plus buttons in **AgentDefaultsPanel/InboxSettingsPanel**. Each is a distinct role/motion decision — surface pixel-moving ones (esp. any that move a route-shell Playwright baseline) before editing.

### 2026-07-23 — cycle 38 (T2.3 cont.: grow `SquareIconButton` with `tone="danger"` + `title`, migrate families A/B/C across 9 files) — owner-directed adoption sweep
- **Context:** cycle 37's "NEXT" enumerated the authorized follow-on clusters. Before editing I mapped the remaining `size-7 rounded-md` icon-button hand-rolls into three role families and surfaced scope + the one new visual as **2 AskUserQuestion rounds**: **Q1 (scope) = "A + B + C"** — migrate all three: **A** = clean bg-fill adopters already matching the primitive byte-for-byte (TerminalDrawer, FileViewer), **B** = color-only-hover sites that GAIN the bg-fill hover (an authorized pixel move, same as cycle 37's `ui/` copies), **C** = danger-hover deletes needing a new destructive variant; **Q2 (danger hover) = "Glyph-tint only"** — hover tints the glyph red (`hover:text-danger`) with **no** background fill, which is byte-identical to what the existing delete buttons already render (they were `text-on-surface-low hover:text-danger` with no chip). Soul guardrail honoured: this is adoption of the shape that already shipped, plus a variant that codifies the delete-hover the copies already used — not a redesign.
- **DONE (the primitive grows — `web/src/ui/SquareIconButton.tsx` + test):** added two orthogonal props. **`tone: 'neutral' | 'danger'`** (default `neutral`): `danger` swaps the neutral hover (`hover:bg-surface-high hover:text-on-surface`) for `hover:text-danger` (glyph-tint, no fill); ignored while `on` (a selected danger control is not a state that exists in the migrated set, and the coral `on`-tint wins by precedence in the className ternary). **`title?: string`** (defaults to `label`): a tooltip override for the sites whose hover hint legitimately differs from the accessible name (e.g. LocalModelManager's gated-download reason, ToolsPage's "Reconnect this server"). A 7th test pins the danger tone: asserts `text-on-surface-low` + `hover:text-danger`, and `not.toContain('hover:bg-surface-high')` / `not.toContain('text-primary')`.
- **DONE (9 call-site migrations, families A/B/C):** **A (byte-identical bg-fill adopters):** `pages/terminal/TerminalDrawer.tsx` (New-session spinner⇄Plus, Open-full, Hide-terminal; 5 raw→2), `pages/files/browse/FileViewer.tsx` (compact Save-as-artifact + Reveal-in-Finder; the non-compact text "Artifact" button and the `<a download>` anchor **deliberately stayed raw** — an anchor can't be a `<button>`, the text button has a visible label; 4→2). **B (color-only-hover → bg-fill, authorized pixel move):** `pages/settings/ProviderCard.tsx` (Recheck spinner + Configure toggle preserving its rotating chevron; 6→4), `pages/settings/ProviderConfigForm.tsx` + `pages/settings/ModelBackends.tsx` (password show/hide eye toggles — **wrapped in a positioning `<span>`** so `whileTap`'s transform doesn't clobber `-translate-y-1/2`; 1→0 each, retiring cycle 37's deliberately-kept ModelBackends toggle). **C (danger deletes → `tone="danger"`):** `pages/tools/ToolsPage.tsx` (Reconnect + danger Remove-server; 8→6), `pages/apps/AppsSection.tsx` (two danger Remove-source + a copy-command Copy⇄Check button; 8→5), `pages/settings/MemoryPanel.tsx` (StudioInspector danger Delete; 9→8), `pages/settings/LocalModelManager.tsx` (Cancel + the dual Download/Delete button whose `title` carries the gated-license reason; 2→0 — the site that motivated the `title` prop).
- **Ratchet: `rawButton` 367 → 350** (delta **−17**, same commit — `web/src/design/primitiveAdoption.baseline.json`). Verified exact via a throwaway `scanDrift()` probe before/after: every touched file's per-file decrement matches (TerminalDrawer −3, FileViewer −2, ProviderCard −2, ProviderConfigForm −1, ModelBackends −1, ToolsPage −2, AppsSection −3, MemoryPanel −1, LocalModelManager −2 → −17). `docs/design/consistency-audit.json` regenerated by vitest carries **real signal** (per-file raw-button decrements for all 9 files; LocalModelManager 2→0 and ProviderConfigForm 1→0 drop off the list entirely; score-rank reshuffle) — a legitimate ride-along, kept in the commit.
- **Doc:** `docs/design/patterns.md` `### SquareIconButton` entry updated — canonical-shape line gains the `tone="danger"` glyph-tint clause; props table gains `title` and `tone` rows; usage snippet gains a `tone="danger"` Remove example **and** the absolute-position wrapper pattern (positioning `<span>` owns `-translate-y-1/2`, button owns the press-scale — the `whileTap`-clobbers-transform lesson).
- **Evidence:** typecheck clean on first run (no orphaned imports this cycle); vitest **120/120** (incl. `primitiveAdoption.test.ts` against 350 + the new danger-tone test); build green; visual **64/64 with ZERO baseline changes** — every migrated surface is interaction-gated or empty-under-seed (no MCP servers / no model apps / no memory items in the E2E fixture), so no route-shell snapshot legitimately moved. UI-validated live on `127.0.0.1:10400` (empty-seed E2E instance): the Providers panel renders **13** migrated `SquareIconButton` "Configure" toggles, and `browser_evaluate` confirmed each is `display:grid` + `place-items:center`, 28×28px, `rounded-md` (12px), with the neutral-tone hover classes — the primitive validated in production geometry. Danger-tone + password-toggle surfaces don't render under the empty seed; their correctness rests on the unit test (danger class-swap), the structural wrapper guarantee, typecheck, and the owner-chosen byte-identical rationale. Committed atomically (`refactor(design): T2.3 grow SquareIconButton (danger tone) + migrate 9 files`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT:** the dense-square role is now broadly adopted with a ratchet floor at 350 and a codified danger variant. Remaining `rawButton` bodies are mixed-role sprawls (CodeCockpitPage ~23, ProjectsSection ~20, ChatPage ~19, LoopPlanReview ~13, CodePlanReview ~12, LoopCockpitPage ~11, WorkflowForm ~10) — not clean dense-square slices; each needs its own role triage (primary actions → `Button`, round large-glyph → `IconButton`, dense square → `SquareIconButton`) and pixel-moving calls surfaced before editing. The clean next slices are any remaining isolated dense-square icon `<button>`s that match the primitive byte-for-byte.

### 2026-07-24 — cycle 39 (T2.2: extract the duplicated `GraphZoomControls` overlay — byte-identical, autonomous) — no taste call
- **Context:** cycle 38's "NEXT" flagged the remaining `size-7` clusters. Triaging them, most need a taste call (mixed-role mega-pages; the always-coral add-item Plus buttons in AgentDefaultsPanel/InboxSettingsPanel would need a new always-primary variant or an `on`-chip pixel move; the `rounded-full` clear buttons are round-pill = `IconButton` territory). **One was a provably byte-identical consolidation needing NO decision:** the SVG pan/zoom overlay (zoom-in / zoom-out / reset-to-fit) is **verbatim-duplicated** across the app's only two graph canvases — `pages/settings/MemoryGraph.tsx` and `pages/knowledge/KnowledgeGraph.tsx` — down to the identical wrapper `<div>`, the three buttons, AND the `zoomBy` clamp logic (`Math.min(6, Math.max(0.3, v.scale * factor))`) + `reset` (`setView({ scale: 1, x: 0, y: 0 })`). This is the same autonomous-authorized class as cycle 4's `unreadRail`/`toneChipBg` and cycle 35's `NumberField`: dedup a duplicate component, no new visual language.
- **DONE (the shared component — `web/src/ui/GraphZoomControls.tsx` + `.test.tsx`):** new `ui/` component (scanner-exempt) rendering the exact panel verbatim — `absolute bottom-3 right-3 flex flex-col gap-1 rounded-lg bg-surface-high/90 p-1 backdrop-blur` wrapper + three `grid size-7 place-items-center rounded text-on-surface-var hover:bg-surface-container hover:text-on-surface` buttons (Plus 15 / Minus 15 / Maximize2 14). Props are three callbacks (`onZoomIn`/`onZoomOut`/`onReset`) — the ONLY thing the two copies varied. **Deliberately NOT `SquareIconButton`:** these buttons live on a `bg-surface-high/90` blur panel where the primitive's `hover:bg-surface-high` would be nearly invisible, and they use `rounded`(4px)/`text-on-surface-var` on-glass chrome — homogenizing onto the primitive would be a pixel move + affordance regression, i.e. a taste call, explicitly deferred (documented in the component note). 2 tests pin the byte-identical chrome + a11y names + per-button callback wiring.
- **DONE (2 call-site migrations):** both files import `GraphZoomControls` and replace their hand-rolled panel with `<GraphZoomControls onZoomIn={() => zoomBy(1.25)} onZoomOut={() => zoomBy(1 / 1.25)} onReset={reset} />`; unused `Plus`/`Minus`/`Maximize2` lucide imports dropped (verified each appeared ONLY in the replaced panel). **Pure a11y GAIN for MemoryGraph:** its copy had `title` only; the shared component carries the `aria-label`s KnowledgeGraph already had, so MemoryGraph's controls now have accessible names too — visually byte-identical, strictly more accessible.
- **Ratchet: `rawButton` 350 → 344** (delta **−6**, same commit). Verified exact via a throwaway `scanDrift()` probe: total 350→344, both graph files 3→0. `docs/design/consistency-audit.json` regenerated with the two per-file decrements (real signal, kept in commit).
- **Evidence:** typecheck clean (no orphaned imports); vitest **122/122** (incl. `primitiveAdoption.test.ts` against 344 + the 2 new GraphZoomControls tests); build green; visual **64/64 with ZERO baseline changes** (both graph overlays are gated behind non-empty data — `nodes.length === 0` early-returns an empty state — so they don't render under the empty E2E seed; correctness rests on the unit test pinning the exact chrome string + a11y + wiring, typecheck, and the verbatim-extraction rationale). Committed atomically (`refactor(design): T2.2 extract duplicated GraphZoomControls overlay`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT:** no clean byte-identical dense-square slices remain — the rest of the `rawButton` body (344) is mixed-role mega-page sprawl + the always-primary add-item Plus buttons, all of which need per-role triage and a surfaced pixel/taste call before editing. These are the items queued for owner input (see the session-end report). Autonomous byte-identical consolidation is exhausted for the icon-button/overlay family.

### 2026-07-24 — cycle 40 (whole-codebase exhaustion sweep — DISCOVERY, no code change) — establishes the autonomous floor
- **Context:** cycle 39 declared autonomous byte-identical work exhausted *for the icon-button/overlay family*. Before queuing everything for the owner I widened the search to the WHOLE codebase across all consolidation lanes, to be sure no other provably-safe (zero-pixel) win remained — honouring the owner's "cover as much as you can" without crossing the taste line.
- **DISCOVERY (duplicate-component lane — drained):** enumerated every Capitalized `function`/`const` defined in ≥2 files (`rg` + dedupe). Findings, each dispositioned: **`VariableRow`** (prompts ×3) — signatures identical but bodies DIVERGE (`PromptForm` has `useId()`+`name=` autofill attrs + a `relative` select wrapper; `SnippetForm` deliberately says `placeholder="Description"` not `"…(shown when invoked)"` — domain copy) → **canonical-version + copy decision, queued for owner, NOT byte-identical.** **`UseCaseRow`** (ModelsPanel/SearchPanel) — same name, structurally different components (models vs search providers, different props/logic) → not a dedup target. **`Centered`** (4 files; 3 byte-identical `flex h-full items-center justify-center`, Onboarding's differs) → a one-line `<div>` wrapper; a generic micro-primitive for it is churn, moves no ratchet (it's a `<div>`, not chrome), and inlining is clearer → **declined as over-engineering, not queued.** **`Stat`/`SchemaField`** — divergent signatures per file, confirmed not verbatim duplicates.
- **DISCOVERY (inline-font-weight lane — floor is real):** the 7 remaining inline `fontVariationSettings "wght"` all live in `ui/composer/liveMarkdown.ts` CodeMirror theme decorations and each deliberately pairs a numeric `fontWeight` FALLBACK with the variation setting. `fvs()`/`withWeight()` emit only `fontVariationSettings` → migrating them DROPS the non-variable-font fallback = a real (if tiny) behavior change → **correctly out of the byte-identical lane; the `inlineFontWeight: 7` floor is genuine, not laziness.**
- **DISCOVERY (raw-input lane — no no-op adopters):** the remaining `rawInput: 173` are non-text types (checkbox/range/color/file) or text fields whose migration needs a pixel move or canonical-shape decision (the cycle 30–35 pattern). No field maps onto an existing `TextInput` size/surface with zero new prop AND zero pixel move (the cycle-30 autonomous-adopter shape) → nothing to grind autonomously here.
- **Outcome:** the autonomous byte-identical floor for S2 is now `{ rawButton: 344, rawInput: 173, rawDialog: 0, inlineFontWeight: 7 }` across ALL lanes (icon-button/overlay, duplicate-component, font-weight, raw-input), not just the icon family. No code changed this cycle; tree left clean at cycle-39 tip (`811b03e`). Everything beyond this floor is a genuine taste/pixel/canonical-copy decision — see the session-end report's owner-input queue.

### 2026-07-24 — cycle 41 (T2.2: consolidate the duplicated `VariableRow` prompt-variable editor — owner-authorized, "use your judgement") — canonical-copy decision made
- **Context:** the cycle-40 exhaustion sweep found `VariableRow` (the typed prompt-variable row: name/type/required/description/default/+choices) **verbatim-triplicated** across `pages/prompts/PromptForm.tsx`, `pages/prompts/PromptEditFields.tsx`, and `pages/prompts/SnippetForm.tsx`, and flagged it as the **one duplicate-component dedup that was NOT byte-identical** — the three copies diverge in two ways, so it needed a canonical-version + copy decision (queued for owner as item 3 of 4). The owner answered **"use your judgement for 1, 2, 3 and 4 all"** — authorizing autonomous resolution. This cycle executes item 3. Soul guardrail honoured: dedup a duplicated component onto its richest existing copy, no new visual language.
- **The two divergences + decisions:** (1) **a11y/autofill richness** — `PromptForm`'s copy carries `useId()`-prefixed `name=` attrs (`var-name-`/`var-type-`/`var-desc-`/`var-default-`/`var-opts-`, browser-autofill defeat) + `aria-label`s + a `relative` select wrapper; the other two copies lacked the `name=` attrs. **Decision: canonical = PromptForm's copy** (strictly more accessible; the other two GAIN the a11y attrs — a pure gain, visually byte-identical). (2) **description placeholder copy** — snippets say `placeholder="Description"` (a snippet is *inserted*), prompts say `"Description (shown when invoked)"` (a prompt is *invoked*). **Decision: make it a `descriptionPlaceholder` prop** defaulting to the prompt wording; `SnippetForm` passes `"Description"` — the domain copy difference is preserved exactly, not flattened.
- **DONE (the shared component — `web/src/pages/prompts/VariableRow.tsx` + `.test.tsx`):** extracted verbatim from PromptForm's copy. Signature `{ v, onChange, onRemove, descriptionPlaceholder='Description (shown when invoked)' }`. **Kept in `pages/prompts/` (NOT `ui/`)** on purpose: it imports `PromptVariable`/`PromptVarType` + `VAR_TYPES` from the prompt domain (`./promptMeta`, `../../lib/api`), so a `ui/`→`pages/` import would invert the dependency boundary (the cycle-29 rule). Being under `pages/` it stays scanner-counted, so the dedup shrinks the ratchet (unlike the `ui/`-exempt component dedups). 4 tests pin: a11y names + `var-*-` autofill attrs sharing the `useId` suffix; the description-placeholder default + snippet override (via rerender); select-type reveals the choices field; name-sanitize (`[^a-zA-Z0-9_]`→`_` via `fireEvent.change` — React's controlled-input value tracker ignores a raw native event, so `fireEvent.change` is required to fire `onChange`) + required-toggle + remove wiring.
- **DONE (3 call-site migrations):** all three files import `VariableRow` and delete their local def; unused `useId`/`X`/`VAR_TYPES` lucide/type imports dropped (each verified used ONLY in the removed def — `PromptEditFields` self-corrected: I first over-dropped `PromptVariable`, still used at its `updateVar`, and restored it before typecheck). `PromptForm` + `PromptEditFields` call sites unchanged (default placeholder = prompt wording, matching their prior literal); `SnippetForm` call site adds `descriptionPlaceholder="Description"` to preserve its distinct copy.
- **Ratchet: `rawButton` 344 → 340 (−4), `rawInput` 173 → 163 (−10)**, same commit. The scanner counts raw `<button>`/`<input>` by **text occurrence**, so removing 2 duplicate copies of a row with 2 raw buttons (required-toggle + X) and 5 raw inputs (name/desc/default/choices + … ) each = −4 button / −10 input. Verified exact via a throwaway `scanDrift()` probe before/after (created, confirmed, deleted). `docs/design/consistency-audit.json` regenerated by vitest carries real per-file signal (the three prompt files' raw-button/input decrements) — legitimate ride-along, kept in commit.
- **Evidence:** typecheck clean (after the self-corrected `PromptVariable` restore); vitest **126/126** (incl. `primitiveAdoption.test.ts` against 340/163 + the 4 new VariableRow tests; one self-fixed test-authoring bug — the sanitize test first used `input.value=`+raw native event, which React's value tracker ignores → switched to `fireEvent.change`, a test bug not a product regression); build green; visual **64/64 with ZERO baseline movement**. **Validated as a user live on `127.0.0.1:10400`** (after `make web-build` refreshed the served bundle — the browser had cached a pre-rebuild `index.html`; forced a root reload): **Prompt editor** (`/prompts/new?kind=user`) — Add-variable mounts the migrated row with all 5 type options, required-toggle flips optional→required, select-type reveals the "Variable choices" field, `browser_evaluate` confirmed every field has a distinct `var-*-` `name=` sharing the `useId` suffix (`:rt:`) and description placeholder = **`Description (shown when invoked)`**; **Snippet editor** (`/prompts/new?kind=snippets`) — same shared row, description placeholder = **`Description`** (the prop override). Both editors render the single shared component exactly as designed. Committed atomically (`refactor(design): T2.2 consolidate the duplicated VariableRow prompt-variable editor`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT:** the duplicate-component lane is now fully drained (GraphZoomControls in c39 + VariableRow here were the two real dedup targets from the c40 sweep; `Centered` declined as over-engineering). Remaining owner-authorized items from "use your judgement 1–4": **item 4** — genuinely-repeated utility className strings → possible micro-primitives (SectionLabel/Chip/FormFooter), extract only verbatim-repeated strings, skip anything needing invented API; **item 2** — the always-primary add-item Plus buttons (AgentDefaultsPanel/InboxSettingsPanel), an always-primary variant or `on`-chip pixel move; **item 1** — the mixed-role mega-pages (CodeCockpit ~23, ProjectsSection ~20, ChatPage ~19, LoopPlanReview ~13, CodePlanReview ~12, LoopCockpitPage ~11, WorkflowForm ~10), one page/cycle with per-role triage (primary→Button, round-large-glyph→IconButton, dense-square→SquareIconButton), regenerating baselines where pixels legitimately move.

### 2026-07-24 — cycle 42 (T2.2: extract the `FormFooter` sticky action-bar primitive — owner-authorized item 4, "use your judgement") — one clean extraction, two candidates surfaced not built
- **Context:** item 4 of the owner's "use your judgement 1–4" — genuinely-repeated utility className strings → possible micro-primitives. This is the item most at risk of over-engineering (cycle 40 already declined `Centered` for exactly that reason), so I investigated empirically before extracting: `grep`-measured real repetition of every candidate family and held each to **three tests — (a) a single VERBATIM string repeats enough to justify a component, (b) NO invented API, (c) NO pixel movement.** Soul guardrail honoured: extract the shape that already ships identically N times; surface anything that would unify variants (a redesign) rather than doing it.
- **The measurement (three candidate families):** **FormFooter** — the sticky edit-mode Cancel/Save bar `sticky bottom-0 -mx-l px-l py-3 bg-surface/95 border-t border-outline-variant/40 flex justify-end gap-s` is **7× byte-identical** across `pages/{schedule/ScheduleDetail, tasks/TaskDetail, triggers/LifecycleDetail, workflows/WorkflowDetail, agents/AgentDetail, prompts/PromptDetail, prompts/SnippetDetail}.tsx`, each wrapping a ghost-`Cancel` + primary-`Save` pair whose only difference is the handlers → **passes all three tests.** **SectionLabel** (`text-on-surface-low text-[0.75rem] uppercase tracking-wide`) — ~60 uses BUT across heterogeneous tags (`div`/`span`/`label`/`h3`) with mixed margins (`mb-1`/`mb-1.5`/`mb-2`/none) and color variants (`text-info`/`text-primary`) → needs a polymorphic `as` + margin + tone API and touches heading/label a11y semantics → **fails (b): invented API + a11y risk.** **Badge/Chip** (`rounded-pill bg-surface-high px-2 h-6 …`) — top string 11× but ~15 near-variants (`px-2`/`px-1.5`, `h-6`/`h-5`, `on-surface-var`/`on-surface-low`, ±`font-mono`) → needs `size`/`tone`/`mono` API + pixel unification → **fails (b)+(c).** Only FormFooter is a clean autonomous extraction; the other two are genuine taste/API-shape calls — **surfaced in the report, NOT built.**
- **DONE (the primitive — `web/src/ui/FormFooter.tsx` + `.test.tsx`):** new `ui/` component (scanner-exempt), pure provider-agnostic chrome — **unlike VariableRow it has NO domain imports** (only `children` + an optional `className` merged via `cx`), so `ui/` is the correct home (no dependency-boundary inversion). Renders exactly the one `<div>` with the verbatim className; the Cancel/Save buttons + their handlers stay at the call site as `children`. JSDoc records the seven origin sites and the "single source" intent, matching the `Surface.tsx` house style. 3 tests pin: the four sticky-footer traits (sticky bottom-0 / -mx-l edge-bleed / border-t hairline / justify-end) so an edit dropping any reddens; children pass-through; `className` merge keeps the base chrome.
- **DONE (7 call-site migrations):** each file imports `FormFooter` from `../../ui/FormFooter` (all 7 already imported `Button` from the sibling path) and replaces the inline `<div className="sticky …">…</div>` with `<FormFooter>…</FormFooter>`, buttons unchanged. Post-migration grep confirms the sticky-footer string now lives in **exactly one place** (`ui/FormFooter.tsx`), with 7 `<FormFooter>` usages + 7 imports.
- **No ratchet move (by design):** these are `<div>` wrappers, not `<button>`/`<input>`, so the primitive-adoption counts are untouched — baseline stays `{ rawButton: 340, rawInput: 163, rawDialog: 0, inlineFontWeight: 7 }` (verified: `primitiveAdoption.test.ts` green unchanged). This is a **single-source / consistency win, not an element-count reduction** — the value is that the sticky-footer chrome can now only ever change in one file. `docs/design/consistency-audit.json` regenerated by the vitest run carries **one real structural delta — `filesScanned` 303→304** (the new `ui/FormFooter.tsx` is walked though it contributes 0 primitive count, being `ui/`-exempt) — plus the timestamp; no primitive/weight/color count moved (a `<div>`-wrapper dedup touches none). This is a legitimate ride-along (same class as cycle 37's `filesScanned +1` for a new primitive file), kept in the commit.
- **Evidence:** typecheck clean (all 7 imports resolve, no orphans); vitest **129/129** (was 126; +3 FormFooter tests; `primitiveAdoption.test.ts` green against the unchanged 340/163 floor); build green (`index-BDjxmSej.js`, chunk-size warnings pre-existing); visual **64/64 with ZERO baseline movement** — the footer is edit-mode-only (interaction-gated, never in a route-shell snapshot) and byte-identical regardless, so no baseline legitimately moves. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; forced a root reload first — the browser had cached a pre-rebuild `index.html`, the recurring stale-chunk gotcha): created a scratch task via the UI, opened it, hit **Edit** to surface `TaskDetail`'s migrated footer, and `browser_evaluate` on `div.sticky.bottom-0.-mx-l` confirmed the real render — intact className, computed `position: sticky`, solid `1px` top border, `justify-content: flex-end`, holding exactly the `Cancel` + `Save` buttons; then Cancelled + Deleted the scratch task (empty seed restored). Committed atomically (`refactor(design): T2.2 extract FormFooter sticky action-bar primitive`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 4 closed; on to items 2 then 1):** the utility-primitive lane's one clean extraction is done; **SectionLabel and Badge/Chip are surfaced as taste/API calls, deliberately not built** (unifying their variants moves pixels + invents API = a redesign, off-limits without an owner pixel decision). Next authorized: **item 2** — the always-primary add-item Plus buttons (AgentDefaultsPanel/InboxSettingsPanel), needing an always-primary `Button`/variant or an `on`-chip pixel move (a genuine taste call, so I'll surface the specific pixel choice before editing); then **item 1** — the mixed-role mega-pages (CodeCockpit ~23, ProjectsSection ~20, ChatPage ~19, LoopPlanReview ~13, CodePlanReview ~12, LoopCockpitPage ~11, WorkflowForm ~10), one page/cycle with per-role triage (primary→Button, round-large-glyph→IconButton, dense-square→SquareIconButton), regenerating baselines where pixels legitimately move.

### 2026-07-24 — cycle 43 (T2.3: migrate the two always-coral add-item Plus buttons to `SquareIconButton` — owner-authorized item 2, "use your judgement") — outlier brought to the existing dense-square idiom
- **Context:** item 2 of the owner's "use your judgement 1–4." Cycle 40/42 had flagged these as needing "an always-primary variant or an `on`-chip pixel move" — i.e. a taste call. Re-examining them under the soul guardrail (*consistency, not redesign — bring outliers to the existing system*) reframed it: the two buttons in `pages/settings/{AgentDefaultsPanel,InboxSettingsPanel}.tsx` are **byte-identical to each other** (`grid size-7 place-items-center rounded-md text-primary` + `<Plus size={15}/>`, conditionally rendered when the sibling input is non-empty, **no `aria-label`, no hover/press state**) and are **outliers on two axes at once**: (a) their own sibling remove-**X** buttons in the same pill rows are neutral ink-low, and (b) the app's canonical "type-a-value → click **+** to append beside the input" idiom (`pages/tasks/formControls.tsx` ×2, `pages/loops/LoopPlanReview.tsx`) idles **neutral**, not coral. So this isn't "invent an always-coral variant" — it's "stop being the one always-coral feedback-less + in an app whose adds are neutral."
- **Decision (surfaced for owner veto, not blocked on):** migrate both to the existing **`SquareIconButton` neutral tone** (`ui/SquareIconButton.tsx`), the primitive built for exactly this dense `size-7 rounded-md` hit area ("tight action clusters… where the 40px round pill is too large"). Its base geometry classes (`grid size-7 place-items-center rounded-md`) are **byte-identical** to the bespoke button, and the `Plus` glyph stays at `iconSize={15}`. **One deliberate pixel change:** idle `text-primary` (coral) → `text-on-surface-low` (ink-low), and the button **gains** the standard hover-fill (`hover:bg-surface-high hover:text-on-surface`) + press-spring + an accessible `label` (was missing entirely — strictly an a11y improvement). This is a taste call I resolved under the owner's judgement grant; the coral→ink-low choice is flagged in the session-end report for veto. Considered and rejected: an always-coral `SquareIconButton` variant (would *codify* the outlier into the primitive — the opposite of bringing it to the system) and an `on`-tinted chip (semantically wrong: `on` means selected/toggled, not "add").
- **DONE (2 call-site migrations):** both files import `SquareIconButton` from `../../ui/SquareIconButton` and replace the inline `<button className="grid size-7 …text-primary">` with `<SquareIconButton icon={Plus} iconSize={15} label={…} onClick={…} />`. AgentDefaultsPanel's label is dynamic (`` `Add ${label.toLowerCase()}` `` — the field's own label, e.g. "Add allowed tools"); InboxSettingsPanel's is `"Add keyword"`. `Plus` import retained (still passed as `icon={Plus}`); the conditional-render guard (`adding.trim() &&` / `kw.trim() &&`) is unchanged, so the button still appears only when the input has text.
- **Ratchet: `rawButton` 340 → 338 (−2), same commit.** The scanner counts by text occurrence; removing the one raw `<button>` from each file = −2 total (each file's remaining `raw-button: 1` is the neutral remove-**X**, untouched). Verified exact via a throwaway `scanDrift()` probe (live 340 at cycle-42 tip; `primitiveAdoption.test.ts` green against the new 338 confirms exactly two removed). `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries the two real per-file decrements (`AgentDefaultsPanel`/`InboxSettingsPanel` raw-button 2→1) + timestamp — legitimate signal, kept in commit.
- **Evidence:** typecheck clean (both imports resolve, `Plus` still used); vitest **129/129** (`primitiveAdoption.test.ts` against 338/163 + all prior tests unchanged); build green (`index-DrdWA18P.js`, chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement** — both buttons are interaction-gated (render only when their input is non-empty), so they never appear in a route-shell snapshot; correctness rests on the live user validation below + the primitive's own `SquareIconButton.test.tsx`. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; forced a fresh root load to dodge the stale-chunk gotcha; confirmed served entry `index-DrdWA18P.js` = disk): on `#/settings/inbox`, typed into "Add keyword…" to trigger the conditional render, and `browser_evaluate` on the resulting button confirmed the real render — `aria-label`/`title` = **"Add keyword"** (the previously-missing a11y name), className = **`grid size-7 place-items-center rounded-md transition-colors text-on-surface-low hover:bg-surface-high hover:text-on-surface`** (geometry byte-identical, idle neutral, hover states present), glyph = `Plus`@15; clearing the input hid the button again (guard intact) and **no keyword persisted** (`persistedKeywords: []` — empty seed untouched, nothing to clean up). AgentDefaultsPanel is the identical primitive call (same geometry, dynamic label) so is covered by the same proof. Committed atomically (`refactor(design): T2.3 migrate always-coral add-item Plus buttons to SquareIconButton`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (items 2, 3, 4 all closed; on to item 1):** the only remaining owner-authorized work is **item 1** — the mixed-role mega-pages (CodeCockpit ~23, ProjectsSection ~20, ChatPage ~19, LoopPlanReview ~13, CodePlanReview ~12, LoopCockpitPage ~11, WorkflowForm ~10), taken **one page per cycle** with per-button role triage (labelled action→`Button`; round large-glyph→`IconButton`; dense square→`SquareIconButton`), regenerating Playwright baselines intentionally wherever pixels legitimately move (these are NOT byte-identical — expect real baseline updates, surfaced per cycle). Cycle 44 starts the smallest/most-uniform of them to establish the triage rhythm before the largest pages.

### 2026-07-24 — cycle 44 (S2: extract the `AddItemButton` quiet-add primitive across 5 sites — item 1 approached WorkflowForm-first, then pivoted to the byte-identical cross-file idiom it belongs to)
- **Context:** cycle 44 opened on **item 1** intending WorkflowForm (smallest mega-page, 10 raw `<button>`) as the triage warm-up. Reading it in full surfaced a scope discovery that reshaped the cycle: WorkflowForm has **zero page-local byte-identical migrations**. Its 10 buttons split into (a) **6 pixel-moving reorder glyphs** — two byte-identical ↑/↓/`X` clusters (`text-on-surface-low hover:text-on-surface disabled:opacity-30 px-1` + a `hover:text-danger` X) at the ref-step and inline-step rows; migrating these to `SquareIconButton`/`IconButton` legitimately *moves pixels* (adds a fixed square hit-area + hover chip), so they belong in a dedicated baseline-moving cycle; (b) **1 semantic list-option row** (the WorkflowRefPicker candidate button) — correctly stays raw, exactly as its twin `DependencyEditor` candidate row does (a full-width selectable option, not chrome); (c) **1 ghost `Cancel`** — instance of a 2-file idiom (`+ formControls.tsx` "Done"); and (d) **2 `Add step`/`Add a workflow` buttons** — instances of a **byte-identical cross-file idiom**.
- **Discovery → decision (pivot, surfaced not blocked):** that add-button idiom (`inline-flex items-center gap-1.5 rounded-md bg-surface-container px-m h-9 text-on-surface-var text-[0.8125rem] hover:bg-surface-high transition-colors`) is duplicated **byte-for-byte across 5 static `<button>` sites in 4 files** — `WorkflowForm.tsx` ×2 (Add step / Add a workflow), `PromptEditFields.tsx`, `PromptForm.tsx`, `SnippetForm.tsx` (all "Add variable"). Migrating it *page-locally in WorkflowForm alone* would **fragment a shared idiom into two implementations** — the exact inconsistency this plan removes. So the correct unit is **the idiom across all its sites**, not the page: extract one `ui/AddItemButton` primitive (the **item-4 utility-primitive pattern**, precedent FormFooter cy42 / VariableRow cy41) and adopt it everywhere. This is a **provably byte-identical, zero-new-visual-language** consolidation — autonomous-safe, no taste call — so I proceeded. The pixel-moving WorkflowForm reorder clusters + the ghost-Cancel idiom are deliberately **left for their own cycles** so the (zero) baseline movement here stays cleanly attributable. (`formControls.tsx`'s add-prerequisite button is the same visual but a `motion.button` with bud-in/out animation + `self-start`; the scanner counts only literal `<button>`, and folding an animated variant into this primitive would either widen its API or drop the animation — a real change, so it's **not** in this byte-identical batch; noted for a follow-up if the animation can be preserved without API creep.)
- **DONE (1 primitive + 1 test + 5 call-site migrations):** new `ui/AddItemButton.tsx` — a `<button type="button">` holding the exact idiom classes via `cx(...)`, `children` = caller's leading glyph + label, optional `className` (three of five sites carry `self-start`, merged through `cx`). New `ui/AddItemButton.test.tsx` (3 tests, mirrors `FormFooter.test.tsx`): locks the 11 chrome classes, renders children, proves `self-start` rides `className` without dropping base chrome. All 5 sites import `{ AddItemButton }` from `../../ui/AddItemButton` and replace the inline `<button>`; `Plus`/`WorkflowIcon` imports retained (now passed as children). WorkflowForm's two use no `className`; the three prompts files pass `className="self-start"`.
- **Ratchet: `rawButton` 338 → 333 (−5), same commit.** Five literal `<button>` removed (WorkflowForm −2, PromptEditFields/PromptForm/SnippetForm −1 each; the new primitive lives under `ui/`, scanner-exempt). `primitiveAdoption.test.ts` green against the new 333 confirms **exactly** five removed. `rawInput`/`rawDialog`/`inlineFontWeight` untouched.
- **Evidence:** typecheck clean (all 6 new imports resolve, no orphaned `Plus`/`WorkflowIcon`); vitest **132/132** (was 129; +3 AddItemButton tests; ratchet green at 333/163); build green (`index-qzzYZ1TA.js`, chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement** — the add-buttons live inside create/edit forms (interaction-gated, never in a route-shell snapshot) and the migration is byte-identical regardless. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance): on `#/workflows/new` both `AddItemButton`s render with correct glyph + label; clicking **Add step** appended a step row (with its ↑/↓/X cluster — next cycle's target), clicking **Add a workflow** opened the bud ref-picker (search + candidate rows + Cancel); on `#/prompts/new`, **Add variable** appended a `VariableRow`. Screenshot `cyc44-workflowform-addbuttons.png` shows the pair rendering identically to the pre-migration markup. Navigated away without saving — no draft persisted, both seed workflows intact (4/5 steps). Committed atomically (`refactor(design): S2 extract AddItemButton quiet-add primitive across 5 sites`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 proper — the pixel-moving mega-page triage):** the byte-identical cross-file lane keeps yielding clean extractions when a mega-page turns out to share an idiom; but the bulk of item 1 is genuinely pixel-moving. Cycle 45 takes **WorkflowForm's reorder clusters** (the ↑/↓/X glyphs, now the page's remaining raw buttons) → `SquareIconButton` (move = neutral tone, remove = `tone="danger"`), which **legitimately moves the Workflows-route baseline** (fixed 28px square + hover chip) — so that cycle regenerates + surfaces the Playwright snapshot intentionally, establishing the pixel-moving rhythm before the larger pages (LoopCockpitPage ~11 → CodePlanReview ~12 → LoopPlanReview ~13 → ChatPage ~19 → ProjectsSection ~20 → CodeCockpitPage ~23). The ghost-`Cancel`/`Done` idiom (WorkflowForm + formControls) and the `formControls` animated add-prerequisite are tracked as their own small consolidations.

### 2026-07-24 — cycle 45 (item 1: migrate WorkflowForm's two reorder clusters — ↑/↓/X glyphs — to `SquareIconButton`) — the outlier text-arrow reorder brought to the dense-square idiom
- **Context:** first genuinely pixel-moving cycle of item 1, per cycle-44's NEXT. WorkflowForm's `StepEditor` renders **two byte-identical reorder clusters** — one at the ref-step row (line ~88), one at the inline-step row (line ~106) — each three raw `<button>`s (move-up ↑, move-down ↓, remove X). These are an **outlier on the reorder idiom**: WorkflowForm used bare text glyphs (`↑`/`↓`) with **no `aria-label`** and hand-rolled `disabled:opacity-30 px-1` chrome, whereas the app's other two reorder-move sites — `CodePlanReview.tsx` and `LoopPlanReview.tsx` — already use lucide `ChevronUp`/`ChevronDown` with accessible names. WorkflowForm is the divergent one; the convergent path is to bring every reorder cluster onto the same dense-square primitive over successive cycles (one page/cycle) so they all land on `SquareIconButton` without fragmenting.
- **Decision (per-role triage, dense-square rule):** the reorder glyphs are the canonical **dense square** case (small glyph, tight cluster, `size-7 rounded-md`) → `SquareIconButton` (`ui/SquareIconButton.tsx`). Move-up/down = **neutral tone** (idle `text-on-surface-low`, hover chip `hover:bg-surface-high`); remove = **`tone="danger"`** (idle neutral, `hover:text-danger`, no fill — matching the primitive's danger semantics). Glyphs swap the bare `↑`/`↓` text for lucide `ChevronUp`/`ChevronDown` (import added) and reuse the existing `X`; each gains a real `label` ("Move step up" / "Move step down" / "Remove step") — the ↑/↓ buttons previously had **no accessible name at all**, so this is also a strict a11y improvement. `disabled` wires to the existing `i === 0` / `i === steps.length - 1` edge guards. **This legitimately moves pixels** (fixed 28px square hit-area + hover chip replacing the bare-glyph `px-1`), so the cycle is baseline-moving by design — but see Evidence: the clusters are interaction+hover-gated and never enter a route-shell snapshot, so no Playwright baseline actually moved.
- **DONE (1 file, 2 clusters):** `WorkflowForm.tsx` imports `SquareIconButton` from `../../ui/SquareIconButton` and adds `ChevronUp, ChevronDown` to the lucide import; both reorder clusters (ref-step + inline-step rows) replace their three raw `<button>`s with `<SquareIconButton icon={ChevronUp} label="Move step up" disabled={i === 0} onClick={() => move(i, -1)} />`, the `ChevronDown` down-mover (`disabled={i === steps.length - 1}`), and `<SquareIconButton icon={X} tone="danger" label="Remove step" onClick={() => remove(i)} />`. The wrapping `opacity-0 group-hover:opacity-100` reveal container is unchanged. `X` import retained (now passed as `icon`). **Two raw `<button>` deliberately left in WorkflowForm** (audit `raw-button: 2`): the WorkflowRefPicker candidate list-row (a semantic selectable option — stays raw, exactly like DependencyEditor's candidate row) and the ghost `Cancel` (part of the 2-file ghost-`Cancel`/`Done` idiom with `formControls`, its own future cycle).
- **Ratchet: `rawButton` 333 → 327 (−6), same commit.** Six literal `<button>` removed (two clusters × three buttons). `primitiveAdoption.test.ts` green against the new 327 confirms **exactly** six removed. `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries one real per-file delta — `WorkflowForm.tsx` raw-button **8→2** (dropping it out of the top-offenders list, where it sat at score 18) — plus a re-ranking artifact (`CommentLayer.tsx`, unchanged 2 raw-button/3 raw-input, bubbles *into* the visible top-N purely because WorkflowForm vacated a slot) and the timestamp; all explained, kept in commit.
- **Evidence:** typecheck clean (both new imports resolve, `X` still used); vitest **132/132** (ratchet green at 327/163; `SquareIconButton.test.tsx` 7 tests unchanged); build green (`index-*.js`, chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement** — despite being a "pixel-moving" cycle, the reorder clusters are **doubly gated** (interaction: a step must exist; hover: `opacity-0 group-hover:opacity-100`), so they never appear in a route-shell Workflows snapshot; correctness therefore rests on the live validation below + the primitive's own tests. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; forced a fresh root reload first — the browser had cached a pre-rebuild `index.html`, the recurring stale-chunk gotcha, and a query-before-hash cachebust attempt broke the SPA router, so I reloaded `/` cleanly): on `#/workflows/new` clicked **Add step** twice, then `browser_snapshot` confirmed the migrated clusters render — step 1 shows **Move up disabled** / Move down enabled / Remove; step 2 shows Move up enabled / **Move down disabled** / Remove (edge-guard `disabled` correct in both directions); a11y names are now **"Move step up" / "Move step down" / "Remove step"** (previously the ↑/↓ had none). Screenshot `cyc45-workflowform-reorder.png` shows the three dense-square buttons with lucide Chevron/X glyphs; `cyc45-remove-hover.png` confirms the **danger-tone hover** on remove (red glyph, no fill). Navigated away without saving — no draft persisted, both seed workflows intact. Committed atomically (`refactor(design): item 1 migrate WorkflowForm reorder clusters to SquareIconButton`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 continues, next mega-page):** WorkflowForm is now at its irreducible floor (2 raw buttons, both deliberate). Cycle 46 takes **LoopCockpitPage (~11 raw buttons)** — the next-smallest mega-page — with the same per-role triage (labelled action→`Button`; round large-glyph→`IconButton`; dense square→`SquareIconButton`), regenerating Playwright baselines wherever pixels legitimately move and surface in a route-shell snapshot. Remaining item-1 queue after that: CodePlanReview ~12 → LoopPlanReview ~13 → ChatPage ~19 → ProjectsSection ~20 → CodeCockpitPage ~23. The ghost-`Cancel`/`Done` idiom (WorkflowForm + formControls) and the `formControls` animated add-prerequisite remain tracked as their own small consolidations.

### 2026-07-24 — cycle 46 (item 1 → LoopCockpitPage: the mega-page turns out already-migrated; the one clean slice is extracting the `QuietButton` compact-toolbar-action idiom across 4 sites) — migrate the idiom, not the page
- **Context:** cycle 45's NEXT put LoopCockpitPage (~11 raw `<button>`) next in the item-1 mega-page queue. Reading it in full surfaced the same scope discovery that reshaped cy44: **LoopCockpitPage is already ~82% migrated** — it imports and uses `IconButton`, `Button`, `HeaderActions`/`HeaderControl`, and its top-of-page/back/nudge/panel actions are already primitives. Per-role triage of the 11 raw buttons: **9 are correctly semantic and stay raw** — the Outputs `role="tab"` strip (map), the task-row + `RailRow` list-rows, the prompt-card / `LiveSubsteps` / `PhaseGroup` disclosures, the clickable project-name nav pill, the rename-title text affordance, and the "Open in Artifacts" link-style button (migrating any of these to a primitive would be *redesign* — a selectable option/tab/row is not chrome; exactly the deliberately-left WorkflowRefPicker candidate row from cy45). **1 is a flagged taste call** — the SidePanel Back-to-list icon (`size-6 rounded-md`, line ~719), a one-off whose only primitive mappings (`SquareIconButton` `size-7` / round `IconButton`) both move pixels → left raw, surfaced for owner veto, not blocked on. **1 is an instance of a byte-identical cross-file idiom** — the findings-log **Download** button.
- **Discovery → decision (the clean slice = the idiom, not the page):** that Download button's className (`inline-flex items-center gap-1 rounded-md px-2 h-7 text-[0.75rem] text-on-surface-low hover:bg-surface-high hover:text-on-surface`) is **verbatim-duplicated across 4 static `<button>` sites in 3 files** — `LoopCockpitPage.tsx` (findings-log Download), `pages/files/artifacts/ArtifactViewer.tsx` ×2 (Source-file + Download), `pages/files/browse/FileViewer.tsx` (Save-as-Artifact). This is the app's **quiet compact inline toolbar action** — deliberately smaller/dimmer/square vs the pill ghost `Button` (h-7/text-[0.75rem]/on-surface-low, not h-8+/pill/on-surface) so it recedes in a content-viewer header row. Migrating it *page-locally in LoopCockpitPage alone* would **fragment a shared idiom into two implementations** — the exact inconsistency this plan removes (the cy44 "migrate the idiom, not the page" lesson). So the correct unit is **the idiom across all 4 sites**: extract one `ui/QuietButton` primitive (item-4 utility-primitive pattern; precedents `AddItemButton` cy44 / `FormFooter` cy42) and adopt it everywhere. Held to the FormFooter three-test discipline — (a) a single VERBATIM string repeats 4×, (b) NO invented API (children + onClick + optional native `title` + className-merge), (c) NO pixel movement — **passes all three → provably byte-identical, autonomous-safe, no taste call**, so I proceeded. Confirmed NOT a name/register collision with `HeaderActions`/`HeaderControl` (that's the responsive h-10 pill header-cluster vocabulary; QuietButton is the h-7 recede-into-a-toolbar register). The delete/revert near-variants in ArtifactViewer (different tokens: `color:var(--color-error)` / warning border) are deliberately NOT folded in — they'd need a `tone`/`variant` API = a real change, not this byte-identical batch.
- **DONE (1 primitive + 1 test + 4 call-site migrations):** new `ui/QuietButton.tsx` — a `<button type="button">` holding the exact idiom via `cx(...)` split across two lines (verified the live string carries **no** `transition-colors`, so the primitive adds none — byte-identical means byte-identical), `children` = caller's leading glyph + label (carry the a11y name), optional `title` (the supplementary tooltip 3 sites pass) + `className`. New `ui/QuietButton.test.tsx` (4 tests, mirrors `AddItemButton.test.tsx`): locks the 10 chrome classes (so a drift toward the h-8 pill ghost reddens), renders children, forwards `title`, merges an extra className without dropping base chrome. All 4 sites import `{ QuietButton }` and replace the inline `<button>`; `Download`/`FileSymlink`/`BookmarkPlus` glyphs retained (now children — each verified still used, no orphaned imports). Post-migration grep confirms the idiom string now lives in **exactly one place** (`ui/QuietButton.tsx`), 0 contiguous occurrences left under `pages/`.
- **Ratchet: `rawButton` 327 → 323 (−4), same commit.** Four literal `<button>` removed (LoopCockpitPage −1, ArtifactViewer −2, FileViewer −1; the new primitive lives under `ui/`, scanner-exempt). `primitiveAdoption.test.ts` green against the new 323 confirms **exactly** four removed. `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries the three real per-file decrements (LoopCockpitPage raw-button 11→10, ArtifactViewer 6→4, FileViewer 2→1) + `filesScanned` 305→306 (the new `ui/QuietButton.tsx` is walked though `ui/`-exempt from counting — same class as cy42's FormFooter `+1`) + timestamp; all legitimate, kept in commit.
- **Evidence:** typecheck clean (all 4 new imports resolve, no orphaned glyph imports); vitest **136/136** (was 132; +4 QuietButton tests; ratchet green at 323/163); build green (`index-ZAQwM3Ht.js`, chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement** — the four migrated actions are all interaction-gated (a file/artifact must be open, or a loop's findings log selected), never in a route-shell snapshot, and the migration is byte-identical regardless. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; forced a fresh root load first — served entry `index-ZAQwM3Ht.js` = disk, dodging the stale-chunk gotcha): in the Files browser (Workspace tab) created a scratch `.md` via **New file** to surface FileViewer's migrated **"Artifact"** QuietButton, and `browser_evaluate` confirmed the real render — className = **exactly** the idiom string, `type=button`, `title="Save as a versioned artifact"` (prop forwarded), computed `height 28px` / `border-radius 12px` / `font-size 12px` / idle `color rgb(154,155,156)` (on-surface-low); a real `.hover()` then flipped it to `background rgb(40,42,44)` (surface-high fill) + `color rgb(227,227,227)` (on-surface brighten) — the `hover:bg-surface-high hover:text-on-surface` behavior live. Screenshot `cyc46-quietbutton-fileviewer.png` shows it receding correctly beside the Preview/Split/Edit segment and the Save CTA. The other 3 sites are the identical primitive call (same proof); LoopCockpitPage's findings-log Download is behind non-empty loop data (none under the empty seed) so it rests on the shared-component proof + unit test + byte-identical rationale. Cleaned up: deleted the 0-byte scratch file `New file` had written to the workspace and closed its tab (instance restored to "No file open"). Committed atomically (`refactor(design): S2 extract QuietButton compact-toolbar-action idiom across 4 sites`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 continues — LoopCockpitPage done at its irreducible floor):** LoopCockpitPage is now at **10 raw buttons, all deliberate** (9 semantic tabs/rows/disclosures/nav/link + the 1 flagged Back-icon taste call) — no further clean slice there without an owner pixel decision on the Back icon. Cycle 47 takes **CodePlanReview (~12 raw buttons)** — the next mega-page — with the same per-role triage (labelled action→`Button`; round large-glyph→`IconButton`; dense square→`SquareIconButton`; semantic rows/tabs stay raw; cross-file idioms extract as their own `ui/` primitive), regenerating Playwright baselines wherever pixels legitimately move + surface in a route-shell snapshot. Remaining item-1 queue after that: LoopPlanReview ~13 → ChatPage ~19 → ProjectsSection ~20 → CodeCockpitPage ~23. Tracked separately: the LoopCockpitPage Back-icon taste call, the ghost-`Cancel`/`Done` idiom (WorkflowForm + formControls), and the `formControls` animated add-prerequisite.

### 2026-07-24 — cycle 47 (item 1 → CodePlanReview: the clean slice is COMPLETING the reorder-stepper idiom across its two remaining raw sites — CodePlanReview + LoopPlanReview) — migrate the idiom, not the page
- **Context:** cycle 46's NEXT put CodePlanReview (~12 raw `<button>`) next in the item-1 mega-page queue. Reading it in full — plus its sibling `LoopPlanReview` (~13 raw), which duplicates the capability-picker flow — surfaced the by-now-familiar scope discovery: **the mega-page is not the unit; the idiom is.** Per-role triage of CodePlanReview's 12 raw buttons: the autopilot/one-by-one **mode-toggle cards** (`aria-pressed`, big selectable radio-like cards — semantic, stay raw), the `PlanArtifacts` **disclosure** (stays raw), the `CapChip` capability **toggle chip** (selectable option, stays raw), the marketplace **Install** button (a hand-rolled `bg-primary/15 … hover:bg-primary/25` tonal fill — see "deferred" below), the quiet **Add-stage** button (a *single-site* compact-add, not an idiom — stays raw this cycle), the exit-criterion / task **remove-X** list-row buttons + stage-**remove Trash2** (destructive list-row affordances, their own future sweep), and the **StageCard reorder ↑/↓ stepper**. Only the last maps cleanly to an established primitive *and* is part of a cross-file idiom.
- **Discovery → decision (the clean slice = the reorder-stepper idiom across ALL its remaining sites):** the reorder up/down stepper is a **genuine 3-site cross-file idiom**, and `SquareIconButton`'s own doc names "reorder up/down" as its purpose. After cycle 45, `WorkflowForm` already uses `SquareIconButton` for its stepper; the **two remaining raw outliers** are `CodePlanReview.StageCard` (lines 540/542, `rounded hover:bg-surface-high hover:text-on-surface disabled:opacity-30`, bare 14px `ChevronUp`/`ChevronDown`) and `LoopPlanReview.PhaseCard` (lines 736/738, `text-on-surface-low hover:text-on-surface disabled:opacity-25 disabled:pointer-events-none`, bare 14px chevrons — note: **no hover-fill**, unlike the other two). Migrating **only** CodePlanReview would *fragment* the idiom (SquareIconButton in two files, raw in the third) — the exact cy44 "migrate the idiom, not the page" anti-pattern. So the atomic unit is **completing the convergence across both remaining sites in one commit**, landing all three reorder steppers on the same primitive. The other `onMove` hits in the tree (ChatPage `TagBoard`, Tasks `TaskBoard`) are **drag-and-drop kanban**, a different idiom — correctly untouched.
- **Not this cycle (deferred, with reasons):** (a) **the two divergent sibling capability pickers** — CodePlanReview uses `CapChip` pill chips + a **tonal-fill** Install (`bg-primary/15 … hover:bg-primary/25`), LoopPlanReview uses `CapRow` checkbox-rows + an **outlined** Install (`rounded-pill border … hover:bg-surface-high`); unifying them is a *redesign*/taste call (which shape wins?), forbidden by the soul guardrail, not an autonomous byte-identical slice. (b) **the hand-rolled tonal-fill idiom** (`Button variant="tonal"` was created expressly to absorb `bg-primary/15 text-primary hover:bg-primary/25`) appears at only 3 page sites, all **divergent shapes/roles** — CodePlanReview's `rounded-md px-2 py-0.5` Install action, its `CapChip` selected-state ternary, and CodeCockpitPage's `aria-pressed` autopilot toggle — none matching `Button`'s pill `tonal` geometry (`size="xs"` = `h-7 px-m text-[0.8125rem]` ≠ `px-2 py-0.5 text-[0.75rem]`), so a swap **moves pixels AND changes semantics** (a toggle chip is not a `Button`). Deferred to a dedicated tonal-convergence cycle with an owner pixel call. (c) the single-site Add-stage quiet button and the destructive remove-X list-rows — each their own future sweep.
- **DONE (2 files, 2 steppers):** `CodePlanReview.tsx` imports `SquareIconButton` from `../../ui/SquareIconButton` and its `StageCard` reorder gutter replaces the two raw `<button>`s with `<SquareIconButton icon={ChevronUp} label="Move stage up" disabled={index === 0} onClick={() => onMove(-1)} />` + the `ChevronDown` down-mover (`disabled={index === count - 1}`). `LoopPlanReview.tsx` imports `SquareIconButton` and its `PhaseCard` gutter does the same with `label="Move phase up"`/`"Move phase down"` wired to `onMoveUp`/`onMoveDown` and the `index === 0` / `index === total - 1` edge guards. `ChevronUp`/`ChevronDown` imports retained in both (now passed as `icon`; CodePlanReview also still uses them for the `PlanArtifacts` disclosure). The surrounding index-number `<span>` and flex-column gutter layout are unchanged. **This legitimately moves pixels** — a bare 14px glyph becomes the standardized `size-7` (28px) `rounded-md` dense-square hit-area; LoopPlanReview additionally *gains* the `hover:bg-surface-high` fill the other two sites already have (the convergence point), and both gain the primitive's expressiveness-scaled press-spring + reduced-motion safety. Both had accessible names already (`aria-label`/`title`), so this is name-preserving, not an a11y change.
- **Ratchet: `rawButton` 323 → 319 (−4), same commit.** Four literal `<button>` removed (CodePlanReview −2, LoopPlanReview −2). `primitiveAdoption.test.ts` green against the new 319 confirms **exactly** four removed (the "not-stale" nudge also passes — live == baseline). `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries the two real per-file decrements (CodePlanReview raw-button **12→10**, LoopPlanReview **13→11**) + timestamp; `filesScanned` unchanged at 306 (no new `ui/` file this cycle — `SquareIconButton` already existed). All legitimate, kept in commit.
- **Evidence:** typecheck clean (both new imports resolve; `ChevronUp`/`ChevronDown` still used in both files); vitest **136/136** (ratchet green at 319/163; `SquareIconButton.test.tsx` 7 tests unchanged); build green (chunk-size warning pre-existing); visual **64/64 with ZERO baseline movement** — both migrated steppers live in **deep sub-flows** (CodePlanReview reached only after a code-project plan is generated; LoopPlanReview after a goal-loop plan), which are never in the top-level route-shell snapshot set, so no baseline moved (correctly). Because those sub-flows need a model backend the offline empty-seed loopback instance lacks, correctness rests on the live validation of the **identical primitive at its reachable site**: **validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; fresh root load first, dodging the stale-chunk gotcha; zero console errors on load + after interaction) — opened `#/workflows` → `debug-failing-test` (5 steps) → **Edit**, and `browser_snapshot` confirmed the `SquareIconButton` reorder stepper renders with correct edge-guard semantics: **step 1 "Move step up" `[disabled]`**, **step 5 "Move step down" `[disabled]`**, all middle steps enabled — the exact primitive + disabled wiring the two migrated plan-review sites now inherit verbatim (screenshot `cy47-reorder-stepper-live.png`; the chevrons are `opacity-0 group-hover:opacity-100` hover-gated in WorkflowForm, hidden in a static shot but present + correctly-disabled in the a11y tree). Cancelled the edit — seed workflows intact. Committed atomically (`refactor(design): item 1 complete reorder-stepper convergence — CodePlanReview + LoopPlanReview → SquareIconButton`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 continues — reorder-stepper idiom now fully converged):** all three reorder steppers (WorkflowForm, CodePlanReview, LoopPlanReview) now land on `SquareIconButton`; the idiom is complete. CodePlanReview + LoopPlanReview each drop to their remaining raw floor (mode-toggle cards, disclosures, capability chips/rows, list-row remove-X's, single-site Add buttons, the divergent tonal/outlined Install buttons) — no further *clean cross-file idiom* slice there without an owner taste decision. Cycle 48 takes **ChatPage (~19 raw buttons)** — the next mega-page — same per-role triage. Remaining item-1 queue after that: ProjectsSection ~20 → CodeCockpitPage ~23. Newly tracked as their own future cycles: the **tonal-fill convergence** (`Button variant="tonal"` vs the 3 divergent hand-rolled sites — needs an owner pixel call), the **sibling capability-picker unification** (CapChip-vs-CapRow redesign — owner taste call), and the **destructive list-row remove-X** sweep (exit-criteria/task/stage removes). Still tracked from before: LoopCockpitPage Back-icon taste call, the ghost-`Cancel`/`Done` idiom, the `formControls` add-prerequisite.

### 2026-07-24 — cycle 48 (item 1 → ChatPage: the clean slice is extracting the `SelectionPill` floating selection-action idiom across its 2 byte-identical cross-file sites — ChatPage + CommentLayer) — migrate the idiom, not the page
- **Context:** cycle 47's NEXT put ChatPage (~19 raw `<button>`, the largest mega-page reached so far) next in the item-1 queue. Reading it in full — plus its lines 188/296/1868/1876/1885/2117/2182/2255/2328/2411/2492/2611/2622/2659/2709/3009/3027/3100/3189 — surfaced the by-now-familiar scope truth: **ChatPage is overwhelmingly semantic chrome, and its one provably-clean migration is a cross-file idiom, not a page-local edit.** Per-role triage of the 19: **~13 are correctly semantic and stay raw** — the View-all-chats nav link (188), the rename-title text affordance (1868), the project-binding nav pill (1885), the attachment-preview chip (2117), the mention-path disclosure (2182), the knowledge-picker checkbox row (2255), the QueueStack header disclosure (2328), the PasteCards link-like preview (2411), the options suggestion chips (2622), the AgentWork/ContextLedger disclosures (2659/2709), the origin nav chip (3009), and the tag-filter toggle chip (3100) — each a tab/row/disclosure/nav/link/chip, not chrome (migrating any would be *redesign*, the deliberately-left-raw class from cy45–47). The remaining 6 are candidate migrations, but 5 of them are **taste/pixel calls or fragment-risks** (see "Not this cycle"). Only the floating selection pill maps to a **provably byte-identical cross-file idiom**.
- **Discovery → decision (the clean slice = the `SelectionPill` idiom across BOTH its sites):** the floating "act on the current text selection" pill is a **genuine 2-site cross-file idiom** whose `<button>` className is **duplicated character-for-character across two files** — `ChatPage.tsx`'s `SelectionQuote` (line 2493, the "Quote" bubble that drops a passage into the composer) and `pages/files/comments/CommentLayer.tsx`'s selection affordance (line 94, the "Comment" bubble) — string: `absolute z-30 -translate-x-1/2 -translate-y-full inline-flex items-center gap-1.5 rounded-pill bg-surface-highest px-3 h-8 text-on-surface text-[0.8125rem] shadow-lg ring-1 ring-outline-variant/50 hover:bg-surface-high`. The author had already annotated CommentLayer *"mirrors ChatPage SelectionQuote"* — the duplication was known, just un-DRY'd. Migrating **only** ChatPage would *fragment* the idiom into two implementations — the exact cy44 "migrate the idiom, not the page" anti-pattern. So the atomic unit is **extracting one `ui/SelectionPill` primitive and adopting it at both sites in one commit**. Held to the FormFooter/AddItemButton/QuietButton three-test discipline — (a) a single VERBATIM chrome string repeats 2×, (b) minimal API with **no invented visual language** (`icon`+`label`+content-relative `x`/`y`+`onPress`, forwarding a `ref` so each page keeps excluding pill-clicks from its own mouseup/mousedown selection handler, and preserving the `preventDefault`+`stopPropagation`-before-action ordering that keeps the browser selection alive), (c) **zero pixel movement** (className emitted verbatim) — **passes all three → autonomous-safe, no taste call**, so I proceeded. The differing halves stay in each page: the selection-detection `useEffect` (ChatPage's transcript-scoped vs CommentLayer's composer-aware variant) and the positioning state are **not** hoisted — extracting them would be a behavioral rewrite, forbidden by the soul guardrail. Only the identical presentational `<button>` is the shared unit.
- **Not this cycle (deferred, with reasons):** (a) the **hover-reveal on/off toggle** (Pin @3028 + Organize @3190) — byte-identical to each other but for the ternary variable (`rounded-md p-1.5 transition-opacity hover:bg-surface-highest` + `{active ? 'text-primary opacity-100' : 'text-on-surface-low opacity-0 group-hover:opacity-100'}`); its base chrome is **shared with the 3-site destructive remove-X family** (`ProjectsSection.tsx:179`, `CodeSection.tsx:384`, `CodePlanReview.tsx:558`, all `hover:text-danger`). Together that is a **5-site `RowActionButton` family with three non-identical variants** (toggle / danger-reveal / danger-always) — a *design* unification needing a `tone`/`active` API, not a byte-identical merge; doing only the 2 ChatPage toggles now would fragment the family. Deferred to a dedicated cross-file cycle (folds in the already-tracked remove-X sweep). (b) the **primary-fill CTA** (switchTo @2611, `rounded-pill bg-primary px-4 h-9 … hover:opacity-90`) — a hand-rolled primary that diverges in geometry from both `Button` `md` (`h-10 px-xl`) and its nearest cross-file cousin (`CommentLayer:216`, `px-2.5 h-6`); swapping to `Button variant="primary"` **moves pixels** → owner pixel call (part of the tracked primary-fill convergence). (c) the **"Continue in full chat"** quiet action (296, `rounded-pill px-m h-7 text-on-surface-low hover:bg-surface-high`) — visually adjacent to `QuietButton` but `rounded-pill`/`px-m` ≠ QuietButton's `rounded-md`/`px-2`, so adoption moves corners+padding → pixel call. (d) the **regen-title micro-glyph** (1876, `size-5 rounded-full`) — smaller than `IconButton`'s 40px round; no matching primitive without a pixel move. Each surfaced for owner veto, none blocked on.
- **DONE (1 primitive + 2 call-site migrations):** new `ui/SelectionPill.tsx` — a `forwardRef<HTMLButtonElement>` `<button type="button">` holding the exact idiom string verbatim, props `icon`(LucideIcon)+`label`+`x`+`y`+`onPress`; the `onMouseDown` fires `preventDefault`→`stopPropagation`→`onPress` (selection-preserving order), `style={{ left: x, top: y }}` takes the parent's content-relative coords. `ChatPage.tsx`'s `SelectionQuote` return replaces its inline `<button>` with `<SelectionPill ref={btnRef} icon={Quote} label="Quote" x={pos.x} y={pos.y} onPress={() => { onQuote(pos.text); setPos(null); window.getSelection()?.removeAllRanges() }} />`; `CommentLayer.tsx`'s selection affordance becomes `<SelectionPill ref={btnRef} icon={MessageSquarePlus} label="Comment" x={sel.x} y={sel.y} onPress={openComposer} />`. Both keep their `btnRef` (still forwarded, still used by each page's own selection-exclusion logic) and their `Quote`/`MessageSquarePlus` glyph imports (now passed as `icon`). Import `{ SelectionPill }` added to both (ChatPage `../ui/SelectionPill`, CommentLayer `../../../ui/SelectionPill`). Post-migration the idiom string lives in exactly one place (`ui/SelectionPill.tsx`); 0 occurrences remain under `pages/`.
- **Ratchet: `rawButton` 319 → 317 (−2), same commit.** Two literal `<button>` removed (ChatPage −1, CommentLayer −1; the new primitive lives under `ui/`, scanner-exempt). `primitiveAdoption.test.ts` green against the new 317 confirms **exactly** two removed. `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries the two real per-file decrements (ChatPage raw-button **19→18**, CommentLayer **2→1**) + `filesScanned` **306→307** (the new `ui/SelectionPill.tsx` is walked though `ui/`-exempt from counting — same class as cy42 FormFooter / cy44 AddItemButton / cy46 QuietButton `+1`) + a top-offenders re-rank artifact (CommentLayer vacates its slot; `pages/chat/MessageActions.tsx` at 3 raw-button bubbles into the visible top-N) + timestamp; all legitimate, kept in commit.
- **Evidence:** typecheck clean (all new imports resolve; `Quote`/`MessageSquarePlus`/`btnRef` all still used, no orphans); vitest **136/136** (ratchet green at 317/163; no new unit test this cycle — the pill is a pure presentational verbatim-string wrapper with no branching logic, and the ratchet's class-lock already guards its chrome; a bespoke class-lock test would duplicate `primitiveAdoption` with no added signal, unlike FormFooter/AddItemButton/QuietButton which carry `className`-merge + prop-forwarding branches worth locking — noted as a deliberate judgement, flagged for veto); build green (`index-5Kf8_lCa.js`, chunk-size warning pre-existing); visual **N/A-by-construction** — the pill only mounts on an active text selection over transcript/preview content (interaction-gated, never in a route-shell snapshot) **and** the emitted markup is byte-identical, so there is no visual-regression surface to move. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; fresh root load, served entry `index-5Kf8_lCa.js` = the just-built disk bundle, dodging the stale-chunk gotcha): navigated to `#chat` and `browser_snapshot` confirmed ChatPage — which now imports `SelectionPill` at module top-level — **boots and renders fully** (composer, task/permission-mode toggles, No-project chip, Persistent/Temporary/Incognito memory tabs, all six suggestion chips), with **zero console errors/warnings**; a load-time import fault would have crashed the page, so the healthy mount proves the wiring. The pill's rendered appearance is byte-identical to the pre-migration inline markup by construction (className preserved verbatim), and its live-appearance path (a text selection over generated transcript / a file preview) is the documented empty-seed sub-flow limit — no model backend to produce selectable transcript content on the offline loopback instance — so correctness there rests on the byte-identical rationale + the shared-component proof. Committed atomically (`refactor(design): S2 extract SelectionPill floating selection-action idiom across 2 sites`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 continues — ChatPage done at its clean-slice floor):** ChatPage's one provably-byte-identical cross-file idiom (`SelectionPill`) is now extracted; its remaining raw buttons are ~13 semantic (tabs/rows/disclosures/nav/links/chips — correctly raw) + 5 flagged taste/pixel calls (the Pin/Organize toggle → the deferred 5-site `RowActionButton` family; switchTo primary-fill → tracked tonal/primary convergence; Continue-in-full-chat → QuietButton pixel call; regen-title micro-glyph → no matching primitive) — no further *clean* slice there without an owner decision. Cycle 49 takes **ProjectsSection (~20 raw buttons)** — the next mega-page — same per-role triage (labelled action→`Button`; round large-glyph→`IconButton`; dense square→`SquareIconButton`; semantic rows/tabs/chips stay raw; cross-file idioms extract as their own `ui/` primitive), then **CodeCockpitPage ~23** closes the item-1 mega-page queue. Note ProjectsSection carries one member of the deferred remove-X family (`:179`) — if its clean slice turns out to *be* that family, cy49 may promote the **5-site `RowActionButton`** cross-file cycle instead. Still tracked as their own future cycles: the **`RowActionButton`/remove-X family** (Pin/Organize toggle + 3 danger-removers), the **tonal/primary-fill convergence** (needs owner pixel call), the **sibling capability-picker unification** (owner taste call), the LoopCockpitPage Back-icon taste call, the ghost-`Cancel`/`Done` idiom, and the `formControls` add-prerequisite.

### 2026-07-24 — cycle 49 (item 1 → ProjectsSection: the clean slice is extracting the `InlineError` dismissible-error-banner idiom across its 3 byte-identical cross-file sites — ProjectsSection ×2 + CodeSection) — migrate the idiom, not the page
- **Context:** cycle 48's NEXT put ProjectsSection (~20 raw `<button>`, at lines 127/178/319/494/502/509/516/555/611/718/725/729/731/741/762/804/808/830/854/860) next in the item-1 queue, flagging that if its clean slice turned out to *be* the deferred 5-site `RowActionButton`/remove-X family it might promote that cross-file cycle instead. Full per-role triage of the 20 settled it differently. **~11 are correctly semantic and stay raw** — the artifact list row (611), the `FolderChip` path button (718), the `TaskListRow` (741) and `TaskListPanel` task row (762), the `DirTreePanel` breadcrumb pair (804/808), the `DirEntryRow` (830), and the `BriefRow` edit affordance (860) are all list-rows / breadcrumbs / disclosures, not chrome; the rename-Cancel X (494) and `FolderChip` Unbind X (731) are single-site inline cancels; the small icon actions (Rename pencil 502, `FolderChip` browse 725) and the labelled toggles (Set-active 509, New-popover trigger 516, `FolderChip` Bind/Change 729) are single-site or divergent-shape. The remaining candidates map to **deferred families or taste calls** (see "Not this cycle"), except one: the dismissible error banner, which is a **provably byte-identical cross-file idiom**.
- **Discovery → decision (the clean slice = the `InlineError` idiom across ALL its sites):** the inline dismissible error strip (`{err && <div role="alert" …>message + corner-×</div>}`) is a **genuine 3-site cross-file idiom** whose chrome is duplicated character-for-character: `ProjectsSection.tsx` ListView (123, `mx-l mt-2` outer margin, no leading icon), `ProjectsSection.tsx` project Hub (551, no margin, no icon), and `CodeSection.tsx` failed-action callout (296, `mx-l mt-2`, WITH a leading `AlertTriangle size={14} className="shrink-0"`). All three share the exact band (`role="alert"` · `flex items-center gap-2 rounded-lg px-3 py-2 text-[0.8125rem]` · `background: color-mix(in srgb, var(--color-danger) 10%, transparent)` · `color: var(--color-danger)`), the `<span className="min-w-0 flex-1">{msg}</span>`, and the verbatim dismiss `<button type="button" … aria-label="Dismiss" className="shrink-0 hover:opacity-70"><X size={14} /></button>`. The only per-site variation is (a) an optional leading `AlertTriangle` and (b) the outer-margin class — both cleanly expressible as props (`icon?: boolean`, `className?`) with **zero pixels moved** (chrome emitted verbatim; icon/margin conditionals reproduce each site exactly). Migrating **only** ProjectsSection would fragment the idiom into two implementations — the cy44 anti-pattern. So the atomic unit is **extracting one `ui/InlineError` primitive and adopting it at all three sites in one commit**. Held to the FormFooter/AddItemButton/QuietButton three-test discipline: (a) a VERBATIM chrome band + dismiss button repeats 3×, (b) minimal API with **no invented visual language** (`children` message + `onDismiss` + `icon` toggle + `className` passthrough — nothing the sites didn't already render), (c) **zero pixel movement** — **passes all three → autonomous-safe, no taste call**, so I proceeded. **This is NOT the promotion the cy48 note hedged on:** ProjectsSection's remove-X member (`:178`) is destructive-row chrome that still needs a `tone`/`active` API to merge with the toggle/danger-always variants (a design unification, owner call) — the *cleaner* byte-identical slice here is the error banner, so the `RowActionButton` family stays deferred exactly as before.
- **Not this cycle (deferred, with reasons):** (a) the **remove-X list-row delete** (178, `shrink-0 rounded-md p-1.5 text-on-surface-low opacity-0 transition-opacity hover:bg-surface-highest hover:text-danger group-hover:opacity-100 focus-visible:opacity-100`) is byte-identical to `CodeSection.tsx:384` and near-identical to `CodePlanReview.tsx:558` + the ChatPage Pin/Organize toggles — the tracked **5-site `RowActionButton` family** whose three non-identical variants (toggle / danger-reveal / danger-always) need a `tone`/`active` API = design unification, not a byte-identical merge; still its own future cross-file cycle. (b) the **primary-fill "Open project" CTA** (319, `rounded-pill bg-primary px-4 h-9 … hover:bg-primary-emphasis` + `fvs(470)`) — a hand-rolled primary diverging in geometry from `Button` `md` (`h-10 px-xl`); swapping moves pixels → owner pixel call (the tracked primary-fill convergence). (c) the **labelled toggles** — Set-active/Active (509, `rounded-md px-2 py-1 text-[0.75rem]`) and the New-popover trigger (516, `rounded-md px-2.5 py-1 … bg-primary/90 text-on-primary`) and `FolderChip` Bind/Change (729) — single-site divergent-shape actions; adopting `Button`/tonal moves pixels + changes semantics (a toggle is not a `Button`) → owner call. (d) the **ghost `Cancel`** in `BriefRow` (854, `rounded-md px-2 py-1 text-[0.75rem] text-on-surface-low hover:text-on-surface`) — instance of the already-tracked ghost-`Cancel`/`Done` idiom. (e) the small single-site cancel/icon buttons (494/502/725/731) — not idioms, correctly raw. Each surfaced for owner veto, none blocked on. **Also NOT folded into `InlineError` (divergent shapes/tones — a convergence call for the owner, since matching them would move pixels):** ChatPage's non-dismissible multi-line turn `ErrorLine` (a transcript message, `items-start`, `whitespace-pre-wrap`, no dismiss), the tighter danger-14% file strip `FilesSection.tsx:351` (`rounded-md px-2.5 py-1.5 text-[0.75rem] items-start`, `X size={12}`), and the animated **warn**-toned `MoveErrorBanner` `TasksListPage.tsx:428` (`motion.div`, `warn` 14%, `items-start`). ModelsPanel:222 is a status/progress box merely danger-*tinted* on error — not an error banner at all. Documented in the primitive's own docblock.
- **DONE (1 primitive + 1 unit test + 3 call-site migrations):** new `ui/InlineError.tsx` — `{ children, onDismiss, icon = false, className }`; renders the exact band via `cx('flex items-center gap-2 rounded-lg px-3 py-2 text-[0.8125rem]', className)` + the verbatim danger `style`, an optional leading `<AlertTriangle size={14} className="shrink-0" />` gated on `icon`, the `<span className="min-w-0 flex-1">{children}</span>`, and the verbatim dismiss button. `ProjectsSection.tsx`: both banners (123 ListView, 551 Hub) → `<InlineError className="mx-l mt-2" onDismiss={() => setErr(null)}>{err}</InlineError>` and `<InlineError onDismiss={() => setErr(null)}>{err}</InlineError>`; import `{ InlineError }` added (`../../ui/InlineError`); `X`/`AlertTriangle` imports retained (both still used at 494/731 and 766). `CodeSection.tsx`: banner (296) → `<InlineError icon className="mx-l mt-2" onDismiss={() => setActionErr(null)}>{actionErr}</InlineError>`; import added; **dead `X` import dropped** (its only use was this banner); `AlertTriangle` retained (still used at 310). Post-migration the idiom string lives in exactly one place (`ui/InlineError.tsx`); 0 canonical-band occurrences remain under `pages/`. Added `ui/InlineError.test.tsx` (4 tests, FormFooter-style class-lock) — unlike cy48's pure-wrapper SelectionPill, this primitive carries real branching (the `icon` conditional, the `cx` className merge, the `onDismiss` wiring) worth locking: asserts the `role="alert"` danger band chrome, message render, `onDismiss` fires on the corner ×, the icon toggle (1 svg default / 2 with `icon`), and the margin-className merge.
- **Ratchet: `rawButton` 317 → 314 (−3), same commit.** Three literal `<button>` removed (ProjectsSection −2, CodeSection −1; the new primitive lives under `ui/`, scanner-exempt). `primitiveAdoption.test.ts` green against the new 314 confirms **exactly** three removed (live == baseline, the not-stale nudge also passes). `rawInput`/`rawDialog`/`inlineFontWeight` untouched. `docs/design/consistency-audit.json` regenerated by vitest carries the two real per-file decrements (ProjectsSection raw-button **20→18**, CodeSection **4→3**) + `filesScanned` **307→308** (the new `ui/InlineError.tsx` is walked though `ui/`-exempt from counting — same class as cy42 FormFooter / cy44 AddItemButton / cy46 QuietButton / cy48 SelectionPill `+1`) + a top-offenders re-rank artifact (CodeSection at 3 raw-button drops out of the visible top-N; `pages/chat/SessionSkillsReview.tsx` bubbles in) + timestamp; all legitimate, kept in commit.
- **Evidence:** typecheck clean (all new imports resolve; the dropped `X` in CodeSection had no other use; `AlertTriangle` still used in both files, no orphans); vitest **140/140** (136 prior + 4 new `InlineError.test.tsx`; ratchet green at 314/163); build green (`index-D7t2wL9e.js`, chunk-size warning pre-existing); visual **N/A-by-construction for the shell + covered by unit test** — the banners are error-state-gated (mount only after a failed action) so they never appear in a route-shell snapshot, and the emitted markup is byte-identical by construction, so there is no visual-regression surface to move; the primitive's exact chrome + behavior is instead locked by the new unit test. **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; served entry `index-D7t2wL9e.js` = the just-built disk bundle, confirmed matching, dodging the stale-chunk gotcha): a headless Playwright smoke loaded both migrated routes — `#/projects` and `#/code`, each now importing `InlineError` at module top-level — and confirmed **both boot and render real content with ZERO console errors** and no error boundary (bodyTextLen 283 / 463); a load-time import fault would have crashed the page, so the healthy mount proves the wiring. The live error-banner appearance path (trigger a real failed project/code action) needs backend failures the offline empty-seed loopback instance can't produce — the documented empty-seed sub-flow limit — so correctness there rests on the byte-identical rationale + the unit-test class-lock + the shared-component proof. (Browser MCP couldn't launch this session — it collided with an already-open Chrome on the shared user-data-dir — so validation used the headless Playwright driver at repo-root `node_modules`, same engine/bundle.) Committed atomically (`refactor(design): S2 extract InlineError dismissible-error-banner idiom across 3 sites`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 continues — ProjectsSection done at its clean-slice floor):** ProjectsSection's one provably-byte-identical cross-file idiom (`InlineError`) is now extracted; its remaining raw buttons are ~11 semantic (list-rows/breadcrumbs/disclosures/nav-chips — correctly raw) + the deferred-family / taste-call members (remove-X `:178` → 5-site `RowActionButton`; Open-project primary-fill → tonal/primary convergence; Set-active/New/Bind toggles → owner pixel calls; `BriefRow` Cancel → ghost-`Cancel`/`Done` idiom) — no further *clean* slice there without an owner decision. Cycle 50 takes **CodeCockpitPage (~23 raw buttons)** — the last mega-page — same per-role triage, closing the item-1 mega-page queue. Still tracked as their own future cycles: the **`RowActionButton`/remove-X family** (Pin/Organize toggle + 3 danger-removers, now 4 confirmed byte-identical/near sites), the **tonal/primary-fill convergence** (owner pixel call), the **sibling capability-picker unification** (owner taste call), the **`InlineError` divergent-variant convergence** (FilesSection danger-14% strip + `MoveErrorBanner` warn tone — owner call, newly tracked this cycle), the LoopCockpitPage Back-icon taste call, the ghost-`Cancel`/`Done` idiom, and the `formControls` add-prerequisite.

### 2026-07-24 — cycle 50 (item 1 → CodeCockpitPage, the LAST mega-page: all 23 raw buttons are semantic / page-local / owner-gated — none is an autonomous raw-button migration; the one provably-byte-identical slice the triage surfaced is deduping the verbatim `Centered` layout helper across 3 files) — migrate the idiom, not the page; when a mega-page has no clean button slice, take the clean *component* dedup it exposes
- **Context:** cycle 49's NEXT put CodeCockpitPage (~23 raw `<button>`, at lines 608/752/910/1149/1349/1492/1587/1675/1702/1879/1948/1952/2364/2385/2750/2764/2768/2773/2780/2834/3118/3344/3352) next — the **last** item-1 mega-page, closing the queue. Full per-role triage confirmed what the file is: a mini-IDE cockpit built almost entirely of **semantic** elements, none of which is a `Button`/`IconButton`/`SquareIconButton` in disguise.
- **Triage → decision (all 23 stay raw; the clean slice is a component dedup, not a button migration):** **~15 are correctly semantic** — the title→rename toggle (608), the CodeToast per-kind "Respond" whose comment already documents its dynamic tone fill as "a fill the Button variants deliberately don't cover" (752), the project deep-link chip (910), the `FileFinder` result row (1149), the `RailTab` tab (1349), the Autopilot `aria-pressed` mode toggle (1492), the `StageGroup` `aria-expanded` disclosure (1587), the navigable `TaskRow` (1675), the `BlockerList` nav chip (1702), the `ChangesPanel` file row (2364), the commit-history row (2385), the editor tab-label + its two close-X buttons (2764/2768/2773, single-site inline), the evidence `aria-expanded` disclosure (3118), and the `FilesTouched` chip (3344) — rows / tabs / disclosures / toggles / nav-links, not chrome. The rest are **page-local pairs or owner-gated families, none a clean primitive migration:** (a) the **quiet text-expander** ("↑ N earlier cycles" 1879 + "+N more" 3352, both `rounded px-1.5 py-0.5 text-[0.75rem] text-on-surface-low/80 hover:text-primary`) — a byte-identical PAIR but **confined to this file** (grep: 2/2 sites here, 0 elsewhere), so page-local, not a cross-file idiom. (b) the **panel-reopen chips** (2750/2780, differ only by `PanelLeftOpen`/`PanelRightOpen` + `mr-0.5`/`ml-0.5`) — also confirmed **unique to CodeCockpitPage** (`reopenPanel`/`collapsedLeft`/`collapsedRight` grep = this file only), page-local. (c) the **text-link** "Remove from queue" pair (1948/1952, `text-primary hover:underline`) — part of an 8-file footprint (Onboarding/VoicePanel/AppsPanel/MemoryPanel/TasksListPage/FileViewer[an `<a>`]/LoopCockpitPage) that is **NOT byte-identical** (varies by element type, size class, icon, inline-flex vs inline-text, margins, `disabled` state) — extraction needs an invented size/icon-slot API → violates "no invented visual language," owner call. (d) the **"Follow the worker"** quiet ghost (2834) — a single-site instance of the broad quiet-ghost *hover treatment* (`text-on-surface-low hover:bg-surface-high hover:text-on-surface`) that already lives in QuietButton/SquareIconButton but with divergent geometry everywhere (size-6/7/8, p-1, px-2 py-1/py-1.5, px-1.5 py-0.5) → a shared hover color, not a byte-identical extractable idiom. **Conclusion: the mega-page has no autonomous raw-button slice** — every button is semantic, page-local, or owner-gated. This is the honest "idiom is the unit" close of the queue: the page's high raw-button *count* (the audit's #1-ranked offender, score 50) is not a consistency *defect* — it's a dense cockpit of legitimately-raw semantic elements.
- **Discovery → the clean slice the triage exposed (verbatim component dedup, GraphZoomControls/cy38 class):** sweeping the page's helpers surfaced that its `Centered` layout wrapper (`function Centered({children}) { return <div className="flex h-full items-center justify-center">{children}</div> }`, 3372) is defined **byte-identically in 3 files** — `CodeCockpitPage.tsx:3372`, `pages/files/browse/FileViewer.tsx:244`, `ui/content/ContentSurface.tsx:321` (all three the exact same one-liner; 9 + 3 + 1 = 13 call sites). A **fourth, same-named helper in `app/Onboarding.tsx:223` had already DRIFTED to a different layout** (`flex items-center py-2`, no `h-full`, no `justify-center`) — the precise hazard uncontrolled duplication invites, and exactly why the dedup is a real consistency win, not cosmetic. This is the same autonomous-authorized class as cy4's `unreadRail`/`toneChipBg`, cy35's `NumberField`, and cy38's `GraphZoomControls`: **dedup a verbatim duplicate component into one `ui/` source, no new visual language, zero pixels moved.** Passes the three-test discipline trivially — (a) a verbatim chrome one-liner repeats 3×, (b) no invented API (`children` only), (c) zero pixel movement. Onboarding's drifted copy is deliberately **left untouched** (it's a genuinely different layout; homogenizing it would move pixels = a taste call), documented in the primitive's docblock.
- **DONE (1 primitive + 3 call-site dedups):** new `ui/Centered.tsx` — `export function Centered({ children }: { children: ReactNode })` returning the exact `<div className="flex h-full items-center justify-center">` verbatim, with a docblock naming the three donor sites + the Onboarding drift it deliberately does not touch. Following cy48's SelectionPill precedent (a pure verbatim wrapper with **no branching** gets NO test — unlike InlineError's real `icon`/`cx`/`onDismiss` branching), **no test added**; the tsc structural check + the byte-identical construction are the guarantee. `CodeCockpitPage.tsx`, `FileViewer.tsx`, `ContentSurface.tsx` each drop their inline `function Centered` and `import { Centered }` from `ui/Centered` (paths `../../ui/Centered`, `../../../ui/Centered`, `../Centered` respectively); all 13 `<Centered>` call sites keep working unchanged. Post-dedup the helper lives in exactly one place.
- **Ratchet: UNCHANGED at `rawButton` 314 (correct — no button migrated).** The `Centered` dedup is a pure layout wrapper, not counted chrome; migrating it moves no `<button>`/`<input>`. `primitiveAdoption.test.ts` stays green at 314/163/0/7. `docs/design/consistency-audit.json` regenerated by vitest carries only `filesScanned` **308→309** (the new `ui/Centered.tsx` is walked but `ui/`-exempt from counting — identical class to cy42/44/46/48/49's `+1`) + timestamp; **no `ranked`/`byFile`/`primitivesByFile`/`drift` change** (the 3 consumers had no chrome-count delta), all legitimate, kept in commit.
- **Evidence:** typecheck clean (all 3 new imports resolve; removing the local `Centered` orphaned no import — `React.ReactNode` in those files resolved via the `@types/react` UMD global, the new primitive uses `import type { ReactNode }` per `ui/` convention; ContentSurface's `type ReactNode` import stays, still used at 75/78/80); vitest **140/140** (unchanged — no new test, ratchet green at 314); build green (`index-i4c2B3BA.js`, chunk-size warning pre-existing). **Validated as a user live on `127.0.0.1:10400`** (empty-seed E2E instance; served entry `index-i4c2B3BA.js` = the just-built disk bundle, confirmed matching, dodging the stale-chunk gotcha): a headless Playwright smoke loaded the routes exercising all 3 migrated files — `#/code` (CodeCockpitPage) and `#/files` (FileViewer + ContentSurface), plus `#/projects` for parity — and confirmed **all boot and render real content with ZERO console errors** and no error boundary (bodyLen 464 / 381 / 283); each migrated file imports `Centered` at module top-level, so a load-time import fault would have crashed the page — the healthy mount proves the wiring resolves. The deeper `<Centered>` render paths (the cockpit loading spinner, the load-failure placeholder, the ContentSurface `<Suspense>` fallback) need a real project/file the offline empty-seed loopback instance can't produce — the documented empty-seed sub-flow limit — so their correctness rests on the byte-identical construction + tsc. **Design-hook note:** the scan flagged a pre-existing `side-tab` accent at CodeCockpitPage:742 — this is the CodeToast's per-kind severity accent (`borderLeft: 3px solid ${tone}`, tone = error/conflict/ok/info), a domain-appropriate toast affordance identical on HEAD and entirely outside this cycle's import/dedup edits; left unchanged (intentional, not a defect). Committed atomically (`refactor(design): S2 dedup verbatim Centered layout helper into ui/ across 3 files`, DCO-signed, no agent trailers, on `feature-design-system-consistency`, **not pushed**).
- **NEXT (item 1 mega-page queue CLOSED — every mega-page triaged):** ChatPage (cy48), ProjectsSection (cy49), and CodeCockpitPage (cy50) are done; the three largest mixed-role pages are triaged and their clean slices extracted (SelectionPill, InlineError, Centered). **No mega-pages remain.** What's left of item 1 is entirely the **deferred owner-gated families / taste calls** already tracked across cy37–50, none of which is an autonomous byte-identical slice: the **5-site `RowActionButton`/remove-X family** (toggle / danger-reveal / danger-always variants need a `tone`/`active` API = design unification), the **tonal/primary-fill convergence** (pixel call), the **retry-button family** (6 divergent treatments — `Button variant="secondary"`/`ghost`, bespoke DiffView, the byte-identical ArtifactViewer+FileViewer "Try again" pair — a new `RetryButton` would *compete* with `Button`, so convergence is an owner call, not a clean extraction), the **sibling capability-picker unification**, the **`InlineError` divergent-variant convergence** (FilesSection danger-14% strip + `MoveErrorBanner` warn tone), the **text-link `text-primary hover:underline`** 8-file family, the LoopCockpitPage Back-icon taste call, the ghost-`Cancel`/`Done` idiom, and the `formControls` add-prerequisite. **Recommendation for the owner:** item 1's autonomous run is at its floor — the next step is a batch of taste/pixel decisions (grouped above), each surfaced for veto. Absent those decisions, the cleanest remaining autonomous S2 work is the next-ranked pages' own byte-identical cross-file idioms (e.g. LoopPlanReview/CodePlanReview capability-picker; MemoryPanel; the loops cockpits), triaged the same idiom-first way.

### 2026-07-24 — OWNER TASTE-CALL RULINGS (the cy50 batch, decided after visual inspection of the real-CSS comparison screenshots) — the taste/pixel lane is now unblocked
- **Context:** the cy50 "batch of taste/pixel decisions" was surfaced to the owner as a set of faithful comparison screenshots (real compiled-CSS harness, dark+light, every divergent variant beside its shared-primitive reference, labelled file:line). Owner inspected all groups ("each of these look great"), approved the convergence direction, and ruled the four genuine forks via AskUserQuestion. **These rulings ARE the owner authorization the soul guardrail requires for taste/pixel moves** — record-of-decision for every subsequent cycle in this batch. Execute one atomic convergence per cycle, chaining without stopping (owner's autonomous-loop-continuity directive), each with its own live validation + intentional baseline regen where pixels move.
- **RULINGS (locked):**
  - **G1 — row-action / remove-X family** → converge outliers onto **`SquareIconButton tone="danger"`** (idle ink-low, hover tints red, no fill; the restrained treatment). **Also FIX the B′ token bug:** `OllamaModelManager:88` uses `hover:text-error` → change to `hover:text-danger` (on-token; same value today via `--color-error:var(--color-danger)` but off-token). This is the `RowActionButton` family resolution: the variants (hover-reveal danger / always-visible danger / always-neutral / toggle) map to `SquareIconButton`'s existing `tone` + `on` + caller-supplied `opacity-0 group-hover:opacity-100 group-hover` reveal class — NO new API invented; the primitive already covers them.
  - **G2 — tonal / primary-fill convergence** → the ~44 genuinely-tonal sites converge onto **`Button variant="tonal"`** (`bg-primary/15 text-primary hover:bg-primary/25`) where geometry matches; **the `/90` near-solid `New` split-button (`ProjectsSection:514`) is PROMOTED to solid `Button variant="primary"`** (owner: "it's a real CTA" — honest convergence, keep its weight). Sites whose geometry/semantics diverge from `Button`'s pill tonal (toggle chips, `aria-pressed` cards, raw `color-mix` badges) converge on the TOKEN/opacity (standardize on /15 tint + on-token color-mix) without forcing the `Button` shape where it changes semantics.
  - **G3 — retry-button family** → converge the 6 divergent retries onto the shared **`Button`** (`variant="ghost" size="xs"` + `className="text-primary"` for the quiet inline retries per CodeCockpitPage:2904/2997 precedent; `variant="secondary"` for the filled one per CodeCockpitPage:471). NO new `RetryButton` primitive (would compete with `Button`).
  - **G4 — text-link idiom** → **extract a shared `<TextLink>` primitive** and migrate all 11 sites (`text-primary hover:underline`, with a size/icon-slot API covering leading-icon / trailing-icon / plain / the one real `<a>`). Owner chose the primitive over leave-as-idiom — fits the primitive-adoption ratchet, prevents future drift.
  - **G5 — capability picker** → **unify on `CapRow` everywhere** (the full-width row + checkbox + description + peek). Rewrite the code-plan `CapChip` surface (`CodePlanReview:391`) to use `CapRow` — hoist `CapRow` to a shared `ui/` (or shared plan-review) component consumed by both `LoopPlanReview` and `CodePlanReview`. **This moves pixels + rewrites the code-plan picker layout — explicitly owner-authorized.**
  - **G6 — ghost Cancel/Done** → converge onto **`Button variant="ghost"`**; the quiet surface-filled pill variants (`FilterMenu:83`, `TaskForm:161`) collapse into ghost.
  - **G7 — LoopCockpit Back-icon** (`LoopCockpitPage:720`) → converge onto **`SquareIconButton`** (neutral tone; size-6 → size-7 + hover:bg-surface-high, a legitimate pixel move).
  - **G8 — add-prerequisite** (`formControls:95`) → converge onto **`Button variant="secondary"`**.
  - **G9 — InlineError divergent strips** → **FOLD ALL 3 IN** (owner: maximum convergence, accept pixel moves): `ChatPage ErrorLine` (2633), `FilesSection` strip (351), AND the warn-toned animated `MoveErrorBanner` (`TasksListPage:428`) all converge onto **`InlineError`** — including **re-toning the warn banner to danger** and giving `InlineError` the API it needs (a `tone`? an `animated`? a non-dismissible mode + multi-line `items-start`). Update the primitive's docblock (which currently documents these as deliberately excluded) to reflect the fold-in.
- **EXECUTION ORDER (cheapest/highest-confidence first, contract-owners before consumers):** (1) **G1 B′ token bug** — one-char on-token fix, trivial, zero-pixel, warm-up; (2) **G7** back-icon + **G8** add-prerequisite — single-site primitive adoptions; (3) **G6** ghost Cancel/Done; (4) **G3** retry family; (5) **G4** `<TextLink>` extraction (new primitive + 11 migrations); (6) **G2** tonal/primary convergence (large footprint, do after the primitive-adoption rhythm is warm); (7) **G1** full `RowActionButton`/remove-X family (the multi-variant sweep); (8) **G5** CapRow unification (layout rewrite); (9) **G9** InlineError fold-all (primitive API extension + re-tone). Each cycle: atomic commit, DCO-signed, no agent trailers, on `feature-design-system-consistency`, NOT pushed; live-validate; regen + surface baselines where pixels move; log DONE per cycle here.

### 2026-07-24 — cy51 DONE (G1 B′ token normalization — warm-up, batch cycle 1/9)
- **DONE:** Converged the last `*-error` off-token color spellings onto the dominant `*-danger` idiom. The B′ bug named in the ruling was `OllamaModelManager:88` `hover:text-error`; auditing the whole tree showed it was one of **6 identical `text-error` usages** in `web/src/pages/settings/` (`OllamaModelManager` L54/88/146/199, `ProjectionRulesPanel` L60, `SecurityPanel` L97/212). Normalized all 6 → `text-danger`. **Zero** `*-error` utility spellings now remain in `web/src/` (was 6; `*-danger` idiom count: 102).
- **DISCOVERY (why it grew from 1 site to 6, still in-scope):** `--color-error` is aliased to `--color-danger` *inside* the Tailwind v4 `@theme` block (`design/tokens.css:53`), so `text-error`/`hover:text-error` render **identical coral** today — this is a **zero-pixel** spelling normalization, not a rendering fix. Fixing only L88 while leaving 5 byte-identical siblings would manufacture a fresh inconsistency and violate clean-break; converging all 6 is the correct atomic unit. Stayed strictly within the danger color family (did not touch `text-warn`/`text-ok`/etc. — different semantic families, outside this ruling).
- **Validation:** `tsc --noEmit` clean; `npm run build` compiles CSS/Tailwind clean (chunk-size warning pre-existing); full vitest **140/140** green incl. `tokenLint`, `primitiveAdoption` ratchet, `consistencyAudit`. Zero-pixel ⇒ build + token-lint is authoritative; no visual delta to drive in-UI, no baseline to regen.

### 2026-07-24 — cy2 DONE (G7 loop-rail back-icon → SquareIconButton + G8 add-prerequisite → Button secondary — batch cycle 2/9)
- **DONE (G7):** `LoopCockpitPage.tsx:721` details-rail collapse arrow was a raw `<button>` re-implementing the `SquareIconButton` chrome by hand (`inline-flex items-center justify-center size-6 rounded-md text-on-surface-var hover:bg-surface-high`). Replaced with `<SquareIconButton icon={ArrowLeft} iconSize={16} label="Back to list" onClick={() => setSelected(null)} />` — same coral-free neutral tone, same surface-high hover, now sourced from the shared primitive (grid `size-7 rounded-md`, `motion.button` press/hover baked in). Ruling was to adopt the primitive; the hand-rolled `size-6` was a one-off, so this converges it to the system without changing the visual language.
- **DONE (G8):** `formControls.tsx:98` "Add prerequisite" quiet-add was a raw `motion.button` with bespoke tonal styling. Replaced with the shared `Button variant="secondary" size="sm"` (`bg-surface-high text-on-surface hover:bg-surface-highest`), wrapped in a `motion.div` (`key="addbtn"`, initial/animate/exit) so the AnimatePresence `mode="wait"` crossfade with the picker `Bud` is preserved — `Button` is a `motion.button` internally and does not accept `initial/animate/exit`, so the wrapper carries the transition. Owner ruling: quiet-add controls adopt `Button variant=secondary`.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` with a real drift reduction — `LoopCockpitPage` raw-button count `10 → 9` (score `22 → 20`) from the G7 conversion. Unlike cy51 (pure timestamp churn, correctly reverted), this is a genuine content delta reflecting the primitive adoption, so it is committed with the cycle (matching the prior cycle's audit-delta discipline). G8's `formControls` raw-button count is unchanged because the picker-toggle `motion.button` was already outside the audit's raw-button heuristic scope.
- **Validation:** live-validated as a user against the dev gateway (isolated `.dev-home`, `AUTH_MODE=none`) in **both dark and light** — G7: opened a `general` loop cockpit with details rail, clicked "Findings Log" to reach the `selected` sub-state, confirmed the back-icon renders with correct neutral tone + surface-high hover fill and no console errors; G8: `#/tasks/new`, confirmed the "Add prerequisite" secondary Button renders, hovers to surface-highest, and buds into the picker with the crossfade intact. `tsc --noEmit` clean; `npm run build` clean; vitest **140/140** green. Neither surface is covered by a Playwright nav-route baseline (`e2e/routes.ts` is top-level shells only; loop-cockpit deep state + task-form are not in the baseline set), so there is no snapshot to regen — the live in-UI validation is authoritative for these two.

### 2026-07-24 — cy3 DONE (G6 ghost Cancel/Done — the two surface-filled pill outliers collapse to Button variant=ghost — batch cycle 3/9)
- **Context:** the Cancel/Done idiom is *already* overwhelmingly converged onto `Button variant="ghost"` (a full-tree grep shows ~30 sites already using it). G6's ruling targets only the **two remaining divergent surface-filled pills** the cy50 screenshots surfaced: `FilterMenu:83` "Done" and `TaskForm:161` "Cancel". This cycle is the last-mile convergence, not a sweep.
- **DONE (FilterMenu Done):** `ui/FilterMenu.tsx:83` popover-footer "Done" was a bespoke `h-9 rounded-pill bg-surface-high text-on-surface hover:bg-surface-container` pill. Replaced with `<Button variant="ghost" size="sm" onClick={close} className="mt-1 w-full">Done</Button>` (added the `./Button` import). Was a filled pill → now the transparent ghost with `hover:bg-surface-high`, `w-full` to keep it spanning the popover footer as before. Owner ruling: the quiet surface-filled pill variants collapse into ghost.
- **DONE (TaskForm create-row):** `pages/tasks/TaskForm.tsx:158-162` inline "New project / New task list" create-row had **two** raw buttons — an `Add` (`bg-primary … h-9 rounded-md`) and a `Cancel` (`bg-surface-high … h-9 rounded-md`) — sitting beside an `h-10` `TextInput`. Converting only the ruled `Cancel` would leave a mismatched raw `Add` sibling and a within-row height break (`Button` sizes are `h-8`/`h-10`, never the raw `h-9`), manufacturing a *fresh* inconsistency — so the **atomic unit is the whole row**. Both converge at `size="md"` (`h-10`, aligning to the input): `Cancel → Button variant="ghost"` (G6), and the solid `bg-primary` `Add` CTA → `Button variant="primary"` — which is exactly **G2's** authorized primary-fill direction (same call as the promoted `ProjectsSection:514` CTA), so no unruled direction is introduced. Kept the existing `{busy ? 'Adding…' : 'Add'}` ternary label (the codebase's dominant busy-CTA idiom, e.g. `KnowledgeListPage:609`) rather than switching to the `loading` spinner — consistency, not redesign. Added the `../../ui/Button` import.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `primitivesByFile` **drops the `pages/tasks/TaskForm.tsx` entry entirely** (raw-button `2 → 0`), a real drift reduction from the two-button conversion; committed with the cycle. `FilterMenu` is under `ui/` (scanner-exempt by the design-system ratchet's ui/ carve-out — primitives may compose raw elements), so its conversion produces no audit delta, as expected. The `totals.driftHits` (6) is a different axis (token drift), unchanged.
- **Validation:** live-validated as a user in **both dark and light** (forced via `localStorage['mode']` in an `addInitScript`, since `ThemeProvider` drives `.light` from React state — a direct DOM toggle gets overwritten) — FilterMenu "Done" now renders as the transparent ghost button spanning the popover footer; the TaskForm create-row ("New project") shows the solid-coral **Add** (correctly disabled-faded on empty input, matching the footer "Create task") beside the ghost **Cancel**, both height-aligned to the `Project name` input. Zero console errors in either mode. `tsc --noEmit` clean; `npm run build` clean; vitest **140/140** green. Neither surface is in the Playwright nav-route baseline set (`e2e/routes.ts` top-level shells; the tasks-list FilterMenu popover is a transient overlay and the create-row is a deep form state) — no snapshot to regen; live in-UI validation is authoritative.

### 2026-07-24 — cy4 DONE (G3 retry-button family — the divergent retries converge onto the shared Button — batch cycle 4/9)
- **Context:** the retry idiom is *already* largely converged onto `Button` — the cy50 audit named CodeCockpitPage:2904/2997 (quiet inline `variant="ghost" size="xs" text-primary`) and CodeCockpitPage:471 (filled `variant="secondary"`) as the two established precedents. G3's ruling targets the **remaining divergent raw-button retries** those screenshots surfaced: one filled retry (DiffView) and a byte-identical quiet-retry pair (ArtifactViewer + FileViewer). Explicitly **no new `RetryButton` primitive** — this is last-mile convergence onto the existing two shapes, not a new component.
- **DONE (DiffView filled retry):** `pages/code/DiffView.tsx:52` "could not load the diff" retry was a raw `<button className="rounded-md bg-surface-high px-3 py-1 text-on-surface-var hover:bg-surface-container hover:text-on-surface">Try again</button>`. Replaced with `<Button variant="secondary" size="sm" onClick={() => setAttempt((n) => n + 1)}><RotateCcw size={15} /> Try again</Button>` (added the `RotateCcw` lucide import + the `../../ui/Button` import) — the filled `variant="secondary"` matching the **CodeCockpitPage:471** precedent named in the ruling, with the RotateCcw affordance the bespoke version lacked.
- **DONE (ArtifactViewer + FileViewer quiet pair):** `pages/files/artifacts/ArtifactViewer.tsx:150` and `pages/files/browse/FileViewer.tsx:163` were **byte-identical** raw quiet retries (`inline-flex items-center gap-1.5 rounded-md px-2.5 py-1 text-[0.75rem] text-primary hover:bg-surface-high` wrapping `<RotateCcw size={13} /> Try again`). Both replaced with `<Button variant="ghost" size="xs" onClick={…} className="mt-1 text-primary"><RotateCcw size={13} /> Try again</Button>` (each added its `Button` import) — the quiet inline `variant="ghost" size="xs" + text-primary` shape from the **CodeCockpitPage:2904/2997** precedent. `size="xs"` is `h-7`, matching the compact placeholder scale; the coral `text-primary` override is preserved verbatim so the affordance reads identically.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` with three real drift reductions from the conversions — `pages/code/DiffView.tsx` **drops out of `primitivesByFile` entirely** (raw-button `1 → 0`), `pages/files/browse/FileViewer.tsx` likewise **drops out** (raw-button `1 → 0`), and `pages/files/artifacts/ArtifactViewer.tsx` falls `4 → 3` raw-button (`ranked` score `9 → 7`, re-sorted). Committed with the cycle (matching prior-cycle audit-delta discipline). `totals.driftHits` (6, the token-drift axis) is unchanged.
- **Scope discipline:** left `OllamaModelManager:212` (an icon-only control, not a labeled retry) and `ErrorBoundary:59` (the app-crash full-view reload in a class component — a different idiom) untouched; the ruling names neither, and converting them would be scope creep beyond "the divergent retries."
- **Validation:** live-validated as a user in **both dark and light** — forced the FileViewer `loadFailed` placeholder by routing `/api/file-read` to a 500 **and** aborting the `/api/file-watch` SSE (the watch's `setContent` push would otherwise defeat the `content===null` guard and keep the read-failure from surfacing). The "Couldn't open this file." placeholder rendered with exactly one "Try again" (measured `h-7` = `size="xs"`), the coral `text-primary` label + RotateCcw icon, ghost `hover:bg-surface-high` on hover, and **zero non-fixture console errors** in both modes. ArtifactViewer shares byte-identical converted markup + the same precedent, and DiffView's `variant="secondary"` matches CodeCockpitPage:471 verbatim, so those two are validated by equivalence. `tsc --noEmit` clean; `npm run build` clean; vitest **140/140** green (via `npm test --workspace web` — note a bare root-level `vitest run` misreports mass `document is not defined` failures because it misses `web`'s jsdom env config; the workspace script is authoritative). None of the three error branches is in the Playwright nav-route baseline set (`e2e/routes.ts` is top-level shells; the file-viewer/diff error states are deep transient branches) — no snapshot to regen; live in-UI validation is authoritative.

### 2026-07-24 — cy5 DONE (G4 text-link idiom — extract the `<TextLink>` primitive + migrate every hand-rolled `text-primary hover:underline` site — batch cycle 5/9)
- **Context:** the inline text-link idiom (`text-primary hover:underline`) was hand-rolled at ~16 call sites with drifting size classes, element types (`<button>` vs `<a>`), icon slots, and margins. G4's ruling: **extract a shared `<TextLink>` primitive** (owner chose the primitive over leave-as-idiom, per the primitive-adoption ratchet) with **a size + icon-slot API covering leading-icon / trailing-icon / plain / the one real `<a>`**. NOT a new visual language — the coral label + hover-underline is unchanged; this is the single-source consolidation of an existing idiom.
- **DONE (new primitive `ui/TextLink.tsx`):** minimal typed props following the `QuietButton` template — `children`, `href?` (+ `external?` → `target=_blank rel="noopener noreferrer"` for off-app URLs; in-app hash links stay bare), `onClick?`, `icon?`/`iconPosition?` (`leading`|`trailing`, default leading)/`iconSize?`, `size?` (`xs`=`text-[0.75rem]` / `sm`=`text-[0.8125rem]` / `inherit`=no size class, the default in-sentence case), `disabled?`, `title?`, `className?` (rides layout extras like `ml-auto`/`mt-1.5`/`normal-case`). Renders a `<button type="button">` by default or an `<a href>` when `href` is set; opts into `inline-flex items-center gap-1` **only when an icon is present** so plain links flow inside running text without a baseline shift. Base class via the house `cx` joiner: `text-primary hover:underline disabled:opacity-50`. Docblock explains the idiom + every knob.
- **DONE (test `ui/TextLink.test.tsx`):** 7 cases in the `QuietButton.test.tsx` style — locks the coral/hover-underline/disabled-dim traits, the default `<button type="button">`, the `<a href>` path (+ bare, no target/rel for in-app), the external off-app safety attrs, the icon-row `inline-flex` opt-in, the `xs`/`sm`/`inherit` size-class mapping, and disabled+title forwarding + className merge. (A first draft of the size case rendered three `TextLink`s into the shared jsdom body and tripped `getByRole('button')`'s multiple-match — scoped each to its own `render().container` and it's green; not a primitive bug.)
- **DONE (14 files, 16 instances migrated):** `app/Onboarding.tsx` (Set up later), `ChatPage.tsx` (Manage in Memory → `<a href>`, normalized a stray `text-primary/90` to standard), `settings/AppsPanel.tsx` (Store), `settings/MemoryPanel.tsx` (↺ show all), `settings/VoicePanel.tsx` (Bind the {kind} model in Models, trailing `ArrowRight`, `size="xs"`), `schedule/ScheduleDetail.tsx` (Show more, `size="sm"`), `tasks/TasksListPage.tsx` (project chip, leading `FolderKanban` icon), `tasks/TaskDetail.tsx` (external Open, `<a href external>` + trailing `ExternalLink`… leading here), `code/CodeCockpitPage.tsx` (×2 Remove from queue, `size="xs"`), `loops/LoopsListPage.tsx` (View all loops, `size="sm"`), `loops/LoopCockpitPage.tsx` (Open in Artifacts, trailing `ExternalLink` + `normal-case tracking-normal`), `files/browse/FileViewer.tsx` (external Download-to-inspect `<a href>`), `apps/AppsSection.tsx` (×2 Manage Sources), `ui/FilterMenu.tsx` (Clear, `size="xs"`).
- **Reconciliation (ruling estimated "11 sites" → 16 instances across 14 files):** the ruling's "11" was an estimate from the cy50 audit; the actual hand-rolled idiom appears 16× (three files carry two instances each: CodeCockpitPage's queue-remove pair, AppsSection's two empty-state branches). All 16 are the genuine idiom; the ruling's intent ("migrate all of them") is honored in full. **DISCOVERY:** a full-tree `grep` after the first pass surfaced `VoicePanel:172` (coral `text-primary hover:underline` with a trailing `ArrowRight`) — the genuine idiom, initially conflated with its ink-low SIBLING at `:176` (`text-on-surface-low hover:text-on-surface`, correctly excluded). Migrated `:172`; left `:176`. **Deliberate EXCLUSIONS (not the idiom):** the Markdown/Knowledge always-underlined link renderers (`underline decoration-primary/40` — a different, permanent-underline treatment for rendered content links); the prompts "Add all" pill trio (`rounded-pill px-2 h-7` — a pill, not a text link); and VoicePanel:176's ink-low sibling (`text-on-surface-low`, not coral). Converting any of these would change their semantics — out of scope for "the text-link idiom." Confirmed via `grep` that no coral `text-primary hover:underline` idiom sites remain unmigrated.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` with genuine raw-button reductions at every `<button>`-based migration — Onboarding `3→2`, AppsSection `5→3`, CodeCockpitPage `23→21`, LoopCockpitPage `9→8`, LoopsListPage `1→0` (drops out), ScheduleDetail `2→1`, AppsPanel `1→0` (drops out), MemoryPanel `8→7`, TasksListPage `7→6`. The three `<a>`-based sites (ChatPage:2730, TaskDetail:101, FileViewer:175) were already anchors so show **no** raw-button delta (correct — the scanner counts raw `<button>`), and `FilterMenu` is in `ui/` (exempt from raw-button scanning, primitives may compose raw elements) so also no delta. `filesScanned 309→310` = the new `ui/TextLink.tsx` (scanned, zero drift — `ui/` exempt); the test file is excluded. `CodeSection.tsx` surfacing in the truncated `ranked` view at score 6 is a re-sort artifact (its byFile count is unchanged), not a regression. `totals.driftHits` (6, the token-drift axis) unchanged. Committed with the cycle per prior audit-delta discipline.
- **Validation:** live-validated as a user in **both dark and light**. **FilterMenu "Clear"** (`size="xs"` button path): renders `<button type="button">`, computed color `rgb(255,107,91)` = the coral `text-primary` token, `text-decoration base=none → hover=underline`. **settings/apps "Store"** (in-sentence `inherit` button path): same coral `rgb(255,107,91)`, `base=none → hover=underline`, bare inline (flows in the sentence). Both variants + the hover-underline idiom confirmed with **zero console errors** across both routes and both modes. The `<a>` external path (target/rel + href) is unit-locked in the test. `tsc --noEmit` clean; `npm run build` clean; vitest **147/147** green (140 prior + 7 new TextLink cases; via `npm test --workspace web`). None of these links sit on a top-level nav-route shell (`e2e/routes.ts`); the migrated sites are deep/conditional states (empty-states, open popovers, error placeholders) not in the visual baseline set — no snapshot to regen; live in-UI validation is authoritative.

### 2026-07-24 — cy6 DONE (G2 tonal / primary-fill convergence — the two faithful moves the ruling's geometry/semantics guardrail actually authorizes — batch cycle 6/9)
- **Context + the "~44" reconciliation (the substance of this cycle):** G2's ruling (line 480) reads as a large sweep — "the ~44 genuinely-tonal sites converge onto `Button variant="tonal"`" — but it carries its own explicit discriminator: **"where geometry matches"** and **"Sites whose geometry/semantics diverge from `Button`'s pill tonal (toggle chips, `aria-pressed` cards, raw `color-mix` badges) converge on the TOKEN/opacity … without forcing the `Button` shape where it changes semantics."** A fresh whole-tree audit (`grep` for every `bg-primary/NN` + `color-mix(var(--color-primary) …)` fill outside `ui/`, ~60 hits, each read in context) resolves the "~44" into **four distinct roles the ruling separates**, NOT 44 Button candidates:
  1. **Genuine tonal CTA with Button-matching geometry — 1 site.** `CodePlanReview:380` "Install" used the *exact* canonical idiom the `tonal` variant's docblock was written to absorb (`bg-primary/15 text-primary hover:bg-primary/25`), as a standalone clickable action hand-rolling its own `Loader2` spinner. This is a real primitive adoption → converted.
  2. **Owner-named promotion — 1 site.** `ProjectsSection:514` `/90` "New" split-button → the honest primary token (below).
  3. **Divergent-geometry clickable micro-pills — left on-token (per the carve-out).** The project-scope chips (`LoopCockpitPage:509`, `DesignCockpitPage:253`, `TasksListPage:515`) are **`h-5`/`h-6` pills sitting in `MetaPill` sibling rows**; `DagView:207` Approve is half of an **Approve/Deny pair** where Deny is `color-mix(--color-danger)` (no tonal-*danger* `Button` variant exists). Converting to `Button`'s smallest `h-7` pill would break meta-row height alignment, and splitting the Approve/Deny pair would manufacture a fresh inconsistency (the cy3 whole-row lesson). All are **already on-token** at the standard 16% `color-mix` — exactly the "converge on the token, don't force the shape" case.
  4. **Toggle chips / `aria-pressed` selection states / static badges / icon-wells / info-washes — left on-token (already converged).** The bulk (~40): `CapChip`/preset-chip toggles (`ScheduleForm:244`, `CodePlanReview:481`, settings `SearchPanel`/`ModelsPanel`/`MemoryPanel`/`DesignPanel`/`DiagnosticsPanel` on/off states), count badges (`NotificationsPage:84`, `LoopCockpitPage:1138`), step-number pills (`WorkflowForm`/`WorkflowDetail`), icon-wells (`AgentsListPage:191`, `SecurityPanel:40`), and 10% drag/info washes. These are **already** on the on-token idiom (`color-mix(var(--color-primary) …)` / `bg-primary/NN`), and their opacity spread (**10% wash → 14–16% chip/well → 18–20% emphasis badge**) is a **deliberate visual hierarchy** — flattening it to one value is a *redesign*, which the soul guardrail ("consistency, not redesign; bring outliers to the system") forbids. No outliers here; nothing to do.
- **DONE (CodePlanReview:380 Install → `Button variant="tonal"`):** replaced the raw `<button className="… bg-primary/15 … hover:bg-primary/25 …">{installing ? <Loader2 …/> : <Plus/>} {installing ? 'Installing…' : 'Install'}</button>` with `<Button variant="tonal" size="xs" className="shrink-0 gap-1 px-2 text-[0.75rem]" loading={!!installing[m.id]} onClick={() => onInstall(m)}><Plus size={11}/> Install</Button>`. Button's `loading` prop natively cross-fades the label under a centred spinner while preserving width — so the hand-rolled `Loader2`/label ternary is dropped (Loader2 import stays; still used at 184/293). **Default pill, not `shape="squircle"`:** an initial edit preserved the outlier's `rounded-md` via squircle, but a grep showed all 3 established `variant="tonal"` precedents (`CodeCockpitPage:1501/1935/3304`) render the **default pill** — squircle would be the only one in the tree, a fresh one-off. Corrected to the house default; `px-2`/`text-[0.75rem]`/`gap-1` retained via `className` to keep the dense in-row footprint. Raw-button count `10→9`.
- **DONE (ProjectsSection:514 "New" split-button → honest primary token):** owner ruling — "it's a real CTA … keep its weight." The trigger is a `Popover` render-prop receiving `(open, toggle)` with `aria-expanded={open}`, sitting in a `rounded-md` `HeaderControl` sibling row; `Button` accepts neither `aria-expanded` nor a non-pill row geometry, so forcing a `Button` pill here would **break the popover a11y contract and the control-row geometry** — the ruling's "don't force the shape where it changes semantics" applies to the promotion too. The faithful convergence is to **normalize the off-token `bg-primary/90` → the honest `bg-primary text-on-primary` token in place**, matching `Button variant="primary"`'s exact semantics (`bg-primary` idle, `hover:bg-primary-emphasis`). Class went `${open ? 'bg-primary text-on-primary' : 'bg-primary/90 text-on-primary hover:bg-primary'}` → `text-on-primary ${open ? 'bg-primary' : 'bg-primary hover:bg-primary-emphasis'}`. Raw-button count unchanged (still a raw `<button>` by necessity — the a11y trigger — just re-toned; correct).
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `CodePlanReview.tsx` raw-button `10→9`, score `29→27`, a genuine drift reduction from the Install conversion; committed with the cycle. ProjectsSection shows no count delta (the re-toned trigger is still a raw `<button>`). `totals.driftHits` (6) and `filesScanned` (310) unchanged. Only other diff line is the `generatedAt` timestamp.
- **Validation:** live-validated as a user in **both dark and light**. **ProjectsSection "New" split-button** (created a real project through the UI on the isolated dev home to render the per-project card): computed **dark** bg = `rgb(255,107,91)` = `--color-primary` (was `/90` translucent), text = `rgb(63,16,8)` = on-primary, hover → `rgb(255,154,134)` = `--color-primary-emphasis`, `borderRadius: 12px` = `rounded-md` preserved; class string confirms `text-on-primary bg-primary hover:bg-primary-emphasis` (the `/90` gone); the Popover still opens (`aria-expanded` toggles true); **zero console errors** both modes. **CodePlanReview Install** is a deep conditional state (a code loop mid-plan-review with `marketplaceToShow` populated from real backend skill/app suggestions) not reproducible on the offline empty-seed loopback instance and not in the `e2e/routes.ts` nav-route baseline (top-level shells only) — the documented empty-seed sub-flow limit (as in cy2/cy5). Its correctness rests on tsc + being a byte-faithful adoption of the *exact* tonal idiom already rendering identically at 3 sibling `CodeCockpitPage` sites, with `loading` replacing the hand-rolled spinner; confirmed the `#/code` route (which lazy-loads CodePlanReview's chunk, imported at module top-level) mounts with **zero console errors** — a load-time import/type fault would have crashed it. `tsc --noEmit` clean; `npm run build` clean; vitest **147/147** green incl. `primitiveAdoption` ratchet + `consistencyAudit`. No pixels moved on any nav-route shell → no visual baseline to regen.
- **Net:** G2 was faithfully executed as **2 convergences**, not 44 — the "~44" figure conflated genuinely-clickable tonal CTAs (nearly all already on `Button` or on-token) with a deliberate on-token badge/toggle/well tier scale the ruling explicitly routes to token-only convergence. Both authorized moves (the one true tonal-CTA outlier + the owner-named primary promotion) landed; the divergent-geometry majority was confirmed already-on-idiom and correctly left untouched per the soul guardrail.

### 2026-07-24 — cy7 DONE (G1 full `RowActionButton`/remove-X family — the 5-site multi-variant sweep onto `SquareIconButton` — batch cycle 7/9)
- **Context + scope (the authoritative 5-site family):** G1's ruling (line 479) converges the row-action/remove-X family onto **`SquareIconButton tone="danger"`** — "idle ink-low, hover tints red, no fill; the restrained treatment … the variants (hover-reveal danger / always-visible danger / always-neutral / toggle) map to `SquareIconButton`'s existing `tone` + `on` + caller-supplied `opacity-0 group-hover:opacity-100` reveal class — NO new API invented." The family's membership was pinned across the mega-page triage (cy49 line 465, cy50 line 474) as **exactly 5 sites, 3 variants**: (a) **danger-reveal** ×2 byte-identical — `ProjectsSection:176` + `CodeSection:380` Delete-project (box + `hover:text-danger` + `opacity-0 group-hover` reveal + `focus-visible:opacity-100` a11y); (b) **danger-always** ×1 — `CodePlanReview:557` Remove-stage (box + `hover:text-danger`, no reveal, `Trash2 size={13}`); (c) **toggle** ×2 — `ChatPage:3027` Pin + `ChatPage:3189` Organize (box + state ternary: `on`→coral-tint / off→`opacity-0 group-hover` reveal). All 5 share the exact dense square chrome (`rounded-md p-1.5` ≈ the primitive's `size-7 rounded-md` = 28px, 12px radius) — the geometry that made this a family and not five one-offs.
- **Deliberately OUT of the family (verified against real code, not the pre-triage guess):** (a) `formControls.tsx` X-removers (63/136/209) are a **leaner bare-glyph idiom** — NO box, no `rounded-md p-1.5`, just an inline `<X>` with `hover:text-danger`; forcing a `size-7` box would *add* chrome (a pixel move the owner's screenshot triage never covered) → different idiom, left raw. (b) `ChatPage:3030` Delete-chat is **already** an `IconButton` primitive (round 26px), not raw. (c) `DesignPanel:117` is an `absolute`-positioned `rounded-pill size-6` swatch-overlay delete with a permanent `bg-surface-high` fill — divergent shape. (d) `ProviderCard:136` is a **labelled** "Disconnect" (text + icon) → `Button` territory. (e) The dashboard-widget `RowAction` (`kit.tsx`) is the widgets' own row-action primitive with its own `tone` API — a separate abstraction, not the raw remove-X family. None folded in (would move pixels / change semantics beyond the ruling).
- **DONE (5 conversions, 3 files + ChatPage):**
  - **Danger-reveal ×2** (`ProjectsSection:176`, `CodeSection:380`): raw `<button …>` → `<SquareIconButton icon={Trash2} tone="danger" label="Delete project" onClick={…} className="shrink-0 opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100" />`. The caller-supplied reveal (`opacity-0 group-hover:opacity-100`) + keyboard-a11y (`focus-visible:opacity-100`) ride through `className` (merged after the primitive's base via `cx`); CodeSection's explanatory comment about `focus-visible` still applies (class preserved verbatim). **The `hover:bg-surface-highest` fill is intentionally dropped** — `tone="danger"` provides no hover fill by design, the exact "restrained treatment, no fill" the ruling names. `e.stopPropagation()` in the handler preserved (row-click must not fire on delete).
  - **Danger-always ×1** (`CodePlanReview:557`): → `<SquareIconButton icon={Trash2} iconSize={13} tone="danger" label="Remove stage" onClick={onRemove} className="shrink-0" />` — `iconSize={13}` preserves the original 13px glyph (this row's icons run one px smaller than the 14px default); already had `SquareIconButton` imported (used at 540/542 for the reorder steppers), so a sibling adoption.
  - **Toggle ×2** (`ChatPage:3027` Pin, `3189` Organize): map to the primitive's **`on`** prop (selected → coral text + 14% `color-mix` fill, the documented toggle-selected state). Pin keeps **`children`** (not `icon`) to preserve its state-swapped `<Pin className={pinned ? 'fill-current' : ''}>` glyph; Organize uses `icon={TagIcon}`. Both preserve the off-state reveal via `className={\`shrink-0 transition-opacity ${active ? 'opacity-100' : 'opacity-0 group-hover:opacity-100'}\`}`. Organize is a `Popover` render-prop trigger `(open, toggle)` — it never used `aria-expanded` (unlike the G2 `ProjectsSection` New trigger), and `SquareIconButton` renders a real `<button>`, so the Popover's rect-measure + focus-restore (`ref.querySelector('button')`) still resolve — the trigger contract is intact.
- **Authorized pixel moves (per the ruling, this is not a zero-pixel cycle):** the 3 removers lose their `hover:bg-surface-highest` grey fill (now hover-tint-red only); the 2 toggles' selected state gains the primitive's 14% coral background well (`color-mix(var(--color-primary) 14%)`) that the hand-rolled versions rendered as bare `text-primary`. Both are exactly the convergence the owner approved from the comparison screenshots.
- **Ratchet: `rawButton` 314 → 290 (−24), committed same cycle.** Five buttons removed this cycle (audit per-file: ChatPage 18→16, ProjectsSection 18→17, CodeSection 3→2, CodePlanReview 9→8) drop live to **290**; the `primitiveAdoption` soft-nudge ("live counts well below baseline — ratchet DOWN to lock in the gain") flagged that the prior batch cycles (cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2) had migrated ~19 raw buttons onto primitives **without** ratcheting the baseline down. Per the ratchet doctrine ("RATCHET THESE DOWN in the same commit; the numbers may only shrink") the baseline is set to the true live count **290** — locking in this cycle's 5 + the accumulated batch gain. `primitiveAdoption.baseline.json` `rawButton: 314 → 290`; `rawInput`/`rawDialog`/`inlineFontWeight` untouched. All 5 ratchet tests green at 290/163/0/7, nudge cleared.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — real per-file raw-button decrements at all four files (above), score drops (ChatPage 46→42, ProjectsSection 41→39, CodePlanReview 27→25), and a top-N re-rank (CodeCockpitPage now #1; CodeSection at 2 raw-button drops out of the visible ranked list, `PromptDetail` bubbles in). `totals.filesScanned` unchanged at 310 (no new `ui/` file this cycle — pure adoption, not extraction). Legitimate deltas, kept in commit.
- **Validation (live, dark + light):** headless Playwright on the fresh built bundle (`index-DP-kTU4R.js`, served through the `static/dist` symlink, confirmed matching disk) across `#/chat`, `#/projects`, `#/code` in both themes — **all boot, render real content, ZERO console errors, no error boundary**. `#/projects` renders a real migrated `SquareIconButton[aria-label="Delete project"]`; a focused hover probe confirmed the ruling's treatment in **both** modes: **idle** `opacity: 0` (reveal working), 28×28, `border-radius: 12px`; **hovered** `opacity: 1`, `background: rgba(0,0,0,0)` (**no fill** — the dropped `hover:bg-surface-highest` verified gone), glyph `rgb(246,108,102)` (danger red). The Pin/Organize toggles + the CodePlanReview Remove-stage render per chat-session-row / mid-plan-review — deep states the offline empty-seed loopback can't seed (the documented empty-seed sub-flow limit); their correctness rests on tsc + being byte-faithful `on`/`tone` adoptions of the primitive whose exact chrome is locked by `SquareIconButton.test.tsx`. **Visual baselines: verified ZERO-diff** — booted a fresh empty-seed tokenless e2e gateway (`:10400`) and ran `e2e/visual.spec.ts` for Projects + Chat (dark + light) against the committed goldens: **4/4 passed, no diff**. The pixel move lives entirely outside the captured route-shell surface (reveal removers are `opacity-0` idle; toggles render per session-row, absent in empty-seed), so no baseline legitimately moves and none is regenerated. `tsc --noEmit` clean; `npm run build` clean; vitest **147/147** green.
- **NEXT (batch cycle 8/9 → G5):** unify the capability picker on `CapRow` — rewrite the code-plan `CapChip` surface (`CodePlanReview:391`) to `CapRow`, hoisting `CapRow` to a shared `ui/`/plan-review component consumed by both `LoopPlanReview` and `CodePlanReview` (ruling line 483 — "moves pixels + rewrites the code-plan picker layout, explicitly owner-authorized"). Then cy9 → G9 (InlineError fold-all + re-tone). Chaining without stopping per the owner's autonomous-loop-continuity directive.

### 2026-07-24 — cy8 DONE (G5 capability-picker unification — hoist `CapRow` to a shared `ui/` component, rewrite the code-plan `CapChip` surface onto it — batch cycle 8/9)
- **Context:** two plan-review screens rendered the "pick the skills/workflows the loop loads every cycle" picker in **two divergent shapes**. `LoopPlanReview` (goal loops) used the richer **`CapRow`** — a full-width row with a checkbox, icon, name, description, a `suggested` chip, and an `Eye` **peek** button opening a `CapabilityPeekModal` that fetches the SKILL.md. `CodePlanReview` (code loops) used **`CapChip`** — an inline `rounded-pill` toggle chip (`px-2 py-0.5`, ✓-or-icon + truncated label + a suggestion dot), laid out in a `flex flex-wrap gap-1.5` container, with **no description and no peek**. Same job, two visual languages. G5's ruling (line 483): **unify on `CapRow` everywhere** — rewrite the code-plan picker onto `CapRow`, hoisting `CapRow` to a shared component. **"This moves pixels + rewrites the code-plan picker layout — explicitly owner-authorized."**
- **DONE (new shared component `ui/CapabilityPicker.tsx`):** extracted **`CapRow`** AND its paired **`CapabilityPeekModal`** verbatim out of `LoopPlanReview.tsx` into a new `web/src/ui/CapabilityPicker.tsx`, exported both. The peek modal moved with the row because `CapRow`'s `onPeek` only has meaning alongside it (the modal is the peek surface). Placing them in `ui/` is in-convention: **10 existing `ui/` files already import `lib/api`** (`ProjectPicker`, `Markdown`, `NotificationBell`, `SystemWidget`, …), so `CapabilityPeekModal`'s `api.skillContent(key)` fetch does not cross a new boundary. Markup/classes/props copied byte-for-byte — this is a **move + share**, not a redesign: the goal-loop picker renders pixel-identical to before (same component, new import path). Used the house idioms already present in `ui/` (`import { …, type ReactNode } from 'react'`; `fvs` from `../design/fontWeight`; `cx` not needed — no conditional class merge here). Added JSDoc noting both are the shared picker row + its peek surface, consumed by both plan reviews.
- **DONE (`LoopPlanReview.tsx` — consume the shared component):** deleted the local `CapRow` (was line 513) + `CapabilityPeekModal` (was 640) definitions; added `import { CapRow, CapabilityPeekModal } from '../../ui/CapabilityPicker'`. Import diet (tsconfig `noUnusedLocals`): dropped now-unused `Modal`, `Markdown` (both only used by the moved peek modal) and `Eye` (only used by the moved `CapRow`); `Loader2`/`Check`/`Sparkle`/`Workflow`/`Download` all retain other in-file uses and stay. `CapabilitiesStep` still owns the `peek` state + renders `<CapabilityPeekModal>` and passes `icon={<Sparkle …>}`/`icon={<Workflow …>}` into `CapRow` exactly as before — zero behavioral change to the goal-loop picker.
- **DONE (`CodePlanReview.tsx` — the authorized layout rewrite):** `PlanCapabilities` rewritten from the `CapChip` flex-wrap chip row to a **`CapRow` vertical list** matching `LoopPlanReview`'s picker: container `flex flex-wrap gap-1.5` → `flex flex-col gap-1.5`; each `<CapChip label icon on suggested onToggle title>` → `<CapRow id name description checked suggested onToggle onPeek icon>` (skills `icon={<Sparkle size={14}>}`, workflows `icon={<Workflow size={14}>}`, `size` bumped 11→14 to match `CapRow`'s row scale). Added a `peek` state (`useState<{kind; skill?; workflow?}>`), declared **before** the existing `if (…) return null` empty-state guard so the hook order stays stable, and render `{peek && <CapabilityPeekModal …>}` at the end of the picker. Deleted the local `CapChip` function entirely. Added `import { CapRow, CapabilityPeekModal } from '../../ui/CapabilityPicker'`; dropped now-unused `Check` from the lucide import (only `CapChip` used it; `Sparkle`/`Workflow` retain other uses). The marketplace-install section (its `Button variant="tonal"` from cy6) is untouched. **CodePlanReview GAINS the peek capability it never had** — code-loop users can now preview a skill's SKILL.md before committing it, same as goal-loop users.
- **Ratchet: `rawButton` 290 → 287 (−3), committed same cycle.** The 2 raw `<button>`s inside `CapRow` (row-body toggle + peek) moved into `ui/CapabilityPicker.tsx` (scanner-**exempt** — the ratchet's `ui/` carve-out lets primitives compose raw elements), so they leave the counted set: `LoopPlanReview 11→9`. `CapChip`'s 1 raw button was deleted: `CodePlanReview 8→7`. Live total falls to **287** (verified by summing `primitivesByFile['raw-button']` across the regenerated audit). `primitiveAdoption.baseline.json` `rawButton: 290 → 287` per the "may only shrink, ratchet in the same commit" doctrine; `rawInput` (163)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All ratchet tests green at 287/163/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `LoopPlanReview.tsx` raw-button `11→9` (score `31→27`), `CodePlanReview.tsx` raw-button `8→7` (score `25→23`); both genuine drift reductions, committed with the cycle. `totals.filesScanned 310→311` = the new `ui/CapabilityPicker.tsx` (scanned; its 2 raw buttons are `ui/`-exempt so contribute 0 to the raw-button total — the net −3, not −1). `totals.driftHits` (6, the token-drift axis) unchanged.
- **Validation (live, dark + light):** seeded a **`review`-status code loop** directly via the `store` module against the isolated dev home (`5c8bf35c`, 2-stage plan, `suggested_skill_ids: [artifacts, delegation]`) — the transient Plan-Review screen isn't reachable on the empty seed without a persisted review draft — then drove `#/code/5c8bf35c` headlessly on the fresh built bundle (`index-BffqsmjB.js`, served through the `static/dist` symlink, confirmed matching disk). **Both dark + light** (light forced correctly via `localStorage['mode']` in an `addInitScript` — the key `theme.tsx` reads at boot; a post-load DOM class toggle gets overwritten by `ThemeProvider`): the code-plan picker now renders as the **unified `CapRow` full-width rows** — checkbox + icon + name + description, the selected `artifacts` row showing the coral checkbox fill + `ring-primary/40` + SUGGESTED chip, all 11 installed skills as rows. **Peek verified working**: clicking a row's Preview opened the shared `CapabilityPeekModal`, fetched the SKILL.md via `api.skillContent`, and rendered it as Markdown — a capability the old `CapChip` never had. **Zero console errors** in both modes. Deleted the seed fixture afterward (`store.delete`; API back to the 2 original loops; tree clean, `.dev-home` gitignored). `tsc --noEmit` clean; `npm run build` clean; vitest **147/147** green. Neither plan-review picker is in the Playwright nav-route baseline (`e2e/routes.ts` is top-level nav shells; both pickers are transient mid-wizard/review screens) — no snapshot legitimately moves, none regenerated; live in-UI validation is authoritative.
- **NEXT (batch cycle 9/9 → G9):** fold `ChatPage` ErrorLine (:2633) + `FilesSection` strip (:351) + `MoveErrorBanner` (`TasksListPage:428`) into the `InlineError` primitive, re-tone warn→danger, and extend `InlineError`'s API (a `tone`? an `animated`? a non-dismissible multi-line `items-start` mode) + its docblock (which currently documents these as deliberately excluded — update to reflect the fold-in). Final cycle of the cy50 batch. Chaining without stopping per the owner's autonomous-loop-continuity directive.

### 2026-07-24 — cy9 DONE (G9 InlineError fold-all — three residual danger banners converge onto the primitive, warn banner re-toned — batch cycle 9/9, FINAL)
- **Context + ruling (line 487):** three failure banners were left OUT of the S2 `InlineError` consolidation because folding them would move pixels — the primitive's docblock named them explicitly as "Deliberately NOT folded in here": **ChatPage's `ErrorLine`** (`:2635`, a non-dismissible multi-line turn error — leading `AlertTriangle`, `items-start` + `whitespace-pre-wrap break-words`, `my-1`), the **FilesSection file-op strip** (`:352`, a tighter danger-**14%** iconless dismissible strip at `px-2.5 py-1.5 text-[0.75rem]` `rounded-md`), and the **Tasks board `MoveErrorBanner`** (`:429`, an *animated* `motion.div` slide-in — but **warn-toned** at `--color-warn` 14%, not danger). G9's owner ruling: **"FOLD ALL 3 IN (maximum convergence, accept pixel moves)"** — all converge onto `InlineError`, including **re-toning the warn banner to danger**, and giving `InlineError` the API it needs (a non-dismissible mode + multi-line `items-start`, an `animated` mode), then updating the docblock that documented them as excluded.
- **DONE (`InlineError.tsx` — three orthogonal API modes, chrome unchanged):** extended the primitive with three additive, orthogonal props — **`onDismiss` made optional** (omit → no corner `×`, the non-dismissible chat-turn case), **`multiline`** (swaps `items-center`→`items-start`, wraps the message `whitespace-pre-wrap break-words`, and offsets the icon `mt-0.5` — the ChatPage turn-error shape), and **`animated`** (renders the band as a `framer-motion` `motion.div` with the exact `initial={{opacity:0,y:-6}} animate={{opacity:1,y:0}}` slide-in the board banner used). The **canonical chrome is unchanged** — same `rounded-lg px-3 py-2 text-[0.8125rem]`, same danger-**10%** `color-mix` band, same `role="alert"`, same optional leading `AlertTriangle`. The class string is now assembled via `cx(...)` with the two conditional axes; `framer-motion` `motion` imported (already a `ui/`-wide dependency — 10+ `ui/` files import it). The three residual variants' bespoke tints/paddings/tones (14%-tint, tighter `px-2.5`, warn) **collapse onto the one canonical danger band** — exactly the pixel moves the owner authorized.
- **DONE (3 call sites migrated + 3 local components deleted):**
  - **ChatPage `ErrorLine`** (`:2547` call, `:2635` def): call site → `<InlineError key={i} icon multiline className="my-1">{seg.text}</InlineError>`; the local `ErrorLine` function deleted. Added `import { InlineError } from '../ui/InlineError'`. `AlertTriangle` stays (still used at the mic-error + another callout, `:1724`/`:1737`).
  - **FilesSection strip** (`:352`): the raw `{fileErr && <div role="alert" …><span/><button/></div>}` → `<InlineError className="mx-m mt-2" onDismiss={() => setFileErr(null)}>{fileErr}</InlineError>`. Added the import; `X` stays (still used at `:296`/`:371`). The strip loses its tighter `px-2.5 py-1.5 text-[0.75rem]` + 14%-tint for the canonical band — the accepted move.
  - **Tasks `MoveErrorBanner`** (`:429` def, `:337`/`:353` calls): both call sites → `<InlineError animated icon onDismiss={() => setMoveError('')}>{moveError}</InlineError>`; the local `MoveErrorBanner` `motion.div` deleted. Added the import; **dropped now-unused `AlertTriangle`** from the lucide import (its only use was the banner); `X`/`motion` both retain other in-file uses and stay. **Re-toned warn → danger** (the banner had a `--color-warn` fill + `text-warn` icon + `text-on-surface` message; now the uniform danger band) per the ruling.
- **DONE (docblock flipped + tests extended):** rewrote the `InlineError` docblock — the "Deliberately NOT folded in here (different shapes/tones …): ChatPage's … `ErrorLine`, the … FilesSection file strip, and the animated warn-toned `MoveErrorBanner`" paragraph is **replaced** with the fold-in record (all three now converge here; the `multiline`/`animated`/non-dismissible modes documented). Extended `InlineError.test.tsx` with **3 new cases** (locking the convergence so it can't silently regress): non-dismissible mode omits the `×` and renders zero glyphs sans `icon`; `multiline` yields `items-start` (and default stays `items-center`); `animated` still renders a `role="alert"` danger band with the full chrome. **150/150** green (was 147 + 3).
- **Ratchet: `rawButton` 287 → 285 (−2), committed same cycle.** Two raw `<button>` dismiss controls were removed from counted files (FilesSection strip's `×`: `FilesSection 5→4`; MoveErrorBanner's `×`: `TasksListPage 6→5`); the one surviving dismiss button now lives in `ui/InlineError.tsx` (scanner-**exempt**). ChatPage's ErrorLine had no button (non-dismissible) so no delta there. Live total falls to **285** (verified by summing `primitivesByFile['raw-button']` across the regenerated audit); `primitiveAdoption.baseline.json` `rawButton: 287 → 285` per the "may only shrink, ratchet same commit" doctrine; `rawInput` (163)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 285/163/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `FilesSection.tsx` raw-button `5→4`, `TasksListPage.tsx` raw-button `6→5`; genuine drift reductions committed with the cycle. `totals.driftHits` (6) and `filesScanned` (311) unchanged (no new `ui/` file — the extension lives in the existing `InlineError.tsx`).
- **Validation (live, dark + light):** the three targets are **conditional error states** (a provider/model turn rejection, a file rename/delete/upload failure, a server-rejected board drag) not reproducible on the offline empty-seed loopback and not in the `e2e/routes.ts` nav-route baseline. Validated two ways on the fresh built bundle (`index-B6FlOGWK.js`, served through the `static/dist` symlink, **confirmed matching disk**): (1) drove `#/chat`, `#/files`, `#/tasks` in **both dark and light** (light forced via `localStorage['mode']` in `addInitScript` — html class confirmed `light`) — **all six loads ZERO console errors, no error boundary**, proving the refactor's imports resolve and the three deleted components leave no dangling runtime refs; the Tasks list renders its real content beneath the probe, confirming the `MoveErrorBanner` deletion + `AlertTriangle` import-diet didn't break the page. (2) Injected the four `InlineError` band variants (plain-dismissible, icon+multiline non-dismissible, file-op, animated icon) into each live page in both themes and eyeballed the screenshots: the canonical **danger-10%** red band, red text, correct multi-line wrap, `⚠` icon, and `×` render identically and legibly in dark **and** light. `tsc --noEmit` clean; `npm run build` clean; vitest **150/150** green incl. `primitiveAdoption` ratchet + `consistencyAudit`. No nav-route shell pixels move (these are conditional error states absent from the captured shells) → no visual baseline legitimately moves, none regenerated.
- **BATCH COMPLETE:** cy9 is the final cycle of the owner-locked cy50 taste-call batch — **G1–G9 all landed** (cy51 G1′, cy2 G7+G8, cy3 G6, cy4 G3, cy5 G4, cy6 G2, cy7 G1-full, cy8 G5, cy9 G9), each an atomic DCO-signed clean-break commit on `feature-design-system-consistency`. The residual per-page failure banners are now one primitive; the raw-button ratchet fell 314→285 across the batch. Soul guardrail held throughout: consistency, not redesign — every pixel move was an owner-authorized convergence onto the existing system, never a new visual language.

### 2026-07-24 — cy10 DONE (S2 raw-input migration wave 1 — the search-field idiom converges onto `SearchField`)
- **Owner grant (2026-07-24):** with the G1–G9 taste-call batch closed, the owner granted full authority to **finish the plan autonomously** — "I approve your judgement on all owner-gated taste families … tackle all the remaining work in whatever order that fits the remaining work best and finish the plan yourself autonomously." So the finish work is now self-directed under the soul guardrail. This begins the honest remaining **S2 debt**: `rawInput` had **never moved** (frozen at 163 since the ratchet's introduction) while `rawButton` fell 420→285 across S2. Raw form-input migration is the untouched half of primitive consolidation.
- **Context + scope (this cycle's idiom = the magnifier-fronted search field → `ui/SearchField`):** a full-tree triage of the 163 raw inputs classified them by role. The cleanest cross-file idiom to converge first is the **search/filter field with a leading magnifier + a search-as-you-type or submit-on-Enter query** — `SearchField` (cy-era S2/S3 primitive) is its *dedicated canonical home* (11 files already adopt it), and three genuine sites still hand-rolled it as raw `<input>` + a bespoke `<Search>` icon:
  1. **ChatPage knowledge picker** (`:2235`, the "Add knowledge to prompt" Modal) — `h-10 rounded-lg bg-surface-high pl-9 pr-3 text-[0.9375rem]` overlay search → `<SearchField>` (overlay `lg` default; `trailingSlot={loading ? <Loader2 spin/> : undefined}` folds the debounce spinner into the primitive's trailing slot). The one legitimate pixel move: `rounded-lg → rounded-pill` — the `lg` scale *names* the prominent-search-bar pill shape the app already draws on its hero search bars (the SearchField docblock's blessed split).
  2. **MemoryPanel memory-explorer search** (`:275`) — `h-8 rounded-md bg-surface-high pl-8 text-[0.8125rem]` → `<SearchField size="sm">` — an **exact scale match** (sm = `h-8 text-[0.8125rem] rounded-md`, `high` surface), no pixel move at all, pure de-duplication.
  3. **OllamaModelManager library search** (`:136`, `BrowseLibrary`) — the `<Search>` + transparent `<input>` inside a caller-styled `rounded-md bg-surface-high` flex row (submit-on-Enter, external Search button) → `<SearchField variant="inline" size="sm" inlineIconSize={13}>`. The `text-[0.75rem]` off-ramp size normalizes onto the inline scale's `0.8125rem` (authorized drift-normalization per the SearchField doc); the caller's row chrome + the external Search `<button>` are untouched.
- **Deliberate EXCLUSIONS (not the search-field idiom — reserved for later input cycles):** the 6 `TextInput leadingIcon={<Search>}` sites (AuditPanel/ArchivePanel/MemoryPanel:531/formControls/WorkflowForm/ArtifactList) are **already on-system `ui/` primitives** (not raw inputs; the design system deliberately draws the line — *plain* leading-icon filter = `TextInput`, magnifier + *trailing affordance* = `SearchField`); the MemoryPanel **Inspect** (`:592`) + **Recall** (`:637`) and **WorkflowDetail "test turn"** (`:286`) fields are *submit-a-query* inputs with an external button and **no magnifier** — `TextInput` candidates for the next input cycle, not search-field convergence.
- **Ratchet: `rawInput` 163 → 160 (−3), committed same cycle.** Three raw search inputs removed (audit per-file: `ChatPage 10→9`, `MemoryPanel 8→7`, `OllamaModelManager 1→0`) drop the live total to **160** (verified by summing `primitivesByFile['raw-input']` across the regenerated audit). `primitiveAdoption.baseline.json` `rawInput: 163 → 160` per the "may only shrink, ratchet same commit" doctrine; `rawButton` (285)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. **This is the first movement of `rawInput` since the ratchet was introduced.** All 5 ratchet tests green at 285/160/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `ChatPage.tsx` raw-input `10→9`, `MemoryPanel.tsx` raw-input `8→7`, `OllamaModelManager.tsx` raw-input `1→0` (dropped from `primitivesByFile`); genuine drift reductions committed with the cycle. `totals.driftHits` (6, token axis) and `filesScanned` (311) unchanged.
- **Validation (live, dark + light):** on the fresh built bundle (`index-Dzdj0rWQ.js`, served through the `static/dist` symlink — **confirmed matching disk**), drove all three surfaces headlessly in **both dark and light** (light forced via `localStorage['mode']` in `addInitScript` — the boot key `theme.tsx` reads). (1) **MemoryPanel** `#/settings/memory` Studio tab: the "Search memories" field present, `type="search"`, typing "timezone" spring-pops the clear-X, sits cleanly beside the kind-filter chips — screenshot confirms canonical sm overlay in both themes, zero console errors. (2) **ChatPage knowledge picker**: opened via composer "Add to message" → "Add knowledge"; the overlay `lg` field renders `type="search"` with the correct geometry (`pl-9 h-10 rounded-pill`), magnifier at left, `focus:ring-primary/50`, and the clear-X spring-pops on "deploy" — screenshot confirms the pill-shaped hero search band in both themes, zero console errors. (3) **OllamaModelManager inline**: no Ollama model-app is installed in the dev home so the manager can't mount live without installing a backend (more invasive than warranted for one field) — validated instead via a **throwaway vitest** that mounted the exported `OllamaModelManager`, clicked "Browse library", and asserted the inline `SearchField` renders `type="search"` + a leading magnifier svg + a clear-X that appears only after typing (all green; test removed after the run, tree clean). `tsc --noEmit` clean; `npm run build` clean; vitest **150/150** green incl. `primitiveAdoption` ratchet + `consistencyAudit`. Neither the knowledge picker (a transient Modal) nor the Ollama manager (backend-gated) is in the `e2e/routes.ts` nav-route baseline; the MemoryPanel field lives inside the Studio tab, not a captured shell → no visual baseline legitimately moves, none regenerated; live in-UI validation is authoritative.
- **NEXT:** continue the `rawInput` drawdown — the next coherent idiom is the **submit-a-query `TextInput`** family (MemoryPanel Inspect/Recall, WorkflowDetail test-turn: an input + an external submit `Button`, no magnifier) onto `ui/forms` `TextInput`, then the broader text-field/textarea/select migration across the top raw-input files (LoopPlanReview 9, KnowledgeCreate/Detail 5 each, VariableRow 5, ProjectsSection 5, ProjectionRules 5). Chaining without stopping per the owner's autonomous-loop-continuity directive.

### 2026-07-24 — cy11 DONE (S2 raw-input migration wave 2 — the submit-a-query idiom converges onto `TextInput`)
- **Context + scope (this cycle's idiom = the query-box-with-external-submit → `ui/forms` `TextInput`):** the exact three sites cy10's NEXT reserved. Each is a single-line text field paired with an external submit `Button` and **no magnifier** — the semantic opposite of `SearchField` (no leading-icon affordance, no clear-X, no `type="search"`), so `TextInput` (the shared `ui/forms` primitive) is their canonical home, not `SearchField`:
  1. **MemoryPanel Inspect (context preview)** (`:591`) — bespoke raw `<input>` (`h-9 rounded-md bg-surface-high px-3 text-[0.8125rem] focus:ring-2 focus:ring-inset focus:ring-primary/50`) + external "Preview" button → `<TextInput size="md" surface="high">` wrapped in a `flex-1` div (the primitive renders `w-full`; the wrapper lets it flex beside the button). `onKeyDown` Enter→run preserved.
  2. **MemoryPanel Recall (deep recall)** (`:638`) — identical raw chrome + external "Recall" button → the same `<TextInput size="md" surface="high">` in a `flex-1` div, Enter→run preserved.
  3. **WorkflowDetail "test turn"** (`:288`, the `TestMatch` box) — raw `<input>` (`h-9 rounded-md bg-surface-container px-3 …`) + external "Test" button → `<TextInput size="md" surface="container" name="workflow-test-turn">` in a `flex-1` div. Kept `name` (the field posts nothing but the pre-existing attr is harmless and preserved); since `name` is present `claimsFieldLabel` is false so `aria-label="Sample turn to test workflow match"` is applied — accessible name preserved.
- **Pixel-identity (no visual move):** every migrated field is byte-for-byte the same rendered chrome — `md` = `h-9 text-[0.8125rem]`, `INPUT_BASE` carries the exact `rounded-md` + `focus:ring-2 focus:ring-inset focus:ring-primary/50`, inline padding `px-m` **== `px-3` == 12px** (`--spacing-m = calc(12px * var(--space-scale))`, scale 1), surfaces `bg-surface-high`/`bg-surface-container` unchanged. Pure de-duplication; **no `e2e/routes.ts` baseline legitimately moves, none regenerated** (the Memory tool tabs + the Workflow SidePanel are not captured shells anyway).
- **Ratchet: `rawInput` 160 → 157 (−3), committed same cycle.** Audit per-file: `MemoryPanel 7→5` (−2), `WorkflowDetail 1→0` (dropped from `primitivesByFile`). Live total 157 verified by summing `primitivesByFile['raw-input']` across the regenerated audit; `primitiveAdoption.baseline.json` `rawInput: 160 → 157` per the "may only shrink, ratchet same commit" doctrine; `rawButton` (285)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 285/157/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `MemoryPanel.tsx` raw-input `7→5`, `WorkflowDetail.tsx` raw-input `1→0` (dropped from `primitivesByFile`). `totals.driftHits` (6, token axis)/`filesScanned` (311) unchanged.
- **Validation (live, dark + light):** on the fresh built bundle served through the `static/dist` symlink, drove all three surfaces headlessly in **both** themes (light forced via `localStorage['mode']` in `addInitScript`). Deep-linked `#/settings/memory?tab=inspect` and `?tab=recall`, and `#/workflows?open=wf-commit-changes` (a native, non-read-only workflow so `TestMatch` renders). All three fields present and located **by their `aria-label` selectors** (accessible names intact), rendering the exact primitive chrome: Inspect/Recall `…h-9 text-[0.8125rem] bg-surface-high px-m`, test-turn `…h-9 text-[0.8125rem] bg-surface-container px-m` with `name="workflow-test-turn"` — identical dark↔light, **zero console errors**. `tsc --noEmit` clean; `npm run build` clean; vitest **150/150** green incl. `primitiveAdoption` ratchet + `consistencyAudit`.
- **NEXT:** continue the `rawInput` drawdown into the broader text-field/textarea/select migration across the top raw-input files (LoopPlanReview 9, KnowledgeCreate/Detail 5 each, VariableRow 5, ProjectsSection 5, ProjectionRules 5). Chaining without stopping per the owner's autonomous-loop-continuity directive.

### 2026-07-24 — cy12 DONE (S2 raw-input migration wave 3 — the model-provider settings text field converges onto `TextInput`)
- **Idiom-selection (why this cluster, not a bulk element sweep):** a full-tree re-triage of the remaining 157 raw fields by element + role showed a native-`<select>` sweep (22 sites) and a `<textarea>`/`<number>` sweep would each be **redesign, not consistency** — those sites carry real chrome/behavior variance (h-7…h-10, chevron-overlay vs native arrow, clamp-on-commit vs raw, schema-`id` binding) that the fixed-shape `Select`/`TextArea`/`NumberField` primitives can't absorb byte-identically, so forcing them would *change pixels* (soul-guardrail violation). The clean idiom is instead the **hand-rolled field constant that already IS a `TextInput` variant**: two files define `const inputCls = 'h-9 w-full rounded-md bg-surface-high px-3 text-[0.8125rem] … focus:ring-primary/50'` — **exactly** `TextInput size="md" surface="high"` (px-3 == px-m == 12px). Converging its plain-text adopters is pure de-duplication.
- **Scope (6 fields, byte-identical):**
  1. **ModelBackends** (`AddInstanceForm` "Instance name" + `EditInstanceForm` AWS region / AWS profile / Endpoint / Default model) — 5 plain `className={inputCls}` + `aria-label` text `<input>`s → `<TextInput size="md" surface="high" ariaLabel=…>`. The `onChange={(e)=>setX(e.target.value)}` wrappers collapse to the primitive's value-first `onChange={setX}`.
  2. **MultiInstanceCard** ("Instance name", the one field using the imported `inputCls + ' mb-2'`) → `<TextInput size="md" surface="high" ariaLabel="Instance name">` in a `mb-2` wrapper div (the primitive is `w-full`; the margin rides the wrapper). Dropped the now-unused `inputCls` import (kept `SchemaField`/`schemaDefaults`).
- **Deliberate EXCLUSIONS (legitimately stay raw this cycle):** the **sensitive/secret** field (ModelBackends `SchemaField` + ProviderConfigForm) needs the conditional `pr-10` + an absolutely-positioned Eye `SquareIconButton` overlay and a password/text `type` toggle — a distinct compound not yet on the primitive; the **enum `<select>`s** (`inputCls + ' cursor-pointer'`) are the select idiom, not text; ProviderConfigForm's `SchemaField` uses `<label htmlFor={id}>`+`id={id}` explicit association that `TextInput` (which owns its own id) can't reproduce byte-identically — left for a Field-wrapper cycle. `inputCls` therefore stays defined in ModelBackends (still used by 2 selects + the sensitive input), so no `noUnusedLocals`.
- **Pixel-identity (no visual move):** `md` = `h-9 text-[0.8125rem]`, `INPUT_BASE` = the exact `rounded-md` + `focus:ring-2 focus:ring-inset focus:ring-primary/50` + tones, `surface="high"` = `bg-surface-high`, inline `px-m` == the prior `px-3` (12px). Rendered chrome is byte-for-byte the prior field. No `e2e/routes.ts` baseline moves (these forms live behind the Providers → Model-providers section, not a captured shell), none regenerated.
- **Ratchet: `rawInput` 157 → 151 (−6), committed same cycle.** Audit per-file: `ModelBackends 8→3` (5 migrated; the 3 remaining = 2 enum selects + 1 sensitive input), `MultiInstanceCard 1→0`. Live total 151 verified by summing `primitivesByFile['raw-input']` across the regenerated audit; `primitiveAdoption.baseline.json` `rawInput: 157 → 151`; `rawButton` (285)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 285/151/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `ModelBackends.tsx` raw-input `8→3`, `MultiInstanceCard.tsx` raw-input `1→0` (dropped from `primitivesByFile`). `totals.driftHits` (6)/`filesScanned` (311) unchanged.
- **Validation:** `tsc --noEmit` clean; `npm run build` clean; vitest **150/150** green incl. `primitiveAdoption` ratchet + `consistencyAudit`. The live section (`RemoteModelProviders`, reached via Providers → "Model providers" → "Add instance") does **not mount in this dev home** — no model-provider app is installed, so the section is absent (the same backend-gating cy10 hit with Ollama). Per that accepted precedent, validated via a **throwaway component test**: mounted the exported `RemoteModelProviders`, mocked `useCachedData` to feed one fake provider type, clicked "Add instance", and asserted the migrated "Instance name" field is an `<input>` located **by its `aria-label`** (accessible name intact) rendering exactly `h-9 text-[0.8125rem] bg-surface-high rounded-md focus:ring-primary/50 px-m` — green; test removed after the run, tree clean. The other five fields take identical props (same primitive, same size/surface), so the one assertion covers the family.
- **NEXT:** continue the `rawInput` drawdown. Remaining top raw-input files skew toward `<textarea>`/`<select>`/`<number>` (element-variance clusters that need a per-role faithfulness check before any primitive convergence) and schema-driven `id`-bound fields (a Field-wrapper cycle). The next clean text-`TextInput` idiom, if one surfaces, else the textarea family onto `ui/forms` `TextArea` where chrome already matches. Chaining without stopping per the owner's autonomous-loop-continuity directive.

### 2026-07-24 — cy13 DONE (S3 a11y parity — the ten non-default color schemes reach WCAG AA in light mode, closing the cy15 DEVIATION; a permanent per-scheme contrast guard replaces the default-only axe rail)
- **Why this, why now (conformance, not taste — so it's in-scope autonomously):** cy15 recorded a standing DEVIATION — the coral **default** scheme was AA-corrected and is enforced live by `e2e/a11y.spec.ts` (axe), but **axe only ever exercises the active/default scheme**, so the other ten curated schemes (`schemes.ts`) still carried **pre-AA light-mode accents** that no rail caught. PRODUCT.md mandates WCAG 2.1 AA (owner-confirmed, owner-task-3), so a shipped scheme below 4.5:1 is a **conformance defect**, not a taste call. Fixing it needs no owner gate. Soul-guardrail honored: this is **not a redesign** — every accent stays the same recognizable hue, darkened only enough to be legible on white; dark mode (already AA) is untouched; no new visual language, no page changes.
- **Measurement (WCAG 2.1 relative-luminance, 4.5:1 small-text):** measured all 11 schemes in the two contexts each light accent actually bears text/UI — `primary` as a filled control (onPrimary over primary) **and** as accent text on the white surface, `primaryEmphasis` as the hover fill, `info` as accent text on white. **6 schemes failed** the light bar (dark passed everywhere — coral, the AA-verified default, sits at 5.9:1 on `surface-container`, so dark was already clean, matching cy15): honey (primary 3.38, emphasis 4.49), jade (3.74), ember (4.17), forest (primary 3.57, info 4.0), rose (4.24), amber (primary 3.25, emphasis 4.27, info 3.25). `lavender`/`ocean`/`slate`/`mono`/`coral` already passed → untouched.
- **Correction (hue-preserving, matches coral's headroom):** darkened only the failing **light** values (HSL-darken at constant hue/sat) to land at coral's ~4.83:1 fill/text headroom, keeping `emphasis` darker than `primary` (hover-darkening invariant): honey `#c17d18→#9d6614` / emph `#a96812→#985e10`; jade `#0d9488→#0b7f75`; ember `#d1543a→#c5482e`; forest `#1f9b58→#1a824a` / info `#0a8f80→#098073`; rose `#d8407e→#d22b6f`; amber `#cf7a23→#a5611c` / emph `#b8651a→#a15817` / info `#cf7a23→#a5611c`. **Every dark value, and all decorative glow/gradient stops, stay exactly as-is** — same coral precedent (glow/gradient are non-text-bearing, keep the brighter accent). The `scheme()` builder maps `--grad-2` light to `primary[1]`, so each fixed scheme's light gradient now rides its AA primary — exactly as coral already does. **After fix: all 11 schemes pass** (min 4.57:1).
- **Structural close of the cy15 hole — new permanent guard `web/src/design/schemeContrast.test.ts`:** iterates **every** `SCHEMES` entry (not just the default) and asserts each one's `primary`/`primaryEmphasis`/`info` meets ≥4.5:1 in both modes — light: filled-control, accent-text-on-white, emphasis-hover; dark: primary/info as accent text on `--color-surface-container` (read live from `tokens.css` so a token retint can't silently drift the guard; `surface-highest` deliberately NOT the reference — even the AA default coral is 4.4:1 there, so it isn't a text-bearing ground). **67 assertions (11×6 + set check), all green.** A new scheme or a regressed edit that dips below AA now turns the build red — the design-side complement to the runtime axe rail, and it covers schemes axe never activates. **This retires the cy15 DEVIATION.**
- **Validation (drove the running dev gateway, not just the source):** bundled Playwright against `127.0.0.1:10000` — forced `.light`, applied each scheme's light color set to `document.documentElement` exactly as `appearance.tsx`'s `applyScheme` does (`root.style.setProperty` per accent var), read the **computed** CSS vars back out, and measured live DOM contrast: **all 11 pass, zero console errors.** Because the **default coral is unchanged**, no `e2e/routes.ts` route-shell baseline moves (they're captured in the default scheme) — none regenerated, per "regen only where pixels legitimately move." `tsc --noEmit` clean; full vitest **217/217** across 25 files (was 150 pre-cy13's earlier count; +67 from the new guard, plus suites that grew in interim cycles); `npm run build` clean.
- **No ratchet delta:** this cycle touches color-token *values* only — no raw `<button>`/`<input>`/`<select>` count changes, so `primitiveAdoption.baseline.json` (285/151/0/7) and `consistency-audit.json` are untouched (the audit's only diff on a suite run is its `generatedAt` timestamp — reverted to keep the commit focused).
- **NEXT:** S3 interaction/responsive parity tail (T3.3) and the remaining `rawInput`/`rawButton` drawdown where a byte-identical idiom exists. Environment-blocked tail stays honestly undone: the **authenticated axe-per-route CI gate** (T3.2/T3.4) and the **V3 full-app walkthrough** need a seeded, authenticated, per-route CI harness this autonomous loop can't stand up — recorded, not faked. Chaining per the autonomous-loop-continuity directive.

### 2026-07-24 — cy14 DONE (S2 raw-button wave — the reveal-X row-delete family in `formControls.tsx` converges onto `SquareIconButton tone="danger"`; owner-authorized under the full-authority grant)
- **Why in-scope now:** cy36's deferral list (plan §"Not this cycle") flagged the **`RowActionButton` family** as owner-gated because converging it "moves pixels" and its variants (toggle / danger-reveal / danger-always) seemed to need a new `tone`/`active` API. Two things resolve that: (1) the owner's **full-authority taste grant** authorizes the pixel move; (2) the API the plan wanted **already shipped** — `SquareIconButton` carries both `tone="danger"` (idle ink-low → hover red glyph, **no fill**) and `on` (active tint), and the canonical reveal-danger row-action **already ships** at `CodeSection.tsx:384` (`SquareIconButton icon={Trash2} tone="danger"` + reveal on `className`). So this is no longer "design a primitive" — it's converging raw adopters onto the shape that already exists. Soul-guardrail honored: consolidation onto a shipped pattern, not a redesign.
- **Scope (3 byte-adjacent sites, one file — the tightest sub-cluster of the family):** `pages/tasks/formControls.tsx` — the three identical reveal-X delete buttons (`shrink-0 opacity-0 group-hover:opacity-100 text-on-surface-low hover:text-danger transition-opacity` + `<X size={14}>`): `DependencyEditor` prerequisite-remove (was **unnamed** — bare `<X>`, no `aria-label`), `ChecklistEditor` `rowInner` remove, `NotesEditor` note-remove → `<SquareIconButton icon={X} tone="danger" label=… onClick=… className="shrink-0 opacity-0 transition-opacity group-hover:opacity-100 focus-visible:opacity-100" />`. Added the `SquareIconButton` import; `X` retained (now the `icon={X}` prop).
- **Faithfulness (what moves, what doesn't):** the danger tone renders `text-on-surface-low hover:text-danger` with **no fill** — byte-identical hover treatment to the raw buttons. The **reveal** (opacity-0 → group-hover) rides `className`, unchanged. Two deliberate, tracked deltas, both improvements: (a) the hit-area standardizes from a bare 14px glyph to the `size-7` (28px) dense square + expressiveness-scaled press-spring (reduced-motion safe) — the accepted stepper/`CodeSection` precedent; (b) **a11y gains** — the `DependencyEditor` button GAINS an accessible name ("Remove prerequisite"; it had none), and all three GAIN `focus-visible:opacity-100` so a keyboard user can see the revealed control on focus (they had none — a genuine keyboard-nav fix, T3.2-adjacent).
- **Deliberately NOT migrated this cycle (correctly stay raw):** `formControls.tsx` keeps 3 raw `<button>`s — the dependency-picker candidate list-row (`flex … text-left enabled:hover:bg-surface-high`, a selectable row not an icon-action), the "Done" ghost-text button (text idiom, not `SquareIconButton`), and the checkbox-toggle (a custom check control). None are the reveal-X danger idiom. The rest of the cross-file `RowActionButton` family (ChatPage Pin/Organize toggles, `CodePlanReview:558`, `DesignPanel:117`, `FileTree:255`, `Markdown:40`) stays for follow-on cycles — each is a different reveal-group/size/tone and merits its own faithfulness check, not a blind sweep.
- **Ratchet: `rawButton` 285 → 282 (−3), committed same cycle.** Live count re-measured via the reporter's own `scanDrift()` = 282 (`formControls` raw-button 6→3). `primitiveAdoption.baseline.json` `rawButton: 285 → 282`; `rawInput` (151)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 282/151/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — `formControls.tsx` raw-button `6→3` (drift score `14→8`), and `localFocusVisibleFiles` `7→8` (the added `focus-visible:opacity-100` — an intentional coverage gain). `totals.driftHits` (6)/`filesScanned` (311) unchanged (formControls still carries its 3 legit raw buttons + 2 inputs, so it stays a drift file).
- **Validation (drove the running dev gateway):** bundled Playwright against `127.0.0.1:10000` → `#/tasks` → opened a New-task form → added an EXIT CRITERIA checklist item → asserted the migrated delete button: found by role/name, `aria-label="Remove"`, **boundingBox 28×28** (the standardized dense square), **zero console errors**; light-mode screenshot confirms the form renders clean (the reveal-X is correctly opacity-0 until row-hover). `tsc --noEmit` clean; full vitest **217/217** across 25 files; `npm run build` clean. No `e2e/routes.ts` route-shell baseline moves (the editor form isn't a captured top-level shell, and the button is opacity-0 by default) — none regenerated.
- **NEXT:** continue the `rawButton` drawdown across the rest of the `RowActionButton` family (per-site faithfulness), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough). Chaining per the autonomous-loop-continuity directive.

### 2026-07-24 — cy15 DONE (S2 raw-button wave, cross-file — the standalone inline danger-remove `RowActionButton` idiom converges onto `SquareIconButton tone="danger"` across four files; owner-authorized under the full-authority grant)
- **Why in-scope now:** direct continuation of cy14's NEXT. cy14 converged the tightest sub-cluster (the 3 reveal-X buttons in one file); cy15 extends the same `SquareIconButton tone="danger"` convergence to the **standalone inline danger-remove idiom** wherever it appears raw across the app — the icon-only `<button className="…text-on-surface-low hover:text-danger">` (no fill, ink-low → hover red) that IS the `tone="danger"` render byte-for-byte. Still consolidation onto the shipped shape, not a redesign; soul-guardrail honored.
- **Scope (4 sites, 4 files — every faithful member of the idiom):** (1) `pages/prompts/VariableRow.tsx:31` variable-remove (was **unnamed** — bare `<X size={14}>`, `px-1`); (2) `pages/loops/LoopPlanReview.tsx:665` phase-remove (`title="Remove phase"`, `<X size={15}>`); (3) `pages/knowledge/KnowledgeCreatePage.tsx:217` picked-file-remove (`<X size={16}>`, `onClick` does `e.stopPropagation()` — the handler receives the event arg unchanged through `SquareIconButton`'s `(e) => …` signature); (4) `pages/settings/VoicePanel.tsx:306` lexicon-term delete (`disabled={busy}`, `title="Delete"`, `h-7 w-7 rounded`). Each → `<SquareIconButton icon={X|Trash2} iconSize={…} tone="danger" label=… onClick=… [disabled] />` with the matching `iconSize` preserved (15/16 where non-default). Added the `SquareIconButton` import to each; the lucide glyph import (`X`/`Trash2`) is retained (now the `icon=` prop, still used elsewhere in each file).
- **Faithfulness (what moves, what doesn't):** `tone="danger"` renders `grid size-7 rounded-md text-on-surface-low hover:text-danger` — **no fill** in idle OR hover, byte-identical hover treatment to all four raw buttons. Deltas, all improvements: (a) hit-area standardizes to `size-7` (28px) — VoicePanel was *already* `h-7 w-7`, so zero size change there; the other three grow from a bare ~14-16px glyph to 28px within rows whose height is governed by taller sibling controls (VariableRow's h-7 required/optional pill; the h-8 loop/knowledge rows), so **no row-height growth**; (b) VoicePanel `rounded` → `rounded-md` (the primitive's canonical radius token — the cy14 precedent); (c) press-spring (reduced-motion safe) added uniformly; (d) **a11y gain** — VariableRow & KnowledgeCreatePage buttons GAIN an accessible name they lacked entirely (icon-only, no `aria-label`/`title`); LoopPlanReview/VoicePanel `title` → formal `aria-label`. VoicePanel's `disabled={busy}` maps to the `disabled` prop (40% opacity + click-suppress — matching the old `disabled:opacity-40`).
- **Deliberately NOT migrated (correctly stay raw — faithfulness triage):** the **dense text-only sub-row** delete buttons where a 28px square would grow row height (a density/redesign move): `CodePlanReview.tsx:559/594` (`py-1` criterion/task sub-rows — note the sibling stage-remove at `:549` is already `SquareIconButton`), `OllamaModelManager.tsx:89` (`py-2` text row), `FilesSection.tsx:366` (`p-0.5` progress-row cancel). The **filled/labeled** variants where `tone="danger"` (no-fill) would move idle pixels: `DesignPanel.tsx:117` (filled `bg-surface-high` pill swatch-delete), `ProviderCard.tsx:136` (labeled `bg-surface-high` button), `DiagnosticsPanel.tsx:151` (`size-8` filled). The **neutral toggle** `VoicePanel.tsx:299` (enable/disable term) stays raw — converging it would *add* the primitive's `hover:bg-surface-high` fill, a hover-state pixel change that belongs in its own cycle. `SystemWidget.tsx:189` is in `web/src/ui/` (ratchet-exempt). Each deferral is a distinct faithfulness reason, not a blind stop.
- **Ratchet: `rawButton` 282 → 278 (−4), committed same cycle.** Live count re-measured via the reporter's own `scanDrift()`; four files each −1 (LoopPlanReview 9→8, VoicePanel 6→5, KnowledgeCreatePage 3→2, VariableRow 2→1). `primitiveAdoption.baseline.json` `rawButton: 282 → 278`; `rawInput` (151)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 278/151/0/7.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — the four files' raw-button counts drop by 1 each (per-file drift scores 27→25, 15→13, 11→7). `totals.driftHits` (6)/`filesScanned` (311)/`filesWithDrift` (5) unchanged (each file still carries raw inputs, so it stays a drift file); `localFocusVisibleFiles` unchanged (these buttons rely on the global `tokens.css:230` `:focus-visible` ring — same as before; no local ring added or removed).
- **Validation (drove the running dev gateway):** bundled Playwright against `127.0.0.1:10000` → all four edited routes (`#/settings`, `#/prompts`, `#/knowledge/new`, `#/loops`) render with real content + app chrome in **both dark and light**, **zero console/pageerrors** across all 8 navigations; actively instantiated the migrated button (New prompt → add variable) — "Remove variable" present by role/name, dark-mode screenshot confirms the clean ink-low X glyph (visually identical to the raw original). `tsc --noEmit` clean; full vitest **217/217** across 25 files (incl. `VariableRow.test.tsx`); `npm run build` clean. Note: the global `:focus-visible` ring already covered these buttons both before and after — focus-visibility was never the gain (the accessible *name* is); recorded precisely to avoid overclaiming. No `e2e/routes.ts` shell baselines move (these are conditional in-form controls, not captured top-level shells) — none regenerated.
- **NEXT:** the standalone danger-remove idiom is now fully converged; remaining `RowActionButton` work is the deferred dense-row and filled/labeled variants (each needs its own density/fill faithfulness decision) plus the neutral-toggle family. Then the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough — not autonomous-completable). Chaining per the autonomous-loop-continuity directive.

### 2026-07-24 — cy16 DONE (S2 raw-input migration wave 4 — the dense modal/side-panel `<textarea>` cluster converges onto `TextArea`, which grows a lockstep `size` axis; owner-authorized under the full-authority grant)
- **Why in-scope now:** rotates from the (now-converged) raw-button idiom back to the S2 raw-**input** drawdown flagged in cy15's NEXT. Earlier cycles (3/5/14) established there is **no byte-identical raw-`<input>` cluster** left (the survivors hardcode `flex-1`/`px-2`/`text-on-surface-var`/custom `onBlur` — a forced `TextInput` migration would move pixels or drop behavior, so it is correctly NOT forced). The faithful cluster that DOES exist is two **dense `<textarea>`** call-sites sharing chrome byte-for-byte with each other; per the plan's explicit lockstep rule ("structural props grown with the first real adopter"), **two** real adopters needing the dense size is the exact trigger to give `TextArea` its `size` axis. Codification onto the shipped primitive, not a redesign; soul-guardrail honored.
- **Primitive growth (lockstep, byte-identical by construction):** `ui/forms.tsx` `TextArea` gains `size?: FieldSize` (default `'lg'`) + `autoFocus?: boolean`, backed by a new `TEXTAREA_TEXT` record (`sm/md → text-[0.8125rem]`, `lg → text-[0.9375rem]` — the same on-ramp type steps as `FIELD_SIZE`, minus the height rung since a textarea sizes from `rows`). The `mono` branch is unchanged: it still appends `font-mono text-[0.8125rem]` **after** the size text, so every existing (default-`lg`) and mono adopter renders token-for-token as before — the migration moves zero pixels for anyone not opting into `sm`. The stale "TextArea will adopt the scale later" comment is replaced with the accurate "DateInput / Select keep the prior fixed chrome" note (they remain un-grown — no adopter yet).
- **Scope (2 sites, 2 files — the dense-textarea cluster):** (1) `pages/agents/AgentDetail.tsx:152` routing-notes editor (`rows={3}`, `aria-label="Routing notes"`); (2) `pages/files/comments/CommentLayer.tsx:285` submit-comments modal instructions (`autoFocus`, `rows={4}`). Both were raw `<textarea className="w-full resize-y rounded-md bg-surface-container px-3 py-2 text-[0.8125rem] …focus:ring-2 focus:ring-inset focus:ring-primary/50">` → `<TextArea size="sm" ariaLabel=… [autoFocus] rows=…>`. Added the `TextArea` import to each.
- **Faithfulness (what moves, what doesn't):** `TextArea size="sm"` renders `w-full rounded-md bg-surface-container px-m py-2 text-on-surface text-[0.8125rem] placeholder:text-on-surface-low outline-none resize-y focus:ring-2 focus:ring-inset focus:ring-primary/50` — the raw markup's chrome exactly, with **one token-cleanup delta**: hardcoded `px-3` → the scale-responsive `px-m` (`calc(12px * var(--space-scale))`, ==12px==`px-3` at default scale, but now honors the density scale like every other form primitive). All other tokens byte-identical. **a11y gain (CommentLayer):** the modal textarea previously had **no accessible name** (only a sibling visual `<label>`, unassociated) — it now carries `ariaLabel="Additional instructions (optional)"` matching that label. AgentDetail already had `aria-label="Routing notes"`, preserved verbatim.
- **Deliberately NOT migrated (correctly stay raw — faithfulness triage):** the **`resize-none` inline composers** in the same CommentLayer file (`:104` add-comment popover, `:258` edit-comment card) — they carry bespoke `resize-none` + custom `onKeyDown` (Enter-saves / Esc-cancels) + non-container surfaces (`bg-surface-container`/`bg-surface-high`), so a faithful migration would need `resize`/`onKeyDown` props grown on the primitive first (a separate lockstep step, not this cluster). The **mono prompt textareas** (`PromptEditFields`/`PromptForm`/`SnippetForm`) already ride `TextArea mono` (default `lg` — untouched, still byte-identical). No raw-`<input>` migrated this cycle (no faithful cluster; see Why).
- **Ratchet: `rawInput` 151 → 149 (−2), committed same cycle.** Live count re-measured via the reporter's own `scanDrift()` (the scanner folds `<textarea>`/`<select>` into the `raw-input` count); two files each −1 (AgentDetail 1→0, CommentLayer 3→2 — its two `resize-none` composers correctly remain). `primitiveAdoption.baseline.json` `rawInput: 151 → 149`; `rawButton` (278)/`rawDialog` (0)/`inlineFontWeight` (7) untouched. All 5 ratchet tests green at 278/149/0/7.
- **Tests (2 added, +grown lock):** `forms.test.tsx` gains (1) *"TextArea size axis: sm/md are the dense on-ramp size, lg the page-form size"* — asserts `sm`/`md`→`text-[0.8125rem]`, `lg`→`text-[0.9375rem]`, and **no size emits the off-ramp `text-[0.875rem]`** (reddens before the impeccable hook would); (2) *"TextArea mono stays byte-identical"* — `mono` (default `lg`) still yields `font-mono` + `text-[0.8125rem]`. The pre-existing *"TextArea default render is the prior fixed chrome"* test still passes unchanged, locking the byte-identical default. forms suite 16 → 18.
- **DISCOVERY (audit content delta, in-scope):** `consistencyAudit` regenerated `docs/design/consistency-audit.json` — AgentDetail `raw-input` 1→0, CommentLayer `raw-input` 3→2, live total raw-input 151→149. `totals.driftHits` (6)/`filesScanned` (311)/`filesWithDrift` (5) unchanged (CommentLayer still carries raw inputs, stays a drift file; AgentDetail was not a top-drift file). impeccable hook flagged `forms.test.tsx` L57/L114 `text-[0.875rem]` — **false positive**: both are inside `.not.toContain('text-[0.875rem]')` negative assertions *guarding against* the off-ramp size (L57 is the pre-existing TextInput guard; L114 my new analogous TextArea guard). Left unchanged; no config-ignore added (no owner confirmation to suppress).
- **Validation (drove the running dev gateway):** bundled Playwright against `127.0.0.1:10000`. Created a throwaway native agent (`cy16-validate-tmp`) via the real `POST /api/agents` (native agents mount `RoutingNotesEditor`; the 6 built-ins are reserved and render only the model editor), opened it at `#/agents?open=native:…`, expanded **Advanced**, and asserted the routing-notes `TextArea` in **both dark and light**: `present, dense (text-[0.8125rem]), NOT off-ramp/lg, px-m, resize-y, bg-surface-container, accessibleName="Routing notes", editable` (filled it live) — **zero console/pageerrors** across the run. Deleted the throwaway agent after (dev home left clean). CommentLayer's `SubmitModal` textarea uses the SAME `<TextArea size="sm">` primitive+size just validated live — reachable only through the select→Comment-pill→compose→Submit gesture chain, so validated by mounting-parity + the `forms.test.tsx` size-axis lock rather than re-driving that fragile deep chain. `tsc --noEmit` clean; full vitest **219/219** across 25 files (forms 16→18); `npm run build` clean (new hash `index-B88qmCwu.js`; the >500 kB chunk warning is pre-existing/benign). No `e2e/routes.ts` shell baselines move (these are in-panel/modal controls, not top-level shells) — none regenerated.
- **NEXT:** the dense-textarea cluster is converged and `TextArea` now carries a tested `size` axis. Remaining S2 raw-input drawdown is (a) the `resize`/`onKeyDown`-bearing inline composers (need those props grown on `TextArea` in a lockstep step), and (b) any remaining faithful `Select`/`<input>` clusters (the survivors are pixel-affecting — take only genuinely byte-identical ones). Then the deferred raw-button dense-row + filled/labeled + neutral-toggle families (cy15's NEXT), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough — not autonomous-completable). Chaining per the autonomous-loop-continuity directive.

### 2026-07-24 — cy17 DONE (S3 T3.1 interaction-pattern standardization — the last two hand-rolled page-empties converge onto the shared `EmptyState` primitive; owner-authorized under the full-authority grant)
- **Why in-scope now:** rotates from the S2 raw-input/raw-button lanes (both drained of *byte-identical* clusters — cy16's Why + cy15's NEXT established the survivors all move pixels or need primitive growth first) to the **S3 T3.1** interaction-pattern lane, which is explicitly "standardize selection/empty/loading/error/confirm via shared primitives + document each in patterns.md." `patterns.md` line 163 already **sanctioned this exact follow-on** ("Ad-hoc empties … should adopt one of these two as they're touched"). A full-tree Explore audit confirmed the empty-state pattern is *already* almost fully converged — ~18 pages use `EmptyState` correctly — leaving exactly **two** hand-rolled blocks that are true page-empties (centered column + `headline-s` + hint + CTA). Codification onto the shipped primitive, not a redesign; soul-guardrail honored.
- **Scope (2 sites, 2 files — the last hand-rolled page-empties):** (1) `pages/loops/LoopsListPage.tsx:157` "No loops yet" — a **Spark-branch drop-in**: it was already `flex flex-col items-center gap-l py-2xl text-center` + `<Spark size={36}>` + `<h2 data-type="headline-s">` + hint + default-md `<Button><Plus size={16}/>>` → `<EmptyState title hint action={{label,onClick,icon:Plus}}>` (no `icon` prop, so the Spark no-icon branch is preserved). (2) `pages/code/CodeSection.tsx:317` "No code projects yet" (inside `CodeListPage`) — same idiom but with a **bare dim `<Code2 size={36} opacity-40>` glyph** and a **`size="sm"` CTA** → `<EmptyState icon={Code2} title hint action={{…,icon:Plus}}>`. Added the `EmptyState` import to each (both files keep their other `Spark`/`Code2`/`Plus`/`Button` imports for unrelated uses).
- **Faithfulness (what moves, what doesn't):** LoopsListPage = **pixel-identical** (the only pre-existing delta, hint `max-w-[400px]` vs the primitive's `[420px]`, is a 20px-wider wrap — imperceptible, and the *correct* shared measure). CodeSection = the **T3.1-intended outlier normalization**: the bare 36px/40%-opacity muted glyph becomes the canonical tinted `size-12 rounded-xl` primary chip wrapping a 26px `text-primary` glyph, and the `size="sm"`/`Plus size={15}` CTA becomes the default-md Button + `Plus size={16}`. This is "bring the outlier to the existing system," not a new visual language — it now reads identically to Tasks/Knowledge/Inbox/every other converged page (validated live below).
- **Deliberately NOT converged (faithfulness triage — these are NOT page-empties, stay distinct):** per the same Explore audit + `patterns.md`, (a) **load-error** blocks (`CodeSection:306` "Couldn't load your projects" + Retry, `CodeCockpitPage:466/2901`, `TerminalDrawer:108`) — they carry a danger/warn icon + retry CTA, a different semantic; (b) **not-found** variants (`CodeCockpitPage:481`, `LoopCockpitPage:363`, `PromptViewPage:56`); (c) **filtered-empty** (`LoopsListPage:168` — a `TextLink` reset not a `Button`, `gap-m`; `SettingsHome:57`; inline one-liners); (d) **slot/panel strips** (`ChatActivityPanel:227`, dashboard `SlotEmptyState`). One block — `DesignCockpitPage:639` "No components yet" — IS a primary "nothing yet" but has **no headline and no CTA**; converging it would mean *inventing* a headline string + CTA + dropping its `grid place-items-center h-full` wrapper. That crosses from convergence into **rewrite**, which the soul guardrail forbids — **deferred honestly**, not forced.
- **No ratchet delta (non-ratchet cycle, like cy13):** both blocks already used `<Button>` (no raw `<button>`), and `consistencyAudit.report.ts` tracks chrome/weights/color — **not** any empty-state or interaction-pattern metric — so `primitiveAdoption.baseline.json` stays `{ rawButton: 278, rawInput: 149, rawDialog: 0, inlineFontWeight: 7 }` (all 5 ratchet tests green, unchanged). The scanner also **excludes `.test.tsx` files** (`consistencyAudit.report.ts:81`), so the new lock test does not move `filesScanned` (311) either — the audit's only diff on a suite run was its `generatedAt` timestamp, reverted to keep the commit focused.
- **Tests (new lock file — the primitive had none):** added `web/src/ui/ListScaffold.test.tsx` (6 tests) pinning the `EmptyState` markup the converged call-sites now depend on: the centered `gap-l py-2xl` column; the **no-icon → Spark** branch (no tinted chip); the **icon → tinted `size-12 rounded-xl` `color-mix` chip wrapping a `text-primary` glyph** branch; the hint on the on-ramp `text-[0.9375rem]` + `max-w-[420px]` (with a negative guard that no `text-[0.875rem]` off-ramp leaks in); the `headline-s` title role; and the **default-md CTA (`h-10`, never sm)** firing `onClick`. This is the guard that keeps a future primitive edit from silently drifting the pattern the whole app inherits.
- **DISCOVERY (impeccable false positive):** the hook flagged `ListScaffold.test.tsx` L60 `text-[0.875rem]` — **false positive**, identical class to the accepted `forms.test.tsx:57/114` guards: the literal lives *inside* `expect(classOf(p)).not.toContain('text-[0.875rem]')`, a negative assertion *forbidding* the off-ramp size. Left unchanged; no config-ignore added (no owner confirmation to suppress). The recurring "DESIGN.md newer than .impeccable/design.json" note is pre-existing sidecar staleness, unrelated to this cycle.
- **Docs:** `patterns.md` — replaced the line-163 "should adopt … as they're touched" TODO-note with a **cy17 convergence record** (both sites now on `EmptyState`, markup locked by the test) + an explicit "remaining centered blocks are load-error / not-found / filtered / slot — don't force them onto `EmptyState`" boundary, so the next session doesn't re-litigate the triage. The `[x] Empty state` checklist item was already checked (patterns documented at cy6→7); this cycle completes the *adoption* it pointed at.
- **Validation (live, dark + light):** on the fresh built bundle served through the `static/dist` symlink, drove both surfaces headlessly with bundled Playwright against `127.0.0.1:10000`. Since the dev home has real loops/projects, intercepted `GET /api/loops` (the shared source for both — `api.uLoops()`, kept/`?kind=code`) to return `{loops:[]}`, forcing the empty branch at the true list routes `#/loops/history` and `#/code/history` (NOT `#/loops`/`#/code`, which land on the create-composer). DOM-asserted the exact convergence: loops = "No loops yet", **no tinted chip (Spark branch)**, CTA `h-10 … text-[0.9375rem]` md; code = "No code projects yet", **tinted chip present**, CTA `h-10` md. Eyeballed the screenshots: code-empty now renders the canonical coral tinted-chip `</>` glyph + md pill — pixel-matching every other converged page; loops-empty is the unchanged Spark drop-in. **Theme caveat (honest):** this app drives theme via a store, not the `.documentElement.light` class my harness toggled, so the two "light" captures rendered identical to dark — a *harness* limitation, not a product gap. `EmptyState` is a theme-agnostic token-only primitive already proven in both themes across ~18 pages, and this cycle adds zero new color (it reuses the exact primitive), so dark/light parity is inherited by construction. `tsc --noEmit` clean; full vitest **225/225** across 26 files (new `ListScaffold.test.tsx` +6); `npm run build` clean (the >500 kB chunk warning is pre-existing/benign). No `e2e/routes.ts` shell baselines move (these empty branches only render on an empty list, not in the captured populated shells) — none regenerated.
- **NEXT:** the empty-state interaction pattern (T3.1) is fully converged + locked; only the deliberately-distinct load-error/not-found/filtered/slot variants remain (correctly NOT `EmptyState`). A future *error-state* primitive (patterns.md's open `[ ] Error state` item) could later fold the load-error twins (`CodeSection:306` ↔ `CodeCockpitPage:466`) — that's a **new-primitive** decision, deferred. Remaining lanes: the deferred raw-button dense-row + filled/labeled + neutral-toggle families (cy15's NEXT — each pixel-moving), the `resize`/`onKeyDown` inline-composer textareas (need those props grown on `TextArea` first), and the S3 T3.3 dark/light + responsive parity tail. Environment-blocked tail unchanged (authenticated axe-per-route CI T3.2/T3.4, V3 walkthrough — not autonomous-completable). Chaining per the autonomous-loop-continuity directive.

### 2026-07-24 — cy18 DONE (assessment/validation cycle — the autonomous-completable convergence lanes are code-verified DRAINED; no source change, and manufacturing a marginal one would violate the soul guardrail)
- **Why a no-code cycle is the honest move (not a stop, not a manufactured edit):** the autonomous-loop-continuity directive says don't stop while authorized work remains — but the soul guardrail ("consistency, not redesign; add a shared primitive *only when the system genuinely lacks a pattern*") forbids inventing work. cy14→cy17 drained every *byte-identical* convergence cluster; this cycle's job was to **prove, against real code (E1 discipline, not prior-cycle memory), that the remaining candidates are genuinely non-convergeable** so a future session doesn't blindly re-scan — and to record the residual tail's true blocker. All four candidate lanes were re-inspected at source this cycle; each fails the guardrail for a *distinct, verified* reason, not a blanket stop.
- **Lane 1 — top raw-button offender re-inspected (`CodeCockpitPage`, 21 raw `<button>`):** read the class signature of all 21. **Zero byte-identical cluster.** Each is bespoke context-chrome — a rename-title trigger (`:610`, `data-type="title-l"` text button), dynamic-tone "Respond" chip (`:754`, inline `style` tone), breadcrumb link (`:912`), file-tree/commit/file rows (`:1151/2364/2385`, full-width `text-left` list rows), a view-tab toggle (`:1351`, `on`-state inline style), an `aria-pressed` autopilot toggle (`:1494`), a collapsible section header (`:1589`, rotating chevron), a task row (`:1677`), a blocker chip (`:1704`), a "show earlier cycles" text link (`:1881`), panel-reopen buttons (`:2750/2780`). None map onto `Button`/`IconButton`/`SquareIconButton` without moving pixels **and** changing semantics — that is the harness-gated *redesign* migration the guardrail forbids doing as a "consistency" pass, not a byte-identical convergence.
- **Lane 2 — the `[ ] Error state` primitive (patterns.md open item) re-evaluated at source:** read both load-error blocks. They **genuinely diverge across 6 dimensions**: `CodeSection:305` = page-level `gap-l py-2xl`, `AlertTriangle` **danger** 32px, `headline-s`, hint `text-[0.9375rem]`, `size="sm"` Retry; `CodeCockpitPage:465` = constrained `max-w-[360px] gap-3 px-6` inside `<Centered>`, `AlertTriangle` **warn** 28px, `title-m`, hint `text-[0.8125rem]`, `variant="secondary"` "Try again" that's **auto-retrying** (`— retrying…`). An `ErrorState` primitive would need a knob per difference and would gain exactly **one** faithful adopter — **premature abstraction with a single real user**, nothing like the 5-copy (`SquareIconButton`) / ~200-copy (`TextField`) bar that justified those extractions. Correctly stays an open follow-on, NOT built.
- **Lane 3 — cy15's deferred filled/labeled raw-button family re-inspected:** `ProviderCard:131/136/140`'s three action pills (Test/Connect/Disconnect) are byte-identical *to each other* but are **not a faithful `Button` adopter** — verified against `ui/Button.tsx`: they render `rounded-md bg-surface-high px-2 py-1 text-[0.75rem]` with **per-action text-color hovers** (`hover:text-on-surface`/`hover:text-danger`/`hover:text-primary`) and an **inline** spinner swap, whereas `Button` is `rounded-pill`, min `h-7`, smallest text `0.8125rem`, hovers by changing **background** (`secondary` = `hover:bg-surface-highest`), and `loading` cross-fades the whole label. Converging would flip radius + grow text + invert the hover model + drop the tint — a control redesign. A tree-wide scan for the dense labeled-chip signature (`bg-surface-high` + `px-2 py-1` + `text-[0.75rem]`) returned **11 hits, but only ProviderCard's 3 share the full idiom** — the rest are a badge `<span>` (VoicePanel:146), a hover-overlay strip (MemoryPanel:319), tri-state toggles (ProjectsSection:508, CodeCockpitPage:1496), and a `<select>` (CodePlanReview:545). **3 same-file copies is below the 5-copy missing-primitive bar** → a `LabeledChipButton` primitive would be premature abstraction; left raw.
- **Lane 4 — neutral-toggle (`VoicePanel:299`) re-confirmed:** the enable/disable term button (`text-on-surface-low hover:text-on-surface`, no fill) converging to `SquareIconButton` would *add* the primitive's `hover:bg-surface-high` fill — a hover-state pixel change, i.e. its own deliberate cycle, not a faithful drop-in. Stays raw (matches cy15's recorded triage).
- **T3.3 parity — re-verified holding (prior-cycle live evidence, no new pixels this cycle):** the autonomous-checkable T3.3 surface already holds from the cy13→cy17 live runs — dark/light contrast (all 11 schemes ≥4.5:1 AA, guarded permanently by `schemeContrast.test.ts`), and the earlier phone-width sweep found **0 horizontal-overflow routes at 375px**. No source moved this cycle, so nothing re-drove; the standing guards (axe rail + `schemeContrast.test.ts`) remain the live enforcement.
- **No ratchet delta, no source change (validation-only cycle, like a V-task):** `primitiveAdoption.baseline.json` stays `{ rawButton: 278, rawInput: 149, rawDialog: 0, inlineFontWeight: 7 }`; `consistency-audit.json` untouched (no suite re-run needed — no code moved); `tokenLint.allowlist.json` remains its single `WidgetFrame` entry; the 6 residual `driftHits` are all legitimately dynamic (guardrail-protected, not convergeable). Tree left clean.
- **Honest state of the plan — this is the autonomous-completable FLOOR:** every convergence a fully-autonomous loop can land *faithfully* (byte-identical extraction, or an outlier normalized onto a shipped primitive) is done. The residual tail is **not** more of the same work — it is three categories that each need something this loop cannot supply: (1) **owner/design decisions** — the `ErrorState` primitive (a new-primitive call with one adopter), and the harness-gated `CodeCockpitPage`-class raw-button *redesign* migration (moves pixels + changes control semantics, which is the redesign the guardrail gates); (2) **primitive-growth prerequisites** — the `resize`/`onKeyDown` inline-composer textareas need those props grown on `TextArea` first (a lockstep step with no current second adopter to trigger it faithfully); (3) **environment-blocked** — the authenticated axe-per-route CI gate (T3.2/T3.4) and the V3 full-app walkthrough need a seeded, authenticated, per-route CI harness this loop can't stand up. **NEXT: surface this floor to the owner rather than manufacture marginal cycles** — continuing to invent sub-5-copy "clusters" or single-adopter primitives would be exactly the redesign-creep the soul guardrail exists to prevent. Recorded honestly, not faked.
