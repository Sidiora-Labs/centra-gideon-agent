# PERSONALITY-THEMES

**Status:** DECOMPOSED — the executable work now lives in [`../atomic/PT.md`](../atomic/PT.md) as 6 atomic plan(s).

This plan was split because parts of it blocked on other plans, which forced it to sit half-done while other work ran. Each atom below its own file executes start-to-finish in one go; the dependency graph lives in [`../atomic/dag.json`](../atomic/dag.json).

The original design record is kept below — execution logs, measured findings and owner rulings are the reason this document still matters.

---
# Plan: Personality Themes — Themes That Carry Behavior, Not Just Color

**Status:** IN PROGRESS — S1 shipped 2026-07-28 (the personality registry + identity behaviors:
`design/personalities.ts` with a CLOSED typed `behavior` block and three entries, the new `phosphor`
scheme inside the existing WCAG sweep, `app/personality.tsx` mounted in `main.tsx` with full restore,
the Settings → Design `PersonalityPicker` with a propose-don't-write rename consent, and the T1.4
backend clean break to a `_PERSONA_THEMES` set imported by `chat_handlers.py` so accepted values and
injectable personas cannot drift).
**S2's four build tasks have ALL landed** — T2.1 (sound cues, atom `PT-2`): `design/soundCues.ts`
synthesised earcons + the master toggle, default OFF. T2.2 (shell elements, atom `PT-3`):
`SHELL_ELEMENTS` closed lazy map in `personalities.ts`, `ui/personality/TerminalStrip.tsx`, and the
`PersonalityShellElement` slot in `App.tsx`. T2.3 (error treatments, atom `PT-4`):
`design/errorTreatments.ts` closed map, `behavior.errorTreatment`, and the skin read by
`ErrorBoundary`/`IncidentBanner`. T2.4 (proofs + C5 rails, atom `PT-5`): `behavior.dials` +
`behavior.soundCues`, both proof identities fully specified, and `personalityA11y.test.ts` rebuilt as
ten fixture-falsified rails. **Remaining: V2 only** (atom `PT-6`) — the as-a-user tour across both
identities, all modes, sound on and off.
Status corrected 2026-08-04 by code audit; T2.2 line added and the whole S2 line rewritten
2026-08-16 (it still claimed T2.1 was missing two atoms after it landed). Created 2026-07-26 (roadmap rev 12; owner ask:
sibling-platform gap analysis greenlight — "personality themes as a brand play")

---

## Context (code recon, 2026-07-26)

- **Theme system (frontend):** curated color schemes are `SCHEMES: Scheme[]` in `web/src/design/schemes.ts` (`Scheme {id, label, emoji?, swatch, colors}` — color-token overrides keyed by CSS varName; `DEFAULT_SCHEME = 'coral'`). The appearance store (`web/src/app/appearance.tsx`, `AppearanceProvider`) applies overrides to `<html>` per mode, persists to localStorage key `appearance` (`Overrides {colors, scalars, selects, scheme, widthPreset}`), pools server custom themes (`themeToScheme`), and sets `root.dataset.theme = ov.scheme` + `root.dataset.ui` (the P19 orthogonal attributes — CSS can already key off the active theme id). Dark/light mode is separately owned by `web/src/app/theme.tsx` (`ThemeProvider`, `.light` class + `data-mode`).
- **Token vocabulary:** `web/src/design/tokenRegistry.ts` (`TOKENS: Token[]`, `ColorToken/ScalarToken/SelectToken`; scalars/selects can feed `web/src/design/runtime.ts` — the mutable bridge DotGlow + motion read: `glow, animSpeed, dotShape, dotPattern, bounciness, expressiveness`). `web/src/design/tokens.css` holds the defaults + the density blocks + the two `@media (prefers-reduced-motion: reduce)` rules (`:398` kills all animation/transition durations; `:420` drops glass blur).
- **Server theme store:** `/api/themes` CRUD in `src/gideon/dashboard/handlers/agents.py` (routes at `dashboard/server.py:585-589`): JSON files under `config_dir()/themes/<slug>.json`, validated by `_validate_theme_data` against the `_THEME_CSS_VARS` allowlist (kept in exact sync with tokenRegistry color varNames) with `_sanitize_css_value` (positive char allowlist + `_CSS_DANGEROUS_FUNC_RE` denylist — url()/expression() blocked). **Unknown keys are rejected** (`"'{mode}' key '{key}' is not a recognized theme variable"`) — a personality block in a theme file needs an explicit allowlist extension, not a drive-by.
- **A11y rails (must extend, never bypass):** `web/src/design/schemeContrast.test.ts` — structurally asserts EVERY scheme in `SCHEMES` meets WCAG AA (4.5:1) for its accent tokens in both modes, reading the real surface value from `tokens.css`; `web/e2e/a11y.spec.ts` (axe, default scheme); root `MotionConfig reducedMotion="user"` (`web/src/app/App.tsx:133`) + `useReducedMotion` at component level (`GideonMark.tsx:14`, `SidePanel.tsx:46`, `IconButton.tsx:37`).
- **Assistant display name (backend, already wired):** `agent.bot_name` (`config/loader.py:362`, sanitized by `_sanitize_bot_name` :312 — ≤50 chars, markdown/braces stripped), in the `_EDITABLE_CONFIG` PATCH allowlist (`dashboard/handlers/core.py`, `"agent.bot_name"` entry), consumed by `ContextBuilder._bot_name` (`context.py:651` — `AppConfig.load().agent.bot_name or "Gideon"`, the `{{bot_name}}` template var), edited today in `web/src/pages/settings/AccountPanel.tsx` via `api.patchConfig('agent.bot_name', v)`. **The name-override seam exists end-to-end** — this plan drives it from the theme, it does not rebuild it.
- **Persona-per-theme precedent (absorb):** the `lumon` easter egg — `_ChatSession.color_theme` (`dashboard/state.py:376`, validated `{"", "lumon"}` in `chat_handlers.py:86`), `_maybe_inject_persona` (`dashboard/chat_utils.py:438` — appends the bundled `persona-lumon` snippet on first turn), persisted per-session (`chat_persistence.py:489`). Proof that a theme can carry conversational flavor; currently hardcoded to one value and **not connected to the visual theme system at all**.
- **Brand/logo surfaces:** wordmark + mark are `Spark`/`Wordmark` (`web/src/ui/Spark.tsx` — `GideonMark` painted with the scheme `--grad-*` gradient; `Wordmark` takes `label = 'Gideon'` as a prop already), mounted in `NavRail.tsx:138`. Favicon is static (`web/index.html`: `<link rel="icon" href="/gideon.svg">`; `web/public/gideon.svg`) — no runtime swap exists; nothing in `web/src` touches `document.title` or the favicon link today.
- **Shell mount points for a decorative element:** `TopBar` (`web/src/ui/TopBar.tsx`, used via `ListScaffold.tsx:21` and pages), the app shell in `web/src/app/App.tsx` (ShellCorners + `Toaster` mounts at :308/:382 show the pattern for a shell-level, route-independent element). Error states: per-page `ErrorBoundary` (`web/src/app/ErrorBoundary.tsx` — class component, renders a fallback panel) and `IncidentBanner` (`web/src/app/IncidentBanner.tsx`) are the state-reactive chrome precedents.
- **Audio:** the only Web Audio in the app is TTS playback (`ChatPage.tsx:1426-1440` — `AudioContext` created inside a click gesture, the autoplay-policy-correct pattern). No audio files are shipped; no sound-cue system exists.
- **Gap:** themes are colors only; `bot_name`, the lumon persona, the logo label, and the favicon are four disconnected knobs with no registry tying them into one switchable identity — and no opt-in sound or state-reactive flourish layer exists at all.

## Design

- **S1 — The personality registry + identity behaviors.** A new `web/src/design/personalities.ts` declares `PERSONALITIES: Personality[]` — each entry references a base color `Scheme` (its colors go through the EXISTING scheme mechanism unchanged) and adds a typed, closed `behavior` block: `displayName?` (assistant name), `wordmarkLabel?`, `faviconSvg?` (a bundled asset path under `web/public/`, never a data-URI from user input), `soundCues?` (named Web-Audio synth recipes), `shellElement?` (a registered component id from a closed map — never arbitrary code), `errorTreatment?` (a registered overlay variant id), `personaSnippet?` (a bundled prompt-snippet name, generalizing the lumon mechanism). Applying a personality = `applyScheme(baseSchemeId)` + activating the behavior layer; **standard schemes have no `Personality` entry, so zero personality code runs for them** (the additive guarantee). The active personality id persists in the existing `appearance` localStorage overrides (`personality?: string` — additive key, old payloads load fine). A `PersonalityProvider` (mounted beside `AppearanceProvider` in `App.tsx`) resolves the entry and exposes it via context; it also drives: `document.title` + favicon `<link>` swap (restore defaults on deactivate), `Wordmark label` (already a prop), and — via the existing sanitized seam — `api.patchConfig('agent.bot_name', displayName)` **only on explicit user confirmation** in the activation flow (propose-don't-write: the theme *offers* the rename, a checkbox in the picker confirms it; deactivation offers the restore the same way).
- **S1 — Persona snippet generalization (backend, small).** `_maybe_inject_persona` (`chat_utils.py:438`) generalizes from the hardcoded `"lumon"` to a validated closed set of bundled persona snippets (`persona-<id>` in `prompt_providers/catalog.py`), with the `color_theme` body-field validation in `chat_handlers.py:86` widened to that same set. Clean break: the lumon special case becomes the first entry of the general mechanism (no dual path).
- **S2 — Sound cues, shell element, error treatment, and the two proofs.** `web/src/design/soundCues.ts`: a tiny synth (`playCue(name)`) building oscillator/gain envelopes on a lazily-created `AudioContext` (the ChatPage gesture-gated pattern) — cue points limited to a closed set (`turn_complete`, `approval_needed`, `error`), **opt-in via a master toggle default OFF**, silenced entirely under `prefers-reduced-motion` and when the tab is hidden. `shellElement`: a closed registry map `{id → lazy component}` rendered at the App shell level (the `Toaster` mount pattern, `aria-hidden`, `pointer-events-none`, reduced-motion-static like `DotGlow`). `errorTreatment`: `ErrorBoundary` + `IncidentBanner` read an optional variant class from the personality (visual skin only — same text, same `role="alert"`, same actions; AA-checked). Proofs: **`retro-terminal`** (mono-green scheme fork, `data-ui` nudged toward `cli` density, scanline shell strip, square dot shape, terminal-bell cue, "TERMINAL" wordmark) and **`gideon-arcade`** (playful placeholder: bouncy expressiveness preset, sparkle dots, coin-blip cue). Both are placeholders for owner-picked brands — the registry entry shape is the deliverable.
- **A11y invariants (enforced, not promised):** every personality's base scheme goes through `schemeContrast.test.ts` automatically (it iterates `SCHEMES`); a new `personalityA11y.test.ts` structurally asserts each `Personality`: base scheme exists, error treatment preserves `role="alert"` semantics, sound cues declare no autoplay, shell element ids resolve in the closed map. Reduced-motion: shell elements and cues check `useReducedMotion`/the media query themselves *in addition to* the global CSS kill at `tokens.css:398`.
- **Forward hook (mention only, not in scope):** APP-PLATFORM-EVOLUTION may later let apps contribute personality entries through a provider seam (manifest-declared, allowlist-validated server-side like `_THEME_CSS_VARS`); the closed component/cue registries here are designed so that seam can extend them without opening arbitrary-code paths.

## Contracts & Interfaces (conventions per [AGENTS.md](../../../AGENTS.md))

### C1 — Personality registry (`web/src/design/personalities.ts`, new)
```ts
export interface Personality {
  id: string                    // 'retro-terminal' | 'gideon-arcade' | …
  label: string
  baseScheme: string            // MUST be an id in SCHEMES (schemes.ts) — colors ride the existing mechanism
  behavior: {
    displayName?: string        // offered agent.bot_name value (user-confirmed, never silent)
    wordmarkLabel?: string      // Wordmark label prop (Spark.tsx)
    faviconHref?: string        // bundled asset under web/public/ only
    personaSnippet?: string     // bundled prompt snippet name ('persona-lumon' generalization)
    soundCues?: Partial<Record<'turn_complete' | 'approval_needed' | 'error', CueRecipe>>
    shellElement?: ShellElementId    // closed union — keys of SHELL_ELEMENTS
    errorTreatment?: ErrorTreatmentId
    uiDensity?: 'comfortable' | 'dense' | 'cli'   // maps to the existing --ui-density select
  }
}
export const PERSONALITIES: Personality[] = [/* retro-terminal, gideon-arcade */]
export const SHELL_ELEMENTS: Record<ShellElementId, LazyExoticComponent<…>> = { … } // closed map
```

### C2 — Sound synth (`web/src/design/soundCues.ts`, new — zero audio files)
```ts
export interface CueRecipe { wave: OscillatorType; freqs: number[]; durMs: number; gain: number }
export function playCue(recipe: CueRecipe): void
// Lazily creates ONE AudioContext (the ChatPage.tsx:1438 gesture-gated pattern);
// no-ops when: sounds toggle off (default), prefers-reduced-motion, document.hidden.
```

### C3 — Provider + persistence (additive)
```ts
// appearance.tsx Overrides gains: personality?: string   (localStorage 'appearance'; absent = none)
// PersonalityProvider (web/src/app/personality.tsx, new): resolves the entry, sets
//   document.title / favicon link / data-personality on <html>; restores ALL defaults on deactivate.
// bot_name flows through the EXISTING sanitized PATCH: api.patchConfig('agent.bot_name', v)
//   (core.py "agent.bot_name" allowlist entry, _sanitize_bot_name at loader.py:312) — no new config field.
```

### C4 — Persona-snippet generalization (backend; §2.2 envelope untouched — existing routes)
```python
# dashboard/chat_utils.py — clean-break rename of the lumon special case:
_PERSONA_THEMES: frozenset[str]  # closed set of bundled persona-<id> snippet ids
def _maybe_inject_persona(message: str, color_theme: str, is_new: bool) -> str: ...
    # generalizes: color_theme in _PERSONA_THEMES → render_snippet_block(f"persona-{color_theme}")
# chat_handlers.py:86 validation widens {"", "lumon"} → {""} | _PERSONA_THEMES
```

### C5 — A11y guard tests (structural, red-on-regression)
```ts
// web/src/design/personalityA11y.test.ts (new):
//   every Personality.baseScheme ∈ SCHEMES (⇒ covered by schemeContrast.test.ts's AA sweep);
//   shellElement/errorTreatment ids resolve in their closed maps;
//   no personality declares a cue without the master-toggle gate.
```

### Integration points
- **Calls:** `applyScheme` + the `Overrides` store (`appearance.tsx`), `Wordmark` label prop (`ui/Spark.tsx`), `api.patchConfig('agent.bot_name', …)` (`lib/api.ts` → `handlers/core.py` allowlist), `render_snippet_block` via `_maybe_inject_persona` (`chat_utils.py:438`), `runtime` dials (`design/runtime.ts`) for expressiveness presets, `useReducedMotion`/root `MotionConfig` (`App.tsx:133`).
- **Called by:** `DesignPanel.tsx` (the picker gains a Personality section), `App.tsx` (provider mount + shell-element slot), `ErrorBoundary.tsx`/`IncidentBanner.tsx` (optional treatment variant).
- **Storage owned:** the `personality` key inside the existing `appearance` localStorage payload; two bundled `persona-<id>.md` snippets in `src/gideon/config/prompt_snippets/`. No new server files; `/api/themes` custom themes remain colors-only in this plan (personality entries are first-party code — the app-provider seam is the forward hook).
- **Zero telemetry:** activation/cue events are never logged anywhere (not even SEL — this is pure presentation, not security-relevant).

## Task breakdown (executor-ready — run under the roadmap session discipline in [AGENTS.md](../../../AGENTS.md))

### Session 1 — Registry + identity layer + persona generalization

| ID | Task | Files | Done when |
|---|---|---|---|
| T1.1 | `personalities.ts` registry + types (C1) with the two proof entries stubbed (base schemes: a mono-green fork added to `SCHEMES` for retro-terminal; an existing scheme for gideon-arcade) | `web/src/design/personalities.ts` (new), `web/src/design/schemes.ts` (one added scheme) | registry typechecks; new scheme passes `schemeContrast.test.ts` in both modes without weakening the test |
| T1.2 | `PersonalityProvider`: resolve + apply (title, favicon link swap, `data-personality` attr, wordmark label via context), persist `personality` in the `appearance` overrides, full default-restore on deactivate; standard-scheme path provably untouched | `web/src/app/personality.tsx` (new), `web/src/app/appearance.tsx` (additive key), `web/src/app/App.tsx` (mount), `web/src/ui/Spark.tsx` (consume label from context, default unchanged) | activating swaps title/favicon/wordmark; deactivating restores byte-identical defaults; with no personality active, a DOM/behavior snapshot matches pre-plan (test) |
| T1.3 | Display-name offer flow: activation dialog with an explicit "Also rename the assistant to <name>" checkbox → `api.patchConfig('agent.bot_name', …)`; deactivation offers the restore; never silent (propose-don't-write) | `web/src/pages/settings/DesignPanel.tsx` (Personality picker section + dialog) | unchecked = `bot_name` untouched; checked = `{{bot_name}}` resolves to the new name in a fresh chat; restore offer works |
| T1.4 | Persona generalization (C4): `_PERSONA_THEMES` closed set, `_maybe_inject_persona` generalized, validation widened, lumon becomes entry #1 (clean break — no special case remains); second bundled snippet for retro-terminal | `src/gideon/dashboard/chat_utils.py`, `src/gideon/dashboard/chat_handlers.py`, `src/gideon/prompt_providers/catalog.py`, `src/gideon/config/prompt_snippets/persona-retro-terminal.md` (new) | existing lumon tests stay green through the general path; an unknown theme value is still rejected 400 |
| V1 | Validation (as a user): activate retro-terminal → colors + wordmark + favicon + title + (confirmed) name all flip; new chat carries the persona; switch back to coral → everything restores; `schemeContrast` + typecheck + vitest green | — | holds |

### Session 2 — Sound cues + shell element + error treatment + proofs polished

| ID | Task | Files | Done when |
|---|---|---|---|
| T2.1 | `soundCues.ts` synth (C2) + master toggle (default OFF) in the Personality picker + cue wiring at the three cue points (turn settled / approval requested / error toast) behind the toggle, reduced-motion, and `document.hidden` gates | `web/src/design/soundCues.ts` (new), `web/src/pages/ChatPage.tsx` (turn-settled hook beside `sessionSkillsEpoch`), `web/src/app/useApprovalToasts.ts`, `web/src/ui/Toaster.tsx` | no sound ever plays with the toggle off or reduced-motion on (tests mock the media query); no audio file exists in the bundle (CI grep) |
| T2.2 | Shell-element closed registry + the retro-terminal scanline strip component (aria-hidden, pointer-events-none, static under reduced-motion — the `DotGlow` discipline), mounted at the App-shell slot | `web/src/design/personalities.ts` (SHELL_ELEMENTS), `web/src/ui/personality/TerminalStrip.tsx` (new), `web/src/app/App.tsx` | element renders only under its personality; axe pass unchanged; reduced-motion = static frame |
| T2.3 | Error-treatment variants: `ErrorBoundary` fallback + `IncidentBanner` accept an optional visual variant from the personality context — skin only (same copy, `role="alert"`, actions) | `web/src/app/ErrorBoundary.tsx`, `web/src/app/IncidentBanner.tsx` | forced error under each personality renders its treatment; under a standard scheme both are pixel-identical to today |
| T2.4 | `personalityA11y.test.ts` (C5) + finish the `gideon-arcade` proof (expressiveness preset via `runtime`, sparkle dot shape, coin-blip cue) | `web/src/design/personalityA11y.test.ts` (new), `personalities.ts` | structural test red on: unknown base scheme, dangling element id, ungated cue; both proofs fully switchable |
| V2 | Validation (as a user, dev home): full tour of both personalities across chat/settings/error states, sounds on and off, reduced-motion on and off, dark and light; then a standard scheme — zero residue (title, favicon, name, DOM). `npm run typecheck && npm test && npm run build` + e2e a11y green | — | holds |

## Owner tasks (real world)
1. **Pick the real brands/skins** the placeholders become (retro-terminal + gideon-arcade are generic stand-ins) — the registry entry shape is stable; swapping content is a follow-up commit per brand.
2. **Decide the sound-cue default posture** — plan ships master toggle OFF (opt-in); confirm you don't want opt-out instead after dogfooding.
3. **Approve the display-name offer wording** in the activation dialog — it writes `agent.bot_name` (visible in every prompt via `{{bot_name}}`), so the consent copy matters.
4. **Ratify the forward hook**: app-contributed personalities wait for APP-PLATFORM-EVOLUTION's provider seam — no third-party path ships here.

## Risks & open questions
- **Favicon/title residue** after a crash mid-personality (defaults restored on deactivate, but a hard reload under an active personality must re-apply, not leak) — the provider re-applies from the persisted override on mount and restores on `personality: undefined`; T1.2's restore test covers the reload path.
- **`bot_name` is instance-global, the personality is per-browser** (localStorage) — a personality activated on one device renames the assistant everywhere. Mitigated by the explicit checkbox (informed consent) + the restore offer; if this bites, the fix is per-entity display name in `entity_settings/` — DISCOVERY-file it, don't build it speculatively.
- **Scope creep into a skinning engine** — the closed `SHELL_ELEMENTS`/treatment maps are the rail: no arbitrary components, no user-supplied code/CSS beyond the already-sanitized color vars. Any pressure to open them = E6, stop.
- **Open:** should the personality also pin dark/light mode (a brand that only works dark)? Deferred — `theme.tsx` ownership stays untouched this plan; a `preferredMode` hint could be additive later.

---

## Execution log

- [2026-07-28][S1] DONE (the personality registry + identity behaviors). (a) **`web/src/design/personalities.ts`** — `Personality` + a CLOSED, typed `behavior` block (`displayName`, `wordmarkLabel`, `faviconHref`, `personaSnippet`, `uiDensity`, `documentTitle`). Three constraints are the design: colors ride the EXISTING scheme mechanism (a personality names a `baseScheme`, never its own colors — which is what puts every personality's palette inside `schemeContrast.test.ts`'s WCAG sweep automatically); the behavior block is closed (no arbitrary CSS/markup/code, so a future app-contributed entry can be allowlist-validated instead of opening a code path); anything touching SAVED state is proposed, never applied. Three entries: `gideon` (the DEFAULT — selecting it IS deactivation, so restore is one code path with no special case) + the two plan-specified placeholders `retro-terminal` and `gideon-arcade`. (b) **New `phosphor` scheme** (mono-green CRT) added to `SCHEMES`; it passed the existing 73-assertion contrast sweep on first run — light-mode greens darkened to hold ≥4.5:1 both as a white-text fill and as text on white, same rule as the other schemes. (c) **`web/src/app/personality.tsx`** — applies title/favicon/`data-personality`/wordmark, mounted INSIDE `AppearanceProvider` so colors+density go through `applyScheme`/`setSelect` rather than a parallel mechanism; persists to `localStorage`; tolerates use outside the provider (identity is decoration, never a hard dependency). **Full restore is the load-bearing property**: the pristine title + favicon are captured at MODULE scope (so a remount can't overwrite pristine values with already-personalized ones) and written back on default. (d) **`PersonalityPicker`** in Settings → Design, above the scheme grid (personality is the coarse choice that SETS the scheme, so it reads top-down). The rename offer is a Toggle in a confirm dialog naming the exact config key it would write; declining changes the look and leaves `agent.bot_name` untouched. A colors-only switch skips the dialog entirely — it's instantly reversible, so a confirm would just be a speed bump. (e) **Backend generalization (T1.4, clean break)** — `_PERSONA_THEMES` closed frozenset + `persona_themes()`; `_maybe_inject_persona` now handles any member and `_cached_persona(theme)` replaced `_cached_lumon_persona` (the hardcoded lumon branch is GONE — lumon is entry #1). `chat_handlers` validation imports the SAME set, so accepted values and injectable personas cannot drift. New bundled `persona-retro-terminal.md` + catalog entry. Verified the closed set rejects `persona-lumon`, `../etc/passwd`, and unknown themes (all coerce to `""` = no persona, the pre-existing behavior). 11 structural tests in `personalityA11y.test.ts` (base scheme exists, behavior keys within the closed set, favicons local-only, `persona-<id>` naming, default has no rename + resolves, unknown id falls back).
- [2026-07-28][S1] Gate: `make lint` green (519 files) · `make test` **8345 passed (50.7s)** · web typecheck + **268** vitest + build green. The primitive-adoption ratchet fired TWICE and both were fixed by ADOPTION, never a baseline bump: a raw Save `<button>` → `Button`, and a raw `<input type=checkbox>` → `Toggle` (the kit has no checkbox primitive; Toggle is its binary control). LANDMINE for future sessions: the adoption scanner counts the literal string `<input` in SOURCE TEXT — including inside a comment. My comment explaining why I avoided a raw checkbox tripped the ratchet; reworded.
- [2026-07-28][S1] Validated as-a-user on :10026 — the full activate/restore tour. Activating Retro Terminal flipped ALL FIVE behaviors together (title `TERM://Gideon`, wordmark `TERM://PC`, scheme `phosphor`, density `cli`, `agent.bot_name` = `TERM`); switching back to Gideon restored every one (title, wordmark, `coral`, `comfortable`, and `bot_name` cleared to `""`); switching to Gideon Arcade with the rename toggle OFF changed the palette to amber while leaving `bot_name` empty — propose-don't-write proven, not asserted. `persona-retro-terminal` appears in `GET /api/prompt-snippets`. Zero console errors. (The gateway log's 6 tracebacks are "no model provider configured" from my `curl` chat probes on a fresh home — expected, unrelated.)
- [2026-07-28][S1] OWNER REVIEW WANTED (PR-time, not a blocker — plan owner task 3): the consent-dialog sentence, since it writes `agent.bot_name`. Current copy: "Also rename the assistant to **TERM**. This writes `agent.bot_name`, which the assistant uses to refer to itself. Turn it off to keep the name you have." Reverse direction: "Also restore the assistant's default name. This clears `agent.bot_name`, so it goes back to Gideon."
- [2026-08-14][S2/T2.3 · atom `PT-4`] DONE (error-treatment variants — skin only). (a) **`web/src/design/errorTreatments.ts`** — a closed `ERROR_TREATMENTS` map (`terminal-frame`, `arcade-panel`), each entry two class strings plus a three-token `paint` triple. The skin-only guarantee is the TYPE: a treatment has no slot for copy, no slot for a handler and no slot for a role, so a personality structurally cannot reword or disarm a failure — that is asserted rather than reviewed (a closed-key test on the entry shape). `getErrorTreatment` is total and non-throwing. (b) **`behavior.errorTreatment`** added to the closed block; both proof personalities declare one, the default declares NONE — which is exactly what makes a standard scheme pixel-stable. (c) **How the skin reaches a CLASS component:** `ErrorBoundary` must stay a class (`componentDidCatch`), so hooks are unavailable in it. The fallback JSX moved into a `ErrorFallback` FUNCTION component that the class renders; the hook read happens where hooks are legal. Chosen over threading a prop through all three `<ErrorBoundary>` call sites and over a legacy `contextType` (which would occupy the class's single context slot and still return `null` outside the provider). (d) **Colour arrives as an inline `style`, not a `bg-*`/`text-*` class** — two colour utilities on one element resolve by STYLESHEET order, not class order, so an appended class is not reliably the winner; the glyph's ink class is likewise REPLACED rather than appended. (e) `useErrorTreatment` in `app/personality.tsx` wraps the read in a `catch`: a throw while the fallback renders escapes the boundary drawing it and blanks the whole app, so "shouldn't happen" is not good enough on the error path.
- [2026-08-14][S2/T2.3] Proofs. **Pixel-identical under a standard scheme** is asserted against markup CAPTURED FROM THE PRE-CHANGE TREE (323265b6) — the ErrorBoundary fallback's full `innerHTML` with lucide's `<svg>` children normalised away (every svg ATTRIBUTE, including the class a treatment overrides, is still compared, so an icon-library bump can't make it red), and the IncidentBanner's `role`/`class`/`style`/icon-class/`textContent`. A snapshot written AFTER the change would have blessed a regression, which is why the literals are pre-change. **Same copy, same actions** is a cross-personality equality sweep: for every registry entry the fallback's `textContent`, the Retry button's accessible name and its full class string equal the default's, the banner keeps `role="alert"` and its copy, and Retry still resets the boundary. **AA measured per treatment per mode** from `tokens.css` (body ink and the alert glyph on the treated surface): terminal-frame 12.86/5.70 dark, 16.48/6.44 light; arcade-panel 11.22/4.98 dark, 14.55/5.68 light. Worst case 4.98:1 — **no palette failed**, nothing weakened. (`schemeContrast.test.ts` could not have covered this: it sweeps per-scheme ACCENT tokens from `schemes.ts`, while a treatment paints global `--color-surface-*`/`--color-danger` values that no rail was checking.) **Falsified twice:** making the treatment unconditional turned 4 tests red including both pixel-identical assertions; changing one copy string under a treatment turned exactly the same-copy guard red. A vacuity assertion (at least one personality declares a treatment) and a throw-counter probe in the resilience test keep both from passing by never running.
- [2026-08-14][S2/T2.3] DEVIATION: the closed map lives in a NEW `design/errorTreatments.ts`, not inside `personalities.ts` as contract C1 sketched. `personalities.ts` imports only the `ErrorTreatmentId` type, so the registry stays a registry and the treatment file carries its own AA rationale next to the tokens it names. No behavioural difference; C5's "errorTreatment ids resolve in their closed map" invariant is asserted in `personalityA11y.test.ts` as written.
- [2026-08-14][S2/T2.3] DISCOVERY (pre-existing, NOT fixed here — fixing it would break this atom's own pixel-identity clause): `IncidentBanner` paints `background: var(--color-error-container); color: var(--color-on-error-container)` and **neither token is defined anywhere** — not in `tokens.css`, not in `schemes.ts`, nowhere in `web/`. Both declarations are therefore invalid at computed-value time, so today the incident banner renders with NO error tint and inherited ink. The inert-utility rail can't see it (that rail scans `text-*`/`bg-*`/`border-*` CLASSES; this is an inline `style`) and token-lint can't either (it hunts raw hex/px, and these are `var()` references to nothing). Recommended follow-up: either define the pair in `tokens.css` with an AA check, or move the banner onto the defined `--color-danger`/`--color-on-danger` pair. Either is a visible change under the DEFAULT scheme, so it belongs in its own change with its own before/after, not inside a skin-only atom.
- [2026-08-14][S2/T2.3] DISCOVERY (adjacent gap, deliberately left): `ErrorBoundary`'s fallback has **no `role="alert"`** and uses a RAW `<button>`, where `IncidentBanner` has both the role and the `Button` primitive. The atom's "same `role=alert`" clause is satisfiable only for the banner. Both fixes are behaviour changes (a11y tree / primitive chrome) and both would alter the default-scheme markup this atom froze, so neither was smuggled in: `primitiveAdoption.baseline.json` `rawButton` stays **272**, unchanged and unbumped. Worth its own atom — an error panel that never announces itself is a real gap.
- [2026-08-14][S2/T2.3] Gate: `make lint` rc 0 (black/isort/flake8/mypy, 811 source files) · web typecheck green · FULL `npx vitest run` **223 files / 2209 tests passed** (baseline on 323265b6 measured first: 220 / 2183 — so +3 files / +26 tests, zero regressions) · `npm run build` green, and all eight treatment utilities verified PRESENT in the built CSS so the skin is live rather than a class string Tailwind never compiled. Python suite skipped deliberately: no file under `src/` or `tests/` was touched. `docs/design/consistency-audit.json` regenerated to a `generatedAt`-only delta and reverted rather than committed as noise.
- [2026-08-16][S2/T2.1 · atom `PT-2`] DONE (sound cues — synthesised, default OFF). (a) **`web/src/design/soundCues.ts`** — three earcons built from oscillator + gain envelopes on the context's own timeline (`turn_complete` a rising major third, `approval_needed` two rising pulses, `error` a falling minor second on a square wave). The cue NAME is the closed thing: `CueName` is a union, so `playCue('ka-ching')` does not compile, and `CUES` is a total `Record`, so adding a member without a recipe fails typecheck. **DEVIATION from contract C2**, which sketched `playCue(recipe: CueRecipe)`: taking a recipe would have closed the wrong door — any caller could invent a tone while no caller could be pinned to a named cue point. `playCue(name)` resolves the recipe from the registry instead, which is what makes "a future caller cannot invent a cue" a type error rather than a review note. (b) **All three suppressors live inside `playCue`**, not at the three call sites — a gate a caller can forget to apply is not a gate. Consequence worth stating: the wiring is three unconditional one-liners carrying no policy, so a fourth cue point (PT-5's coin-blip) inherits every guarantee for free. (c) **Default OFF is a property of the comparison**: the only enabling value is the literal `'on'` in the `soundCues` localStorage key, so an absent, corrupt, `'1'`, `'true'` or half-written preference is silence. A `?? false` would have been one truthy leftover away from audible. (d) **ONE AudioContext, and `playCue` never constructs it.** Construction is one private function with two gesture-scoped callers: the toggle's own click (which IS the user activation — priming anywhere else swallows the session's first cue) and the one-shot pointerdown/keydown primer `armCueAudio()` that `App` arms at mount, because none of the three cue points is a gesture. A context built without activation comes back permanently suspended and a second one leaks an audio thread, so both properties are asserted rather than commented. A suspended context is resumed on play AND on enable — without the first, the session's first tab-blur would silence the feature forever with nothing in a log. (e) `MAX_GAIN` (0.1) clamps every recipe in both directions, so no future cue — however it is tuned, on whatever speakers — can startle a headphone user. (f) **`behavior.soundCues` was deliberately NOT added to the personality registry** in this atom: no personality declares a cue yet, so the field would ship inert. It belongs with PT-5's coin-blip, which is the first thing that reads it.
- [2026-08-16][S2/T2.1] Proofs. **Each suppressor is falsified ALONE.** The three tests share one audible baseline (cues on, reduced-motion clear, tab visible, context primed by a real gesture — with a vacuity assertion that the baseline actually built a context) and each breaks exactly one condition, then ASSERTS the other two are still clear. A single test with all three suppressors active would have passed with any two of the three checks deleted from the source; measured, deleting each one reds exactly its own test (toggle 4 red, reduced-motion 2, `document.hidden` 1). **Zero audio files is a rail with two vacuity floors** (`design/noAudioAssets.test.ts`): it sweeps `web/public` + `web/src` + `web/dist` (517 built files at measurement, independently confirmed by a raw `find`), proves the extension matcher matches planted audio paths and rejects non-audio ones, and floors the tree-walk so an empty listing cannot report "zero audio files" on a tree full of them. Planting a `.ogg` in the BUILT dist reds it, which is how the dist half was shown to be live rather than merely present. 🪤 The second half of that rail — "the cue module reaches for no audio FILE by any route" — **must strip comments, and does**: `soundCues.ts`'s own header names `HTMLAudioElement` and `new Audio(` to explain why it avoids them, so a naive grep would go red on the explanation. There is a test asserting the header STILL names them, so the stripping stays load-bearing instead of becoming decoration. The ChatPage cue site is asserted against source (a 3.5k-line page with a live socket and no component test in the tree — a mounted assertion would be a fixture, not evidence), but against the CALL SITE: it extracts `markStreaming`'s settle branch by brace balance and requires the cue inside it, with a floor that the extraction found real code. Commenting the call out, and moving it one line below the guard, both red.
- [2026-08-16][S2/T2.1] DISCOVERY (measured, fixed here): `armCueAudio()` armed its primer BEFORE asking whether the platform has Web Audio at all, so on a browser without it the shell held a `pointerdown` + `keydown` listener for the life of the page, retrying a construction that could never succeed. Invisible by design — the symptom is silence, which is the correct outcome either way. It surfaced as cross-test interference: the stale listener outlived `vi.resetModules()` and built a SECOND context in the next test, tripping the single-context assertion. Fixed by hoisting the constructor lookup into one `audioCtor()` helper and bailing before arming.
- [2026-08-16][S2/T2.1] DISCOVERY (pre-existing, NOT part of this diff): the committed `docs/design/consistency-audit.json` is stale on `main`. A vitest run regenerates it to `filesScanned` **476** / `localFocusVisibleFiles` **32**, while the committed copy claims 470 / 30. Measured on the PARENT commit (584e12a5) as well as on this branch — both produce 476/32, so the delta is not mine: the audit's scan skips `design/` (it defines the tokens) and skips test files, which is exactly the four files this atom adds. Reverted rather than committed as noise, following the same call in the T2.3 log. Worth a one-line regen in whatever change next touches the design system, so the drift does not keep masking a real one.
- [2026-08-16][S2/T2.1] Gate: web typecheck green · FULL `npm test --workspace web` **276 files / 2772 tests passed (91.8s)**, zero failures (+3 files / +43 tests over the parent) · `npm run build --workspace web` green (504 SPA assets, 0 audio) · `tests/test_roadmap_dag_derived.py` 9 passed and `regen_dag_derived.py --check` clean after the status flip. `make lint` deliberately NOT run: no `.py` file was touched. Three pre-existing `design-system-font-size` findings in `PersonalityPicker.tsx` and two in `ChatPage.tsx` were left alone — all five are PT-1-era lines outside this diff; the new toggle block uses `data-type="body-s"` (on the documented ramp) rather than matching the file's bracketed-rem habit.
- [2026-08-16][S2/T2.2 · atom `PT-3`] DONE (shell-element closed registry + TerminalStrip). (a) **`design/personalities.ts`** — `ShellElementId` (a literal union) + `SHELL_ELEMENTS: Record<ShellElementId, LazyExoticComponent<ComponentType>>`, per contract C1, with `getShellElement` as the runtime resolver. Closure is asserted three ways because the type alone is not a runtime guarantee: the union makes an unregistered id unwritable in `behavior.shellElement`, the total `Record` makes a member without an entry a compile error, and the resolver refuses anything arriving from outside the compiler (a stale persisted override; the plan's forward-hooked app-contributed manifest). (b) **`ui/personality/TerminalStrip.tsx`** — the first entry, `terminal-scanlines`, declared by `retro-terminal`. Two layers on purpose: `.crt-raster` (static hairline lattice) and `.crt-beam` (one travelling band), both new in `design/tokens.css` beside the `.blueprint-scan` precedent, both painting through `--color-on-surface`/`--color-primary` so the raster re-tints with the scheme rather than pinning a second unthemed green. (c) **`PersonalityShellElement`** in `app/personality.tsx`, mounted once in `App.tsx`'s main shell beside `Toaster` — and deliberately NOT in embed mode or on `#/companion`, both of which return early with page content only (a host iframe inheriting shell atmosphere is the nesting embed mode exists to prevent). (d) `z-[55]` chosen against the measured scale: page content tops out at `z-50`, the overlay stack starts at `z-[60]` (Modal), so a dialog and a toast stay crisp above the raster.
- [2026-08-16][S2/T2.2] **Measured defect, fixed here — `map[id] ?? null` was never closed.** The resolver's first draft copied `getErrorTreatment`'s shape and the closure test reddened immediately: a plain index reads the **prototype chain**, so `getShellElement('constructor')` returned `Object`, a value React would try to render as a component. Probing the sibling confirmed the same hole was live on `main`: `getErrorTreatment('constructor')` returned `Object` and `treatmentPaint` then threw `Cannot read properties of undefined (reading 'bg')` — **inside the ErrorBoundary render path, the one place PT-4's own doc comment says must never throw** (one broken page becoming a blank app, exactly the failure that comment guards). Both resolvers now gate on `Object.hasOwn`; both carry a five-key inherited-id case (`constructor`, `toString`, `hasOwnProperty`, `__proto__`, `valueOf`) and the treatment one also asserts `treatmentPaint` does not throw. This is the shape memory calls a live reader of a value nobody writes — no first-party id is inherited today, so nothing would ever have surfaced it until an app-contributed personality did.
- [2026-08-16][S2/T2.2] Proofs. **The decorative contract is asserted by RENDERING every registry entry** (`design/shellElements.test.tsx`), not by reading source: `aria-hidden="true"`, `pointer-events-none`, a `data-shell-element` marker naming its own id, and zero tabbable descendants. The loop covers present and future members, so a new entry cannot ship without the contract — and a source scan would have passed on a component writing `aria-hidden` in a comment or on the wrong node. **Reduced motion is absence, not a paused animation**: the beam is not rendered when the query matches (a frozen CSS animation still costs a compositor layer and still reads as stuck), and there is deliberately **no** `prefers-reduced-motion` rule for `.crt-beam` in the CSS because the React gate already makes it unreachable — such a rule would be a second, inert mechanism. The gate reads `design/motion.ts`'s call-time `prefersReducedMotion()` rather than framer-motion's `useReducedMotion`, whose probe is cached in a **module singleton** (a sibling atom lost time to exactly this: a `matchMedia` stub applied after any earlier render in the same file is inert); the two cases still live in separate files with the stub at module scope, so a future switch back to `useReducedMotion` reddens instead of silently passing. **"Only under its personality" is paired in both directions with both populations proved non-empty** (`app/personalityShellElement.test.tsx`, driving the real provider → localStorage → context → Suspense): nothing mounts under `gideon`, `gideon-arcade`, no stored personality, or a removed-entry id; the strip mounts under `retro-terminal`. Asserted on node count **and** `container.innerHTML === ''`, so an overlay hidden with `display:none` still fails — it is `fixed inset-0`, a stacking context every standard-scheme user would otherwise pay for.
- [2026-08-16][S2/T2.2] **Lazy verified in the BUILD, not just declared.** Two rails — every registry value must be a React lazy type, and no module outside the registry may reference `ui/personality/` statically (comments stripped, two vacuity floors). **Falsification found a real gap in that rail:** the bare side-effect form `import '../ui/personality/X'` has no `from` clause, slipped through the first regex, and bundles the module just as thoroughly; the pattern now covers `import … from`, `export … from`, `import type`, and the bare form, each confirmed red, with the comment-only case confirmed still green (the repo has already been bitten by a rail matching prose). Confirmed against `npm run build`: the entry chunk contains only the id string `terminal-scanlines`, while `crt-raster`/`crt-beam`/`data-shell-element` appear **only** in a separate 0.75 kB `TerminalStrip-*.js`. The ~250 bytes of `.crt-*` CSS do ship in the global stylesheet for everyone, as every other `tokens.css` class does — noted, not split.
- [2026-08-16][S2/T2.2] Fourteen mutations against the live lines. All ten that had to red did: drop `aria-hidden` (3 tests), drop `pointer-events-none` (3), remove the reduced-motion branch (3), mount under every personality (5), non-lazy registry entry (2), static import from `App.tsx` (1), revert the `Object.hasOwn` fix (2, one per registry), remove `data-shell-element` (3), render nothing under reduced motion (3 — the case that keeps "no beam" from passing on a deleted element), and remove the `shellElement` declaration (2 vacuity floors). **Nothing reded nothing.** `axe` is untouched: no e2e route activates a personality, and the element is `aria-hidden` with no focusable content. `uiDocs.drift` checked rather than assumed — `extractUiProps` uses a flat `readdirSync`, so `ui/personality/` owes no `.doc.ts`.
- [2026-08-16][S2/T2.2] Gate: `npm run typecheck --workspace web` clean · FULL `npm test --workspace web` **278 files / 2771 tests passed** (baseline on the branch point measured first: 274 / 2743 — +4 files / +28 tests, zero regressions, no ratchet or baseline touched) · `npm run build --workspace web` clean. No `.py` touched, so `make lint`/pytest were not required; `tools/regen_dag_derived.py --check` is clean after re-escaping. `docs/design/consistency-audit.json` is a side effect of `consistencyAudit.test.ts` and was already stale on `main` — left out of the commit as noise, per the same call PT-4 made.
- [2026-08-16][S2/T2.2] DISCOVERY (adjacent, deliberately left): PT-5's row asks `personalityA11y.test.ts` to go red on a dangling `shellElement`/`errorTreatment` id. PT-4 already added the treatment half, so this atom added the symmetric `shellElement` assertion — the registry's own structural guard. PT-5's remaining scope there (unknown base scheme, a cue declared without the master-toggle gate) is untouched and still needs `PT-2`.
- [2026-08-16][S2/T2.4 · atom `PT-5`] DONE (both proofs finished + the registry's structural rails). (a) **`behavior.dials`** is the expressiveness preset the plan's Integration-points line asked for ("`runtime` dials for expressiveness presets"), and it reaches `runtime` the long way ON PURPOSE: four dials (`expressiveness`, `bounciness`, `dotShape`, `dotPattern`) name EXISTING tokens through `PERSONALITY_DIAL_TOKENS`, and `activate` writes those tokens through the appearance store — the same `setScalar`/`setSelect` the sliders call. Writing `design/runtime.ts` directly would have been one line shorter and wrong twice: the store's effect re-derives `runtime` from `TOKENS` on the next token change, so a direct write is erased at the user's first slider drag, and it would never land in the saved overrides, so the identity's temperament could not be adjusted or persisted. A dial the target does NOT declare is `resetToken`-ed rather than skipped, which is what makes "switch back" a real restore; `resetToken` deletes the override instead of writing the default over it, so no second copy of those defaults exists and a future default change is not pinned by an old personality switch. (b) **The cue split.** PT-2's single `CueName` became `CuePoint` (the three moments, still closed) and `CueName` (registered voices — the three, plus `coin_blip` and `terminal_bell`), with `behavior.soundCues: Partial<Record<CuePoint, CueName>>`. That is the shape that lets an identity change what a moment SOUNDS LIKE while structurally denying it the three things a skinning engine would want: it cannot add a moment (the point union is closed and `playCue` takes only a point), cannot author a tone (it names a voice, it does not supply a `CueRecipe` — which is exactly why PT-2's `playCue(name)` deviation from contract C2 was right), and cannot sound anything outside `playCue`, which still owns all three suppressors. **PT-3 deferred `behavior.soundCues` because no personality declared a cue and the field would have shipped inert; this atom is its first reader, and a rail now asserts every registered voice is reachable** from some personality or is a point's own default. (c) **Voices are validated at the BOUNDARY**, not at every read: `setCueVoices` copies a pair only if the key is one of the three points and the value is `Object.hasOwn(CUES, …)` — the same prototype-chain hole PT-3 measured live in both personality registries, where `'constructor'` resolved to `Object`. Here it would have handed `synth` the `Object` constructor as a recipe. Anything rejected leaves the point on its own voice, so the failure mode is the default sound rather than silence or a throw on an error path. (d) **Voices are installed in the provider's EFFECT, dials in `activate`** — deliberately different homes: the appearance store persists the dials and re-applies them on reload, while a voice has no store of its own and is derived purely from the active identity, so a reload under gideon-arcade would look right and sound default if the voice were only set at activation. There is a test for exactly that. (e) **Both proofs are now fully specified and opposite.** gideon-arcade: amber, sparkle dots on a diamond lattice, expressiveness 1, bounciness 1, a coin blip on a finished turn. retro-terminal: the same four dials driven the other way (square dots on a straight grid, expressiveness 0.25, bounciness 0) plus the ASCII BEL on an approval — one identity moving a dial in each direction is what proves the value comes from the registry rather than from a hardcoded "personality look". A rail asserts the two proofs' union covers EVERY key in the closed behavior block, so no field can rot unread. The picker's copy now names the motion dials and says which moments the active identity re-voices, because otherwise the only way to discover a changed tone is to hear it and wonder what broke.
- [2026-08-16][S2/T2.4] Proofs. **`personalityA11y.test.ts` is now ten NAMED rails, each proven red by a fixture that breaks exactly it** — 53 tests where there were 11. The old file checked the real registry only, which cannot distinguish a working rail from one that can never fail; every check is now a function run over both the registry (must be clean) and a one-mutation fixture (must be flagged), with three meta-floors: the rail↔fixture mapping is asserted total in BOTH directions (a new invariant without a falsifying fixture fails the file), the base fixture is asserted clean, and each fixture must trip **only** its own rail — because a fixture that breaks three things at once lets a dead rail look alive. Under the seven conditional rails (they only look at entries declaring the field) sits a non-empty-population floor each, so deleting a declaration reddens here instead of quietly retiring a check. **The cue-gate half reads SOURCE**, because the registry cannot express reachability and the bypass is not a registry edit — a contributor who wants a sound somewhere new calls Web Audio where it is convenient. Four pins: `playCue` checks all three suppressors *before* the `synth` call (its body extracted by brace balance, comments stripped, both floored), `playCue` is the ONLY caller of `synth` (counted module-wide minus the ones inside it), `synth` is unexported, and no module in `web/src` outside `design/soundCues.ts` calls `createOscillator` — a tight rail rather than an allowlist because the TTS player decodes buffers and the recorder analyses a mic stream, so exactly one module can voice a tone (asserted, as the sweep's vacuity floor). **`DotGlow.reducedMotion.test.tsx` is new**: gideon-arcade is the first identity that turns the backdrop into motion a user did not individually opt into, and DotGlow's reduced-motion branch was previously asserted by nothing. It proves one frame is painted and no successor scheduled, PAIRED with "and it IS painted, in the sparkle glyph" (`quadraticCurveTo` is unique to the sparkle path in `drawDot`) — without which a DotGlow that drew nothing would satisfy the absence claim perfectly. Both the reduced and the motion-allowed case live in one file on purpose: DotGlow reads `matchMedia` at effect time, and the motion-allowed case running second is what proves it, since a probe cached in a module singleton (framer-motion's `useReducedMotion` is, and T2.2 lost time to it) would red the second describe instead of silently measuring the first twice.
- [2026-08-16][S2/T2.4] Fourteen mutations against the live lines; **thirteen red, one deliberately reded nothing and that mutation was KEPT as the fix.** Red: a dangling `shellElement` id, a dangling `errorTreatment` id, an unknown `baseScheme` (each flagged with the offending id in the message); dropping `playCue`'s master-toggle guard (3 tests, including the new "a re-voiced cue is still silenced by the master toggle"); adding a second ungated export that calls `synth`; exporting `synth`; building an oscillator in `PersonalityPicker` (2, including the sweep's own floor); removing DotGlow's `!reduce`; making the sparkle glyph unreachable; dropping the `resetToken` branch so undeclared dials survive a switch-back (5); replacing `Object.hasOwn` with a truthiness check in `setCueVoices`; pointing a personality at an unregistered cue voice (2); removing gideon-arcade's cue declaration, which reds the anti-inert rail with `these cue recipes can never play: ['coin_blip']`; pointing `PERSONALITY_DIAL_TOKENS.dotShape` at a token that exists but feeds no runtime dial (`--ui-density`); and giving the DEFAULT identity a cue voice (2). **The mutation that reded nothing:** the provider's `setCueVoices(isDefault ? undefined : b.soundCues)` — simplifying it to `setCueVoices(b.soundCues)` changed no test, because the default identity's `soundCues` is `undefined`, so the branch was unreachable. That is dead code under the clean-break tenet, so the simpler form is what shipped: the invariant it was defending (the default declares no voice, no dial, no shell element, no treatment) is now a rail that reds — measured, by the "default identity declares a cue voice" mutation above — rather than an untestable ternary saying so. One assertion was also strengthened after a mutation exposed it: the "clears the override" test now asserts the override EXISTS after activation before asserting it is gone, and the DotGlow instrument pre-seeds its counters at 0 so "never called" fails as `expected 0 to be greater than 0` instead of a `TypeError` on `undefined`.
- [2026-08-16][S2/T2.4] Gate: `npm run typecheck --workspace web` clean · FULL `npm test --workspace web` **293 files / 2996 tests passed**, zero failures — baseline measured first on the branch point (3c6f1374): **2931 tests**, so +65 with no regressions and no ratchet or baseline touched · `npm run build --workspace web` clean · `tests/test_roadmap_dag_derived.py` 9 passed and `regen_dag_derived.py --check` clean after the status flip and re-escape. No `.py` file was touched, so `make lint` was not required. Three pre-existing `design-system-font-size` findings in `PersonalityPicker.tsx` (PT-1-era bracketed-rem lines) were left alone for the third atom running — my new blocks use `data-type="body-s"`, on the documented ramp. `docs/design/consistency-audit.json` regenerates during any vitest run and is stale on `main`; left out of the commit as noise, the same call T2.1/T2.2/T2.3 made.
- [2026-08-16][S2/T2.4] DISCOVERY (adjacent, deliberately left): **DotGlow's reduced-motion branch had no test before this atom** — the `animate = … && !reduce` line at `DotGlow.tsx:142` has shipped since the component was written, is the reason the halftone backdrop is honest under reduced motion for EVERY scheme (not just the arcade), and nothing would have caught its removal. This atom pinned it because gideon-arcade's sparkle preset now leans on it, but the gap was general, and `DotGlow`'s other three modes (`still`, `glow`, `none`) are still unasserted — `still` in particular is the same "one frozen frame" contract reached by a different condition. Worth an atom of its own; not smuggled in here.
- [2026-07-28][S1] Remaining: **S2** (sound cues via a tiny synth — opt-in, default OFF, silenced under reduced-motion; the shell decorative element; error treatments; and the two proof personalities fleshed out). Owner task 1 (pick real brand identities) is explicitly NOT blocking — the plan states both entries are placeholders and "the registry entry shape is the deliverable".
