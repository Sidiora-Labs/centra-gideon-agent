# DESKTOP-CAPABILITIES

**Status:** DECOMPOSED — the executable work now lives in [`../atomic/DC.md`](../atomic/DC.md) as 6 atomic plan(s).

This plan was split because parts of it blocked on other plans, which forced it to sit half-done while other work ran. Each atom below its own file executes start-to-finish in one go; the dependency graph lives in [`../atomic/dag.json`](../atomic/dag.json).

The original design record is kept below — execution logs, measured findings and owner rulings are the reason this document still matters.

---
# Plan: Desktop Capabilities — The Electron App as a Capability Surface

**Status:** DESIGNED — deepened 2026-07-18 with code recon (initial PROPOSED 2026-07-18; owner GO: live audio + OS capabilities for the platform *and* apps via the Electron app)
**Created:** 2026-07-18
**Wave:** 2/3. Not launch-gating — DISTRIBUTION carries the launch; the desktop ships when it is *better* than a browser tab.
**Depends on:** CI-RELEASE-ENGINEERING (build/signing in the pipeline), DISTRIBUTION (release artifacts + install-kind detection: the shell sets `GIDEON_INSTALL_KIND=desktop`). Coordinates with MULTIMODAL-IO (voice capture lands on this bridge; screen-context is its flagship consumer), AMBIENT-SURFACES (menu-bar companion rides the tray), INBOX-NOTIFICATIONS-UNIFICATION (native notifications as a rules target), **COMPANION-APPS (plan 54 — its "connect to a gateway you did not spawn: LAN or remote" mode is added here in that plan's S4/T4.1, consuming plan 54's client contract; the spawn-local default is unchanged)**.

> **Rev-11 note (2026-07-26):** a code audit confirmed the desktop shell is **already Electron**
> (`desktop/main.js`, Electron ^43) — there is **no Tauri anywhere in the repo**, so the
> "Tauri→Electron migration" some backlog framing implied is a no-op: this plan (committed to
> Electron, "no forked frontend, no second API") stands as-is. The real host-capability gap the
> owner cares about — **mic/live audio capture** — is this plan's **Session 3** (unbuilt), not a
> re-platform.
**Scope:** complete the Electron app and make it the **OS-capability surface**: live/system audio capture, global hotkeys, native notifications, tray/menu-bar presence, consent-gated screen capture, login-item lifecycle — exposed to core and, permission-gated, to apps. **Soul guardrail:** the desktop hosts the same SPA and the same gateway — no forked frontend, no second API. OS capabilities enter through ONE typed bridge with per-capability consent mirroring the app-platform's permission voice; nothing is silently granted; **no always-on/ambient capture ships in this plan.**

---

## Context (code recon, 2026-07-18)

- **The shell is further along than assumed:** `desktop/main.js` already implements login-shell PATH resolution, backend spawn (`gideon gateway --port auto --json-ready --no-open`) via `findPersonalclawBin` (bundled `backend-dist` with PATH fallback — dependency-injected and unit-tested), 2-min readiness wait on the `--json-ready` line, loading screen via `preload.js`'s minimal `contextBridge` (`electronAPI.onStatus`) — **the bridge pattern to extend exists**. `Tray, Menu, nativeImage, nativeTheme` are already imported in main.js (presence work has a head start — verify how much is wired).
- **Build chain exists:** `make pyinstaller` (spec excludes torch/faiss by design — local-model extras degrade with UI guidance) → `make desktop` (stages `backend-dist`, npm install) → `make desktop-dist` (`electron-builder --mac` → dmg). `backend-dist` was deleted pre-split (PUBLICATION follow-up → this plan's S1). No signing/notarization config in-repo; dist target macOS-only.
- Desktop tests exist (`desktop/test/`: context-menu, find-bin, packaging).

## Design

- **S1:** fresh backend bundle in CI (mac runner: `make desktop` → electron-builder with signing + notarization via CI secrets) attached to GitHub Releases; auto-update via electron-updater against Releases (the desktop's DISTRIBUTION §C kind: shell sets `GIDEON_INSTALL_KIND=desktop`; the in-app Updates panel shows "managed by the desktop app").
- **S2 — the capability bridge:** `preload.js` grows a namespaced typed API (`window.pclawDesktop.capabilities`) — registry `{audio_capture, global_hotkey, native_notifications, tray, screen_capture, login_item}`; each: `probe()` (availability + OS-permission state via macOS TCC queries), `request()` (triggers the OS prompt through Electron's `systemPreferences`/`desktopCapturer` paths), `state` events. Gateway-side: a `desktop` provider seam — the shell registers itself with the gateway on boot (loopback call carrying a capability manifest + a shell token); capability state surfaces in **Settings → Security → Desktop capabilities** (grant list in the app-permission consent voice; SEL events on grant/use). Apps consume via a manifest permission (`desktop: ["audio_capture"]`) enforced like `api`/`events` — gateway mediates every app→bridge call (apps never talk to Electron directly).
- **S3 — live audio:** push-to-talk global hotkey → mic capture in the renderer (getUserMedia, TCC-prompted via the bridge) → existing `/api/stt/transcribe` (bound STT provider — faster-whisper local by default) → composer insertion or voice-chat surface; system-audio capture only where the OS allows it natively (macOS: screen-capture-audio path; documented honestly, likely deferred) — mic is the S3 deliverable.
- **S4 — presence + platforms:** tray/menu-bar companion (pending-approvals count, running loops, quick capture — AMBIENT-SURFACES' menu-bar item lands here against its tile registry when available); native notifications as a plan-42 rules target (`native` on desktop replaces `dashboard` toasts when focused-away); login-item toggle; **then** Windows/Linux electron-builder targets *only after* PLATFORM-REACH proves the backend there (desktop follows platform support, never leads it).

## Contracts & Interfaces (conventions per [AGENTS.md](../../../AGENTS.md))

### C1 — Capability bridge (`desktop/preload.js` contextBridge — EXTENDS the existing `electronAPI` pattern, verified `preload.js`)

```typescript
window.pclawDesktop = {
  capabilities: {
    // caps: "audio_capture"|"global_hotkey"|"native_notifications"|"tray"|"screen_capture"|"login_item"
    probe(cap: string): Promise<{ available: boolean; granted: 'granted'|'denied'|'not-determined'|'unavailable'; reason?: string }>,
    request(cap: string): Promise<{ granted: boolean }>,   // triggers OS/TCC prompt via systemPreferences
    on(cap: string, cb: (state) => void): () => void,
  }
}
```
`contextIsolation` stays on; the renderer reaches ONLY this namespace (asserted in desktop tests). Main-process handlers in `desktop/capabilities.js` (new, split from main.js).

### C2 — Gateway desktop seam (`dashboard/handlers/desktop.py`, new thin handler)
Shell registers on boot: `POST /api/desktop/register {capabilities: {...state}, shell_token}` (loopback; shell_token minted per session, misuse → 403 + SEL). State lands in `DashboardState`; `GET /api/desktop/state` reflects it (absent/"not connected" in a browser tab). App→bridge calls are gateway-mediated (apps never touch Electron IPC).

### C3 — App manifest `desktop` permission (via plan 32's manifest-field pattern, §3.8)
```jsonc
{ "permissions": { "desktop": ["native_notifications", "audio_capture"] } }
```
Enforced in `apps/permissions.py` exactly like `api`/`events`: an app calling a bridge-backed route without the declared cap → 403 + SEL `capability_denied`. Shown on the install consent surface.

### C4 — Install-kind + updater
Shell sets `GIDEON_INSTALL_KIND=desktop` in the spawn env (consumed by DISTRIBUTION C1 `detect_install_kind` → `desktop_delegate` branch). electron-updater against GitHub Releases; user-initiated apply only.

### Integration points
- **Calls:** existing `desktop/main.js` spawn machinery + `Tray`/`Menu` (already imported — extend), `systemPreferences`/`desktopCapturer` (Electron), `/api/stt/transcribe` (audio → STT, S3), plan-42 rules engine (`native` notification target), `sel()`.
- **Called by:** apps declaring `desktop:` perm (gateway-mediated); AMBIENT-SURFACES (20) menu-bar tiles render in the tray this plan owns.
- **Depends on:** plan 33 (signing in CI), plan 34 (`detect_install_kind` desktop branch), plan 39 (non-mac targets gate on its rungs), plan 42 (`native` target).
- **Storage:** capability grants surfaced in Settings → Security; SEL events on grant/use.

## Task breakdown (executor-ready — run under the roadmap session discipline in [AGENTS.md](../../../AGENTS.md))

### Session 1 — Rebuild + signing + updater

| ID | Task | Files | Done when |
|---|---|---|---|
| T1.1 | CI job (macos runner, release.yml): `make desktop` → electron-builder signed+notarized dmg/zip → attach to the GitHub Release; secrets consumed from the `release` environment (owner task 1 provides them) | `.github/workflows/release.yml`, `desktop/package.json` build config (hardenedRuntime, entitlements incl. microphone for S3, notarize) | rc release carries a notarized dmg; `spctl -a` passes on a clean Mac |
| T1.2 | electron-updater wired against Releases (check on launch + daily; user-initiated apply; no silent installs) | `desktop/main.js`, package.json publish config | one-version-behind install prompts and updates |
| T1.3 | `GIDEON_INSTALL_KIND=desktop` in the spawn env; gateway Updates panel renders the desktop-managed state (DISTRIBUTION T4.x coordination — verify its instructions-payload branch handles `desktop`) | `desktop/main.js`, updates handler | panel shows "updates managed by the desktop app" under the shell |
| T1.4 | Packaging test refresh: `desktop/test/packaging.test.js` asserts bundle staging + version stamping against the new pipeline | desktop tests | `node --test` green |
| V1 | Validation on a clean macOS machine/VM: install dmg → first run (Gatekeeper clean) → onboarding → chat; update from a previous rc | — | recorded |

### Session 2 — Capability bridge

| ID | Task | Files | Done when |
|---|---|---|---|
| T2.1 | Bridge API in preload (typed, namespaced, per-capability probe/request/state; deny-by-default) + main-process handlers (TCC state via `systemPreferences.getMediaAccessStatus` etc.) | `desktop/preload.js`, `desktop/main.js` (+ split `desktop/capabilities.js`), desktop tests with injected stubs | unit tests per capability state machine; renderer cannot reach ipc channels outside the namespace (contextIsolation asserted) |
| T2.2 | Shell↔gateway registration: on ready, POST a capability manifest to a new loopback gateway route (shell token minted per session; gateway stores desktop state in DashboardState) | `desktop/main.js`, new `dashboard/handlers/desktop.py` (thin), state wiring | gateway `/api/status` (or a `desktop/state` route) reflects capability availability; token misuse rejected + SEL-logged |
| T2.3 | Settings → Security → Desktop capabilities panel: per-capability state (unavailable / not-granted / granted) with request buttons routed through the bridge; consent copy in the app-permission voice (copy-sensitive — reuse phrasing patterns) | `web/src/pages/settings/` new panel | states render truthfully on desktop; panel absent (or "desktop app not connected") in a browser tab |
| T2.4 | App-facing permission: manifest `desktop: [caps]` parsed + enforced (gateway mediates app calls to bridge-backed routes exactly like `api` prefixes); consent surface shows it at install | `apps/manifest.py`, `apps/permissions.py`, install consent UI | fixture app with `desktop: ["native_notifications"]` can fire one, an app without it gets 403 + SEL entry |
| V2 | Validation: grant/deny each capability from Settings; verify TCC prompts appear exactly once per grant; SEL trail complete | — | holds |

### Session 3 — Live audio (the owner's headline)

| ID | Task | Files | Done when |
|---|---|---|---|
| T3.1 | Push-to-talk: global hotkey registration (bridge capability; default chord configurable in Settings) toggling capture state with tray/menu-bar indication | `desktop/capabilities.js`, settings wiring | hotkey captures only while held/toggled; visible indicator always on while capturing |
| T3.2 | Mic capture → STT: renderer getUserMedia (TCC via bridge grant) → chunked upload to `/api/stt/transcribe` (bound provider; verify the route's streaming/chunk contract — record in log) → composer insertion at cursor (chat) with an interim-text affordance | renderer capture module (`web/src/` voice util or desktop-injected), composer integration | spoken sentence lands in the composer ≤2s after release on faster-whisper local; mic indicator truthfulness verified |
| T3.3 | Honest system-audio note: probe + documented deferral (macOS system audio requires screen-capture entitlement paths); `docs/guides/desktop.md` states mic-only for now | guide + probe stub returning `unavailable` with reason | doc + probe agree; no half-shipped system audio |
| V3 | Validation: full voice round-trip in chat; deny-mic path degrades with the actionable prompt; hotkey conflict case (already-registered chord) surfaces cleanly | — | holds |

### Session 4 — Presence + platforms

| ID | Task | Files | Done when |
|---|---|---|---|
| T4.1 | Tray/menu-bar companion: icon + menu (pending approvals count with click-through, running loops, quick-capture note→inbox, open dashboard, quit); wired to gateway state over the loopback WS/API (verify what `Tray` wiring already exists in main.js — extend, don't duplicate) | `desktop/main.js`/`capabilities.js` | counts live-update; click-throughs deep-link into the SPA |
| T4.2 | Native notifications as a plan-42 target: `native` target routed to Electron Notification when the desktop shell is connected; falls back to dashboard toasts otherwise | notification target registration (plan 42's rules engine), shell handler | a rule with target `native` fires an OS notification; tap focuses the relevant surface |
| T4.3 | Login item toggle (Settings, via bridge) + graceful gateway shutdown on quit (verify current quit path kills the child cleanly — PPID-reaping interplay) | `desktop/main.js`, settings | login-item survives reboot (manual check); no orphan gateway after quit (process table verified) |
| T4.4 | Windows/Linux targets — **gated**: only if PLATFORM-REACH's corresponding rung is proven; then electron-builder targets + per-OS signing docs; else record DEFERRED with the gate condition | `desktop/package.json`, docs | either shipped-with-proof or a dated deferral note |
| V4 | Validation: a day of desktop dogfood (owner task 3) — tray counts honest, notifications sane, quit/restart clean | — | recorded |

## Owner tasks (real world)

1. **Apple Developer Program** ($99/yr) — needed for signing + notarization (S1): create the Developer ID Application cert, an app-specific password / App Store Connect API key for `notarytool`, and hand the four values to CI as `release`-environment secrets (names listed in the workflow file). ~1 hour first time.
2. **Approve the consent copy** for capability grants (S2 — security-voice surfaces).
3. **Desktop dogfood day** (V4) and the mic-privacy sanity pass (S3): confirm the capture indicator behavior matches your expectations before any release.
4. Decide the **default push-to-talk chord** (S3) — trivial but personal.

## Risks & open questions

- **Notarization pipeline flakiness** is a known industry papercut — retries + `notarytool` (not legacy altool) + a documented manual fallback in the runbook.
- **Bundle size** (PyInstaller + Electron): measure in S1 and record; torch-class exclusions keep it sane; if >400MB, note options (no action without measurement — bottleneck-gated).
- **Open:** whether quick-capture from the tray writes an inbox item or a chat message — default: inbox `system` item (plan 42), revisit after dogfood.

## Execution log

Format: one line per task/event — `DONE` / `DEVIATION` / `DISCOVERY` / `BLOCKED` — under the roadmap session discipline in [AGENTS.md](../../../AGENTS.md).

### Session 2 — Capability bridge (2026-08-13)

- **DECISION (namespace — ONE bridge, not two).** C1 says "extends the existing `electronAPI` pattern" while the atom's `done_when` names `window.pclawDesktop`. Keeping both would have been the dual-mechanism drift this repo has paid for three times recently (two spring vocabularies, two approval renderers, two verdict enums): the next person adding a capability would have to guess which namespace owns it, and a security review would have two surfaces to audit. So `electronAPI` is **deleted**, `onStatus` moved into `window.pclawDesktop` alongside `capabilities`, and `loading.html` migrated in the same change (clean break under the pre-1.0 banner — the shell's bridge is not persisted state, so there is nothing to migrate). `desktop/test/capabilities.test.js` pins it: exactly one `exposeInMainWorld` call, and `loading.html` must not mention the retired name.
- **DECISION (shell_token — no new secret on disk).** `POST /api/desktop/register` is gated on loopback + the EXISTING `$GIDEON_HOME/.local_secret` (the rail `GET /api/token/local` already uses) and returns an in-memory `secrets.token_urlsafe(32)` that authorizes only `/api/desktop` writes. Rationale: any process that can read the home can already mint a full API token, so a second secret FILE would add something to back up and leak without moving the boundary — while a per-session in-memory token is strictly narrower than the local secret, dies with the process, rotates on re-register (so a stale shell stops writing), and adds no `durability/inventory.py` entry. It is never written to disk, never logged, never in an error body, and never handed to a renderer (the preload exposes probe/request/on and no token accessor).
- **DONE T2.1** — `desktop/capabilities.js` (new, split from main.js): 6 capabilities x 5 grant states over injected OS handles. `probe()` never throws (an OS failure degrades to `unavailable` + reason); `request()` prompts ONLY on the `not-determined` → granted/denied transition and returns `prompted` so "one TCC dialog per grant" is an assertion; `denied`/`restricted` return the System-Settings route without re-asking; disclosure-only capabilities (screen recording, notification authorization — macOS exposes no prompt/no readable state) report `requestable: false` with a reason instead of offering a dead control; non-darwin reports `unavailable`, never an optimistic guess. Preload validates the closed vocabulary and the MAIN process validates it again (a compromised renderer can pass any string over IPC). 33 `node --test` cases; rails assert `contextIsolation: true` / `nodeIntegration: false` on every window, exactly one exposed namespace, no IPC channel outside `IPC_CHANNELS`, and that the shell token never reaches a log line, a renderer send, or disk.
- **DONE T2.2** — `dashboard/desktop_registry.py` + `dashboard/handlers/desktop.py` (new, thin) on `DashboardState.desktop`: `POST /api/desktop/{register,state,unregister}` (loopback checked BEFORE the credential so a remote caller cannot use the route as a token oracle) and `GET /api/desktop/{state,capabilities/{cap}}`. Fail closed everywhere: unregistered → `{connected: false, capabilities: {}}`; an unparseable capability entry normalizes to `unavailable`/not-requestable; an unknown capability name is dropped rather than stored; no local secret minted → 503 rather than a loopback-only fallback. Every rejection emits a SEL row (`desktop.register`, `desktop.state.push`, `desktop.unregister`, `desktop.capability_denied`) whose `resources` names the failure class and never any part of a presented credential. `main.js` registers after the gateway's READY line and unregisters on `before-quit`; a grant pushes a refreshed manifest so the panel needs no polling.
- **DONE T2.3** — Settings → Security → **Desktop capabilities**. Renders only what the shell reported: a browser tab shows "Desktop app not connected" with zero grant buttons and zero capability names (`connected: false` wins even over a stale non-empty map). A grant button appears only where the shell said `requestable`, and its accessible name carries the capability ("Allow microphone") so six rows are not six identically-named controls. An unmapped capability name is humanized and still rendered — dropping it would make the UI less honest than the API.
- **DONE T2.4** — App-manifest `desktop: [caps]`: `Permissions` (+`to_dict`/`from_dict`) → `PermissionChecker.can_use_desktop` → the desktop handler → `AppPermissionsWire` → `PermissionList`. EXACT-match only, no `*` wildcard (the vocabulary is closed, and "everything native this host can do" is not a grant a user should click through), deny-by-default, and the Store discloses both the declared capabilities (enforced bullets) and the deny-by-default case (caption) — the APE-12 lesson applied.
- **DEVIATION (T2.4 first call site).** The task's "fixture app with `desktop: ["native_notifications"]` can fire one" cannot land in S2: no bridge-backed notification route exists until S4/plan-42 registers the `native` target. Enforcement therefore lands on the gateway-mediated capability READ (`GET /api/desktop/capabilities/{cap}` → 403 + SEL `desktop.capability_denied` for an undeclared capability), so the gate ships ahead of its first USE site by design. S3/S4 must route their new use routes through `can_use_desktop` rather than re-deriving a check.
- **DISCOVERY (a non-loopback POST never reaches this handler in a real deployment).** Driving `POST /api/desktop/register` from the machine's LAN IP against a `0.0.0.0`-bound gateway is refused earlier by the origin/CSRF guard (403 "request origin not allowed"), and with an Origin the guard accepts, by the token's IP binding (403 "IP mismatch"). The handler's own loopback rail is therefore a third, independent layer that is unreachable over the wire on a correctly configured gateway — which is exactly why it is covered by a unit test that clones the request with a non-loopback peer, and why disabling it must turn that test red (verified: it does).
- **DISCOVERY (a coral "Granted" read as "Denied").** The first panel build styled `granted` with `text-primary`; in this theme the brand primary is a coral almost identical to `text-error`, so a granted capability and a denied one rendered the same colour. Caught by looking at the real page, not by any test. Now `text-success` (measured live: `rgb(14,188,95)` vs `rgb(246,108,102)`).
- **REMAINING (V2 + owner task 2).** Electron was never launched during this session, so V2's on-device walk-through — real TCC dialogs, "exactly once per grant" on a device, the grant→push→panel loop through the actual shell — is unproven; the state machine's once-only property is proven by unit test only. Owner task 2 (approve the consent copy in the panel and the Store bullet) is also outstanding. `DC-2` therefore stays `todo` in `dag.json` with those two named remainders even though T2.1–T2.4 are complete and gated.

### Session 3 — Live audio (2026-08-16)

- **DECISION (the shell forwards the press; the RENDERER owns the microphone).** `desktop/pushToTalk.js` registers the chord and sends `{action:"toggle"}`. It opens no capture device, and a source rail asserts the module contains no capture API at all. Two reasons. First, the transcript's destination (the composer) lives in the renderer, so capture belongs where the text lands. Second, and the one that decided it: **one owner of a live microphone is auditable where two are not.** The consequence is that the main process learns the mic is live only because the renderer TELLS it (`setCapturing`), which is exactly what makes the menu-bar indicator honest — an indicator driven from "we forwarded a press" would stay lit when a denied mic meant no stream ever opened, and would go dark over a live one if the renderer started capture any other way (the mic button does). `clearCapturing()` on `before-quit` covers the reverse case.
- **DECISION (the renderer binds the chord, not the shell).** The chord lives in `voice.push_to_talk_chord`, and the renderer is already the process that reads config. Having the shell read `config.json` itself would have given it a second, independently-drifting view of a user setting, and having it ask the gateway would have meant widening the narrow `shell_token` (which authorizes only `/api/desktop` writes) into something that can read config. So `usePushToTalk` reads the chord and calls `bind` over the bridge, and the conflict result lands directly in the Settings control that caused it.
- **DEVIATION (T3.1 — the chord TOGGLES; it does not read key-release).** Electron's `globalShortcut` delivers one callback per press and exposes **no global key-up event**. A true "while physically held" global gesture therefore needs a native key-event tap — an accessibility-permission-class API this plan deliberately does not request, and one that would be a much larger consent ask than the feature warrants. The `done_when` says "captures only while **held/toggled**", so the toggled half ships and is labelled honestly in `docs/guides/desktop.md`, in the Settings hint, and in the module header. A toggle has a failure mode a hold does not — press it, walk away, and the microphone stays live — so `MAX_CAPTURE_MS` (2 minutes) asks the renderer to stop. The shell **asks** rather than assuming: it sends `{action:"stop"}` and leaves the indicator lit until the renderer reports the stop, because until it does, the microphone genuinely still is.
- **DONE T3.1** — Chord grammar + bind/conflict state machine over an injected `globalShortcut`; `bind()` releases ours FIRST so re-saving the same chord is not reported as a conflict with itself, and **conflict** ("another app owns it") is a separate result from **invalid**, because the two need different sentences and collapsing them is how a user retypes a perfectly good chord. A modifier-less chord is REFUSED — it would be taken from every app on the machine (the restrictive reading, chosen deliberately). The indicator is the menu-bar item's *title* (`● Listening`), not only its tooltip: an indicator you must hover to discover is not an indicator, and the chord is global, so capture can start while the window is hidden. 35 `node --test` cases.
- **DONE T3.2** — Push-to-talk drives the **existing** `useMicRecorder`, the same recorder the mic button drives, rather than a second `getUserMedia` call site (the repo already has two, and `displayCapture` set the one-call-site precedent). So there is one owner of the microphone and one teardown path, and a chord press cannot leave a track orphaned by racing the button. The grant is requested through the bridge BEFORE the stream opens, so a denied mic produces an actionable sentence naming System Settings (macOS will not prompt twice, so "try again" would be a lie) instead of a bare `getUserMedia` rejection. Transcript insertion reuses the composer's `insertAtCaret` (CodeMirror `replaceSelection`) — one line at the touchpoint, no composer rewrite.
- **DONE T3.3** — `system_audio` joins the capability vocabulary specifically so "no, and here is why" is a state the panel and the gateway can READ rather than a silence. It probes `unavailable` with the Screen-Recording reason on EVERY platform, and that branch runs **before** the platform test — "not implemented on darwin" would read as a porting gap a later release closes, when the real answer is a refusal no platform lifts. `docs/guides/desktop.md` (new) states mic-only in prose; a test asserts the doc and the probe agree, and a census asserts no desktop source file captures system audio.
- **DISCOVERY (the packaging rail earned its keep).** `desktop/test/packaging.test.js` reds on any local `require()` missing from `build.files` — and it caught `pushToTalk.js`. Without it the dmg would have shipped a shell that throws on launch, and nothing else in the suite would have noticed.
- **DISCOVERY (asserting the appended string would have passed the WRONG implementation).** The insert-at-cursor test first asserted `'draft spoken words'` and failed with `'spoken wordsdraft '`. That output is the *proof*: a freshly mounted input has its caret at offset 0, so insert-at-cursor prepends while an append concatenates — the two implementations produce different strings, and the assertion as first written would have PASSED on append and failed on the correct code. Both forms are now pinned, with the discriminator explained, plus a source rail on the mechanism (jsdom cannot move a CodeMirror caret, so the caret-anywhere-else case is a rail rather than a drive).
- **DISCOVERY (the ratchet asked for a primitive, and it was right).** The chord control's raw `<button>` tripped the primitive-adoption ratchet (273 > 272). `Button` exposes no `onKeyDown`/`onBlur`, so a key *recorder* genuinely is not a plain action button — but the answer was not to bump the baseline. The keyboard semantics (swallow every key while armed so `⌘S` does not also save the page; `Escape` cancels rather than being recorded; a modifier-only press stays armed) are the reusable part, so they became `ui/ShortcutRecorder` with a doc object, and `pages/` holds only the bind-then-save policy.
- **DISCOVERY (a worktree's Python silently resolves to the MAIN checkout).** The venv's editable install points at the main checkout's `src/`, so `python`/`pytest` run from this worktree imported the WRONG tree: the first `generate_config_baseline.py` run produced no diff and `VoiceConfig` reported no `push_to_talk_chord`, which reads exactly like "the field isn't wired". Every Python invocation here needs `PYTHONPATH=<worktree>/src`, which does win over the editable finder. A green suite without it is a green suite over someone else's code.
- **REMAINING (V3 + owner tasks 3/4).** Electron was never launched, so **three legs are unexercised**: that macOS delivers the chord at all (`globalShortcut` is stubbed), the real TCC prompt, and a real microphone (jsdom implements neither `getUserMedia` nor `MediaRecorder`, so both are fakes). What IS driven end to end is the whole renderer path — chord push → grant → capture → chunked upload → insert at cursor — including the privacy property asserted as *every track's `stop()` was called*, never as a flag. The `<=2s after release on faster-whisper local` latency figure is likewise unmeasured: no local STT model was run. Owner task 3 (mic-privacy sanity pass) and owner task 4 (approve the default chord, currently `⌘⇧Space`) are outstanding. `DC-3` therefore stays `todo` in `dag.json` with those remainders named, even though T3.1–T3.3 are complete and gated.

### Session 4 — Presence (T4.1 + T4.3, 2026-08-17)

- **DECISION (extend the existing tray; move its logic OUT of the glue).** T4.1 says "verify what `Tray` wiring already exists — extend, don't duplicate", and it did exist: DC-2/DC-3 left a static six-item menu built inline in `main.js` plus a `setTitle` capture indicator. So `createTray()` is deleted, not supplemented, and the menu's logic moved to `desktop/trayPresence.js` behind injected Electron handles (`Tray`/`Menu`/`nativeImage`). The reason is testability, not tidiness: Electron cannot be launched in this environment, so anything left inside the glue is unverifiable by construction. As pure functions the menu shape, the title arbitration, the count folding and all five degradation paths are asserted by 46 `node --test` cases; only "does macOS draw it" is left to the dogfood day. The tray's old `New Tab` / `Merge All Windows` rows are dropped rather than carried — both remain in the app menu's Tab submenu with their accelerators, and a menu-bar item is for presence, not window management.
- **DECISION (ONE writer for the menu-bar title).** DC-3's `● Listening` indicator and DC-4's approvals badge want the same pixels, and two callers of `tray.setTitle()` would mean whichever fired last wins — a mic indicator silently erased by a count refresh is precisely the "indicator that lies" DC-3 built its whole design around avoiding. Both now go through `composeTrayTitle()`, and **capture always wins**: an approvals badge can wait five seconds, a live-microphone signal cannot. `setCaptureIndicator()` in `main.js` is now a one-line delegation, so the arbitration cannot be bypassed by a future caller.
- **DECISION (poll the loopback API; do not open a WS for a count).** The `done_when` allows "WS/API". A 5s `GET /api/approvals` + `GET /api/loops` poll is what shipped: the menu bar needs a low-frequency integer, a poll has no half-open socket to leak on quit, and it cannot wedge the quit path it would otherwise have to be torn down inside. A failed poll renders as **"not connected"**, never as `0` — a zero that looks like good news is the worse of the two failures.
- **DEVIATION (quick capture deep-links; it does not write the note).** T4.1 names "quick-capture note→inbox", and **no endpoint exists that creates an inbox item from text**: every `/api/inbox` POST acts on an item that already exists (`seen`/`send`/`draft`/`restore`/`favorite`/`dismiss-all`), and `POST /api/inbox/proposals` requires an enabled proposal registration and 403s otherwise. Minting `POST /api/inbox/capture` from a desktop atom would be a consumer defining its owner's contract: the attention-path contracts belong to INBOX-NOTIFICATIONS-UNIFICATION, and this plan's own Risks section already records quick-capture's shape as **open** ("default: inbox `system` item (plan 42), revisit after dogfood"). So the tray row ships as a deep link to `#/inbox?capture=1` and the write half is INBOX-NOTIF's to define. The SPA does not yet honour that query flag — recorded here rather than papered over, because a deep link nobody consumes is an inert control.
- **DEVIATION (the login-item toggle ships on the bridge AND in the tray, not in Settings).** T4.3 says "Settings, via bridge". The bridge half is complete (`pclawDesktop.loginItem.get/set` over two new `IPC_CHANNELS`, handled by `registerLoginItemIpc`), but the Settings *control* lives in `web/src` and was out of this session's fence. Shipping only the bridge would have been an inert mechanism, so the user-reachable control is the tray's own **"Open at Login"** checkbox — a real surface inside the same fence. Settings can adopt the identical call with no backend change.
- **DONE T4.1** — Live menu: connection header, pending-approvals row (click-through to `#/chat`, where `ApprovalPrompt` renders — and **click-through at zero too**, because a row that disables itself the moment the count clears is a dead end), a running-loops submenu deep-linking `#/loops/<id>` per loop, quick capture, Open Dashboard, the login-item checkbox, Quit. `summarizePresence()` folds the two real shapes (`/api/approvals` is a **bare array**; `/api/loops` is `{loops:[...]}`) and treats only `running` as running — `paused`/`blocked`/`needs_input` are `ACTIVE_STATUSES`, not running, and conflating them would have inflated the badge. The inline loop list caps at 8 while the COUNT stays truthful. AMBIENT-SURFACES tiles render when a tile array is supplied and add **nothing** (not an empty section) when it is not, so this atom never blocks on that plan.
- **DONE T4.3 (graceful quit)** — The pre-DC-4 path was `kill("SIGTERM"); gatewayProcess = null;` inside a **synchronous** `before-quit`: it never learned whether the child exited, and because the gateway is spawned `detached: false` with no process group of its own, a SIGTERM that landed slowly (mid-write, mid-flush) left an **orphan** holding the port and the home dir. `desktop/gatewayShutdown.js` now waits for the child's `exit`, escalates to SIGKILL after 8s, and resolves one of five named outcomes; `before-quit` takes the quit back once (`preventDefault()` → await → `app.quit()`) so Electron cannot exit while the gateway is still stopping. The outcome is logged — including **`orphaned`**, the one case the shell cannot fix, which is named precisely so a quit that might have leaked a gateway says so instead of looking clean. Falsified: making the shutdown resolve without waiting reds the ordering test with "shutdown resolved before the gateway exited".
- **DONE T4.3 (login item)** — Opt-in (construction performs **zero** writes — asserted), reversible by the same call with `false`, and idempotent: `set()` reads the current registration and skips a matching write, so enabling twice cannot mint a second entry (falsified: removing the guard reds with `actual: 2, expected: 1` writes). It touches **only** macOS Login Items for this app bundle via `app.setLoginItemSettings({openAtLogin, openAsHidden: true})` — no `~/Library/LaunchAgents` plist, nothing under `/Library`, no elevated privilege — and the result is **read back from the OS** rather than inferred from the void-returning setter, so a silently-ignored write reports `ok: false` instead of becoming an inert toggle. Every test injects a fake `app`, so nothing in the suite can reach the real registry.
- **DISCOVERY (tray availability is a WINDOW-REACHABILITY invariant, not a cosmetic).** `main.js` hides the window on close instead of closing it, and `window-all-closed` keeps macOS alive with no windows — both correct *only while a menu-bar item exists to bring the window back*. A tray that failed to build (unreadable icon, `new Tray()` throwing, a platform with none) would therefore have left a running app with no window and no affordance: a phantom state. The close/quit decisions now read the tray's real availability (`shouldHideOnClose`, `shouldQuitOnAllWindowsClosed`), so a tray-less run closes for real and quits on last-window-close **even on darwin**. Falsified: forcing `available` to `true` reds six degradation tests. This coupling pre-dates DC-4 — the old `createTray()` could throw and the close handler would still have hidden the window.
- **DISCOVERY (the desktop test tier is not run by CI).** `desktop` is an npm workspace member with a real `node --test` suite (145 cases now), but **no workflow in `.github/workflows/` mentions it** and the root `package.json` has `test:web` with no `test:desktop`. Every desktop rail — including `packaging.test.js`, which S3 recorded as having saved a shipping dmg — is enforced only by whoever remembers to run it. Not fixed here (CI config was outside this session's fence, and four sibling sessions share those files); flagged as the cheapest reliability win available to this plan.
- **REMAINING (V4 + the two runtime clauses).** Electron was never launched, so **three claims are test-backed but unobserved at runtime**: that macOS draws the icon and title (only "the menu template we hand `buildFromTemplate` is correct" is proven), that the login item **survives a reboot** (the `done_when`'s manual check — `setLoginItemSettings` is called against a fake in every test), and that **the process table shows no orphan** after a real quit (the ordering, escalation and outcome reporting are proven against a fake child; a real gateway's SIGTERM handling is not). The SPA also does not yet honour `?capture=1`. `DC-4` therefore stays `todo` in `dag.json` with those four remainders named, even though T4.1 and T4.3 are complete and gated.
### Reliability fix — the node tier was never enforced (2026-08-18)

- **DISCOVERY (the packaging rail that "earned its keep" was enforced by nobody).** Session 3 recorded that `desktop/test/packaging.test.js` caught a `pushToTalk.js` missing from `build.files` and that "nothing else in the suite would have noticed" — which was true twice over. Measured today: `grep -rln desktop .github/workflows/` found **nothing**, the root `package.json` carried `test:web` with no `test:desktop`, and `scripts/run_prepush.sh` runs typecheck/vitest/build/render-smoke and skips the tier too. So all **92** `node --test` cases across the five `desktop/test/*.test.js` files — the capability state machine (33), push-to-talk (35), the context menu (12), `find-bin` (9) and packaging (2) — were enforced only by whoever remembered to type `npm test` inside `desktop/`. The rail did not earn its keep because CI ran it; it earned its keep because one session happened to run it by hand.
- **DONE (fix).** Root `test:desktop` script + one step in ci.yml's existing `web` job, which has already run the single root `npm ci` that installs `desktop/` (npm/cli#4828 — npm runs from the repo root, never `cd desktop && npm ci`). Cost is ~0.2s on a runner it already pays for. Scoped to what runs headless **by construction, not by luck**: every case injects its own platform (`buildMenuTemplate(params, "darwin", ...)`, `makeCapabilities({platform})`) or its own `fs`/`os` fakes, no case reads ambient `process.platform`, and no case launches Electron — so an `electron .` smoke test, which needs a display, is deliberately still absent and V2/V3's on-device remainders above are unchanged by this.
- **DONE (generalized).** `tests/test_ci_tier_enforcement.py` turns the class of gap into a red: any npm workspace member declaring a `test` script must have a root `test:<name>` script **and** a ci.yml step invoking it. Both rails carry a vacuity floor (the `run:`-line scan must still find `npm run test:web` + `uv run pytest`; workspace discovery must still find both `web` and `desktop`), because a scan that matches nothing passes every assertion it makes. Falsified in both directions: dropping `find-bin.js` from `build.files` reds `npm run test:desktop` (`Missing from build.files: find-bin.js`), and removing either the CI step or the root script reds the new rail.

### Session 4 remainder — the orphan clause, actually measured (2026-08-25)

- **DISCOVERY (the orphan clause was true of the pid and false of the tree).** S4 left "the process table shows no orphan after a real quit" as a named remainder, proven only against a fake child. Measured it for real: a gateway spawned with `main.js`'s exact options on an isolated `GIDEON_HOME`, driven through the real `shutdownGateway()`, then `ps` before and after. The **gateway itself is clean** — pid 86976 → `exited`, code 0, in 7ms, zero survivors in its subtree and nothing left referencing the home. But that run's subtree was size 1, so it only exercised the easy case. With one child of its own the same shape leaks: stand-in gateway 91088 exited cleanly while its child **91089 survived with `ppid=1`**, reparented to launchd, still holding the inherited stdout pipe. `child.kill()` signals one pid, and the gateway spawns real subprocesses (ACP CLIs, MCP servers, terminal sessions) — so the clause as shipped was a claim about the pid, not about what quitting leaves running. The surviving pipe holder is also why the probe process itself would not exit, which is the same phenomenon as a `proc.kill()` that takes 30s to return.
- **DEVIATION (the first measurement of this reported a false clean).** Probe v1 filtered the process table for a marker COMMENT in the command line; `exec` discards it from argv, so the filter matched nothing and printed `GRANDCHILD_ORPHANED=NO` while `ps` showed the child alive. Recorded because the shape is the reusable lesson, not the typo: the rail that matched nothing looked exactly like a rail that found nothing wrong. Probe v2 tracks the grandchild **by pid** and aborts if it is not alive before shutdown, so a vacuous run fails loudly instead of passing.
- **DONE (group-scoped shutdown).** `main.js` now spawns the gateway `detached: true` so it leads its own process group (Node's `detached` calls `setsid()`), and `shutdownGateway` signals the GROUP. Both halves are required and neither is sufficient. Under the old `detached: false` shape the measured pgid was **the app's own group**, so `process.kill(-pid)` would have killed the app and every sibling in it — the group signal was unavailable, not merely unused. That is why `killGroup` is opt-in **and re-verified against the OS**: a caller's request is not enough, `readPgid(pid)` must equal `pid`, or it degrades to the single pid and logs what that costs. `detached: true` at the spawn site and `killGroup: true` at the call site are two halves of one contract that nothing in the language ties together, so a source rail on `main.js` pins both — with comment-stripping, because the file documents the old shape and a prose-reading scanner would red on the explanation of its own fix.
- **DONE (the sweep, and why it is not paid on every quit).** After the gateway exits, its children may have ignored the group SIGTERM. The group is probed with signal `0`: ESRCH means the last member is gone and the quit finishes immediately; only a group that still has members waits `sweepMs` and is then SIGKILLed, reported as `groupSwept`. So the delay lands on the quits that have residue (measured 514ms) and not on the clean ones (18ms with a real gateway). `exit` also now clears the escalation timers, so an `orphaned` verdict cannot fire while a sweep is still running — that outcome is only worth having if it never cries wolf.
- **VERIFIED (the clause, under the shipped configuration).** Real gateway, isolated home, `detached: true` + `killGroup: true`: pgid **94413 == pid** (so the group path engaged), outcome `exited` in 18ms, `BEFORE_PIDS=94413`, `SURVIVOR_PIDS=(none)`, nothing in the table still referencing the isolated home. Grandchild case re-run under the fix: both parent and child gone. Falsified three ways, each red-then-green-after-restore: dropping the `pgid === pid` verification reds only the degradation test (the guard is what makes the request safe, not the request); flipping the spawn back to `detached: false` reds the source rail; disabling the signal-`0` probe reds the fast-quit test and hangs the suite for 15s, which is the tax the probe exists to avoid.
- **STILL UNVERIFIED, and not claimable from here.** (1) **Login-item reboot survival** — `setLoginItemSettings` is called against a fake in every test and the machine was deliberately not rebooted; the write and the read-back are proven, the persistence across a boot is not. (2) **macOS actually drawing the icon and title** — Electron is not installed in this environment (`node_modules/electron` absent), so no `electron .` run happened; only the menu template handed to `buildFromTemplate` is proven. Both remain V4 dogfood items.
- **DISCOVERY (the login-item bridge is one-sided — its IPC has zero consumers).** `preload.js:108` exposes `loginItem.get/set` and `main.js:928` registers `registerLoginItemIpc`, but `web/src/lib/desktopBridge.ts` — the single owner of the renderer's `declare global` — does not declare `loginItem` at all, and no SPA surface calls it. The tray checkbox reaches `loginItem.set()` directly in the main process, so those two IPC channels are invoked by nobody: deleting `registerLoginItemIpc` would break no test and no surface. Left in place rather than deleted, on this plan's own S2 precedent that a gate may ship ahead of its first use site when the reason is recorded — but the done_when's "**Settings** via bridge" is genuinely unmet, and closing it is an IA decision, not a wiring one (see below).
- **OWNER DECISION NEEDED (where "Open at Login" lives).** Settings has 34 panels and none is a defensible home for an app-startup toggle: `SecurityPanel` owns desktop *capabilities* (a permissions surface), `VoicePanel` owns the push-to-talk chord. The options are a new `desktop` subpage — a nav entry, an icon and an IA call — or shoehorning it into Security, which is the "surfaces solve one problem several ways" drift the coherence rules warn about. Not decided unilaterally, because the control is **already user-reachable** in the menu-bar menu, which is where macOS apps conventionally put it: this is a placement question, not a missing capability.
- **CONFIRMED (quick capture's write half is still not this plan's to define).** Re-checked S4's premise rather than inheriting it. Every `/api/inbox` POST registered in `server.py:1242-1257` acts on an item that already exists (`seen`, `restart`, `dismiss-all`, `send`, `{id}` PUT, `{id}/draft`, `{id}/open`, `{id}/favorite`, `{id}/restore`, `{id}/apply`) and `proposals` requires an enabled registration; **nothing creates an inbox item from free text**. So the tray row still deep-links `#/inbox?capture=1` and the SPA still ignores the flag — the hash-query grammar parses it (`useHashRoute.ts:40-54`), and all five deep-link targets are routable (`App.tsx:102`), so the link works and only the capture intent is dropped. Minting the endpoint here would be a consumer defining INBOX-NOTIFICATIONS-UNIFICATION's contract, which the roadmap's hard rules forbid by name, and this plan's own Risks section still records the shape as open.

### Session 5 — `DC-5` native notifications as a plan-42 rules target (2026-08-27)

- **CONFIRMED (the audit's premise, measured before writing anything).** `native` was declared and inert on **both** sides. Backend: `notification_rules.TARGETS` carried it with the comment "accepted and persisted but inert until MOBILE-COMPANION / DESKTOP-CAPABILITIES land", and a census of every consumer of `rule.targets` in `src/` found exactly **one** — `dashboard/state.py`'s `note["targets"] = list(rule.targets)`, a metadata annotation no reader acted on. Shell: `desktop/main.js` imported Electron's `Notification` for a single purpose, `isSupported()` inside a capability probe; `git grep 'new Notification'` over `desktop/` returned **zero** hits. Frontend: the rules matrix rendered `native` **dimmed** via `INERT_TARGETS = ['push', 'native']`, so a user could tick a checkbox that was labelled as a promise and did nothing. S2's `DEVIATION (T2.4 first call site)` predicted exactly this shape — the gate shipped ahead of its first USE site, and this is that site.
- **DECISION (the gateway decides, the renderer relays, the shell actuates).** The natural-looking design — the gateway pushes a notification straight to the Electron main process — is **not buildable**: `main.js` holds no socket to the gateway at all (only outbound `postGateway` POSTs to `/api/desktop/{register,state,unregister}`), and adding an inbound push channel would mean widening the deliberately-narrow `shell_token` into a subscribe credential. The renderer already holds the ONE multiplexed WS every notification rides. So the note's `native` verdict travels on the existing `notification` envelope and the renderer hands it to the main process over a new IPC channel. Three consequences worth naming: no new transport, no new credential, and the process that owns the SPA's route vocabulary is the one that supplies the tap's destination.
- **DECISION (the ROUTE is frontend IA and stays in the frontend).** "The tap focuses the relevant surface" needs a kind→surface map, and there is no server-side one: hash routes exist only in `App.tsx`'s `NAV` + `ROUTABLE`. Putting a second copy in Python would have created exactly the remote-copy drift this repo has paid for repeatedly. Instead the gateway adds `note["source"]` — the **rule key's** source, which it already computed — and `web/src/lib/nativeNotifications.ts` maps the twelve sources in `notification_kinds.py` onto twelve routes `App.tsx` already serves. Two rails keep that honest: one parses `NotificationKind("<source>"` out of the Python so a new source reds the map instead of silently deep-linking every one of its notes to the feed, and one parses `App.tsx`'s own two lists so a renamed route reds it instead of producing a dead link. **`source` is not a prefix of `kind`** and a consumer that split the wire string would be wrong — `app.route.drift` resolves to source `system`.
- **DECISION (native is ADDITIVE; the "fallback" needed no code).** The `done_when` says it "falls back to dashboard toasts when the shell is not connected". Measured: **there is no toast renderer for a `notification` WS frame.** The three consumers (`NotificationBell`, `DashboardLive`, `NotificationsPage`) all treat it as a refresh signal for the bell/feed. So the dashboard delivery *is* the fallback, it already ran unconditionally, and the honest implementation adds nothing and suppresses nothing. A native banner is an addition to the record, never a replacement — which also means a shell appearing or vanishing mid-session can never lose a note.
- **DECISION (only `immediate` interrupts).** The decision runs after `never`/`digest`/`badge` have already returned, so a rule that sets Badge *and* ticks Desktop raises nothing. Badge's own docstring says the badge IS the delivery; an OS banner is the loudest thing this system can do, and letting a target override a mode would make "quieten this kind" mean the opposite. Pinned by `test_badge_mode_never_raises_native_even_when_targeted`.
- **DONE (backend).** `notification_rules.native_delivery(rule, capability)` — one pure function, three distinct outcomes: `None` (the rule did not name the target ⇒ no key on the note, so nothing downstream can raise one), `{deliver: False, reason}` (it named it but no shell can deliver ⇒ the reason is quotable, because "I asked for native and got a bell" needs an answer), `{deliver: True}`. It consults `available` **only** — gating on `granted == "granted"` would refuse to deliver on macOS, the one platform that never reports notification authorization. Wired at `dashboard/state.py`'s single delivery choke point, fail-OPEN like every other layer there (a raising registry degrades to dashboard-only rather than dropping the note).
- **DONE (shell).** `desktop/nativeNotifications.js` + two `IPC_CHANNELS` (`notify`, `notificationActivate`) + a `notifications: {show, on}` namespace on the preload bridge. Narrow in the same way `loginItem.js` is: construction raises **zero** notifications (asserted), every string is coerced and truncated in the main process (the preload shaping is a courtesy), a titleless note is **refused** rather than shown as a blank banner, and a tap focuses the window then forwards `{route}` — this module never builds a URL, never calls `loadURL`, never opens an external link. `normalizeRoute` **rejects rather than repairs** (a half-cleaned route deep-links somewhere the user did not mean); `https://`, `javascript:`, `../../etc/passwd` and a query string all normalize to `""`, which is a fine answer because the tap still focuses. On its own channel, not folded into `registerCapabilityIpc` — that channel set is ratcheted to exactly probe/request/snapshot, and showing a notification is a USE of a capability, not a question about one. Added to `build.files` (the rail S3 recorded as saving a shipping dmg would otherwise have caught it).
- **DONE (frontend + product).** `useNativeNotifications(navigate)` mounted once in the app shell — not on the notifications page, because a native note must be raised whatever route the user is on. `native` came **off** `INERT_TARGETS` and its label changed from "Desktop (desktop app required)" to "Desktop notification (when the desktop app is running)": leaving it dimmed would have been the exact opposite lie to the one the dimming existed to prevent. `docs/guides/desktop.md` gains a "Native notifications" section (where the switch is, why Badge/Digest never raise one, why the dashboard stays the record, and that macOS's own Notifications setting is the real off switch), plus a CHANGELOG entry.
- **FALSIFIED, four mutations, each restored from a file copy at the literal path.** (1) `native_delivery` forced to always return `None` (dispatch broken) → the call-site test `test_a_rule_naming_native_reaches_the_shell` reds with `KeyError: 'native'`, **and the vacuity test still passes** — which is the point: the two legs are independent, so the positive one is not proving itself. (2) The `NATIVE_TARGET not in rule.targets` guard deleted (targets ignored) → **only** `test_a_rule_not_naming_native_never_fires_it` reds, while the positive leg stays green. A test suite with just the positive leg would have shipped this. (3) `shouldDeliverNatively` changed to `!!note.native` (presence instead of `deliver`) → the web vacuity leg reds, which is what stops a `{deliver: false}` fallback note from double-delivering the moment a shell appears. (4) `notification.show()` commented out in the shell → 2 node cases red, so the "constructs one" assertion really does reach `.show()` and is not satisfied by construction alone.
- **DISCOVERY (a partially-constructed `DashboardState` reached the new read).** `test_entity_settings_routes.py::test_state_notify_respects_gate` builds its state with `object.__new__` to skip `__init__`, so `self.desktop` did not exist and the new decision raised. Fixed on **both** sides deliberately: the call site fails open (matching the three fail-open layers already in `notify()`), *and* the test now supplies a real `DesktopRegistry` — because leaving it off would have made that test pass through the `except` branch and quietly stop exercising the live path. A new test covers the fail-open branch on purpose.
- **REMAINING (one unobservable leg + a genuinely unrunnable one).** Electron was never launched, so **"macOS draws a banner"** and **"clicking it fires the `click` handler"** are proven only against a recording double — the same class of remainder DC-2/DC-3/DC-4 each carry, and it pairs with DC-1's dmg for a single on-device pass. Everything upstream of `new Notification()` is driven for real: the rule decision, the note that goes on the wire, the renderer's relay, the route derivation, the tap→navigate hop and the unsubscribe. Counts, per leg, measured not estimated: **12 real** Python cases added to `tests/test_notification_rules.py` (7 over the pure decision, 5 driving `notify()` end to end) plus one existing `test_entity_settings_routes.py` case repaired; **12 real** jsdom cases with a simulated bridge (`web/src/lib/nativeNotifications.test.tsx`); **17 simulated** node cases against a fake `Notification` (`desktop/test/nativeNotifications.test.js`, taking the desktop tier 157 → 174). No leg is skipped — a skipped surface reads like a pass, so the unobservable half is named here rather than marked xfail. `DC-5` is therefore marked 🟡 in `DC.md`, not ✅.
