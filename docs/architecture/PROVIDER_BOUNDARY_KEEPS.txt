runtime/gideon/config/loader.py — the CRED_SLACK_* credential-store KEY NAMES ("SLACK_APP_TOKEN", …) existing installs already hold; the loader is the credential store's home (bottom layer, below all apps). Renaming breaks existing installs for zero gain. Re-exported to apps via packages/python-client/channel.py.
runtime/gideon/config/safety.py — NOT a credential key: one "SLACK_BOT_TOKEN" inside SandboxConfig.env_passthrough's `_meta` HELP TEXT, as the example of a name an operator might legitimately let a child inherit. The sweep's own comment says it targets literals "NOT plain vendor words in prose", but SLACK_[A-Z_]*TOKEN cannot tell the two apart, so the judgment is recorded here. It arrived by PHF-14 moving SandboxConfig out of config/loader.py (a keep above) — the residue did not change, its file did. Deliberately NOT reworded: `_meta` help strings are rendered into the config JSON schema, so editing this sentence would be observable config-schema drift.
runtime/gideon/sdk/channel.py — re-exports the CRED_SLACK_* key names from config/loader as the app-facing surface, so the slack app imports them via the SDK (no inverted import direction).
runtime/gideon/sandbox.py — the SLACK_* env-var denylist: credential env vars (SLACK_BOT_TOKEN/APP_TOKEN/USER_TOKEN) that must never reach a sandboxed child. Secret-detection data; renaming breaks the control.
runtime/gideon/security.py — the xox[bpas]- token-detection regexes: secret-DETECTION data that redacts/blocks leaked Slack credentials. Renaming breaks the control.
runtime/gideon/gateway.py — reads the CRED_SLACK_* keys (imported from config/loader) to hand the reference channel transport its token at boot; the credential-key names are the documented keep above, not vendor logic (Socket-Mode lives in the slack-channel app).
runtime/gideon/cli_setup.py — the remote dashboard-URL prompt reads the CRED_SLACK_* keep key names to detect whether a channel is configured (a channel delivers the remote token). Reads the documented keep, not vendor logic. DISCOVERY: a generic "is any channel configured" helper would remove this reference — future work with the channel-abstraction seam (plan 40 CHANNEL-EXPANSION), out of scope for plan 32's enumerated residue.
runtime/gideon/cli_doctor.py — the auth-mode section reads the CRED_SLACK_* keep key names to warn when a non-loopback bind has no channel to deliver a token. Same class + same DISCOVERY as cli_setup.py above.
runtime/gideon/workflows/secrets.py — the inline-credential lint's token SHAPE regexes (sk-/sk-ant-/ghp_/gho_/github_pat_/hf_/xox[baprs]-/AKIA/AIza/JWT/PEM): the ONE credential-shape list in the workflows package, which validator.py's save-time lint now reads instead of keeping a second copy. Same class as security.py's xox[bpas]- regexes — recognizing a vendor's key SHAPE is secret-DETECTION data, not vendor logic, and narrowing the list would silently stop catching those providers' keys.
