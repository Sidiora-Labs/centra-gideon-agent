# Registry Fixture (clean)

A one-file search provider that exists so the registry validator has something that
legitimately passes every check. Its scanner verdict is `clean`.

Not intended for installation.
