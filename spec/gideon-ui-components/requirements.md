<!-- GENERATED by spec/specgen — DO NOT EDIT. -->

# Requirements

## Requirement aui-031: Web search

### Acceptance Criteria

1. Web search uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-032: Inline citation

### Acceptance Criteria

1. Inline citation uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-033: Link preview

### Acceptance Criteria

1. Link preview requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-034: Image generation

### Acceptance Criteria

1. Image generation uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-035: Image gallery

### Acceptance Criteria

1. Image gallery requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-036: Retrieval chunks

### Acceptance Criteria

1. Retrieval chunks uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-037: Document reference

### Acceptance Criteria

1. Document reference uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-038: Memory

### Acceptance Criteria

1. Memory uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-039: Research report

### Acceptance Criteria

1. Research report uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-040: Map

### Acceptance Criteria

1. Map uses actual assistant-ui source in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-041: Geo map

### Acceptance Criteria

1. Geo map requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/knowledge/KnowledgeDetailPage.tsx; features/knowledge/ReportsPage.tsx; features/capabilities/knowledge/Page.tsx; features/chat/SessionWorkspace.tsx; populated from actual retrieval records, citations, document IDs, memory anchors and research results; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-042: Data table

### Acceptance Criteria

1. Data table uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-043: Number ticker

### Acceptance Criteria

1. Number ticker uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-044: Chart

### Acceptance Criteria

1. Chart uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-045: Media player

### Acceptance Criteria

1. Media player requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-046: Web preview

### Acceptance Criteria

1. Web preview uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-047: Diagram

### Acceptance Criteria

1. Diagram uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-048: Flow graph

### Acceptance Criteria

1. Flow graph uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-049: Activity graph

### Acceptance Criteria

1. Activity graph uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-050: Math

### Acceptance Criteria

1. Math uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-051: Spec sheet

### Acceptance Criteria

1. Spec sheet uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-052: Comparison

### Acceptance Criteria

1. Comparison uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-053: Timeline

### Acceptance Criteria

1. Timeline uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-054: Job progress

### Acceptance Criteria

1. Job progress uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-055: Score breakdown

### Acceptance Criteria

1. Score breakdown uses actual assistant-ui source in features/artifacts/ArtifactViewer.tsx; features/knowledge/ReportsPage.tsx; features/tasks/TaskGraph.tsx; features/chat/toolRenderers/registry.tsx; populated from actual validated typed payloads from tools/workflows; render graceful unknown states; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-056: Agent plan

### Acceptance Criteria

1. Agent plan uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-057: Subagent list

### Acceptance Criteria

1. Subagent list uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-058: Task card

### Acceptance Criteria

1. Task card uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-059: Agent status

### Acceptance Criteria

1. Agent status uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-060: Approval card

### Acceptance Criteria

1. Approval card uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-061: Option list

### Acceptance Criteria

1. Option list requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-062: Question flow

### Acceptance Criteria

1. Question flow requires source resolution, then a clearly attributed Gideon composition of verified donor primitives (never claim a nonexistent upstream component) in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-063: Recommendation card

### Acceptance Criteria

1. Recommendation card uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-064: Artifact card

### Acceptance Criteria

1. Artifact card uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-065: Todo list

### Acceptance Criteria

1. Todo list uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-066: Agent card

### Acceptance Criteria

1. Agent card uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-067: Handoff

### Acceptance Criteria

1. Handoff uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-068: Background runs

### Acceptance Criteria

1. Background runs uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-069: Checkpoints

### Acceptance Criteria

1. Checkpoints uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-070: Schedule

### Acceptance Criteria

1. Schedule uses actual assistant-ui source in features/agents/AgentDetail.tsx; features/tasks/TaskDetail.tsx; features/chat/WorkflowProgressCard.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from actual agent/task/run states and controls with authorized mutations; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-071: Trace waterfall

### Acceptance Criteria

1. Trace waterfall uses actual assistant-ui source in features/chat/ChatActivityPanel.tsx; features/schedule/ScheduleDetail.tsx; features/dashboard/widgets/SystemHealth.tsx; populated from actual run spans, usage/quota data, schedule execution trace; unknown when unavailable; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-072: Cost meter

### Acceptance Criteria

1. Cost meter uses actual assistant-ui source in features/chat/ChatActivityPanel.tsx; features/schedule/ScheduleDetail.tsx; features/dashboard/widgets/SystemHealth.tsx; populated from actual run spans, usage/quota data, schedule execution trace; unknown when unavailable; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-073: Quota banner

### Acceptance Criteria

1. Quota banner uses actual assistant-ui source in features/chat/ChatActivityPanel.tsx; features/schedule/ScheduleDetail.tsx; features/dashboard/widgets/SystemHealth.tsx; populated from actual run spans, usage/quota data, schedule execution trace; unknown when unavailable; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-109: MCP config dialog

### Acceptance Criteria

1. MCP config dialog uses actual assistant-ui source in features/ChatPage.tsx; features/chat/SessionWorkspace.tsx; features/chat/ToolCard.tsx; populated from AssistantRuntimeProvider backed by Gideon gateway/session stream and history; matching event/action bridge; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-115: Sources

### Acceptance Criteria

1. Sources uses actual assistant-ui source in features/ChatPage.tsx; features/chat/SessionWorkspace.tsx; features/chat/ToolCard.tsx; populated from AssistantRuntimeProvider backed by Gideon gateway/session stream and history; matching event/action bridge; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-116: Image

### Acceptance Criteria

1. Image uses actual assistant-ui source in features/ChatPage.tsx; features/chat/SessionWorkspace.tsx; features/chat/ToolCard.tsx; populated from AssistantRuntimeProvider backed by Gideon gateway/session stream and history; matching event/action bridge; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-117: File

### Acceptance Criteria

1. File uses actual assistant-ui source in features/ChatPage.tsx; features/chat/SessionWorkspace.tsx; features/chat/ToolCard.tsx; populated from AssistantRuntimeProvider backed by Gideon gateway/session stream and history; matching event/action bridge; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-121: Markdown text

### Acceptance Criteria

1. Markdown text uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/chat/toolRenderers/registry.tsx; populated from actual assistant message parts or validated structured tool payloads; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-122: Syntax highlighter

### Acceptance Criteria

1. Syntax highlighter uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/chat/toolRenderers/registry.tsx; populated from actual assistant message parts or validated structured tool payloads; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-123: Shiki highlighter

### Acceptance Criteria

1. Shiki highlighter uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/chat/toolRenderers/registry.tsx; populated from actual assistant message parts or validated structured tool payloads; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-124: Mermaid diagram

### Acceptance Criteria

1. Mermaid diagram uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/chat/toolRenderers/registry.tsx; populated from actual assistant message parts or validated structured tool payloads; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-125: Generative UI

### Acceptance Criteria

1. Generative UI uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/chat/toolRenderers/registry.tsx; populated from actual assistant message parts or validated structured tool payloads; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-126: Tooltip icon button

### Acceptance Criteria

1. Tooltip icon button uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from Gideon design tokens plus accessible controls in real UI; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-128: Heat graph

### Acceptance Criteria

1. Heat graph uses actual assistant-ui source in shared/ui/chat/MessageAssistant.tsx; features/dashboard/widgets/ActiveWork.tsx; populated from Gideon design tokens plus accessible controls in real UI; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-129: Riverside Loft

### Acceptance Criteria

1. Riverside Loft uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-130: Table reservation

### Acceptance Criteria

1. Table reservation uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-131: Order tracking

### Acceptance Criteria

1. Order tracking uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-132: Flight tracker

### Acceptance Criteria

1. Flight tracker uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-133: Portfolio overview

### Acceptance Criteria

1. Portfolio overview uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-134: Create event

### Acceptance Criteria

1. Create event uses actual assistant-ui source in features/schedule/ScheduleDetail.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-135: View event

### Acceptance Criteria

1. View event uses actual assistant-ui source in features/schedule/ScheduleDetail.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-136: Current weather

### Acceptance Criteria

1. Current weather uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-137: Ride status

### Acceptance Criteria

1. Ride status uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-138: Draft email

### Acceptance Criteria

1. Draft email uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-139: Shopping cart

### Acceptance Criteria

1. Shopping cart uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-140: Playlist

### Acceptance Criteria

1. Playlist uses actual assistant-ui source in features/capabilities/media/LibraryPage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-141: Channel message

### Acceptance Criteria

1. Channel message uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-142: Purchase receipt

### Acceptance Criteria

1. Purchase receipt uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-143: Weekly active users

### Acceptance Criteria

1. Weekly active users uses actual assistant-ui source in features/dashboard/DashboardPage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-144: Player card

### Acceptance Criteria

1. Player card uses actual assistant-ui source in features/capabilities/media/LibraryPage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-145: Conference session

### Acceptance Criteria

1. Conference session uses actual assistant-ui source in features/schedule/ScheduleDetail.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-146: Confirm dialog

### Acceptance Criteria

1. Confirm dialog uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-147: Response time

### Acceptance Criteria

1. Response time uses actual assistant-ui source in features/dashboard/DashboardPage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-148: Create task

### Acceptance Criteria

1. Create task uses actual assistant-ui source in features/tasks/TaskCreatePage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-149: Software purchase

### Acceptance Criteria

1. Software purchase uses actual assistant-ui source in features/chat/auiResultRegistry.tsx; app/shell/appGenUiLayer.ts; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement aui-150: Support tickets

### Acceptance Criteria

1. Support tickets uses actual assistant-ui source in features/dashboard/DashboardPage.tsx; features/chat/auiResultRegistry.tsx; populated from validated UISpec and allowlisted action registry wired to real Gideon handlers; never dispatch arbitrary model actions; interactive actions persist via existing callbacks and errors remain visible. Pass changed-code coverage, meaningful1:1 ratio, keyboard/mobile and actual consumer-path evidence in both editions.
2. Record donor export/hash, integration import, runtime payload type, producing API/tool, exact route/trigger, action handler and test/evidence in component-map.json. Source presence or a gallery example alone is not delivery.

## Requirement product: Complete approved UI

### Acceptance Criteria

1. Production redesign of full OSS and hosted UI from approved conceptual demo, actual use of all 150 requested assistant-ui surfaces, and hosted Windows Electron client. Demo is design reference only. No broad backend rewrite or new business-service integrations inferred from decorative template examples.

## Requirement windows: Hosted Windows app

### Acceptance Criteria

1. Windows x64 NSIS executable, Gideon product identity/icon, checksum manifest, installation/start/uninstall evidence. Build cross-platform through Node script; use Windows runner for native qualification. No update server or signing claim without configured service/credentials.

