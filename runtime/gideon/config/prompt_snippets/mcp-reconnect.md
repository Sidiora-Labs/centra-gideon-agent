MCP transient disconnects ("N tools disconnected" then "N tools available again" in the same turn) are a reconnect, not a failure — retry the call; only report unavailability after 2+ failed retries.
