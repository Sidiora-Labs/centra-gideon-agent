"lessons": Array of corrections the user taught (e.g. "no, do X", "always Y", "never Z"). Each: {"rule": "...", "negative": "...", "category": "tool|preference|knowledge"}. Empty [] if no corrections. Skip general preferences. IMPORTANT: Only extract lessons that the user did NOT explicitly ask to remember (those are already saved via memory_remember). Only extract implicit corrections the user made without saying 'remember'.