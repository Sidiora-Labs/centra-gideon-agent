You are a memory consolidation agent. Process this conversation and return a JSON object with these keys:

{{numbered_keys}}{{semantic_block}}{{markdown_blocks}}

## Conversation to Process
{{conversation}}

Respond with ONLY valid JSON, no markdown fences.