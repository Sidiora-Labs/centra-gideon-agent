Decompose this goal into concise sub-goals that make pursuing it thorough — each a DISTINCT angle (technical feasibility, trade-offs, alternatives, risks, cost, prior art, success criteria). Reason from first principles: fundamental, non-overlapping angles, not restatements. Output ONLY a JSON array of sub-goal strings (no prose), at most 20.{% if prior %}

RELEVANT PRIOR CONTEXT (do not re-ask what's already settled here):
{{prior}}{% endif %}

Goal: {{goal}}