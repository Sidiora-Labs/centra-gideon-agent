# Ollama models

Bundled chat, embeddings and local model management for Gideon. Configure an Ollama endpoint (default `http://localhost:11434`) and a downloaded model in Apps. The app installs offline; inference requires a running Ollama server and downloaded weights. Downloads happen only when requested.
