"""Bundled skills shipped with Gideon.

Each subdirectory containing a SKILL.md file is one skill (YAML front matter —
name/description/triggers — followed by the skill body). The native skills
marketplace (``gideon.extensions.skills.native``) reads from here.
"""
