{
  "name": "bash-action",
  "version": "1.0.0",
  "displayName": "Bash Hooks",
  "description": "Run shell commands on agent lifecycle events. Supports the ACP exit-code-2 contract for blocking PreToolUse calls.",
  "icon": "SquareTerminal",
  "author": "Gideon",
  "tags": [
    "action",
    "bundled"
  ],
  "provider": {
    "type": "action",
    "entity": "shell",
    "implementation": "gideon.action_providers.bash_provider:create_provider",
    "capabilities": [
      "execute",
      "blocking"
    ],
    "settingsSchema": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "command": {
          "type": "string",
          "x-meta": {
            "label": "Command",
            "help": "Shell command run via /bin/sh -c. Receives GIDEON_HOOK_EVENT + GIDEON_HOOK_CONTEXT env vars and the event JSON on STDIN. Exit code 2 blocks a PreToolUse call."
          }
        },
        "timeout": {
          "type": "integer",
          "x-meta": {
            "label": "Timeout (s)",
            "help": "Optional timeout in seconds (0 = the caller's default: 300s for a scheduled command, 30s elsewhere). The command is killed when it expires.",
            "tags": [
              "advanced"
            ]
          }
        }
      }
    }
  },
  "native": true
}
