"""Shipped offline agent reference (PLATFORM-LEGIBILITY §3.1).

The markdown files here are GENERATED, not hand-edited — run
``python -m gideon.manifest_reference`` to regenerate them from the live
registries. ``tests/test_agent_reference.py`` byte-compares the checked-in copy
against a fresh render, so a stale file reddens the build. They ship in the wheel
(``pyproject.toml`` package-data) so ``gideon doctor --paths`` can point an
external agent at exact tool/route signatures from the installed binary alone.
"""
