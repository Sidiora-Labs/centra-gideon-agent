{
  "name": "optimize-harness",
  "version": 1,
  "description": "Run a budgeted hill-climbing search over one of Gideon's own artifacts — a template, a skill, an SOP — and file the winner as a proposal. Candidates live in a throwaway sandbox; the live artifact is never written to, and a candidate that touches it is dead regardless of its score.",
  "source": "bundled",
  "provenance": "user",
  "tags": [
    "learning",
    "evals",
    "refiner"
  ],
  "inputs": {
    "target_template": {
      "type": "string",
      "required": true,
      "help": "The Gideon template being optimized — also the results.tsv subject its best-ever score is read back under."
    },
    "live_target": {
      "type": "string",
      "required": true,
      "help": "The live artifact's path. This and its .gideon-lock.json are the FROZEN region: read-only for the whole search."
    },
    "sandbox": {
      "type": "string",
      "required": true,
      "help": "A throwaway directory for candidates and the .experience/ index. Must be outside the frozen region — preflight refuses otherwise."
    },
    "budget_usd": {
      "type": "number",
      "required": true,
      "help": "The hard dollar ceiling (guardrails SpendMeter). There is no default: an unbudgeted search over model calls has no ceiling at all."
    },
    "suite_threshold": {
      "type": "number",
      "required": false,
      "default": 0.8,
      "help": "Half one of the dual gate: the harvested regression suite's pass floor. A candidate below it is discarded even when it beats the best-ever."
    },
    "hypothesis_abandon_after": {
      "type": "number",
      "required": false,
      "default": 3,
      "help": "Halt after this many consecutive attempts at the SAME fix — at that point the diagnosis is wrong, not the execution."
    },
    "no_improvement_halt": {
      "type": "number",
      "required": false,
      "default": 5,
      "help": "Halt after this many consecutive iterations whose best score never rose above the window's opening value."
    },
    "max_iterations": {
      "type": "number",
      "required": false,
      "default": 12,
      "help": "The iteration floor under the other three halts — reached only when none of them fires first."
    }
  },
  "runtime_hints": {
    "execution": {
      "depth": "thorough",
      "breaker": {
        "fingerprint_window": 3,
        "hypothesis_abandon_after": 3,
        "no_improvement_halt": 5
      }
    }
  },
  "metadata": {
    "risk": "medium",
    "capabilities": [
      "read",
      "write",
      "execute"
    ],
    "steering_examples": [
      {
        "event": "kickoff",
        "description": "Try to improve the code-project template's audit prompt, spend at most two dollars, and show me the best candidate"
      },
      {
        "event": "mutation",
        "description": "Stop searching if three candidates in a row fail to beat the current best — do not burn the rest of the budget on a plateau"
      }
    ],
    "keywords": [
      "optimize",
      "tune the prompt",
      "search for a better template",
      "budgeted search",
      "hill climb",
      "improve the harness"
    ],
    "example_outputs": [
      "one template-diff proposal that cleared both the suite threshold and the best-ever score, plus a ledger of every candidate including the discards",
      "no proposal and a named halt reason, when nothing beat the incumbent"
    ],
    "shapes": [
      "triage"
    ],
    "when_not_to_use": "not for editing an artifact directly — this only ever proposes, and it costs a model call per candidate. Reach for `refine-template` when one look at a template's run ledger is enough."
  },
  "root": {
    "kind": "sequence",
    "id": "root",
    "children": [
      {
        "kind": "action",
        "id": "preflight",
        "label": "Refuse or report, before the first model call",
        "config": {
          "provider": "bash",
          "payload": {
            "GIDEON_OPT_SUBJECT": "{{inputs.target_template}}",
            "GIDEON_OPT_LIVE_TARGET": "{{inputs.live_target}}",
            "GIDEON_OPT_SANDBOX": "{{inputs.sandbox}}",
            "GIDEON_OPT_BUDGET_USD": "{{inputs.budget_usd}}",
            "GIDEON_OPT_ABANDON_AFTER": "{{inputs.hypothesis_abandon_after}}",
            "GIDEON_OPT_NO_IMPROVEMENT_HALT": "{{inputs.no_improvement_halt}}",
            "GIDEON_OPT_MAX_ITERATIONS": "{{inputs.max_iterations}}"
          },
          "with": {
            "command": "python3 -m gideon.assurance.evals.optimize preflight </dev/null",
            "timeout": 120
          }
        }
      },
      {
        "kind": "loop",
        "id": "search",
        "label": "Propose, scope-check, score, keep or discard",
        "config": {
          "mode": "until",
          "condition": "{{last.output.halt}}",
          "max_iterations": 12,
          "timeout_stall_secs": 1800
        },
        "body": {
          "kind": "sequence",
          "id": "iterate",
          "children": [
            {
              "kind": "stage",
              "id": "propose",
              "config": {
                "agent": "gideon-template-refiner",
                "model_tier": "reasoning",
                "tools_posture": "verify",
                "allowed_write_paths": [
                  "{{inputs.sandbox}}"
                ],
                "write_scope_mode": "reject",
                "prompt": "You are running ONE iteration of a budgeted search over a Gideon artifact. You may only READ and PROPOSE. Your tool set enforces that; do not fight it.\n\nThe artifact under optimization:\n{{inputs.target_template}}\n\nThe LIVE copy, which is frozen:\n{{inputs.live_target}}\n\nYour sandbox — the ONLY path you may write to:\n{{inputs.sandbox}}\n\nWhat the preflight measured, including the score you have to beat:\n{{nodes.preflight.output}}\n\nWhat previous iterations of this search already tried is in `{{inputs.sandbox}}/.experience/index.json`, with each candidate's raw diff beside it. Read the diffs, not just the index: the point of keeping them is that the raw prior attempt tells you more than a summary of it.\n\nMethod, in order:\n  1. Call `refiner_evidence` for the target. Work from its ranked failure clusters, not from a hunch.\n  2. Read `.experience/index.json`. A fix whose fingerprint is already there and already failed is not a candidate — repeating it is how a search burns its budget confirming a wrong diagnosis.\n  3. Propose ONE candidate edit as typed ops (`update_node` / `insert` / `delete` / `move` / `set_input`). You may NOT touch the artifact's id, name, triggers, or surfacing metadata — those decide WHEN it runs and are frozen.\n  4. Write the candidate's diff into your sandbox. Writing anywhere else — above all into {{inputs.live_target}} — is a scope violation, and the adjudicator kills the candidate for it whatever it scored.\n  5. Score the candidate against the target's harvested suite and report the number honestly. A score you inflated wins this iteration and loses the search.\n  6. Give the fix a stable `fix_fingerprint`: the same string for the same DIAGNOSIS, even when you word the edit differently. That is what lets the search abandon a wrong hypothesis instead of rewording it forever.\n\nReturn JSON: {\"fix_fingerprint\": string, \"score\": number, \"diff_text\": string, \"rationale\": string, \"ops\": array}",
                "schema": {
                  "fix_fingerprint": "string",
                  "score": "number",
                  "diff_text": "string",
                  "rationale": "string",
                  "ops": "array"
                }
              }
            },
            {
              "kind": "action",
              "id": "scope_check",
              "label": "Diff the frozen region before anything is scored",
              "config": {
                "provider": "bash",
                "payload": {
                  "GIDEON_OPT_SANDBOX": "{{inputs.sandbox}}",
                  "GIDEON_OPT_LIVE_TARGET": "{{inputs.live_target}}"
                },
                "with": {
                  "command": "python3 -m gideon.assurance.evals.optimize scope-check </dev/null",
                  "timeout": 120
                }
              }
            },
            {
              "kind": "gate",
              "id": "verify_scope",
              "label": "A frozen-region touch is REFUSED, not merely recorded",
              "config": {
                "kind": "expression",
                "expr": "{{nodes.scope_check.output.outcome}} != 'scope_violation'",
                "prompt": "This candidate wrote into the frozen region — the live artifact or its .gideon-lock.json. It is dead regardless of what it scored, and the search must not carry it forward. Approve only if you have personally checked which path it touched and why."
              }
            },
            {
              "kind": "action",
              "id": "adjudicate",
              "label": "The dual gate, then the halts",
              "config": {
                "provider": "bash",
                "payload": {
                  "GIDEON_OPT_SUBJECT": "{{inputs.target_template}}",
                  "GIDEON_OPT_LIVE_TARGET": "{{inputs.live_target}}",
                  "GIDEON_OPT_SANDBOX": "{{inputs.sandbox}}",
                  "GIDEON_OPT_SUITE_THRESHOLD": "{{inputs.suite_threshold}}",
                  "GIDEON_OPT_BEST_EVER": "{{nodes.preflight.output.best_ever}}",
                  "GIDEON_OPT_ROWS_CONSIDERED": "{{nodes.preflight.output.rows_considered}}",
                  "GIDEON_OPT_SCORE": "{{nodes.propose.output.score}}",
                  "GIDEON_OPT_FIX_FINGERPRINT": "{{nodes.propose.output.fix_fingerprint}}",
                  "GIDEON_OPT_DIFF_TEXT": "{{nodes.propose.output.diff_text}}",
                  "GIDEON_OPT_BUDGET_USD": "{{inputs.budget_usd}}",
                  "GIDEON_OPT_ABANDON_AFTER": "{{inputs.hypothesis_abandon_after}}",
                  "GIDEON_OPT_NO_IMPROVEMENT_HALT": "{{inputs.no_improvement_halt}}",
                  "GIDEON_OPT_MAX_ITERATIONS": "{{inputs.max_iterations}}"
                },
                "with": {
                  "command": "python3 -m gideon.assurance.evals.optimize adjudicate </dev/null",
                  "timeout": 120
                }
              }
            }
          ]
        }
      },
      {
        "kind": "stage",
        "id": "file-proposal",
        "label": "The winner becomes a proposal a human installs",
        "config": {
          "agent": "gideon-template-refiner",
          "model_tier": "reasoning",
          "tools_posture": "verify",
          "prompt": "The search has halted. File its winner — if it has one — as a proposal, and nothing else.\n\nWhy it stopped, and the last adjudication:\n{{nodes.search.output}}\n\nThe candidate ledger, every iteration including the discards:\n{{inputs.sandbox}}/.experience/index.json\n\nDo exactly one of these:\n  * If the ledger's last `admitted` candidate exists, call `propose_template_diff` with ITS ops, a rationale that cites the score it reached and both halves of the gate it cleared (the suite threshold AND the best-ever), and the halt reason. You are filing a proposal for a person to read and install — you are NOT applying it, and you have no tool that could.\n  * If nothing was admitted, propose NOTHING and say so. A search that found no improvement has produced a result, not a failure; inventing a proposal to have something to show is the one outcome worse than an empty one.\n\nNever file a candidate whose outcome was `scope_violation`. It touched the frozen region, which makes it dead regardless of what it scored.\n\nReturn JSON: {\"proposed\": boolean, \"halt_reason\": string, \"winner_score\": number, \"rationale\": string, \"proposal_id\": string}",
          "schema": {
            "proposed": "boolean",
            "halt_reason": "string",
            "winner_score": "number",
            "rationale": "string",
            "proposal_id": "string"
          }
        }
      }
    ]
  }
}
