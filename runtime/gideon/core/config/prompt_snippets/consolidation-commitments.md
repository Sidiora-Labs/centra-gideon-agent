"commitments": Array of inferred FUTURE check-ins you could proactively follow up on, that the user did NOT explicitly ask to be reminded about. ONLY genuinely useful, time-bound, high-confidence follow-ups (e.g. user said "the migration ships Friday" → check in Monday). Each: {"text": "<natural one-line check-in message to send the user>", "due_window": "<ISO-8601 UTC timestamp, when to check in>", "confidence": 0.0-1.0}. Use confidence>=0.8 ONLY for clear, useful follow-ups; below that, omit it. A wrong check-in is intrusive — when unsure, return []. Max {{max_commitments}}.