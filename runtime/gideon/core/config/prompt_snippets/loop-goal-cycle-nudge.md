Run the next autonomous cycle for goal loop {{loop_id}} (working dir for loop files: {{loop_dir}}). Steps: (1) check status.json — if not 'running', stop; (2) read brief.md; (3) apply + delete guidance.txt if present; (4) do ONE adaptive step toward the goal.

Before you end this turn you MUST write findings/cycle_NNN.json to {{loop_dir}} (next sequential N) — this is the deliverable, not optional, and the turn is incomplete without it. The finding is the structured cycle record {cycle, summary, key_insight, sources_checked, new_findings_count, evidence, metric?}. Report what you DID and the EVIDENCE; do NOT write a self-verdict on whether the goal is met — a separate check decides that.{% if has_deliverables %}
This goal has {{deliverables_count}} SEPARATE deliverables ({{deliverables_names}}). Save/update EACH as its own artifact via `artifact_save` (tagged `loop:{{loop_id}}`) — never combine them into one document. Advance whichever deliverables this cycle's work touched.{% endif %}{% if show_single_deliverable %}
Also maintain {{deliverable}} in {{loop_dir}} — create it with full section structure on cycle 1, and UPDATE it in place every later cycle, folding in this cycle's findings, so it reads as the finished deliverable by the end.{% endif %}{% if is_verifiable %}
This is a verifiable goal: there is no document deliverable. Make real progress toward the success criteria; the supervisor runs the check itself.{% endif %}{% if is_multi_agent_with_roster %}
You are the ORCHESTRATOR (see brief.md): delegate this cycle's step to the right roster persona(s) as subagents, wait for their results, then write the finding from what they produced.{% endif %}
Actually write the files (use your file-write/editor tools); do not just describe them. Then end the turn.