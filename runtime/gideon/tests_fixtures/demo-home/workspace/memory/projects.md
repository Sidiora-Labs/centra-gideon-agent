# Active Projects

<!-- Current work context -->

#### Reading Pipeline
Turning the week's saved long-reads into one cited digest. Three sources are in
flight; two summaries are done. The third argues against its own headline in
section 4, so its summary has to carry both claims — see the note on
"Summarize the three saved long-reads".

The bulk import last week produced duplicate notes under different titles.
Merging them is deliberately waiting until the digest is written, because the
merge would drop the citation trail that the digest depends on.

#### Home Server
One box runs everything, so the goal is boring: verified backups, predictable
disk headroom, and a restore path someone else could follow. The restore runbook
is written and was tested cold. The remaining open question is not technical —
the drive in bay 2 has 3 reallocated sectors and four years on it, and replacing
it only buys a year. Deciding that means deciding whether the media library
stays local at all.

#### Standing context
- The weekly capture template is settled at five fields; the "mood" field was
  dropped because it never once changed what got reread.
- Disk-usage alerts currently fire at 70% and have never been actionable there.
