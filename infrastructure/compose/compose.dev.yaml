services:
  gideon-gateway:
    volumes:
      - gideon_home:/data
      - ../../runtime/gideon:/opt/venv/lib/python3.13/site-packages/gideon:ro
    environment:
      GIDEON_LOG_LEVEL: DEBUG
    command: [ "gideon", "gateway", "--no-browser" ]

  gideon-web:

    profiles:
      - disabled
