---
id: T1.example-config-field
type: task
title: Add a runtime-editable config field (worked task-spec example)
scenario: null
taskType: feature
intent: Add one new user-editable config field and wire it through the full contract.
touchedAreas:
  - runtime/gideon/config/loader.py
  - runtime/gideon/dashboard/handlers/core.py
  - apps/console/src/pages/settings
expectedUserBehavior:
  - The new setting appears in the Settings UI, persists across a gateway restart, and is
    reflected in behavior that reads it.
requiredProfiles:
  - fast
  - web
requiredRules:
  - config-four-points
requiredTests:
  - checks/runtime/test_config_roundtrip.py
acceptance:
  positive:
    - The field round-trips (set in UI → persists after restart → reload shows the value).
    - test_config_roundtrip.py passes with the new field mutated.
  negative:
    - The field is NOT dropped on save (present in to_dict()).
    - The field does NOT revert to default on reload (present in load()'s mapping).
    - No second/duplicate config path is introduced for the same setting.
---

# T1.example-config-field — a worked task-spec example

This is a **reference task spec**, not a live task: it exists so a coding agent can see the
shape of a well-formed task spec and so `python -m checks.harness explain T1.example-config-field`
has something to demonstrate. It encodes the [[config-four-points]] rule as its governing
invariant and carries the mandatory **negative acceptance** clauses that half prose LEDGER
entries always drop — the three ways a config field silently misbehaves.

Real task specs replace this: one per fix/feature, `id` = the roadmap task id (e.g.
`T2.3`), `scenario` pointing at the triage playbook when one applies.
